# CEC 24-pin Module partition table
# Target: ESP32-S3-MINI-1 N4R2 (4 MB flash)
#
# Minimal single-app layout (no OTA yet). factory fills the rest of the 4 MB
# flash after nvs + phy_init (ends exactly at 0x400000 = 4 MB). nvs holds the
# learned profiles + calibration. Add OTA / storage later by shrinking factory.
#
# Name,    Type, SubType,  Offset,   Size,
nvs,       data, nvs,      0x9000,   24K,
phy_init,  data, phy,      0xf000,   4K,
factory,   app,  factory,  0x10000,  0x3F0000,
