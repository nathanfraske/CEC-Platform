# CEC EPS Module partition table — OTA layout (16 MB dev board)
#
# Two app slots + otadata so the Hub can re-flash this module over CAN
# (cec_canota), after the one-time initial USB flash. Converting an
# already-flashed board needs ONE USB flash (offsets moved).
#
# Name,     Type, SubType,  Offset,    Size
nvs,        data, nvs,      0x9000,    0x6000
otadata,    data, ota,      0xf000,    0x2000
phy_init,   data, phy,      0x11000,   0x1000
ota_0,      app,  ota_0,    0x20000,   0x300000
ota_1,      app,  ota_1,    0x320000,  0x300000
