---
name: convergence-blocker-mechanism-not-corpus
description: "Why runs don't converge a fresh board — it's enforcement/mechanism (pour-clip), not a corpus-knowledge gap; the deterministic route already converges the committed board."
metadata: 
  node_type: memory
  type: project
  originSessionId: f4532aca-d14d-4db6-907e-4f75a063ae3c
---

**Can a run converge (pass the HARD gates kelvin_ok+diffpair_ok, finishing-only DRC) right now, or does the corpus need more first?** Answer (verified 2026-06-16, workflow wf_7b4f6e0a + independent trace): **YES it converges on a hand-finished board via the deterministic route; the blocker for a fresh/agentic run is MECHANISM, not corpus.**

- **Existence proof:** routing the committed `eps-8pin` through the deterministic pipeline → `kelvin_ok=true, diffpair_ok=true, drc=0` (live, 18.2s). SB-08 golden locks this. The golden's only "red" is a THERMAL band (max_T over budget) — the owner-gated AM-04 re-freeze, NOT a hard-gate/DRC blocker.
- **The crux divergence (same board, converges vs stalls):** `cec_router.route()` bakes a route-time vital-corridor keepout (`_vital_keepouts_from_rules`, cec_router.py:631) reserving the connector→shunt high-current corridors, so FR routes foreign signals AROUND the pours → they stay solid → converges. `route_directed` (the full-stack route, cec_overnight_directed.py:188) does NOT — it bakes only REACTIVE avoid intents (one round late, and the finder targets the FENCED sense nets → refused). So foreign signals route THROUGH the corridors → pours fragment (`/SENSEC2_LO` clipped 36–40/46 rounds) → `gates=FAIL`.
- **Corpus is NOT the bottleneck:** the rules are encoded + ratified + checked — `cec_constraints.py:82` high-current-corridor-keepout, `:98` high-current-pour-integrity, `:116` kelvin-sense-from-inner-pad — and the pour-integrity checker correctly DETECTS the clip. The "notched-corridor keepout / re-pour-after-route" fix is only a TODO string (cec_fullstack.py:2786); no notch/defrag function exists; layer-stagger is wired-but-inert (fired 31×, "flipped 0 crossings, reverted" 31/31).
- **To unblock a fresh-board converge (5 CODE, 1 advisory-CORPUS):** (1) notched-corridor keepout at route time [highest value; `bake_hints` only makes solid rects today]; (2) re-pour/defrag-after-route; (3) make layer-stagger actually relocate a crossing to the B.Cu mirror; (4) same-route corridor reservation (avoid is next-round-reactive); (5) domain-aware placer (corridor keepout + sense-adjacent-to-shunt as HARD constraints — CLAUDE.md item -2, the real fresh-board unblock); (6) [CORPUS, advisory] bench-calibrate min-pour-cross-section / FEM-k (OQ-10/12) — gates physics sign-off, not the hard gates.

The placement actuation lever (wired 2026-06-16, see [[current-work-handoff]]) is one enforcement mechanism; the DOMINANT eps blocker is the pour-clip (#1/#2). Build #1 if the goal is a self-converging fresh-board run.

**UPDATE 2026-06-16 (close-the-loop build, commit 91b7d68): #1 (route-time corridor keepout) is built BUT is COUPLED WITH PLACEMENT — it is the ROUTE half, not a standalone fix.** `cec_fr.corridor_keepouts(board)` reserves each connector→shunt corridor (shared by `cec_router.route()` and now `route_directed`). Two measured facts: (a) a REAL bug — the keepout's `DoNotAllowZoneFills` blocked ~89% of the same-net pour (122.8→10.5 mm²); fixed via a `block_fills` flag (corridor keepouts set False → FR routes foreign tracks around, the same-net pour still fills solid). (b) THE COUPLING — applying the keepout to the COMMITTED eps placement (which is NOT corridor-clean: corridor_cross≥3, /DETC,/THRESH,/I2C are forced through the corridor) drops foreign crossings 31→6 and makes pours solid (the mechanism works) BUT strands the sense taps + those nets (kelvin_ok True→False, unconnected 2→16). The existence proof (kelvin pass on committed eps) was via `cec_golden`, which uses NO keepout. So the keepout DEFAULTS OFF (`CEC_OVD_CORRIDOR_KEEPOUT=1` to enable) and the loop only closes with the keepout + the corridor-PACKING placer (seed_anchors corridor-clean columns, the "#5" half) TOGETHER, validated on a FRESH corridor-clean placement — NOT on the committed (hand) eps placement. So "fully close the loop" = keystone(route) ⊗ placer(place), coupled.

**UPDATE 2026-06-26 (research wf_e7bc946c, deep re-diagnosis — see docs/why-loop-wont-close-2026-06-26.md):** The "domain-blind placer / item -2" framing is STALE — TWO placers DO model the domain (cec_synth_pipeline CorridorModel+veto+rank; cec_place_planner w_kelvin_seat+w_orient+w_partition+CONVERGED gate). The actuation EXISTS; it never reaches `corridor_cross=0`. The REAL blocker: **nothing produces a corridor-clean placement BY CONSTRUCTION** — it's a GLOBAL min-cut (partition foreign logic off the corridors) attacked with LOCAL body-eviction + LLM hill-climb; human does cross=6 in minutes, machine stalls at 15-24 over 384 rounds (2 improvements). Keepout ON→pours solid (clips 4-5) but sense stranded (kelvin false, unconn 13-15); OFF→routable but clips 17-91 — oscillates, converges to neither. PATH: (1)[S] wire cec_fr.edge_keepout into route()+route_directed (kills ~100% edge-clr DRC); (2)[S-M] post-route re-pour+defrag (decouples keystone from placer, clips→deterministic cleanup, keepout default-on); (3)[L] constructive min-cut corridor-clean SEED (cross=0 by construction) — the deferred hard piece; (4)[S] wire placer into run_pipeline+flip keepout on+golden; (5) fix CLAUDE.md item -2. LEARNING = ACTUATION-BEFORE-LEARNING: corpus is judge-only (0 place/route promoted, rules in staging behind an un-done owner re-sign), live-rules reset empty/never reload, CL-13 label chain CODED-BUT-NEVER-FIRED (160 runs/112 pour-clip fails → 0 grade-1 settles, 0 ADV fires), CL-08 run-over-run engine doesn't exist. VERDICT: build steps 1+2 (days = cheap existence proof on the committed placement); full FRESH-board closure NOT worth chasing now (needs the L placer, sufficiency never demonstrated); the human-PCB split WORKS — owner was right to revert. Make CL-13 fire (≈free). Don't rewrite the placer; fix the doc.

**UPDATE 2026-06-27 (BREAKTHROUGH, commits 21a722c/2e67cce) -- it was a WRONG METRIC, not corpus and not (just) mechanism.** Design panel wf_d53e2eab + verified: the OLD `corridor_cross_count` (cec_synth_pipeline) is TOPOLOGICALLY UNREACHABLE to 0 -- the hub(ESP)->per-cable fan-out (DETC1 cable1 vs DETC2 cable2) MUST straddle a corridor on a single layer for K>=2 cables with the hub on one side. So the 384-round place-planner hill-climb chased an IMPOSSIBLE target (stalled 15-24); even the HUMAN scores 6, not 0. The fix is the HONEST metric: the corridor keepout clips each pour to the connector-pad ROWS (not full board height) -> the top/bottom channels are physically CLEAR -> a straddling net routed along a channel cuts NO pour. Built `corridor_cross_channel_aware` (== predicted post-route F.Cu clips) + `channels_of` + `_body_clear` + `channels_feasible` (bounded H-grow trigger, NOT a search) in cec_synth_pipeline; host unit test tests/test_channel_aware_cross.py. **PROVEN on the REAL committed eps: OLD corridor_cross=6, CHANNEL-AWARE=0** -- the human's corridor-clean placement IS cross=0 in the honest metric. So fresh-board cross=0 is ACHIEVABLE BY CONSTRUCTION (reserve channels + bounded H-grow; residual unavoidable straddles route UNDER on B.Cu via CEC_ROUTE_UNDER). The deferred "L-effort min-cut placer" is NOT needed. REMAINING actuation (mechanical): wire corridor_cross_channel_aware as the placer rank/gate (replace the unreachable old metric), add _channel_veto to the anneal + the H-grow re-seed in synth_one, point cec_place_planner._region_boxes at channels_of, then route-validate a FRESH eps end-to-end. See docs/why-loop-wont-close-2026-06-26.md "BREAKTHROUGH".
