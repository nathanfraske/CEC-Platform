---
name: cec-thermal2d-field-solver
description: The fine-density field electrothermal solver ALREADY EXISTS — scripts/cec_thermal2d.py. USE it for thermal; do NOT rebuild or forget it.
metadata: 
  node_type: memory
  type: reference
  originSessionId: 6a1b6a2a-dfb9-4421-89b6-0ca014fb9552
---

`scripts/cec_thermal2d.py` (≈2161 lines, self-tests A–K passing, dated 2026-06-19) IS the fine-density GPU-capable field electrothermal solver. The owner built it and remembers it; I forgot it existed, built an analytic-only narrative on `cec_synth_pipeline.electrothermal_solve`, and that lumped per-net model HID a real 181 °C shredded-pour failure (foreign traces routed through the high-current pours). **Before any thermal check, gate, or claim that a board is fab-ready, reach for cec_thermal2d FIRST.**

What it does (the physics the analytic lacks): rasterizes real pcbnew copper to a uniform grid (grid_mm 0.3 coarse → 0.1/0.05 fine), one electrical node per copper cell per std-layer (F.Cu/In1/In2/B.Cu); same-layer neighbors linked by sheet resistance; **layers couple ONLY through real PCB_VIA barrels** (`_barrel_segment_R`) — so a sparse via field runs the fed layer hot (self-test B: 1.95× vs a stitched plane); tracks rasterized with EFFECTIVE-WIDTH sheet conductance so a 0.2 mm trace stays grid-independent (`_rasterize_tracks`, self-test F within 1.2%) — the shredded-neck resolver; shunts = lumped I²R sources; connector pads = area-injection Neumann BCs. It RESOLVES the LOCAL current+temperature field, so it catches the via-fusing + shredded-neck hotspots the analytic averages away (self-test H: via-barrel hotspot lands at the physical via at 186.6 °C). GPU/AMG path is already coded in `_spd_solve` (cupy + pyamg).

Status: being WIRED into `cec_synth_pipeline.physics()` as the high-fidelity thermal tier (the analytic `electrothermal_solve` stays the lumped fallback) + cupy/pyamg/torch installed in the routing container (they were absent). Research that rediscovered it: workflow wf_35c90d29. Relates to [[thermal-gate-required]] (a routed board isn't clean until a thermal solver passes — and cec_thermal2d is the RIGHT one).

**GPU STACK ALSO ALREADY BUILT (forgot this too, 2026-06-27):** `docker/Dockerfile.routing-gpu` builds image `cec/routing:gpu` = the kicad10 routing image + `cupy-cuda12x[ctk]` (CUDA-toolkit wheels because the 5090 / Blackwell sm_120 needs cupy to JIT kernels) + matplotlib/shapely/pyamg. `docker/compose.gpu.yaml` is the OPT-IN GPU override (nvidia device reservation + `CUPY_CACHE_DIR` on a named volume so the Blackwell JIT compiles ONCE). Build: `docker build -f docker/Dockerfile.routing-gpu -t cec/routing:gpu .`; start: `docker compose -f docker/compose.yaml -f docker/compose.gpu.yaml up -d routing` (the dashboard's `docker compose exec routing` then attaches to it unchanged). cec_thermal2d auto-picks the cupy/GPU path once cupy imports + n>=20000, with try/except CPU-scipy fallback under GPU contention with the LLM broker. The PLAIN routing container has NO GPU (`nvidia-smi` absent); the `:gpu` one does. **WIRED INTO THE GATE 2026-06-27**: `field_electrothermal_solve` + `physics()` honors `CEC_THERMAL_FIELD=1` / `cfg.params['thermal_field']` (analytic fallback). The pattern keeps repeating — I forgot the solver AND the GPU container AND the compose; CHECK docker/ + scripts/cec_thermal*.py before assuming a thermal/GPU thing doesn't exist.
