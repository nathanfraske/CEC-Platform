# Hub Rev2 — sectioned regen + mezzanine stack (2026-06-24)

Generated by `scripts/gen-hub-rev2.py` from the hand-maintained **Hub Standard** netlist
(extracted to `extract.json`, BOM/LCSC preserved), laid out in 10 LABELLED FUNCTIONAL SECTIONS,
with the **Rev2 delta**: the mezzanine SOCKET (`CEC_MEZZANINE_16P`, mirror of the 24-pin rev3
header) wired as "port 0" — pre-muxed `+5V_SYS`→`+5VSB`, the shared CAN bus, `/DETECT1`, GND.
Population variant: `J_MEZZ` XOR the `J2` RJ-45 port.

STATUS: schematic DRAFT — ERC clean (0 errors; benign lib_symbol_mismatch/pin_to_pin warnings only),
78 parts, BOM sourced (inherited from Hub Standard). The live `hubs/hub-standard/` stays the source of
truth; this is a fresh Rev2 board. NEXT: PCB layout (shared alignment frame w/ the 24-pin), and the
mezzanine socket on B.Cu (the mirror). Pinout verification + the 2 new-vs-24pin IC datasheets: see the
respin docs.
