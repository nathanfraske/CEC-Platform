# Retired Quilter constraints for ATX 24-pin rev3

This file is retained only so old links fail safely. The prior contents applied
to a four-layer board, an obsolete component map, and a router that is not the
current pipeline router. They must not be used as placement or routing input.

Use `LAYOUT-GUIDE.md` for the current six-layer placement, POFV, segmented
mezzanine, passive, pour, and release rules. The automatic route flow uses the
legal-layer policy carried through the current DSN export and Freerouting import
path.
