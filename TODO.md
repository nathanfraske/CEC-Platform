# TODO

The LIVE list of what the agent is ACTIVELY working on, in checkbox form. **APPEND-ONLY** — items are
never deleted; completed / obsolete ones stay as a timestamped history.

Format:
- `- [ ] [added YYYY-MM-DD HH:MM] <task>` — active
- `- [x] [added YYYY-MM-DD HH:MM · done YYYY-MM-DD HH:MM] <task>` — completed (left in place)
- `- [~] [added YYYY-MM-DD HH:MM · obsolete YYYY-MM-DD HH:MM → <tombstone>] <task>` — obsolete, with a
  tombstone pointing where it went (e.g. `FOLLOWUPS.md "<entry>"`, `PR #N`, `docs/owner-queue.md`, another line)

Distinct from FOLLOWUPS.md (deferred / non-blocking backlog) and docs/owner-queue.md (owner-action items).
Times are UTC.

## Active

- [ ] [added 2026-06-15 00:15 · in progress] **FR-on-Hub routing FIX + full hour run** (owner: "do the fixes"). FIXED FR routing the Hub: (1) on-board OFFSET — synth 0-origin coords were placed off the committed outline at (70,90) → FR routed nothing (THE unblock); (2) `_seat_antenna_ic` — ESP off the ganged ports → residual 4→0 (+ antenna term fixed to use courtyard near-edge: ref 0.565→1.0, synth 0.0→0.903, hub_penalty 0.373→0.125); (3) two-process GND fill (Remove footgun). RESULT: low-effort route_once 628 tracks / 39 unconnected / kelvin+diffpair pass / len 2240 (was 2/216). 103 placer tests green. Discriminator test: committed HAND placement routes 389 wires (baseline). NEXT: commit + LAUNCH the hour run.
- [x] [added 2026-06-14 22:50 · done 2026-06-14 23:30 · SUPERSEDED 2026-06-15 00:15 by the FR fix above] **FULL pipeline (place→route→check) wired + run on the Hub** (owner: "if it's all wired up, run the full pipeline for an hour"). scripts/hub_pipeline_run.py wires the route leg; `materialize_onto_reference` fixed the materialize gap (build_board output isn't DSN-exportable → copy the committed Hub + reposition, in a spawn subprocess to dodge the pcbnew Remove footgun). Chain runs end-to-end. **BUT Freerouting does not route the Hub** — emits 1 wire (FR log: autoroute pass#1 "1 change" → pass#2 "0 changes" → stops), 216 unconnected; the identical path routes cable modules to 556 tracks. So an hour run is NOT worthwhile (reproduces the 1-trace result) — reported instead of burned. Placement is solid (correct frame, gates pass, max_T 68.7). FR-on-Hub fix path → FOLLOWUPS item 4. Findings: docs/placer-upgrade-2026-06-14/STATUS.md "FULL PIPELINE".
- [x] [added 2026-06-14 22:40 · done 2026-06-14 23:15] **Hub pipeline TEST RUN (in kicad10 container)** — place→materialize→render→DRC→score ALL ran on hubs/hub-standard with the oracle. Result: correct oracle frame (98.1×74.1, 8 edges), 80-part board, residual 4, corridor_cross 0, **similarity 0.705**, **HPWL 1.25× the hand board (down from 1.84× pre-MV)**, hub_penalty 0.373 (antenna 0.0 = the known gap). DRC 575 (~506 cosmetic silk/lib; ~64 real-structural tracking residual-4; 211 unconnected = UNROUTED, route leg not wired). **The pipeline WORKS at the placement level.** Recipe + numbers: docs/placer-upgrade-2026-06-14/STATUS.md "HUB PIPELINE TEST RUN".
- [x] [added 2026-06-14 21:50 · done 2026-06-14 22:55] **MV2→MV5 placer-upgrade build + consultative audit** (owner: fan out for impl + audit at the end). IMPL committed 26d9a77; 4-skeptic parallel audit (geometry/charter/regression/test-rigor) = SHIP-WITH-FIXES, 0 blocker; ALL findings verified + fixed in a follow-up commit (H1 similarity renorm, M2 sense-net rail exclusion, M3 edge_override validation, charter MEDIUM topological power-loop derivation, regression MEDIUM oracle-failure logging, 2 HIGH test-rigor MV3/MV4 plan-validation tests + scramble/sort-key teeth + LOW comments). 41 tests green, checklist exit 0. Status+audit: docs/placer-upgrade-2026-06-14/STATUS.md. DEFERRED→FOLLOWUPS: MV5 generative closers + the Hub full-stack run.
- [x] [added 2026-06-14 21:50 · superseded by line above] **MV2→MV5 placer-upgrade build** (continue the MV checklist; owner: fan out for impl if needed + consultative audit at the end). DONE (cec_synth_pipeline.py + tests/test_placer_oracle.py, 31 tests, checklist exit 0): MV2 net-aware `_role`/`_connector_net_role`/`_is_rail_net` + `oracle_stage1_answers`/`apply_oracle_stage1` + `seed_anchors(edge_override=)` 4-edge + `place_mechanical(mount_pos_override)`; MV3 `oracle_similarity` DIAGNOSTIC (identity 1.0/scramble<1, NEVER a rank key — `_candidate_sort_key` test enforces); MV4 `proxy_score` (==hpwl with no ref → zero regression; ref-normalized HPWL-dominant composite) + sort-key swap + cfg knobs; MV5 `build_hub_model`/`hub_score` (port_even/antenna/power_cluster mux-loop/usb_prox, gated to ≥2 ports+ESP, reference scores well hub_penalty≈0.27 vs synth≈0.60). Status: docs/placer-upgrade-2026-06-14/STATUS.md. Anti-overfit charter honored (reference=validation only). DEFERRED→FOLLOWUPS: MV5 generative closers (power cohesion sweep + antenna-edge ESP seed). NEXT: parallel adversarial audit → verify → fix → SB-08 in-container.
- [x] [added 2026-06-13 23:25 · done 2026-06-13 23:33] Await DeepSeek new-impl audit → consolidate the Opus-4.8 + DeepSeek verdict (both merge-ready, 0 blockers; DeepSeek's 3 HIGHs = diff-only false positives)
- [x] [added 2026-06-13 23:33 · done 2026-06-14 00:10] new-impl polish fixes — #56 portion: H1 temper T0-overpromise prompt; M1 corridor fallback+log; M2 lane-gate T0/kelvin_stall (_t0_should_fire helper); M3 wire host suites into checklist.sh+CI; L3 in_family_only n_in; L4 progress history[:-1]; L1 deepseek_audit _coerce parity; L6 delete dead PENALISABLE (+6 tests; 159 host tests green)
- [x] [added 2026-06-14 00:10 · done 2026-06-14 00:30] new-impl polish fixes — #57 portion (claude/bot-push-guard, commit 70e176d): H2 pre-push GIT_TERMINAL_PROMPT=0 on the credential-fill probe (fail-closed, no-hang verified); L14 drop unused `root` var. Deferred pre-push LOWs L12/L13/L15/L16 → FOLLOWUPS.md (owner-scoped)
- [x] [added 2026-06-14 00:35 · done 2026-06-14 01:05] Adversarially verify the #56 polish fixes (wf_789eb5bc-b59, 8 skeptics + synth = SHIP-WITH-FIXES) → harden M1 (log even on empty fence) + L3 (header counts shown-under-truncation) + 2 tests; residuals (12vhpwr corridor siting, run()-level H1/L4 coverage, verifier worktree-isolation) → FOLLOWUPS. 161 host tests green
- [ ] [added 2026-06-13 23:25 · in progress 2026-06-14 01:20] Build `claude/seat-bakeoff` PR: cloud-seat shim → 2-D variant×model bake-off → 6-judge leave-one-out panel → de-overfit → data-chosen `--seats cloud` defaults (+ DeepSeek `--stream` live thinking). Owner: BUILD + RUN the full sweep.
- [x] [added 2026-06-14 04:33 · done 2026-06-14 04:50] Corridor-aware reseed placer **Phase 0** (docs/placement-strategy-2026-06-14.md): `build_corridor_model` + `corridor_cross_count` (cec_synth_pipeline) + the two missing checkers `shunt-inline-in-corridor` / `high-current-corridor-keepout` (cec_constraints) + `tests/test_corridor_model.py` (19 tests). PROVEN on committed eps-8pin: model reports ≥3 through-crossers (/DETC1,/THRESH,/I2C), /CAN_L=0; keepout checker has teeth (foreign track→FAIL). Wired into checklist host suite (134 green). Branch claude/placement-corridor.
- [x] [added 2026-06-14 04:50 · done 2026-06-14 05:25 · CLAIM RETRACTED 2026-06-14 06:00 → see audit-remediation line] Corridor-aware reseed placer **Phase 1a**: `Candidate.corridor_cross` + model build in synth_one + sort key `(residual, corridor_cross, hpwl)` + opt-in `proxy_reject(corridor_max=)`. ⚠ The "ranking alone breaks the ceiling" claim was a measurement artifact (degenerate bands) — RETRACTED by the audit; Phase 1a stands as honest rank-key plumbing only. Real ceiling-break = Phase 2 (corridor formation).
- [x] [added 2026-06-14 05:40 · done 2026-06-14 06:00] **AUDIT REMEDIATION** of Phase 0/1a (4 parallel skeptics, PR #60). BLOCKER confirmed + claim RETRACTED: synth winner's `cc=0` is a degenerate-band artifact (shunts land x~7.5 vs connectors x~30-43 → bands ~73mm/96mm, can't be straddled). Fixed: (1) band from connector+2-pad-shunt pads only, exclude INA SMD (matches derive_power_pours; reviewer A F-1, B F-4). (2) `formed`/degeneracy guard (`board_w`) → unformed corridors score 0 INERTLY not falsely-clean. (3) checker false-FAILs on 12VHPWR/24-pin (shared-bus → N/A, Phase-5 variant) + `_sense_nets` exclusion (reviewer B F-1/2/3/4 — 2 BLOCKERs). (4) determinism: sorted() set iteration in relative_place/anneal_macros (reviewer C F-2 — compact s6 now stable 0, was {0,1,2}). (5) tests: place_candidates production-sort (tautology, reviewer D F-1 BLOCKER) + via teeth + shunt-FAIL teeth + shared-bus-NA + multi-cable + no-kelvin + degenerate-guard; honest reframe (34 corridor / 159 host green). (6) doc claim retracted honestly. Deferred→FOLLOWUPS: seg-AABB exact, CI pcbnew wiring, parity re-freeze (owner).
- [x] [added 2026-06-14 06:25 · done 2026-06-14 06:55] **Phase 2 — corridor FORMATION** (89266ab): `_cable_topology` + `_seed_corridor_spine` (align J_OUT's force-pad COLUMN under J_IN's, seat shunt on the column axis at rot270/H3 as fixed anchors) + `_corridor_veto` + `anneal_macros(veto=)` (no HOT/SENSITIVE body in a foreign band). Synth bands now 15.6mm = the committed board's. **KEY FINDING: cc=6 is the INHERENT FLOOR** — the committed (best hand) board is also cc=6; the 6 foreign signals must cross the full-height current corridors to reach the central ESP. Placement CANNOT reach cc=0; the pour-cut failure is route-time LAYER ASSIGNMENT, not placement. OPEN (FOLLOWUPS): residual regressed 2→6 (veto/legalizer tuning); the layer-assignment pivot is the owner's call.
- [~] [added 2026-06-14 06:25 · obsolete 2026-06-14 06:40 → re-run clean] **Audit (1st attempt) corrupted the shared worktree** — a verify-agent's `git checkout` reverted my uncommitted Phase 2 edits (worktree-isolation hazard). Stopped (TaskStop wpnhp01ff), re-applied Phase 2, committed. Re-run on the committed tree (no uncommitted work to lose) → next active line.
- [ ] [added 2026-06-14 06:55 · in progress] **Audit the remediated Phase 0/1a + Phase 2** (workflow wt1frw8xk, on the COMMITTED tree c867f34, verify-agents in isolated worktrees so a git-checkout can't lose work). Triage + verify + fix confirmed findings. NOTE: launched before the overhang commit 192503d; reconcile the cc-floor over-claim (channels DO exist -> the metric over-counts) when it lands.
- [x] [added 2026-06-14 07:00 · done 2026-06-14 07:05] **Auto-overhang cable ports** (owner insight, 192503d): synth_one defaults connector_overhang="edge" for cable-corridor boards -> J_IN/J_OUT seat at edges, corridor spans full height, **residual 6→0**. Plot tool scripts/gen-corridor-plot.py. Remaining: cc 14 vs committed 6 = peripheral parts (CAN/USB/ESP) scattered into crossing positions → next lever "keep non-cable nets off the corridor side".
- [ ] [added 2026-06-14 07:05] **Peripheral placement lever**: keep non-cable parts (CAN xcvr, USB, ESP, RJ45) clustered away from the corridors so their nets don't cross (the committed board does this → cc 6 not 14). Distinct from corridor formation.
- [x] [added 2026-06-14 07:25 · done 2026-06-14 07:45] **Phase 1+2 audit remediation** (4d2c98e): shared-bus exclusion in build_corridor_model (12vhpwr/24pin inert), overhang value→power_able, spine pad-clamp, corridor_cross docstring de-overclaimed, doc CORE-PREMISE rewritten honestly (cc=6 = hand-board analytic floor, placer is WORSE at legal cc 8-24), 24pin test-filename fix + residual==0/band-width asserts + _corridor_veto/_shared_bus unit tests. 37 tests green.
- [x] [added 2026-06-14 07:45 · done 2026-06-14 08:05] **Wire the corridor lever into the tier loop** (c25ee66): `corridor_violations` (board→sensitive bodies in foreign formed bands) + cec_place `apply_corridor_evict`/`evict` directive/refine injection + cec_router `corridor_evict_repair` in MANAGER_REPAIRS. Verified end-to-end (detect→evict→cleared; manager emits place_nudge). +6 tests (43/68 green). Lever in BOTH planes (nightly refine + routing-tier manager).
- [x] [added 2026-06-14 08:05 · done 2026-06-14 08:25] **Panel: corridor lever vs EI + routing tier** (wzx07zl6f, WORKS-WITH-GAPS). Findings: shape-PARITY with the routing tier confirmed (corridor_evict_repair ≅ kelvin_inversion_repair; dual-plane). **G1 (HIGH) FIXED (e01ddd7)**: apply_corridor_evict had no containment guard → dragged the corridor shunt in refine() → false-clean. Deferred → FOLLOWUPS: G2/G3 (EI-instrumentation: not lane-tagged / no corpus_state / not model-proposable / replace-Delta inert) = the overnight-run prereq; cluster-divergence (router nudge bare-ref vs cec_place carries cluster); band-edge math duplicated across planes. DOMAIN: the evict lever clears body-in-band but is ORTHOGONAL to corridor_cross (the cc 14→6 driver) → reconfirms the LAYER lever is the real fix.
- [ ] [added 2026-06-14 08:10] **ROADMAP (owner, 2026-06-14): (1) land the panel/audit → (2) add the LAYER-TIER lever → (3) overnight full-loop run.** Layer lever = the route-time fix the cc=6 floor finding points at: route each corridor-crossing foreign signal so it does NOT neck the F.Cu+B.Cu pour pair (the 4-layer stackup carries current on BOTH outers, inners are GND/12V planes — so it's "stagger the cut across F/B so the mirror always carries + stitch", NOT "move to a free signal layer"). The `high-current-corridor-keepout` checker already identifies the crossing nets; the lever plugs into cec_fr (DSN per-net layer / pour-after-route) as a routing-tier lever. Design firms up once the panel lands. Gated on the panel.
- [x] [added 2026-06-14 08:50 · done 2026-06-14 09:10] **LAYER-LEVER REWORK** (audit w23i0d8nq: 3 HIGH — safe-but-USELESS as built; owner: HOLD the run, rework first). ALL 5 ITEMS LANDED (03c4d0b, 1ad2b42, eab3752, 837a7f4): (4/5) `_route_quality` safe-revert metric (DRC + unrouted ratlines + hard-gate penalty; +inf on failure, not the old `except:return 0` mask) + temp-staged in-place safety (fixes the copy2(path,path) crash). (2) L-bend detection: `_seg_band_clip` (Liang-Barsky) + net-extent crossing test + `_relayer_segment_inband` → multi-segment/45° crossings caught (was 0). (3) `_adopt_staggered_board` re-points rec["routed"]+refreshes metrics (never downgrades a passing board) + the pour RE-FILL that heals the corridor. (1) `_route` lays the B.Cu MIRROR + via stitching (synthesize_power_copper strip_redundant=False, additive, both lanes, _route_quality safe-revert) → the "un-cut mirror carries" premise is now TRUE. +14 tests; checklist wired.
- [x] [added 2026-06-14 09:10 · done 2026-06-14 10:25] **Re-audit the layer-lever rework + launch the clean run** (owner: run audit, implement findings, launch + babysit). DONE. 4 parallel skeptics found 1 BLOCKER (gate-launder) + 3 HIGH → fixed (fb182b5: full re-measurement closes the launder, stale-Pareto refresh, connectivity-repair via, revert-on-disconnect). Focused re-audit confirmed the BLOCKER CLOSED → 1 MEDIUM + 2 LOW fixed (ec0d1d7: objective_base refresh, FEM-stale drop, mkstemp/sidecar leak, test-isolation). Night-watch date fixed (78ae674). **7h run LAUNCHED** ~05:14 UTC (PID 842244, deepseek-v4-flash auditor); round 1 ran clean: route → 4 F.Cu + **4 B.Cu MIRROR** pours + 140 vias (item-1 confirmed LIVE) → T4 panel → T6 pour → layer-stagger (full re-measure live, safe-reverted, no launder) → T5 auditor. Panel LIVE on **:8095**. Watchdog up (PID 846815, 8h). Host suite green both test orders.
- [ ] [added 2026-06-14 10:25 · in progress] **Babysit the 7h run** — round 1 healthy in the slow deepseek-auditor stage; watchdog (846815) self-heals run/panel over 8h + health-logs every 5 min to docs/fullstack-run-2026-06-14/watchdog.log. Monitor measurement.jsonl growth + :8095. Owner signed out for the night; status in chat + handoff.
- [~] [added 2026-06-14 08:30 · obsolete 2026-06-14 08:50 → rework line above] LAYER-TIER LEVER (owner: do it): route each band-crossing foreign signal staggered across F.Cu vs B.Cu so the un-cut outer 12V pour mirror always carries (cable-board-scoped; layer-keepout sibling of the FR-02 corridor-avoid in cec_fr). EI-INSTRUMENTED from the start (lane-gated via lane_for + a corpus_state/lane-tagged measurement row + OWNED_LEVERS entry) so it doesn't repeat the corridor-evict's EI gap AND it satisfies the overnight-run prereq. Then AUDIT it (panel).
- [ ] [added 2026-06-14 08:30] **Overnight full-loop run @ 8h** (after the layer lever + its audit): the EI-02 laned loop (cec_fullstack / cec_overnight) with all levers wired (layer-assignment + corridor-evict + kelvin/separate/adjacent), lane+corpus_state tagged so it's A/B-measurable. Launch detached, BABYSIT the first stretch (verify rounds tick, measurement rows write, no crash/thrash), fix issues that assert themselves.
- [ ] [added 2026-06-14 19:05 · in progress] **Make PLACEMENT a first-class EI-instrumented actuating tier** (owner: panel-review what placement implements vs how routing applies the EI points → checklist → implement all → test). Closes the FOLLOWUPS:127 G2/G3 gap: placement corridor lever (cec_place.apply_corridor_evict / cec_router.corridor_evict_repair) fires unconditionally, NO lane/corpus_state/measurement-row, NOT model-proposable, cec_fs_actuator `replace` Delta inert. Wire it to apply the SAME EI points routing does (EI-01 corpus_state, EI-02 A/B lane gate, EI-07 real_anchor_ratio, DeltaLog symmetric-outcome + control-rollback, fence/bound, OWNED_LEVERS). Panel workflow launched.
- [x] [added 2026-06-14 19:23 · done 2026-06-14 19:35] **Claude-RC session survivability** (owner interrupt: tmux + recovery script + WSL config + wire the RC systemd auto-reup to tmux). DONE (artifacts in /home/nathan/CEC-Platform/ops/, stable main checkout): (1) `claude-rc-tmux.sh` — systemd ExecStart wrapper runs each `claude remote-control` bridge inside a DEDICATED tmux server (socket claude-rc, session crc-<proj>), foreground-waits so systemd Restart=always still reups; tmux survives console drops + is attachable (`tmux -L claude-rc attach -t crc-CEC-Platform`). (2) `claude-session.sh` — persistent tmux launcher for the INTERACTIVE `claude` (survives terminal drops; re-attach by re-running). (3) `rc-recover.sh` — clean-restart the zombied bridge via systemctl, with a self-guard against killing the current session. (4) Edited `~/.config/systemd/user/claude-rc@.service` ExecStart→wrapper + ExecStop=tmux kill-session + KillMode=mixed; daemon-reloaded (LIVE on next WSL restart, or `systemctl --user restart claude-rc@*`/rc-recover.sh to apply now). Canonical unit + README snapshotted to ops/. tmux wrapper mechanics self-tested (create/env-inject/supervise-exit all verified). WSL mirrored-networking DEFERRED → FOLLOWUPS (risk to the tuned compute-plane NAT: localhostForwarding + broker:8080 ↔ host-LLM:8007 ↔ containers) — owner decision pending.
- [x] [added 2026-06-14 19:40 · done 2026-06-14 20:05] **Implemented the placement EI-parity checklist PL-03…PL-10** (PL-01/02 done earlier). PL-03: cec_place.nearest_evict_delta (dedup, regression-pinned) + sorted _cluster + apply_corridor_evict gains fence param + restore record; cec_router.corridor_evict_repair→place_cluster (cluster+fence-aware, RS*/J* skip) + apply_edit place_cluster case. PL-04: cec_fullstack.apply_placement_move (in-container, resolves body+band via corridor_violations, 2nd fence wall via is_fenced, evicts on a COPY of the committed→placed-r{N}.kicad_pcb, restore record) + cec_place.restore_poses. PL-05: _placement_source_for (control NEVER sees override) + --board-pcb-override threaded through route_one_worker/_exec_route_one/CLI. PL-06: settle via the existing kind-opaque DeltaLog + two-phase placement_base promote(vindicated)/revert(refuted/control) + refuted_placement_keys anti-ratchet. PL-07: gate-launder INHERITED (the pour_integrity→gates_pass fold at cec_fullstack:1761 runs on the moved+routed board; a fragmented move can't vindicate) + test. PL-08: _placement_row_fields on the measurement row (lane+corpus_state ride it) + placement_moved_rate/vindicated/refuted in ab_aggregate/render_ab_table. PL-09: real_anchor_ratio placement_moved kwarg (applied move=+1 deterministic, model unchanged, default no-op). PL-10: +27 tests across test_fs_actuator/test_prompt_audit_fixes/test_corridor_model/test_ei02_control_lane + NEW test_placement_actuation_e2e.py (host dry-run + REAL container leg) + test_fs_actuator wired into checklist.sh. VERIFIED: checklist exit 0 ("host suites pass"); 27 tests green both orders; e2e 6/6 incl. real container apply_placement_move; SB-08 IDENTICAL to committed baseline (max_T 253.2 = pre-existing owner-gated AM-04 red, NOT a regression). Found+fixed a real bug (CONTAINER_ROOT undefined in cec_fullstack) via the container leg. NEXT: adversarial audit.
- [x] [added 2026-06-14 20:05 · done 2026-06-14 20:45] **Adversarial audit of the placement EI-parity impl + REMEDIATION** (wf wmuedf7s6: 28 agents, 4 skeptics → verify each → synth; verdict needs-work, 1 blocker + 4 high + 4 med + 2 low confirmed, 10 dismissed). ALL FIXED + missing-tooth tests added: **BLOCKER-01** gate-launder (settle_outcome credits a both-gates-fail objective_base win which is pour-BLIND → a fragmented-pour move could 'vindicate') → KIND-AWARE fix `cec_fullstack._placement_keep(verdict,pour_integrity_ok)` (keep iff vindicated AND pour_integrity_ok is not False) so a fragmenting move is never promoted; settle_outcome UNTOUCHED (routing progress-crediting preserved — the audit's blanket-fix would've regressed routing). **FENCE-01** manager place_cluster bypassed the fence → corridor_evict_repair now derives+skips the locked sense ICs (union of cables' sense_ics) + accepts a fence param + stamps it into the edit; apply_edit passes fence to apply_corridor_evict (+del b). **AUDIT-PL4-001** CONTROL_EVERY<=0 deadlock → no-baseline settle branch drops+reverts (releases the one-in-flight guard). **MED-02** silent override-missing fallback → host-side isfile pre-check reverts+drops the unsettled move. **CTRL-01** _placement_source_for now gets concrete committed. **SWIG-01** del b. **AUDIT-PL4-003** skipped-delta logged. **TEST-RIGOR-01/02/03 + DETERMINISM-01** tests added. VERIFIED: checklist exit 0; test_fs_actuator 26 / test_ei02 32 / test_prompt_audit 34 / test_corridor_model 62 / e2e 8/8 (real container legs) green. KEY JUDGMENT: fixed BLOCKER-01 kind-aware (placement-scoped) NOT via the audit's literal settle_outcome change, per the audit's OWN residual-risk note that a blanket gate-dominance would break routing.
- [x] [added 2026-06-14 20:45 · done 2026-06-14 20:55] **Focused re-audit — VERDICT: SHIP** (wf wzgvmfh1r, 4 agents). All 8 prior findings independently verified CLOSED with code citations + a full green test run (test_ei02 32 / test_corridor_model 62 / test_fs_actuator 26 / test_prompt_audit 34 / test_stagger_feedback 12 / e2e 8). ZERO new blocker/high defects. Confirmed: _placement_keep applies only to placement deltas (settle_outcome byte-identical → routing preserved); pour_integrity_ok is always a strict bool on both route paths so `is not False` can't be bypassed; AUDIT-PL4-001 drops only settlement bookkeeping (routing carry untouched); FENCE-01 refuses locked sense ICs while ESP still moves. **PLACEMENT EI-PARITY (PL-01…PL-10) IS COMPLETE + FULLY AUDITED.**
- [x] [added 2026-06-14 20:53 · done 2026-06-14 20:56] **Found: the body-in-band placement lever is INERT on eps** (corridor_violations(committed eps)==[]; routing never moves parts → []every round). eps's cc=6 stall = foreign SIGNAL nets crossing the corridor (routing/LAYER problem), orthogonal to body-in-band eviction. So a placement-enabled run on eps would show "proposed→no_corridor→inert→stalls" — reconfirms the layer lever is eps's fix, NOT placement.
- [x] [added 2026-06-14 21:00 · done 2026-06-14 21:12] **Design plan: upgrade the synth placer before the Hub run** (wf w31yt6d78, 9 agents → docs/placer-upgrade-2026-06-14/plan.json). Verified LIVE: the committed Hub is ALREADY ROUTED (length 2359 / 769 tracks / kelvin+diffpair PASS) so calibration ground-truth is FREE; the placer is ~1.8x wirelength (HPWL 3677 vs 2002) / ~5x congestion (rudy 15.5 vs 3.0) worse than the hand Hub, and the proxy already discriminates them (so "rank the reference high" is well-posed). TWO real blockers reproduced: (B1) materialize() FileNotFoundError — the Hub dir has no .net, cfg.net='' → build_board crashes; (B2) seed_anchors mis-groups connectors (RJ-45 'host' + USB + J_5VSB all collapse onto the 'right' edge; _role mis-keys J_5VSB). MINIMAL-VIABLE (≈2.25 days S/M, host-runnable, enough to LAUNCH a scored+similarity-validated Hub run): MV1 feed netlist→materialize (the unblocker, S); MV2 derive Stage-1 (edges/outline/mounts/antenna) FROM the reference → fixes the wrong frame (M); MV3 reproduce-the-reference similarity metric = the validation number (M); MV4 wire the computed rudy/thermal proxy into selection (currently THROWN AWAY — sort key is only (residual,corridor_cross,hpwl)) (S); MV5 the 4 Hub-domain terms (RJ-45 even-spacing/antenna-on-edge/power-cluster/USB-ESP) (L). SEQUENCE: MV1 unblocks→MV2 correctness→MV3 instruments→MV4 free leverage→MV5 measured quality. GUARDRAILS (over-engineering to AVOID): no routing campaign for calibration (Hub already routed); defer Tier-B least-squares regression; thermal-sep/symmetry/LED terms are not first-run gates; don't refactor the anneal hot loop; oracle-seeding only as ONE competing candidate (overfitting).
- [ ] [added 2026-06-14 21:12 · in progress] **BUILD the placer-upgrade set MV1→MV5** (owner: do the whole stackup) then launch the Hub full-stack run scored vs the reference. Plan: docs/placer-upgrade-2026-06-14/plan.json. **GOVERNING CONSTRAINT (owner): the reference must NOT teach board-specific rules — encode the underlying WHY, treat the reference like the holdout (validate, never tune toward) or it's corpus-poisoning overfit.** Codified → docs/placer-upgrade-2026-06-14/anti-overfit-charter.md (3 rules: rules encode WHY-not-WHERE; reference=validation-only never an optimization target; per-board REQUIREMENTS [outline/edge/mounts] ≠ generalizable RULES). DONE: **MV1** (S) — _ensure_netlist_path + materialize() now sch-exports a netlist when cfg.net='' → a Hub candidate MATERIALIZES (tested in-container: place_candidates@98x74 residual 7 → materialize wrote 80-part board → read_placement parity; the FileNotFoundError blocker is gone). REMAINING: MV2 (split: GENERAL _role connector-function fix [principled] + per-board edge/outline/mount INPUT derived-from-reference-as-requirement [tagged board-specific, not a rule]); MV3 similarity DIAGNOSTIC (never a rank key); MV4 proxy_score (reference normalizes scale only, no per-board weight regression); MV5 the 4 net-derived domain terms (variance/edge/cohesion/proximity, no hardcoded ref values).
- [ ] [added 2026-06-14 20:56] **PIVOT (owner): validate the full stack on the HUB (a real fab-ready board) from a base stackup, using the committed Hub as a READ-ONLY reference oracle** — instead of eps (never hand-finished). DOABILITY CONFIRMED: Hub netlist exports+parses (77 comps / 83 nets), Config.load('hubs/hub-standard') + View ingest it, _classify partitions cleanly (8 connectors [4×RJ-45 J2-J5 + J_5V/J_5VSB power-in + J_KVM + J_USB], 9 ICs, 0 shunts, 60 passives), run_pipeline/run_sweep accept a board path, Hub is 4-layer + has fab/hub-standard-proto-v1. NOTE shunts=0 → corridor formation inert (cleaner generic-placement test than eps). CAVEATS: synth placer is DOMAIN-BLIND (CLAUDE.md item -2: no model of RJ-45 spacing / ESP antenna keepout / power front-end / LED chain) → produces routable-but-not-hand-quality; Stage-1 asks must be supplied (--answers, derivable from the reference Hub); "base stackup" = the Hub's 4-layer GND-plane. PLAN: extract netlist+reference (read-only→build/) → derive Stage-1 answers from the ref → run synth place→route→check → score the gap vs the fab Hub (HPWL/area/DRC/gates). NEXT: scope + kick off a bounded first pass.
- [x] [added 2026-06-14 19:40 · done 2026-06-14 20:05 → split into the two lines above] **Implement the placement EI-parity checklist (PL-01…PL-10)**. DONE + TESTED: **PL-01** (cec_fs_actuator.py) — `replace` Delta now carries a LIVE placement intent (`_placement_intent`: ref-target→{ref,op evict,band None,cluster True}, net-target→ref None for on-board resolution); v4_structural_escape populated; Delta.as_record compact-summarizes a placement intent; fence still refuses pinned/Kelvin BEFORE intent build. +5 tests (tests/test_fs_actuator.py TestPlacementDeltaLive), 22/22 green. **PL-02** (cec_fullstack.py) — placement clause added to OWNED_LEVERS, worded WITHOUT 'corridor'/'pour'/'around' so a model echo classifies to 'replace' not 'avoid' (verified _lever_kind round-trip). +3 tests (tests/test_prompt_audit_fixes.py TestPlacementLeverAdvertised), 34/34 green. REMAINING: PL-03 (dedup nearest_evict_delta + cluster/fence-aware manager corridor_evict_repair=place_cluster + sorted _cluster; cec_place/cec_router/test_corridor_model), PL-04 (apply_placement_move consumer, in-container, 2nd fence wall, edits committed COPY→placed-r{N}, restore record), PL-05 (lane-gate + thread --board-pcb-override into route_one_worker/_exec_route_one), PL-06 (settle via existing DeltaLog + two-phase override promote/rollback + _refuted_placement_keys), PL-07 (gate-launder guard: pour-integrity into gates_pass on moved+routed board), PL-08 (lane+corpus_state placement fields on measurement row + A/B table), PL-09 (applied-move=deterministic anchor EI-07, default no-op), PL-10 (update existing tests + e2e dry-run + host suite both orders + SB-08 in-container). Full checklist: docs/placement-ei-parity-checklist.json.
- [x] [added 2026-06-14 19:18 · done 2026-06-14 19:25] **Panel review DONE** (wf wxo5qk6l0; 9 agents: 4 recon → 4-lens panel → synthesis). Produced a 10-item dependency-ordered checklist (PL-01…PL-10) + 8 invariants + 38-test matrix + 4 open questions. Saved → docs/placement-ei-parity-checklist.json. CHECKLIST: PL-01 live `replace` intent (kill intent=None in finding_to_delta + v4_escape); PL-02 OWNED_LEVERS placement clause (worded w/o 'corridor' to dodge _lever_kind misclassification); PL-03 dedup nearest_evict_delta + cluster+fence-aware manager corridor_evict_repair (place_cluster) + sorted _cluster; PL-04 apply_placement_move consumer (in-container, resolves body+band, 2nd fence wall, edits committed COPY→placed-r{N}.kicad_pcb, restore record); PL-05 lane-gate + thread --board-pcb-override into route_one_worker/_exec_route_one (control routes committed); PL-06 settle via existing kind-opaque DeltaLog + two-phase override promotion (vindicated-only) + rollback + _refuted_placement_keys; PL-07 gate-launder guard (fold pour-integrity into gates_pass on moved+routed board); PL-08 lane+corpus_state placement fields on measurement row + A/B table; PL-09 applied-move = deterministic anchor (EI-07, default no-op); PL-10 update existing tests + e2e dry-run + host suite both orders + SB-08 untouched. OQ DECISIONS (panel-recommended, owner-vetoable): OQ1 fullstack-loop-scoped; OQ2 applied-move=deterministic anchor; OQ3 word OWNED_LEVERS w/o 'corridor'; OQ4 vindicated override persists/compounds, reverts on refute/overturn/control.
- [x] [added 2026-06-14 19:10 · done 2026-06-14 19:10] **Recon/grounding (pre-panel)** — mapped the exact placement-vs-routing EI gap in the live code. FINDINGS: (1) DEAD-END is `cec_fullstack.py:1932-1941` — at the actuator BOUND step ONLY `kind=="avoid"` deltas with `intent` feed the live carry `pending_corridor_avoid`; a `replace` (move/rotate) delta from `act.finding_to_delta` (:1915) is `kept` but its `intent` is None (`cec_fs_actuator.py:108-109`) and NOTHING consumes it → inert. (2) DOWNSTREAM IS ALREADY KIND-AGNOSTIC + reusable: control-gate settle `dlog.record_outcome` (:1862-1900) rolls back any non-vindicated delta; measurement row (:2001-2028) already carries lane/corpus_state(EI-01)/real_anchor_ratio(EI-07)/n_deltas_applied; `pending_corridor_avoid` (augmented-only :1622, suppressed-on-control :1627) is the EXACT carry pattern to mirror. (3) MECHANISM: each round routes from the committed board `ovd.BOARD_PCB[board]` via `route_directed(board_pcb,intents,...)`; placement = apply `cec_place.apply_corridor_evict(board,ref,band)` (already moves part+cluster, excludes structural RS*/J*, containment guard e01ddd7) to a COPY on the augmented lane, route that copy; control routes unmutated = rollback baseline. `apply_directive` dispatches an `evict` directive. (4) TO-WIRE: make `finding_to_delta` replace branch resolve a real intent {ref,band,op} (not None); add a `pending_placement` carry mirroring pending_corridor_avoid; add placement to `OWNED_LEVERS` (:83) so the model proposes it; fence must refuse moving a pinned/Kelvin/shunt ref.
- [~] [added 2026-06-14 07:25 · obsolete 2026-06-14 07:45 → done above] Phase 1+2 audit remediation (superseded by the done line above) Audit CONFIRMS cc=6 is the real hand-board floor (analytic: shared I2C bus → 4 forced + DETC1 + THRESH), but the placer is now WORSE (legal cc 8-24, not "hand-quality") — overhang(192503d) fixed residual but not peripheral placement. Fixes: (1) doc CORE-PREMISE retract "cc=6 never lower / hand-quality corridors" → honest legal cc + the analytic floor; soften "topological invariant"/"no channel"/"is exactly how committed routes". (2) build_corridor_model/corridor_cross_count MISSING the shared-bus exclusion (spurious 12vhpwr rank key) → factor out + apply. (3) overhang value "edge"→"power_able" (canonical). (4) _seed_corridor_spine clamp pads to [0,W]. (5) tests: fix 24pin filename (silent skip), assert residual==0 + band-width in test_phase2, add _corridor_veto unit test. (6) FOLLOWUPS: residual-misattribution now moot (residual 0). Defer (FOLLOWUPS): seg-AABB, _P/_N classifier parity, degeneracy pre/post-inflation.

## Done / obsolete (history)

- [x] [added 2026-06-13 19:45 · done 2026-06-13 20:25] Prompt-tier audit (Claude panel + DeepSeek cross-check): catalog + 30 confirmed/3 rejected + punchlist
- [x] [added 2026-06-13 20:30 · done 2026-06-13 23:00] Implement prompt-audit fixes (P1-P12 incl. H1/M2/H2/M1/M3/M4/M5 + P5/P6) → PR #56 (20/20 host tests)
- [x] [added 2026-06-13 21:55 · done 2026-06-13 22:10] Bot-push guard hook + gh-bot wrapper (push/gh as the bot, not the owner) → PR #57
- [x] [added 2026-06-13 22:48 · done 2026-06-13 22:48] FOLLOWUPS SessionStart hook + FOLLOWUPS.md → PR #58
- [x] [added 2026-06-13 23:25 · done 2026-06-13 23:28] TODO SessionStart hook + TODO.md → PR #58
- [x] [added 2026-06-13 22:40 · done 2026-06-13 22:48] Render eps-8pin before/after board PNGs
- [ ] [added 2026-06-15 01:35] **Integrate the agentic model stack into the placer/Hub route pipeline** (owner: panel the proper integration + toggle cloud/local, default overnight→local & short-midday→cloud; forensics on why it wasn't wired despite the docs → doc). Workflow: forensics (why bypassed) → design panel (tier wiring / toggle+defaults / corpus gating / placer-router feedback + dashboard) → implement → forensics doc.
- [x] [added 2026-06-15 01:55 · done 2026-06-15 02:25] **Forensics + design-panel continuation** (wf_cf91431f-a2a, 7 agents). Recovered 3 cached findings from the killed run + regenerated the seam inventory → forensics synth (smoke-first ossification root cause; 3-kinds-of-not-wired distinction) + 4-lens design panel → design synth (Path A wiring / cloud-local toggle / corpus gating / dashboard, 11-step checklist). Synopsis+forensics+design → **docs/agentic-integration-forensics-2026-06-14.md**.
- [x] [added 2026-06-15 02:25 · done 2026-06-15 03:05] **Implement the agentic integration (owner: "implement them + fix the stack to use the proper wiring")**. Owner default RESOLVED: explicit `--seats/--judge` wins; else `--hours>=2 → local`; else cloud (defaulting to cloud); cloud effort=MAX in all cases (Opus 4.8 max). DONE (checklist steps 1-10 + dashboard + audit): cec_seats.py resolver; model=/url=/effort= threaded through make_manager_swarm/make_worker_swarm→_panel→_chat_json→_chat_json_cloud (--effort max); hub_pipeline_run.py `--seats` + tier wiring into cec_router.route (cloud panel=1/local panel=3, fail-safe→deterministic) + post-route conformance SELECTION gate + REF intake + cec_policy.assert_loadable + corpus_fit_review sidecar + ledger; hub_run.sh forwards toggle/effort + broker --add-host; cec_dashboard.py agentic-decisions card (/api/agentic). **Adversarial audit wf_28d3ca68-a24 (5 skeptics→verify→synth): ship-with-fixes** — 2 cloud-effort defects + doc-accuracy FIXED (commit 31daa59). Commits 8ef8e07→5f1d7e5→f738df1→31daa59 (PR #60). 240 host tests green. Doc: docs/agentic-integration-forensics-2026-06-14.md. DEFERRED→FOLLOWUPS (step 11): Path-B generalization (find_board hubs/ + register Hub in BOARD_PCB + placer↔router feedback) + cloud-seats-in-container claude CLI + conformance abort-level promotion.
- [ ] [added 2026-06-15 03:05 · restarted clean 03:57 · in progress] **Overnight ~10h full-pipeline run (owner ask)** — `CEC_FS_DATE=2026-06-15 cec_fullstack --board eps-8pin --hours 10` (**PID 1233137**, run dir docs/fullstack-run-2026-06-15). RESTART REASON: the box local date is 2026-06-14 (CDT) but I'd named the dir 2026-06-15 (UTC) → cec_fullstack's PERM (strftime %Y-%m-%d) wrote measurement/findings/bundle into docs/fullstack-run-2026-06-14 (co-mingling with yesterday's run + blind dashboard + empty babysit). Fixed by relaunching with CEC_FS_DATE=2026-06-15 so ALL artifacts co-locate. Owner CONFIRMED the auditor proposed_lever being advisory/noop is BY DESIGN (shadow ruling for EI-02 self-influence proof) — NOT changed. Watchdog relaunched (PID-rotated, CEC_FS_DATE=2026-06-15 + CLAUDE_PROJECT_DIR=worktree). Note: yesterday's docs/fullstack-run-2026-06-14 archive is slightly polluted (aborted run appended ~3 measurement rows + overwrote round-001..003 findings) — left as-is (past archive). FULL T0-T9 + EI-02 A/B control lane (control_every=4) + corpus gating (brief 35/6) + fence (nets=4 refs=5) + **placement actuators VERIFIED wired** (_t0_should_fire→gr02_repair line 1975/1981; finding_to_delta→select_deltas→apply_placement_move 2094/2112/2129; control-gated record_outcome + _placement_keep gate-launder guard + fence + rollback). Auditor=deepseek-v4-flash (warmed). Watchdog PID 1199904 (worktree-pinned, crash-relaunch only). Panel http://localhost:8095. CAVEAT: on eps the body-in-band placement lever is largely inert (eps's stall is foreign-signal-in-corridor = routing/layer, not placement) — actuators are PRESENT+ready, may not fire much; eps's active fix is the layer lever. BABYSIT: monitor run.log + measurement.jsonl + :8095; converges or 10h deadline.
