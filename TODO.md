# TODO

The LIVE list of what the agent is ACTIVELY working on, in checkbox form. **APPEND-ONLY** — items are
never deleted; completed / obsolete ones stay as a timestamped history.

Format:
- `- [ ] [added YYYY-MM-DD HH:MM] <task>` — active
- `- [x] [added YYYY-MM-DD HH:MM · done YYYY-MM-DD HH:MM] <task>` — completed (left in place)
- `- [~] [added YYYY-MM-DD HH:MM · obsolete YYYY-MM-DD HH:MM → <tombstone>] <task>` — obsolete, with a
  tombstone pointing where it went (e.g. `FOLLOWUPS.md "<entry>"`, `PR #N`, `docs/owner-queue.md`, another line)

Distinct from FOLLOWUPS.md (deferred / non-blocking backlog) and docs/owner-queue.md (owner-action items).
Times are UTC.

## Active

- [x] [added 2026-06-24 05:20 · done 2026-06-24 05:35] **12VHPWR thermal integration — wire production case-cooling into the dashboard** (closes the named FOLLOWUP). board_thermal_config now returns a 4th `cooling` spec; render_per_layer applies the metal-case model (TIM on RS1-6 shunts + M3 mounts) so the 12VHPWR dashboard shows the owner-validated maxT 72.95 / dT 22.95 = PASS, not the still-air 151/dT101. Env knobs CEC_THERMAL_NO_COOLING / _TIM_WK / _MOUNT_WK walk the envelope. Verified in-container (prod / still-air / conservative-TIM). SB-08 golden proven INERT (cec_golden imports cec_fr/cec_score/cec_synth_pipeline only). Committed bb60c65 (thermal) + 55be140 (place-planner WIP banked). EPS/PCIe generalization → FOLLOWUPS (owner sign-off).
- [x] [added 2026-06-24 06:05 · done 2026-06-24 06:55] **24-pin ATX shrink study (owner overnight ask)** — got the loop running on the 24-pin; compared the two connector revisions. RESULT: rev2 (90° rotated output, modules/atx-24pin-rev2) = 6576 mm², routes clean, **21% SMALLER than straight-through (8342 mm²)** — owner's hypothesis confirmed. Both at their rigid-shrink floor (0%, connector/part-bound). Found ~1270 mm² (~19%) dead space in rev2's stale U1 keepout (lib already 16×21, board 45×35) → Update-from-Library + guided re-place could reach ~36% vs straight. Pour-resize won't shrink the outline (part-bound). Dashboard :8095 (build/24pin-rev2-shrink); writeup docs/24pin-shrink-2026-06-24.md. Next levers → FOLLOWUPS (owner greenlight).
- [x] [added 2026-06-24 05:40 · done 2026-06-24 06:15] **Push placement-corridor → PR #60** (owner: "Go ahead and push it up"). Pushed 3089680..c45c179 (thermal integration + place-planner WIP + doc fixes); no CODEOWNERS path touched.
- [ ] [added 2026-06-24 05:40 · in progress] **Fine-grained 12VHPWR board passover** (review workflow wf_b3e2a860-3a6: conformance / route-fab / thermal / loop-fit / doc-accuracy + adversarial verify). Anchored ground truth: board is FULLY ROUTED (0 unconnected, 0 parity, 15 cosmetic-silk DRC), not DRAFT, fab/12vhpwr-standard-proto-v1 exists. CLAUDE.md action-item-4 ("47 ratlines remain / Fill zones / dangling vias") is STALE → doc-fix pending the reviewer. Worst-case 12A pin-hog thermal re-checked under the new solver (result pending).

- [ ] [added 2026-06-15 00:15 · in progress] **FR-on-Hub routing FIX + full hour run** (owner: "do the fixes"). FIXED FR routing the Hub: (1) on-board OFFSET — synth 0-origin coords were placed off the committed outline at (70,90) → FR routed nothing (THE unblock); (2) `_seat_antenna_ic` — ESP off the ganged ports → residual 4→0 (+ antenna term fixed to use courtyard near-edge: ref 0.565→1.0, synth 0.0→0.903, hub_penalty 0.373→0.125); (3) two-process GND fill (Remove footgun). RESULT: low-effort route_once 628 tracks / 39 unconnected / kelvin+diffpair pass / len 2240 (was 2/216). 103 placer tests green. Discriminator test: committed HAND placement routes 389 wires (baseline). NEXT: commit + LAUNCH the hour run.
- [x] [added 2026-06-14 22:50 · done 2026-06-14 23:30 · SUPERSEDED 2026-06-15 00:15 by the FR fix above] **FULL pipeline (place→route→check) wired + run on the Hub** (owner: "if it's all wired up, run the full pipeline for an hour"). scripts/hub_pipeline_run.py wires the route leg; `materialize_onto_reference` fixed the materialize gap (build_board output isn't DSN-exportable → copy the committed Hub + reposition, in a spawn subprocess to dodge the pcbnew Remove footgun). Chain runs end-to-end. **BUT Freerouting does not route the Hub** — emits 1 wire (FR log: autoroute pass#1 "1 change" → pass#2 "0 changes" → stops), 216 unconnected; the identical path routes cable modules to 556 tracks. So an hour run is NOT worthwhile (reproduces the 1-trace result) — reported instead of burned. Placement is solid (correct frame, gates pass, max_T 68.7). FR-on-Hub fix path → FOLLOWUPS item 4. Findings: docs/placer-upgrade-2026-06-14/STATUS.md "FULL PIPELINE".
- [x] [added 2026-06-14 22:40 · done 2026-06-14 23:15] **Hub pipeline TEST RUN (in kicad10 container)** — place→materialize→render→DRC→score ALL ran on hubs/hub-standard with the oracle. Result: correct oracle frame (98.1×74.1, 8 edges), 80-part board, residual 4, corridor_cross 0, **similarity 0.705**, **HPWL 1.25× the hand board (down from 1.84× pre-MV)**, hub_penalty 0.373 (antenna 0.0 = the known gap). DRC 575 (~506 cosmetic silk/lib; ~64 real-structural tracking residual-4; 211 unconnected = UNROUTED, route leg not wired). **The pipeline WORKS at the placement level.** Recipe + numbers: docs/placer-upgrade-2026-06-14/STATUS.md "HUB PIPELINE TEST RUN".
- [x] [added 2026-06-14 21:50 · done 2026-06-14 22:55] **MV2→MV5 placer-upgrade build + consultative audit** (owner: fan out for impl + audit at the end). IMPL committed 26d9a77; 4-skeptic parallel audit (geometry/charter/regression/test-rigor) = SHIP-WITH-FIXES, 0 blocker; ALL findings verified + fixed in a follow-up commit (H1 similarity renorm, M2 sense-net rail exclusion, M3 edge_override validation, charter MEDIUM topological power-loop derivation, regression MEDIUM oracle-failure logging, 2 HIGH test-rigor MV3/MV4 plan-validation tests + scramble/sort-key teeth + LOW comments). 41 tests green, checklist exit 0. Status+audit: docs/placer-upgrade-2026-06-14/STATUS.md. DEFERRED→FOLLOWUPS: MV5 generative closers + the Hub full-stack run.
- [x] [added 2026-06-14 21:50 · superseded by line above] **MV2→MV5 placer-upgrade build** (continue the MV checklist; owner: fan out for impl if needed + consultative audit at the end). DONE (cec_synth_pipeline.py + tests/test_placer_oracle.py, 31 tests, checklist exit 0): MV2 net-aware `_role`/`_connector_net_role`/`_is_rail_net` + `oracle_stage1_answers`/`apply_oracle_stage1` + `seed_anchors(edge_override=)` 4-edge + `place_mechanical(mount_pos_override)`; MV3 `oracle_similarity` DIAGNOSTIC (identity 1.0/scramble<1, NEVER a rank key — `_candidate_sort_key` test enforces); MV4 `proxy_score` (==hpwl with no ref → zero regression; ref-normalized HPWL-dominant composite) + sort-key swap + cfg knobs; MV5 `build_hub_model`/`hub_score` (port_even/antenna/power_cluster mux-loop/usb_prox, gated to ≥2 ports+ESP, reference scores well hub_penalty≈0.27 vs synth≈0.60). Status: docs/placer-upgrade-2026-06-14/STATUS.md. Anti-overfit charter honored (reference=validation only). DEFERRED→FOLLOWUPS: MV5 generative closers (power cohesion sweep + antenna-edge ESP seed). NEXT: parallel adversarial audit → verify → fix → SB-08 in-container.
- [x] [added 2026-06-13 23:25 · done 2026-06-13 23:33] Await DeepSeek new-impl audit → consolidate the Opus-4.8 + DeepSeek verdict (both merge-ready, 0 blockers; DeepSeek's 3 HIGHs = diff-only false positives)
- [x] [added 2026-06-13 23:33 · done 2026-06-14 00:10] new-impl polish fixes — #56 portion: H1 temper T0-overpromise prompt; M1 corridor fallback+log; M2 lane-gate T0/kelvin_stall (_t0_should_fire helper); M3 wire host suites into checklist.sh+CI; L3 in_family_only n_in; L4 progress history[:-1]; L1 deepseek_audit _coerce parity; L6 delete dead PENALISABLE (+6 tests; 159 host tests green)
- [x] [added 2026-06-14 00:10 · done 2026-06-14 00:30] new-impl polish fixes — #57 portion (claude/bot-push-guard, commit 70e176d): H2 pre-push GIT_TERMINAL_PROMPT=0 on the credential-fill probe (fail-closed, no-hang verified); L14 drop unused `root` var. Deferred pre-push LOWs L12/L13/L15/L16 → FOLLOWUPS.md (owner-scoped)
- [x] [added 2026-06-14 00:35 · done 2026-06-14 01:05] Adversarially verify the #56 polish fixes (wf_789eb5bc-b59, 8 skeptics + synth = SHIP-WITH-FIXES) → harden M1 (log even on empty fence) + L3 (header counts shown-under-truncation) + 2 tests; residuals (12vhpwr corridor siting, run()-level H1/L4 coverage, verifier worktree-isolation) → FOLLOWUPS. 161 host tests green
- [ ] [added 2026-06-13 23:25 · in progress 2026-06-14 01:20] Build `claude/seat-bakeoff` PR: cloud-seat shim → 2-D variant×model bake-off → 6-judge leave-one-out panel → de-overfit → data-chosen `--seats cloud` defaults (+ DeepSeek `--stream` live thinking). Owner: BUILD + RUN the full sweep.
- [x] [added 2026-06-14 04:33 · done 2026-06-14 04:50] Corridor-aware reseed placer **Phase 0** (docs/placement-strategy-2026-06-14.md): `build_corridor_model` + `corridor_cross_count` (cec_synth_pipeline) + the two missing checkers `shunt-inline-in-corridor` / `high-current-corridor-keepout` (cec_constraints) + `tests/test_corridor_model.py` (19 tests). PROVEN on committed eps-8pin: model reports ≥3 through-crossers (/DETC1,/THRESH,/I2C), /CAN_L=0; keepout checker has teeth (foreign track→FAIL). Wired into checklist host suite (134 green). Branch claude/placement-corridor.
- [x] [added 2026-06-14 04:50 · done 2026-06-14 05:25 · CLAIM RETRACTED 2026-06-14 06:00 → see audit-remediation line] Corridor-aware reseed placer **Phase 1a**: `Candidate.corridor_cross` + model build in synth_one + sort key `(residual, corridor_cross, hpwl)` + opt-in `proxy_reject(corridor_max=)`. ⚠ The "ranking alone breaks the ceiling" claim was a measurement artifact (degenerate bands) — RETRACTED by the audit; Phase 1a stands as honest rank-key plumbing only. Real ceiling-break = Phase 2 (corridor formation).
- [x] [added 2026-06-14 05:40 · done 2026-06-14 06:00] **AUDIT REMEDIATION** of Phase 0/1a (4 parallel skeptics, PR #60). BLOCKER confirmed + claim RETRACTED: synth winner's `cc=0` is a degenerate-band artifact (shunts land x~7.5 vs connectors x~30-43 → bands ~73mm/96mm, can't be straddled). Fixed: (1) band from connector+2-pad-shunt pads only, exclude INA SMD (matches derive_power_pours; reviewer A F-1, B F-4). (2) `formed`/degeneracy guard (`board_w`) → unformed corridors score 0 INERTLY not falsely-clean. (3) checker false-FAILs on 12VHPWR/24-pin (shared-bus → N/A, Phase-5 variant) + `_sense_nets` exclusion (reviewer B F-1/2/3/4 — 2 BLOCKERs). (4) determinism: sorted() set iteration in relative_place/anneal_macros (reviewer C F-2 — compact s6 now stable 0, was {0,1,2}). (5) tests: place_candidates production-sort (tautology, reviewer D F-1 BLOCKER) + via teeth + shunt-FAIL teeth + shared-bus-NA + multi-cable + no-kelvin + degenerate-guard; honest reframe (34 corridor / 159 host green). (6) doc claim retracted honestly. Deferred→FOLLOWUPS: seg-AABB exact, CI pcbnew wiring, parity re-freeze (owner).
- [x] [added 2026-06-14 06:25 · done 2026-06-14 06:55] **Phase 2 — corridor FORMATION** (89266ab): `_cable_topology` + `_seed_corridor_spine` (align J_OUT's force-pad COLUMN under J_IN's, seat shunt on the column axis at rot270/H3 as fixed anchors) + `_corridor_veto` + `anneal_macros(veto=)` (no HOT/SENSITIVE body in a foreign band). Synth bands now 15.6mm = the committed board's. **KEY FINDING: cc=6 is the INHERENT FLOOR** — the committed (best hand) board is also cc=6; the 6 foreign signals must cross the full-height current corridors to reach the central ESP. Placement CANNOT reach cc=0; the pour-cut failure is route-time LAYER ASSIGNMENT, not placement. OPEN (FOLLOWUPS): residual regressed 2→6 (veto/legalizer tuning); the layer-assignment pivot is the owner's call.
- [~] [added 2026-06-14 06:25 · obsolete 2026-06-14 06:40 → re-run clean] **Audit (1st attempt) corrupted the shared worktree** — a verify-agent's `git checkout` reverted my uncommitted Phase 2 edits (worktree-isolation hazard). Stopped (TaskStop wpnhp01ff), re-applied Phase 2, committed. Re-run on the committed tree (no uncommitted work to lose) → next active line.
- [ ] [added 2026-06-14 06:55 · in progress] **Audit the remediated Phase 0/1a + Phase 2** (workflow wt1frw8xk, on the COMMITTED tree c867f34, verify-agents in isolated worktrees so a git-checkout can't lose work). Triage + verify + fix confirmed findings. NOTE: launched before the overhang commit 192503d; reconcile the cc-floor over-claim (channels DO exist -> the metric over-counts) when it lands.
- [x] [added 2026-06-14 07:00 · done 2026-06-14 07:05] **Auto-overhang cable ports** (owner insight, 192503d): synth_one defaults connector_overhang="edge" for cable-corridor boards -> J_IN/J_OUT seat at edges, corridor spans full height, **residual 6→0**. Plot tool scripts/gen-corridor-plot.py. Remaining: cc 14 vs committed 6 = peripheral parts (CAN/USB/ESP) scattered into crossing positions → next lever "keep non-cable nets off the corridor side".
- [ ] [added 2026-06-14 07:05] **Peripheral placement lever**: keep non-cable parts (CAN xcvr, USB, ESP, RJ45) clustered away from the corridors so their nets don't cross (the committed board does this → cc 6 not 14). Distinct from corridor formation.
- [x] [added 2026-06-14 07:25 · done 2026-06-14 07:45] **Phase 1+2 audit remediation** (4d2c98e): shared-bus exclusion in build_corridor_model (12vhpwr/24pin inert), overhang value→power_able, spine pad-clamp, corridor_cross docstring de-overclaimed, doc CORE-PREMISE rewritten honestly (cc=6 = hand-board analytic floor, placer is WORSE at legal cc 8-24), 24pin test-filename fix + residual==0/band-width asserts + _corridor_veto/_shared_bus unit tests. 37 tests green.
- [x] [added 2026-06-14 07:45 · done 2026-06-14 08:05] **Wire the corridor lever into the tier loop** (c25ee66): `corridor_violations` (board→sensitive bodies in foreign formed bands) + cec_place `apply_corridor_evict`/`evict` directive/refine injection + cec_router `corridor_evict_repair` in MANAGER_REPAIRS. Verified end-to-end (detect→evict→cleared; manager emits place_nudge). +6 tests (43/68 green). Lever in BOTH planes (nightly refine + routing-tier manager).
- [x] [added 2026-06-14 08:05 · done 2026-06-14 08:25] **Panel: corridor lever vs EI + routing tier** (wzx07zl6f, WORKS-WITH-GAPS). Findings: shape-PARITY with the routing tier confirmed (corridor_evict_repair ≅ kelvin_inversion_repair; dual-plane). **G1 (HIGH) FIXED (e01ddd7)**: apply_corridor_evict had no containment guard → dragged the corridor shunt in refine() → false-clean. Deferred → FOLLOWUPS: G2/G3 (EI-instrumentation: not lane-tagged / no corpus_state / not model-proposable / replace-Delta inert) = the overnight-run prereq; cluster-divergence (router nudge bare-ref vs cec_place carries cluster); band-edge math duplicated across planes. DOMAIN: the evict lever clears body-in-band but is ORTHOGONAL to corridor_cross (the cc 14→6 driver) → reconfirms the LAYER lever is the real fix.
- [ ] [added 2026-06-14 08:10] **ROADMAP (owner, 2026-06-14): (1) land the panel/audit → (2) add the LAYER-TIER lever → (3) overnight full-loop run.** Layer lever = the route-time fix the cc=6 floor finding points at: route each corridor-crossing foreign signal so it does NOT neck the F.Cu+B.Cu pour pair (the 4-layer stackup carries current on BOTH outers, inners are GND/12V planes — so it's "stagger the cut across F/B so the mirror always carries + stitch", NOT "move to a free signal layer"). The `high-current-corridor-keepout` checker already identifies the crossing nets; the lever plugs into cec_fr (DSN per-net layer / pour-after-route) as a routing-tier lever. Design firms up once the panel lands. Gated on the panel.
- [x] [added 2026-06-14 08:50 · done 2026-06-14 09:10] **LAYER-LEVER REWORK** (audit w23i0d8nq: 3 HIGH — safe-but-USELESS as built; owner: HOLD the run, rework first). ALL 5 ITEMS LANDED (03c4d0b, 1ad2b42, eab3752, 837a7f4): (4/5) `_route_quality` safe-revert metric (DRC + unrouted ratlines + hard-gate penalty; +inf on failure, not the old `except:return 0` mask) + temp-staged in-place safety (fixes the copy2(path,path) crash). (2) L-bend detection: `_seg_band_clip` (Liang-Barsky) + net-extent crossing test + `_relayer_segment_inband` → multi-segment/45° crossings caught (was 0). (3) `_adopt_staggered_board` re-points rec["routed"]+refreshes metrics (never downgrades a passing board) + the pour RE-FILL that heals the corridor. (1) `_route` lays the B.Cu MIRROR + via stitching (synthesize_power_copper strip_redundant=False, additive, both lanes, _route_quality safe-revert) → the "un-cut mirror carries" premise is now TRUE. +14 tests; checklist wired.
- [x] [added 2026-06-14 09:10 · done 2026-06-14 10:25] **Re-audit the layer-lever rework + launch the clean run** (owner: run audit, implement findings, launch + babysit). DONE. 4 parallel skeptics found 1 BLOCKER (gate-launder) + 3 HIGH → fixed (fb182b5: full re-measurement closes the launder, stale-Pareto refresh, connectivity-repair via, revert-on-disconnect). Focused re-audit confirmed the BLOCKER CLOSED → 1 MEDIUM + 2 LOW fixed (ec0d1d7: objective_base refresh, FEM-stale drop, mkstemp/sidecar leak, test-isolation). Night-watch date fixed (78ae674). **7h run LAUNCHED** ~05:14 UTC (PID 842244, deepseek-v4-flash auditor); round 1 ran clean: route → 4 F.Cu + **4 B.Cu MIRROR** pours + 140 vias (item-1 confirmed LIVE) → T4 panel → T6 pour → layer-stagger (full re-measure live, safe-reverted, no launder) → T5 auditor. Panel LIVE on **:8095**. Watchdog up (PID 846815, 8h). Host suite green both test orders.
- [ ] [added 2026-06-14 10:25 · in progress] **Babysit the 7h run** — round 1 healthy in the slow deepseek-auditor stage; watchdog (846815) self-heals run/panel over 8h + health-logs every 5 min to docs/fullstack-run-2026-06-14/watchdog.log. Monitor measurement.jsonl growth + :8095. Owner signed out for the night; status in chat + handoff.
- [~] [added 2026-06-14 08:30 · obsolete 2026-06-14 08:50 → rework line above] LAYER-TIER LEVER (owner: do it): route each band-crossing foreign signal staggered across F.Cu vs B.Cu so the un-cut outer 12V pour mirror always carries (cable-board-scoped; layer-keepout sibling of the FR-02 corridor-avoid in cec_fr). EI-INSTRUMENTED from the start (lane-gated via lane_for + a corpus_state/lane-tagged measurement row + OWNED_LEVERS entry) so it doesn't repeat the corridor-evict's EI gap AND it satisfies the overnight-run prereq. Then AUDIT it (panel).
- [ ] [added 2026-06-14 08:30] **Overnight full-loop run @ 8h** (after the layer lever + its audit): the EI-02 laned loop (cec_fullstack / cec_overnight) with all levers wired (layer-assignment + corridor-evict + kelvin/separate/adjacent), lane+corpus_state tagged so it's A/B-measurable. Launch detached, BABYSIT the first stretch (verify rounds tick, measurement rows write, no crash/thrash), fix issues that assert themselves.
- [ ] [added 2026-06-14 19:05 · in progress] **Make PLACEMENT a first-class EI-instrumented actuating tier** (owner: panel-review what placement implements vs how routing applies the EI points → checklist → implement all → test). Closes the FOLLOWUPS:127 G2/G3 gap: placement corridor lever (cec_place.apply_corridor_evict / cec_router.corridor_evict_repair) fires unconditionally, NO lane/corpus_state/measurement-row, NOT model-proposable, cec_fs_actuator `replace` Delta inert. Wire it to apply the SAME EI points routing does (EI-01 corpus_state, EI-02 A/B lane gate, EI-07 real_anchor_ratio, DeltaLog symmetric-outcome + control-rollback, fence/bound, OWNED_LEVERS). Panel workflow launched.
- [x] [added 2026-06-14 19:23 · done 2026-06-14 19:35] **Claude-RC session survivability** (owner interrupt: tmux + recovery script + WSL config + wire the RC systemd auto-reup to tmux). DONE (artifacts in /home/nathan/CEC-Platform/ops/, stable main checkout): (1) `claude-rc-tmux.sh` — systemd ExecStart wrapper runs each `claude remote-control` bridge inside a DEDICATED tmux server (socket claude-rc, session crc-<proj>), foreground-waits so systemd Restart=always still reups; tmux survives console drops + is attachable (`tmux -L claude-rc attach -t crc-CEC-Platform`). (2) `claude-session.sh` — persistent tmux launcher for the INTERACTIVE `claude` (survives terminal drops; re-attach by re-running). (3) `rc-recover.sh` — clean-restart the zombied bridge via systemctl, with a self-guard against killing the current session. (4) Edited `~/.config/systemd/user/claude-rc@.service` ExecStart→wrapper + ExecStop=tmux kill-session + KillMode=mixed; daemon-reloaded (LIVE on next WSL restart, or `systemctl --user restart claude-rc@*`/rc-recover.sh to apply now). Canonical unit + README snapshotted to ops/. tmux wrapper mechanics self-tested (create/env-inject/supervise-exit all verified). WSL mirrored-networking DEFERRED → FOLLOWUPS (risk to the tuned compute-plane NAT: localhostForwarding + broker:8080 ↔ host-LLM:8007 ↔ containers) — owner decision pending.
- [x] [added 2026-06-14 19:40 · done 2026-06-14 20:05] **Implemented the placement EI-parity checklist PL-03…PL-10** (PL-01/02 done earlier). PL-03: cec_place.nearest_evict_delta (dedup, regression-pinned) + sorted _cluster + apply_corridor_evict gains fence param + restore record; cec_router.corridor_evict_repair→place_cluster (cluster+fence-aware, RS*/J* skip) + apply_edit place_cluster case. PL-04: cec_fullstack.apply_placement_move (in-container, resolves body+band via corridor_violations, 2nd fence wall via is_fenced, evicts on a COPY of the committed→placed-r{N}.kicad_pcb, restore record) + cec_place.restore_poses. PL-05: _placement_source_for (control NEVER sees override) + --board-pcb-override threaded through route_one_worker/_exec_route_one/CLI. PL-06: settle via the existing kind-opaque DeltaLog + two-phase placement_base promote(vindicated)/revert(refuted/control) + refuted_placement_keys anti-ratchet. PL-07: gate-launder INHERITED (the pour_integrity→gates_pass fold at cec_fullstack:1761 runs on the moved+routed board; a fragmented move can't vindicate) + test. PL-08: _placement_row_fields on the measurement row (lane+corpus_state ride it) + placement_moved_rate/vindicated/refuted in ab_aggregate/render_ab_table. PL-09: real_anchor_ratio placement_moved kwarg (applied move=+1 deterministic, model unchanged, default no-op). PL-10: +27 tests across test_fs_actuator/test_prompt_audit_fixes/test_corridor_model/test_ei02_control_lane + NEW test_placement_actuation_e2e.py (host dry-run + REAL container leg) + test_fs_actuator wired into checklist.sh. VERIFIED: checklist exit 0 ("host suites pass"); 27 tests green both orders; e2e 6/6 incl. real container apply_placement_move; SB-08 IDENTICAL to committed baseline (max_T 253.2 = pre-existing owner-gated AM-04 red, NOT a regression). Found+fixed a real bug (CONTAINER_ROOT undefined in cec_fullstack) via the container leg. NEXT: adversarial audit.
- [x] [added 2026-06-14 20:05 · done 2026-06-14 20:45] **Adversarial audit of the placement EI-parity impl + REMEDIATION** (wf wmuedf7s6: 28 agents, 4 skeptics → verify each → synth; verdict needs-work, 1 blocker + 4 high + 4 med + 2 low confirmed, 10 dismissed). ALL FIXED + missing-tooth tests added: **BLOCKER-01** gate-launder (settle_outcome credits a both-gates-fail objective_base win which is pour-BLIND → a fragmented-pour move could 'vindicate') → KIND-AWARE fix `cec_fullstack._placement_keep(verdict,pour_integrity_ok)` (keep iff vindicated AND pour_integrity_ok is not False) so a fragmenting move is never promoted; settle_outcome UNTOUCHED (routing progress-crediting preserved — the audit's blanket-fix would've regressed routing). **FENCE-01** manager place_cluster bypassed the fence → corridor_evict_repair now derives+skips the locked sense ICs (union of cables' sense_ics) + accepts a fence param + stamps it into the edit; apply_edit passes fence to apply_corridor_evict (+del b). **AUDIT-PL4-001** CONTROL_EVERY<=0 deadlock → no-baseline settle branch drops+reverts (releases the one-in-flight guard). **MED-02** silent override-missing fallback → host-side isfile pre-check reverts+drops the unsettled move. **CTRL-01** _placement_source_for now gets concrete committed. **SWIG-01** del b. **AUDIT-PL4-003** skipped-delta logged. **TEST-RIGOR-01/02/03 + DETERMINISM-01** tests added. VERIFIED: checklist exit 0; test_fs_actuator 26 / test_ei02 32 / test_prompt_audit 34 / test_corridor_model 62 / e2e 8/8 (real container legs) green. KEY JUDGMENT: fixed BLOCKER-01 kind-aware (placement-scoped) NOT via the audit's literal settle_outcome change, per the audit's OWN residual-risk note that a blanket gate-dominance would break routing.
- [x] [added 2026-06-14 20:45 · done 2026-06-14 20:55] **Focused re-audit — VERDICT: SHIP** (wf wzgvmfh1r, 4 agents). All 8 prior findings independently verified CLOSED with code citations + a full green test run (test_ei02 32 / test_corridor_model 62 / test_fs_actuator 26 / test_prompt_audit 34 / test_stagger_feedback 12 / e2e 8). ZERO new blocker/high defects. Confirmed: _placement_keep applies only to placement deltas (settle_outcome byte-identical → routing preserved); pour_integrity_ok is always a strict bool on both route paths so `is not False` can't be bypassed; AUDIT-PL4-001 drops only settlement bookkeeping (routing carry untouched); FENCE-01 refuses locked sense ICs while ESP still moves. **PLACEMENT EI-PARITY (PL-01…PL-10) IS COMPLETE + FULLY AUDITED.**
- [x] [added 2026-06-14 20:53 · done 2026-06-14 20:56] **Found: the body-in-band placement lever is INERT on eps** (corridor_violations(committed eps)==[]; routing never moves parts → []every round). eps's cc=6 stall = foreign SIGNAL nets crossing the corridor (routing/LAYER problem), orthogonal to body-in-band eviction. So a placement-enabled run on eps would show "proposed→no_corridor→inert→stalls" — reconfirms the layer lever is eps's fix, NOT placement.
- [x] [added 2026-06-14 21:00 · done 2026-06-14 21:12] **Design plan: upgrade the synth placer before the Hub run** (wf w31yt6d78, 9 agents → docs/placer-upgrade-2026-06-14/plan.json). Verified LIVE: the committed Hub is ALREADY ROUTED (length 2359 / 769 tracks / kelvin+diffpair PASS) so calibration ground-truth is FREE; the placer is ~1.8x wirelength (HPWL 3677 vs 2002) / ~5x congestion (rudy 15.5 vs 3.0) worse than the hand Hub, and the proxy already discriminates them (so "rank the reference high" is well-posed). TWO real blockers reproduced: (B1) materialize() FileNotFoundError — the Hub dir has no .net, cfg.net='' → build_board crashes; (B2) seed_anchors mis-groups connectors (RJ-45 'host' + USB + J_5VSB all collapse onto the 'right' edge; _role mis-keys J_5VSB). MINIMAL-VIABLE (≈2.25 days S/M, host-runnable, enough to LAUNCH a scored+similarity-validated Hub run): MV1 feed netlist→materialize (the unblocker, S); MV2 derive Stage-1 (edges/outline/mounts/antenna) FROM the reference → fixes the wrong frame (M); MV3 reproduce-the-reference similarity metric = the validation number (M); MV4 wire the computed rudy/thermal proxy into selection (currently THROWN AWAY — sort key is only (residual,corridor_cross,hpwl)) (S); MV5 the 4 Hub-domain terms (RJ-45 even-spacing/antenna-on-edge/power-cluster/USB-ESP) (L). SEQUENCE: MV1 unblocks→MV2 correctness→MV3 instruments→MV4 free leverage→MV5 measured quality. GUARDRAILS (over-engineering to AVOID): no routing campaign for calibration (Hub already routed); defer Tier-B least-squares regression; thermal-sep/symmetry/LED terms are not first-run gates; don't refactor the anneal hot loop; oracle-seeding only as ONE competing candidate (overfitting).
- [ ] [added 2026-06-14 21:12 · in progress] **BUILD the placer-upgrade set MV1→MV5** (owner: do the whole stackup) then launch the Hub full-stack run scored vs the reference. Plan: docs/placer-upgrade-2026-06-14/plan.json. **GOVERNING CONSTRAINT (owner): the reference must NOT teach board-specific rules — encode the underlying WHY, treat the reference like the holdout (validate, never tune toward) or it's corpus-poisoning overfit.** Codified → docs/placer-upgrade-2026-06-14/anti-overfit-charter.md (3 rules: rules encode WHY-not-WHERE; reference=validation-only never an optimization target; per-board REQUIREMENTS [outline/edge/mounts] ≠ generalizable RULES). DONE: **MV1** (S) — _ensure_netlist_path + materialize() now sch-exports a netlist when cfg.net='' → a Hub candidate MATERIALIZES (tested in-container: place_candidates@98x74 residual 7 → materialize wrote 80-part board → read_placement parity; the FileNotFoundError blocker is gone). REMAINING: MV2 (split: GENERAL _role connector-function fix [principled] + per-board edge/outline/mount INPUT derived-from-reference-as-requirement [tagged board-specific, not a rule]); MV3 similarity DIAGNOSTIC (never a rank key); MV4 proxy_score (reference normalizes scale only, no per-board weight regression); MV5 the 4 net-derived domain terms (variance/edge/cohesion/proximity, no hardcoded ref values).
- [ ] [added 2026-06-14 20:56] **PIVOT (owner): validate the full stack on the HUB (a real fab-ready board) from a base stackup, using the committed Hub as a READ-ONLY reference oracle** — instead of eps (never hand-finished). DOABILITY CONFIRMED: Hub netlist exports+parses (77 comps / 83 nets), Config.load('hubs/hub-standard') + View ingest it, _classify partitions cleanly (8 connectors [4×RJ-45 J2-J5 + J_5V/J_5VSB power-in + J_KVM + J_USB], 9 ICs, 0 shunts, 60 passives), run_pipeline/run_sweep accept a board path, Hub is 4-layer + has fab/hub-standard-proto-v1. NOTE shunts=0 → corridor formation inert (cleaner generic-placement test than eps). CAVEATS: synth placer is DOMAIN-BLIND (CLAUDE.md item -2: no model of RJ-45 spacing / ESP antenna keepout / power front-end / LED chain) → produces routable-but-not-hand-quality; Stage-1 asks must be supplied (--answers, derivable from the reference Hub); "base stackup" = the Hub's 4-layer GND-plane. PLAN: extract netlist+reference (read-only→build/) → derive Stage-1 answers from the ref → run synth place→route→check → score the gap vs the fab Hub (HPWL/area/DRC/gates). NEXT: scope + kick off a bounded first pass.
- [x] [added 2026-06-14 19:40 · done 2026-06-14 20:05 → split into the two lines above] **Implement the placement EI-parity checklist (PL-01…PL-10)**. DONE + TESTED: **PL-01** (cec_fs_actuator.py) — `replace` Delta now carries a LIVE placement intent (`_placement_intent`: ref-target→{ref,op evict,band None,cluster True}, net-target→ref None for on-board resolution); v4_structural_escape populated; Delta.as_record compact-summarizes a placement intent; fence still refuses pinned/Kelvin BEFORE intent build. +5 tests (tests/test_fs_actuator.py TestPlacementDeltaLive), 22/22 green. **PL-02** (cec_fullstack.py) — placement clause added to OWNED_LEVERS, worded WITHOUT 'corridor'/'pour'/'around' so a model echo classifies to 'replace' not 'avoid' (verified _lever_kind round-trip). +3 tests (tests/test_prompt_audit_fixes.py TestPlacementLeverAdvertised), 34/34 green. REMAINING: PL-03 (dedup nearest_evict_delta + cluster/fence-aware manager corridor_evict_repair=place_cluster + sorted _cluster; cec_place/cec_router/test_corridor_model), PL-04 (apply_placement_move consumer, in-container, 2nd fence wall, edits committed COPY→placed-r{N}, restore record), PL-05 (lane-gate + thread --board-pcb-override into route_one_worker/_exec_route_one), PL-06 (settle via existing DeltaLog + two-phase override promote/rollback + _refuted_placement_keys), PL-07 (gate-launder guard: pour-integrity into gates_pass on moved+routed board), PL-08 (lane+corpus_state placement fields on measurement row + A/B table), PL-09 (applied-move=deterministic anchor EI-07, default no-op), PL-10 (update existing tests + e2e dry-run + host suite both orders + SB-08 in-container). Full checklist: docs/placement-ei-parity-checklist.json.
- [x] [added 2026-06-14 19:18 · done 2026-06-14 19:25] **Panel review DONE** (wf wxo5qk6l0; 9 agents: 4 recon → 4-lens panel → synthesis). Produced a 10-item dependency-ordered checklist (PL-01…PL-10) + 8 invariants + 38-test matrix + 4 open questions. Saved → docs/placement-ei-parity-checklist.json. CHECKLIST: PL-01 live `replace` intent (kill intent=None in finding_to_delta + v4_escape); PL-02 OWNED_LEVERS placement clause (worded w/o 'corridor' to dodge _lever_kind misclassification); PL-03 dedup nearest_evict_delta + cluster+fence-aware manager corridor_evict_repair (place_cluster) + sorted _cluster; PL-04 apply_placement_move consumer (in-container, resolves body+band, 2nd fence wall, edits committed COPY→placed-r{N}.kicad_pcb, restore record); PL-05 lane-gate + thread --board-pcb-override into route_one_worker/_exec_route_one (control routes committed); PL-06 settle via existing kind-opaque DeltaLog + two-phase override promotion (vindicated-only) + rollback + _refuted_placement_keys; PL-07 gate-launder guard (fold pour-integrity into gates_pass on moved+routed board); PL-08 lane+corpus_state placement fields on measurement row + A/B table; PL-09 applied-move = deterministic anchor (EI-07, default no-op); PL-10 update existing tests + e2e dry-run + host suite both orders + SB-08 untouched. OQ DECISIONS (panel-recommended, owner-vetoable): OQ1 fullstack-loop-scoped; OQ2 applied-move=deterministic anchor; OQ3 word OWNED_LEVERS w/o 'corridor'; OQ4 vindicated override persists/compounds, reverts on refute/overturn/control.
- [x] [added 2026-06-14 19:10 · done 2026-06-14 19:10] **Recon/grounding (pre-panel)** — mapped the exact placement-vs-routing EI gap in the live code. FINDINGS: (1) DEAD-END is `cec_fullstack.py:1932-1941` — at the actuator BOUND step ONLY `kind=="avoid"` deltas with `intent` feed the live carry `pending_corridor_avoid`; a `replace` (move/rotate) delta from `act.finding_to_delta` (:1915) is `kept` but its `intent` is None (`cec_fs_actuator.py:108-109`) and NOTHING consumes it → inert. (2) DOWNSTREAM IS ALREADY KIND-AGNOSTIC + reusable: control-gate settle `dlog.record_outcome` (:1862-1900) rolls back any non-vindicated delta; measurement row (:2001-2028) already carries lane/corpus_state(EI-01)/real_anchor_ratio(EI-07)/n_deltas_applied; `pending_corridor_avoid` (augmented-only :1622, suppressed-on-control :1627) is the EXACT carry pattern to mirror. (3) MECHANISM: each round routes from the committed board `ovd.BOARD_PCB[board]` via `route_directed(board_pcb,intents,...)`; placement = apply `cec_place.apply_corridor_evict(board,ref,band)` (already moves part+cluster, excludes structural RS*/J*, containment guard e01ddd7) to a COPY on the augmented lane, route that copy; control routes unmutated = rollback baseline. `apply_directive` dispatches an `evict` directive. (4) TO-WIRE: make `finding_to_delta` replace branch resolve a real intent {ref,band,op} (not None); add a `pending_placement` carry mirroring pending_corridor_avoid; add placement to `OWNED_LEVERS` (:83) so the model proposes it; fence must refuse moving a pinned/Kelvin/shunt ref.
- [~] [added 2026-06-14 07:25 · obsolete 2026-06-14 07:45 → done above] Phase 1+2 audit remediation (superseded by the done line above) Audit CONFIRMS cc=6 is the real hand-board floor (analytic: shared I2C bus → 4 forced + DETC1 + THRESH), but the placer is now WORSE (legal cc 8-24, not "hand-quality") — overhang(192503d) fixed residual but not peripheral placement. Fixes: (1) doc CORE-PREMISE retract "cc=6 never lower / hand-quality corridors" → honest legal cc + the analytic floor; soften "topological invariant"/"no channel"/"is exactly how committed routes". (2) build_corridor_model/corridor_cross_count MISSING the shared-bus exclusion (spurious 12vhpwr rank key) → factor out + apply. (3) overhang value "edge"→"power_able" (canonical). (4) _seed_corridor_spine clamp pads to [0,W]. (5) tests: fix 24pin filename (silent skip), assert residual==0 + band-width in test_phase2, add _corridor_veto unit test. (6) FOLLOWUPS: residual-misattribution now moot (residual 0). Defer (FOLLOWUPS): seg-AABB, _P/_N classifier parity, degeneracy pre/post-inflation.

## Done / obsolete (history)

- [x] [added 2026-06-13 19:45 · done 2026-06-13 20:25] Prompt-tier audit (Claude panel + DeepSeek cross-check): catalog + 30 confirmed/3 rejected + punchlist
- [x] [added 2026-06-13 20:30 · done 2026-06-13 23:00] Implement prompt-audit fixes (P1-P12 incl. H1/M2/H2/M1/M3/M4/M5 + P5/P6) → PR #56 (20/20 host tests)
- [x] [added 2026-06-13 21:55 · done 2026-06-13 22:10] Bot-push guard hook + gh-bot wrapper (push/gh as the bot, not the owner) → PR #57
- [x] [added 2026-06-13 22:48 · done 2026-06-13 22:48] FOLLOWUPS SessionStart hook + FOLLOWUPS.md → PR #58
- [x] [added 2026-06-13 23:25 · done 2026-06-13 23:28] TODO SessionStart hook + TODO.md → PR #58
- [x] [added 2026-06-13 22:40 · done 2026-06-13 22:48] Render eps-8pin before/after board PNGs
- [ ] [added 2026-06-15 01:35] **Integrate the agentic model stack into the placer/Hub route pipeline** (owner: panel the proper integration + toggle cloud/local, default overnight→local & short-midday→cloud; forensics on why it wasn't wired despite the docs → doc). Workflow: forensics (why bypassed) → design panel (tier wiring / toggle+defaults / corpus gating / placer-router feedback + dashboard) → implement → forensics doc.
- [x] [added 2026-06-15 01:55 · done 2026-06-15 02:25] **Forensics + design-panel continuation** (wf_cf91431f-a2a, 7 agents). Recovered 3 cached findings from the killed run + regenerated the seam inventory → forensics synth (smoke-first ossification root cause; 3-kinds-of-not-wired distinction) + 4-lens design panel → design synth (Path A wiring / cloud-local toggle / corpus gating / dashboard, 11-step checklist). Synopsis+forensics+design → **docs/agentic-integration-forensics-2026-06-14.md**.
- [x] [added 2026-06-15 02:25 · done 2026-06-15 03:05] **Implement the agentic integration (owner: "implement them + fix the stack to use the proper wiring")**. Owner default RESOLVED: explicit `--seats/--judge` wins; else `--hours>=2 → local`; else cloud (defaulting to cloud); cloud effort=MAX in all cases (Opus 4.8 max). DONE (checklist steps 1-10 + dashboard + audit): cec_seats.py resolver; model=/url=/effort= threaded through make_manager_swarm/make_worker_swarm→_panel→_chat_json→_chat_json_cloud (--effort max); hub_pipeline_run.py `--seats` + tier wiring into cec_router.route (cloud panel=1/local panel=3, fail-safe→deterministic) + post-route conformance SELECTION gate + REF intake + cec_policy.assert_loadable + corpus_fit_review sidecar + ledger; hub_run.sh forwards toggle/effort + broker --add-host; cec_dashboard.py agentic-decisions card (/api/agentic). **Adversarial audit wf_28d3ca68-a24 (5 skeptics→verify→synth): ship-with-fixes** — 2 cloud-effort defects + doc-accuracy FIXED (commit 31daa59). Commits 8ef8e07→5f1d7e5→f738df1→31daa59 (PR #60). 240 host tests green. Doc: docs/agentic-integration-forensics-2026-06-14.md. DEFERRED→FOLLOWUPS (step 11): Path-B generalization (find_board hubs/ + register Hub in BOARD_PCB + placer↔router feedback) + cloud-seats-in-container claude CLI + conformance abort-level promotion.
- [x] [added 2026-06-15 03:05 · restarted clean 03:57 · done 2026-06-15 09:10 (read 15:15)] **Overnight ~10h full-pipeline run (owner ask)** — COMPLETED full 10h/46 rounds, 0 crashes; **negative result**: 0 gate-passing, A/B augmented≈control, placement levers inert (all deltas noop), real blocker = pour-clip 40/46 rounds. Shadow auditor declined-with-restraint (good). T7 capstone hit FileNotFoundError (fail-safe→no_opinion). Writeup → docs/fullstack-run-2026-06-15/RESULT.md; 3 deferrals → FOLLOWUPS.md. — `CEC_FS_DATE=2026-06-15 cec_fullstack --board eps-8pin --hours 10` (**PID 1233137**, run dir docs/fullstack-run-2026-06-15). RESTART REASON: the box local date is 2026-06-14 (CDT) but I'd named the dir 2026-06-15 (UTC) → cec_fullstack's PERM (strftime %Y-%m-%d) wrote measurement/findings/bundle into docs/fullstack-run-2026-06-14 (co-mingling with yesterday's run + blind dashboard + empty babysit). Fixed by relaunching with CEC_FS_DATE=2026-06-15 so ALL artifacts co-locate. Owner CONFIRMED the auditor proposed_lever being advisory/noop is BY DESIGN (shadow ruling for EI-02 self-influence proof) — NOT changed. Watchdog relaunched (PID-rotated, CEC_FS_DATE=2026-06-15 + CLAUDE_PROJECT_DIR=worktree). Note: yesterday's docs/fullstack-run-2026-06-14 archive is slightly polluted (aborted run appended ~3 measurement rows + overwrote round-001..003 findings) — left as-is (past archive). FULL T0-T9 + EI-02 A/B control lane (control_every=4) + corpus gating (brief 35/6) + fence (nets=4 refs=5) + **placement actuators VERIFIED wired** (_t0_should_fire→gr02_repair line 1975/1981; finding_to_delta→select_deltas→apply_placement_move 2094/2112/2129; control-gated record_outcome + _placement_keep gate-launder guard + fence + rollback). Auditor=deepseek-v4-flash (warmed). Watchdog PID 1199904 (worktree-pinned, crash-relaunch only). Panel http://localhost:8095. CAVEAT: on eps the body-in-band placement lever is largely inert (eps's stall is foreign-signal-in-corridor = routing/layer, not placement) — actuators are PRESENT+ready, may not fire much; eps's active fix is the layer lever. BABYSIT: monitor run.log + measurement.jsonl + :8095; converges or 10h deadline.
- [ ] [added 2026-06-15 15:25] **Full actuation lever — complete the flag→clean-evidence-gate→tally→steer-not-gate loop** (owner ask). Plan of record: docs/actuation-lever-design.md (4-state rule lifecycle + the transitive-influence-lineage trap + 4 staged steps). Hard gates stay deterministic+human (CLAUDE.md ratification boundary); ratified rules STEER only, never gate.
  - [x] [added 2026-06-15 15:25 · done 2026-06-15 15:40] **Step 1 — per-rule transitive influence lineage** (commit d6ddfba): rule_id() + influence_signature() + per-run influence cone (transitive over placement_base lineage, carried only when kept) + `influenced_by` on the measurement row + clean_pairs() comparator (firewall: a row with R in its cone is never a baseline for R) + host tests. Additive/non-behavioural.
  - [x] [added 2026-06-15 15:25 · done 2026-06-15 16:05] **Step 2 — cross-run persistence + tally**: rule_tally() pools clean evidence over independent runs (firewall-safe across runs); actuation_lever_report + load_runs_from_dirs (board-filtered); _emit_lever_ledger durable obs; --tally CLI. (Caught + fixed a real glob bug in load_runs_from_dirs that silently disabled cross-run pooling.)
  - [x] [added 2026-06-15 15:25 · done 2026-06-15 16:05] **Step 3 — graduation bar**: graduation_verdict (min_runs/min_pairs/improving-frac sign-test/per-metric effect floor + direction); holdout fixture tests/holdout/actuation/bar-fixtures.json (4 boundary cases) validates the DEFAULT bar (bar code never reads holdout). Promotion stays owner-gated (advisory only).
  - [x] [added 2026-06-15 15:25 · done 2026-06-15 16:05] **Step 4 — steer-only chokepoint**: assert_steer_only() + SteerViolation + STEER_FIELDS/GATE_FIELDS (generalizes _placement_keep); a ratified rule may only bias the search, never write a gate field / ship.
  - [~] [added 2026-06-15 15:25 · obsolete 2026-06-15 15:50 → split into the two lines below] **Validation**: Hub or synthetic.
  - [x] [added 2026-06-15 15:50 · done 2026-06-15 15:50] **Hub generalization (Path-B infra)**: BOARD_PCB[hub-standard] + find_board resolves hubs/ + INTENTS[hub-standard]=[] + FRESH_ROUTE_BOARDS (_placement_only_copy strips committed copper so the Hub routes fresh from placement). Import-verified; host suite green.
  - [x] [added 2026-06-15 15:50 · done 2026-06-16] **Lever ENGAGES live + placement resolves a body**: hubsmoke2 (--rounds 5) confirmed the `.lever`→`.type|.action` fix (ff299ff) — round-5 `effort` lever actuated for the first time (status=applied). REMAINING gap closed 2026-06-16: a placement finding that named the body only in prose (r2 "Shift component C1") now resolves via `_prose_ref` (refdes-from-prose, validated against the manifest refs) + a finder-prompt nudge. Real r2 finding: `replace ref=None`→noop became `replace ref=C1 status=applied`. tests/test_actuation_lever.py TestProseRefFallback (7); host suite 597 green (9 pre-existing fr02/vision discover-order failures, baseline-identical).
  - [x] [added 2026-06-16 · done 2026-06-16] **Adversarial review of the prose-ref fix + harden + close r3** (wf_6653dbfc, 6 lenses/25 agents, 16 confirmed): r3 lever-misclassification CLOSED (_lever_kind failure_class override — real r2 AND r3 now resolve replace→C1); prose-ref hardened (underscore/long-prefix refs via known-ref alternation, case-insensitive, verb-proximity); known_refs hoisted. commit f8ddbd9; +11 tests (suite 63 green).
  - [~] [added 2026-06-16 · obsolete 2026-06-16 → FOLLOWUPS "KEY BLOCKER ... CORRIDOR-GATED" + owner-queue] **Longer Hub validation run** — WRONG TARGET. The review + a host-pcbnew trace proved the placement actuator is corridor-gated: `corridor_violations()` == [] for BOTH committed Hub (shared-bus) and committed eps (clean placement), so NO body can move on either regardless of ref resolution. The prose-ref fix is necessary but not sufficient. The actuator chain is already proven on an injected-violation EPS fixture (test_placement_actuation_e2e). Superseded by the owner decision below.
  - [x] [added 2026-06-16 · done 2026-06-16] **Option (1) — validate the chain on EPS (owner: "Validate the chain on EPS first")**: DONE, lever FIRES live. Injected U10/INA238 into the /SENSEC2 corridor; a live `cec_fullstack` loop (Sonnet finder) showed the chain end-to-end. KEY finding+fix (commit 7b10fed): `corridor_violations()` was only called INSIDE apply_placement_move, NEVER surfaced to the finder → it diagnosed routing + targeted the fenced sense net (refused). Added `corridor_body_facts` → pourcheck → `_audit_prompt` BODY-IN-CORRIDOR directive. **Baseline placement_moved_rate 0.0 (0/5) → with fix 0.667 (2/3, verdict=placed, U10 +9mm).** Evidence: docs/fullstack-run-2026-06-16-epsinject{,2}/RESULT.md. Det full-chain proof also in test_placement_actuation_e2e + the eps_chain_validate script.
  - [ ] [added 2026-06-16] **OWNER DECISION still open — option (2) generalize the lever** to a non-corridor "make-room" eviction (congestion band from GR-01/failed-waypoint geometry) so Hub-class "no routing room" failures move a body. Larger; the corridor-evict safety model doesn't transfer. docs/owner-queue.md §1 + FOLLOWUPS 2026-06-16. Option (1) is validated; (2) is the only remaining path to placement movement on a shared-bus (no-corridor) board like the Hub.
- [ ] [added 2026-06-16 17:55] **FULLY CLOSE THE LOOP — self-converging place→route→converge (owner: "build it all out")**. Mechanism, not corpus (memory: convergence-blocker-mechanism-not-corpus). Phases:
  - [x] [done 2026-06-16] P0: implementation-level recon → sequenced plan (workflow wf_ad6be7d1). Confirmed clip-at-shunt=notch, layer-stagger already exists, build order.
  - [x] [done 2026-06-16 · commit 91b7d68] P1 (keystone, ROUTE half): cec_fr.corridor_keepouts shared helper + cec_router delegation + route_directed wiring. **+ found & fixed a REAL bug the recon missed: the keepout's DoNotAllowZoneFills blocked ~89% of the same-net pour → block_fills flag (corridor keepouts set False).** 6 tests, golden track-metrics byte-identical, 0 new host failures.
  - [x] [done 2026-06-16 · KEY FINDING] P1-validate: live route_directed on committed eps. The keystone WORKS (foreign crossings 31→6, pours solid 122.8mm²) BUT **is COUPLED with placement** — committed eps isn't corridor-clean (corridor_cross≥3: /DETC,/THRESH,/I2C forced through), so the keepout strands them + the sense taps (kelvin True→False, unconn 2→16). The existence proof was via cec_golden (no keepout). → keepout DEFAULTS OFF; the loop closes with keystone+P5 TOGETHER on a fresh clean placement. NOT "closes the existing-board loop" as first assumed.
  - [ ] **P5 (now the critical coupled half): corridor-packing placer** — seed_anchors seats each cable's connector→shunt→sense-IC as a CLEAN COLUMN (no foreign airwire forced through the corridor) so a FRESH eps placement is corridor-clean. Then enable the keepout (P1) + corridor_max on it. THIS is what makes the keystone converge.
  - [ ] P5-validate: fresh eps place(corridor-clean)→route(keepout on)→converge: kelvin_ok stays True AND foreign crossings ~0 AND pours solid → gates pass. The actual close-the-loop proof.
  - [ ] P2: defrag/re-pour-after-route — heal a pour fragmented by a RESIDUAL crossing (a foreign pad inside the corridor vias through; keepout is allow_vias=True). After P5.
  - [ ] P3: (DEFERRED — recon: layer-stagger already exists, wired-but-inert; keystone reduces its workload; no new stagger work in scope unless P5 leaves residual crossings.)
  - [ ] P4: (SUBSUMED by P1 — the deterministic route-time keepout replaces the next-round-reactive avoid; residual = dedupe corr_* vs avoid_* keepouts, low-priority.)
  - [ ] P6: adversarial review of the whole loop + a fresh-board end-to-end convergence demo.
- [ ] [added 2026-06-16] **BUILD the LLM-guided placement loop (owner: "let's do it")** — the actuation-lever pattern pointed at PROACTIVE placement planning (deterministic placer PROVEN insufficient: fresh route kelvin=False + 62 clips; FOLLOWUPS 2026-06-16). New scripts/cec_place_planner.py: place_candidates(seed) → placement_context(parts/roles/positions/corridors/failures) → LLM planner seat proposes MOVES → apply+legalize → route+score (measure) → iterate. Reuse cec_judge_local (seat), cec_place (move/legalize), build_corridor_model, place_candidates/materialize, cec_fr.route_once+cec_score. Goal: drive a fresh eps placement to converge (kelvin pass, clips→low, gates pass).
  - [x] [done 2026-06-17] AUDITOR tier built + run (18 rounds, run9.log): nothink worker descended clips 91→48 (past the old ~70); the auditor (cec-worker-quality) DIAGNOSES the partition correctly every round but its absolute-coord re-layouts all REGRESS → ceiling = the move REPRESENTATION, not intelligence/speed.
  - [x] [done 2026-06-17] Partition MATERIALIZER `--pack` (`w_pack`/`_shelf_moves`) + `--keepout` measure flag. Deterministic all-right pack = clips=76/drc=6/kelvin (vs LLM 48/drc=28 not-fab-ready). Found the structural truth: +3V3/+5VSB feed BOTH corridors → the partition is a center-gap spine, not "all one side".
  - [ ] [added 2026-06-17] **NEXT: region-AWARE partition** — LLM assigns each foreign IC a region {cable-1-side, center-spine, cable-2-side, right}; per-region pack via `_shelf_moves`. Then rebalance `run()` score for finishing-DRC. Then a long run (~50s/round). The clean execution path (`_shelf_moves`) is done; needs the region-map plan seat.
- [ ] [added 2026-06-17 08:04] **BABYSIT the 8h region-partition run** (owner ask) — eps-8pin, build/place-planner-overnight, log docs/place-planner-2026-06-16/overnight-run.log, deadline ~16:03Z. PID 2380367. Region-aware partition tiers (commit 1b950ff) + fab-gated score (kelvin→drc≤16→clips→drc). Watch the trajectory; when it plateaus, diagnose the structural reason (owner: "find the reason behind it").
- [ ] [added 2026-06-17 08:09] **8h run RELAUNCHED with smooth clips+drc score** (the DRC ceiling rejected r1's clips=47 win) — eps-8pin, build/place-planner-overnight2, log docs/place-planner-2026-06-16/overnight-run2.log, PID 2384349, deadline ~16:08Z. Score now minimize(clips+drc) tiebreak clips (commit e27a45d). Babysitting; diagnose the plateau when it stalls.

- [x] [added 2026-06-18 22:35] Board-grow lever (last loop lever): scale board up ~5mm at deep plateau, re-converge; testing whether more channel room beats clips=40 (build/place-planner-grow, from r7) · done 2026-06-18 23:07 (verdict: does NOT beat clips=40; floor is topological)

- [x] [added 2026-06-19 00:02] Route spanning signals UNDER pours on B.Cu — building opt1 (post-route F.Cu->B.Cu layer-swap) + opt3 (KiCadRoutingTools pour-first), verifying COMPLETENESS, comparing (workflow wf_f3215343) · done 2026-06-19 00:24 (OPT1 layer-swap WON: 40->32 complete+clean; OPT3 KRT incomplete; ceiling-breaker = +3V3 inner plane)

- [x] [added 2026-06-19 00:44] Combine routers: +3V3 inner plane + FR + KiCadRoutingTools under-routing + layer-swap, vs control (plane+FR+swap); does KRT earn its place? (workflow wf_76757fc3) · done 2026-06-19 01:11 (FULL=30 complete, CONTROL=31; +3V3-plane buys only ~1-2 clips -> de-risks V2)
- [x] [added 2026-06-19 01:11] 2D electro-thermal solver + dashboard heatmap + 0.5oz-inner stackup verdict (workflow wf_70000bc3) · done 2026-06-19 01:52 (V2 dual-0.5oz-GND PASSES 2D solve 74C; V1 fails; heatmap on dashboard)
- [ ] [added 2026-06-19 01:52] OVERNIGHT run: place-loop + grow + route-under (CEC_ROUTE_UNDER=1), from r7, 10h, V2 stackup, target < clips=32 complete (build/place-planner-overnight)
- [x] [added 2026-06-19 20:00] GND-plane fixes (owner caught on dashboard): (1) grow lever now EXTENDS the inner GND plane(s) to the grown edge — was left at the old size, a bare strip at the grown edge; (2) 12V-pin clearance BERTH; (3) solid GND connector returns. New `finish_gnd_plane` in cec_place_planner, wired into w_grow; demo board eps-8pin-r418-fixed (GND now fills the 106mm grown edge) · done 2026-06-19 20:10
- [x] [added 2026-06-19 20:00] Dashboard live-view fixes: coarse 0.8mm thermal grid (~2s/board) + max_boards 48→8 (was a runaway of ~48 concurrent slow solves); plot now blank when no layer selected (removed the svg never-blank `/board.svg` fallback, matching thermal) · done 2026-06-19 20:10
- [ ] [added 2026-06-19 20:10] Route eps-8pin-r418-fixed → show a COMPLETE board (traces + filled GND plane + 12V berth) in the dashboard (the loop's candidates are unrouted placements, so they show no traces)
- [x] [added 2026-06-19 21:00] SHRINK lever (reverse-grow, owner ask): `_scale_at_split` (signed delta; w_grow/w_shrink wrappers) + courtyard-overlap feasibility gate + RE-PACK-past-overlap (`--shrink-legalize`, preserves anchors→kelvin) + `shrink_sweep` host driver (`--shrink-sweep`, writes run.log/measurement.jsonl for the dashboard) · done 2026-06-19 21:30 (lever verified: rigid W-1 overlap=2 → re-pack overlap=0)
- [x] [added 2026-06-19 21:30] RUN the shrink sweep from the clean converged eps-8pin-r34 (96.2×37.2) → find the minimum routable size (build/place-planner-shrink, legalize) · done 2026-06-19 22:00 (FIRST run said 0% — but that was CONFOUNDED by the finish_gnd_plane berth bug, see below)
- [x] [added 2026-06-19 22:15] Wire the synth placer as the shrink re-pack to hunt the floor (owner ask) · done 2026-06-19 22:45 — KEY FINDINGS: (1) the `finish_gnd_plane` 12V berth used pad-local-clearance, which Freerouting ignores then DRC flags → spiked drc_p 0→50 on EVERY finished board, confounding the whole sweep (and the grow, and r418-fixed); FIXED to zone-clearance (drc_p=0). (2) The smart placer (`--repack smart`) OVER-perturbs (clips 110-135 vs greedy 45-59) — greedy minimal-nudge is the right re-pack, now the default. (3) With both fixes the shrink WORKS: r34 96.2×37.2 → 94.2×33.2 (drc_p=0, kelvin, **12.6% smaller**) in one pass
- [x] [added 2026-06-19 22:45] Multi-pass floor-hunt (re-sweep until no axis frees the other) from r34 → the definitive eps minimum · done 2026-06-19 23:05 (FLOOR = 94.2×33.2mm, clips=52, drc_p=0, kelvin, routed — 12.6% smaller than 96.2×37.2; pass 2 found nothing more → confirmed floor. board: build/place-planner-shrink/eps-8pin-shrink10.kicad_pcb)
- [x] [added 2026-06-19 23:20] Floor-hunt on the other boards (owner ask): PCIe-2port, PCIe-3port, 12VHPWR, Hub — generalized the lever (anchors on any J* not just J_; conditional kelvin gate for the Hub) · done 2026-06-20 — RESULTS: **PCIe-2 −27.2%** (86.5×44→86.5×32, hit the 40-step cap so likely more), **PCIe-3 −13.6%** (104×44→104×38), Hub **0%** (already size-optimal, connectors on all edges), 12VHPWR killed (398-clip baseline → impractically slow, needs a cleaner start / lower effort — FOLLOWUPS)
- [x] [added 2026-06-20] Curated showcase dashboard (owner ask): build/shrink-showcase with eps/pcie2/pcie3 start+MIN pairs, all 6 with per-layer thermal heatmaps, dashboard pinned at :8095 · done 2026-06-20
- [x] [added 2026-06-20] FAST fine-resolution thermal solver (owner ask: assemble-once refactor + fine grids) · done 2026-06-20 — (1) assemble-once: `_thermal_solve` now builds the matrix once + setdiag in place each Picard iter (was rebuild+tocsc every pass); correctness exact (eps r34 @0.5mm 75.7 match). (2) ROOT CAUSE found: Jacobi-CG needs ~4260 iterations on the 2D screened-Poisson thermal matrix (the real fine-grid cost, NOT the linear-solve size — which is why the GPU only gave 1.2×). (3) FIX = pyamg algebraic-multigrid (~15 iters, mesh-independent): @0.2mm **376s → 127s (3×)**, maxT match. Baked pyamg into cec/routing:gpu. NEXT: AMG-reuse (build hierarchy once, not per Picard iter — the setup is now ~90s of the 127s → another ~3× → ~40s)
- [x] [added 2026-06-20] GPU thermal solver · done 2026-06-20 — WIRED + MEASURED. (1) cec/routing:gpu image (cupy-cuda12x[ctk] + matplotlib/shapely which the base kicad10 never had) + compose.gpu.yaml (opt-in GPU overlay + persistent cupy kernel-cache volume); fixed the solver's GPU CG kwarg tol→rtol + raised the GPU threshold 20k→120k cells (env CEC_THERMAL_GPU_MIN_N). (2) MEASURED: GPU CG 2.4× @250k cells, 3.7× @640k; CROSSOVER ~120k (below it CPU wins on overhead). (3) CAVEAT: Blackwell sm_120 is so new cupy JIT-compiles ALL kernels on first use (~10min, now cached to a volume → one-time EVER). NET: GPU for FINE-grid/batch solves (≤~0.15mm); live dashboard stays CPU at 0.4-0.8mm (below threshold, already fast). Higher resolution = just grid_mm (dashboard grid now env CEC_DASH_THERMAL_GRID)
- [x] [added 2026-06-20] New thermal renders (owner ask) + hover-temp feature: rebuilt showcase from the VALID re-run boards (no clipping), dashboard renders at CEC_DASH_THERMAL_GRID=0.4 so the 0.6mm berth voids show; overlay exports thermal-grid.json (full T field), dashboard serves /thermal-grid, frontend reads off T(x,y)+mm on mouseover in thermal mode · done 2026-06-20 (verdicts from grids: EPS pass [start dT26/min dT29], PCIe all FAIL [50-69] — shrink trades thermal headroom)
- [ ] [added 2026-06-20] OWNER REVIEW of the showcase found 4 issues → fixing: (1) CLIPPING — the floor-hunt had NO on-board constraint so it shrank past where logic/logo fit (D2/SW1/LOGO1 clipped off PCIe-2's edge); added `offboard` check to `_scale_at_split` (movers must stay on-board, connectors may overhang) + sweep rejects on it; verified it flags the exact clipping parts. RE-RUNNING eps/pcie2/pcie3 (build/shrink-{eps2,pcie2v2,pcie3v2}). (2) berth IS there (0.6mm zone clearance) but the 0.8mm thermal raster is too coarse to show the void → made dashboard grid env-configurable (CEC_DASH_THERMAL_GRID=0.4). (3) boards are unrouted placements (no traces) → will ROUTE the MINs. (4) logo clipped → fixed by the on-board constraint
- [x] [added 2026-06-20 00:30] Resolve owner's 12VHPWR thermal pushback (149C too hot vs prior ~14C calc) · done 2026-06-20 01:10 → solver verified UNBIASED (trace==pour 0.0%), FR4+GND-plane spreading confirmed, slab test (1.26W uniform 25C) proved copper is not the limit; 99C was no-cooling still-air worst case. Added chassis_refs/g_chassis solver hook; with production cooling (metal case: TIM'd shunts + chassis-coupled mounts) dT 24C / maxT 74C = PASS. Follow-ups (still-air 2x conservatism, AMG-reuse non-det, dashboard wiring) in FOLLOWUPS.md.
- [x] [added 2026-07-01 23:35] Enterprise + Mission Critical planning: survey latest module/hub state, inventory MCP servers, draft the prod-requirements plan for both enterprise-tier variants across all modules (branch claude/enterprise-modules-planning-zpjmir) · done 2026-07-02 13:16 → docs/enterprise-mc-requirements-plan-2026-07-01.md (+§1a owner direction)
- [x] [added 2026-07-01 23:40] Enterprise/MC customer + integration audit — fan-out workflow (owner ask): audit both tiers against enterprise-customer expectations and required integrations; synthesize into docs/enterprise-requirements/research/ · done 2026-07-02 13:16 → docs/enterprise-requirements/research/customer-integration-audit-2026-07-01.md
- [x] [added 2026-07-01 23:58] Tamper-protection module-roadmap research (owner ask): deep-research run on what OTHER modules a ~300-workstation fleet with explicit tamper-protection needs would want; rank new CEC module concepts; write to docs/enterprise-requirements/research/ · done 2026-07-02 13:16 → docs/enterprise-requirements/research/tamper-module-roadmap-2026-07-02.md + plan §3a
- [x] [added 2026-07-02 15:30] Phase 1 enterprise requirements drafting: REQUIREMENTS-FORMAT + hub register + module family registers + conformance matrix + req-trace lint (plan Phase 1, seeded from the two research reports) · done 2026-07-02 15:38 → docs/enterprise-requirements/ (86 REQs, 6 registers, lint wired into checklist.sh)
- [x] [added 2026-07-02 15:42] Phase 2 research surveys 1-8 (PolarFire sizing, uplink, RJ-11 trust, redundant power, redundant CAN/uplinks, RTOS stack, compliance regime, radio-free MCU) — 8 parallel sonnet agents, assemble into docs/enterprise-requirements/research/phase2-surveys-2026-07-02.md · done 2026-07-02 16:01 → docs/enterprise-requirements/research/phase2/ (8 surveys + INDEX.md synthesis)
- [x] [added 2026-07-02 16:11 · done 2026-07-02 16:11] Apply owner rulings 2026-07-02 to registers (CRA EU-entry-conditional, NanoKVM AIR exclusion, S-suffix, RJ-11 define, radio-free (a), SNMPv3 prune, eFuse fronts, REQ 054-059) — resolve-all pass → registers now 100 REQs, lint OK
- [x] [added 2026-07-02 16:17 · done 2026-07-02 16:17] CEC-KVM assessment (plan §3a.6) + spec revision v1.2.0 draft (10 surgical edits, 2 owner decision boxes) — owner asks 2026-07-02
- [x] [added 2026-07-02 16:25 · done 2026-07-02 16:25] Second owner ruling: fold MC into ENT as SKU ladder (watchdog / fail-functional voting pair) — plan §1a.6, REQ 103-105, spec draft §13.8 + OQ-79, gates flipped; survey 9 launched
- [x] [added 2026-07-02 16:28] ENT hub full spec sheet + engineering BOM (6 SKUs) + module spec/BOM-delta drafts — owner ask 2026-07-02 · done 2026-07-02 16:31 → docs/enterprise-requirements/spec-sheets/ (hub 6-SKU sheet + engineering BOM; module deltas x4 families)
- [x] [added 2026-07-02 16:33] ENT hub variants plan + COMPLETE detailed BOM (passives, real MPNs, datasheets) — 4 subsystem agents now, watchdog subsystem after survey 9, then assembly · done 2026-07-02 17:03 → spec-sheets/bom-detailed/ (A/B/C/D subsystem files + master assembly w/ reconciliations; T1 rows pending survey 10)
- [x] [added 2026-07-02 16:54] Survey 10 (100BASE-T1 module link: PHYs, hub 8-port fabric architecture, PTP sync, RMII MCU, dual-mode cost) — grounds the 3rd ruling / OQ-80 · done 2026-07-02 17:21 → survey-10 applied: LAN9370x2 hub architecture, P4 uniform MCU, RS-485 compat dropped (owner-review tag), REQ-043/003/106 refined
- [x] [added 2026-07-02 17:11] Survey 11 (module-port mis-plug fail-safe: per-pin PoE current paths, pin-1 60V blocking, DETECT hardening, T1 fault rating, injection test spec) — 4th owner ruling / REQ-110+053 · done 2026-07-02 17:40 → survey-11 applied: TPS26621 module eFuse (diode CANNOT protect pin 1), TJA1051 ±58V continuous confirmed, hub +$5.6 / module +$2.15-2.7 protection rows in master BOM §6a, REQ-110/053 refined, pin-7 rows reconciled to the 5th-ruling driven line
- [x] [added 2026-07-02 17:46] Pin-7 heartbeat challenger (owner exploration): draft PROPOSED REQ pair + per-port pin-7 hub realization + spec-draft/OQ-81 fold-in · done 2026-07-02 17:46 → REQ-HUB-COMMON-114 + REQ-MOD-COMMON-013 (PROPOSED, gate D-ENT-5), REQ-112 per-port fabric realization, REQ-113 surface list +heartbeat, v1.2.0 OQ-81 extended, owner-queue adopt/decline row; 114 REQs lint OK
- [x] [added 2026-07-02 17:55] SIXTH RULING implementation: adopt heartbeat (114/013 → ADOPTED) + extend T1 to the 24-pin (P4 uniform, DETECT 10k) + propagate; sweep survey-10 T1-only drift (OQ-5 moot on ENT, RS-485 remnants in matrix/spec-sheet/variants-plan/BOM-C) · done 2026-07-02 17:58 → registers/spec-draft §13.2a+OQ-81/spec-sheets/master-BOM §6a/owner-queue all updated; 24-pin ENT delta now +$5-7 (~$40-44 class); 114 REQs lint OK
- [ ] [added 2026-07-02 18:16] Full-suite fan-out review (Standard/Pro/Max/ENT) — workflow wf_fd0ca2c2-929: 10 sonnet lenses → opus triage → adversarial verify → opus synthesis; apply confirmed fixes + report on completion
- [x] [added 2026-07-02 18:23] Next-trajectory scoping (ENT variants, owner ask): 5 parallel sonnet scoping agents (firmware/fabric, security-protocols, board program, validation/compliance, ratification package) → synthesize into docs/enterprise-requirements/next-trajectory-2026-07-02.md · done 2026-07-02 18:40 → all 5 scopes banked (research/next-trajectory/scope-*.md) + synthesis doc (waves 0-3, dependency graph, start-now list, owner decision queue incl. NEW dev-kit/Libero spend asks mirrored to owner-queue)
- [x] [added 2026-07-02 19:11] SEVENTH RULING ("I sign off"): MPFS095TC = production baseline (FAE-conditional), MPFS095TS = HS population option, part-agnostic SerDes-free land · done 2026-07-02 19:11 → REQ-001 rewritten (gate D-ENT-3), REQ-030 SerDes-free uplink note, REQ-104 fabric/LVDS link standardized, spec-sheet §2/§3.A rows, master BOM §3a baseline flip, v1.2.0 §13.1 + tier row, owner-queue f3 resolved; lint 114 OK
- [x] [added 2026-07-02 19:11] Suite-review reconciliation batch (20 findings) — agent applying; commit remainder + final report on completion · done 2026-07-02 19:15 → all 20 applied (F01 critical + math cluster + MCU staleness + register contradictions + status reconciliation), totals re-summed NET −$5 / AIR −$3-4, lint green
- [x] [added 2026-07-02 18:40] MPFS095TS stock verification (owner flag) + sourcing ladder + MPF050TC assessment · done 2026-07-02 18:45 → master BOM §3a: no authorized 095-density FCVG484 stock (S or non-S; non-S 52-wk!), in-stock hedge = 64× MPFS250TS-FCVG484I @$399.74 DigiKey, broker 160TS = provenance-unacceptable, FCSG536 = package fork last resort; MPF050TC = fabric-only Core FPGA (NO MSS — cannot host the hub compute; noted as Max-tier data-plane candidate + supply intel); owner-queue (f3) hedge-buy decision added
- [x] [added 2026-07-02 19:19] ENT product matrix map (owner ask) · done 2026-07-02 19:19 → docs/enterprise-requirements/product-matrix-2026-07-02.md (ladder context, 6 hub SKUs × HS silicon axis = 12 configs, 5 module SKUs, cross-compat, orderable summary)
- [x] [added 2026-07-02 19:19] WAVE 0 EXECUTION (owner "go ahead"): 5 parallel agents — ratification brief + one-pagers, RFQ package (incl. FAE set), CEC-KVM cited recs, threat model + key hierarchy drafts, verification-matrix generator — integrate + commit as they land · done 2026-07-02 19:29 → ALL FIVE LANDED: ratification/ (brief: 5 nods + 7 reviews w/ one-pagers; RFQ 26 lines + 6 FAE Qs), research/cec-kvm-recommendations (10 recs, carrier-premise CORRECTED), enterprise-security/ (threat model = canonical honest-limits + key hierarchy), scripts/cec_req_verify_matrix.py (114/114 mapped, teeth verified, checklist wiring deferred)
- [x] [added 2026-07-02 19:43] EIGHTH RULING (owner walked the ratification brief): record all 12 answers — REQ-111 declined/tombstoned, provenance=evidence-source (REQ-007), mezzanine adopted (REQ-24PIN-020, AIR-only stacked SKU + flags), Phase-5 strict, KVM kicked off, RFQ ratified-hold-send + CUSTOMER PROTOTYPE ask, custody direction ratified, v1.2.0 staged behind N1 confirm · done 2026-07-02 19:43 → registers + brief status block + owner-queue + FOLLOWUPS ×2; prototype-demo-plan-2026-07-02.md written; OQ-11 selection agent launched
- [ ] [added 2026-07-02 19:43] Pending owner picks after my answers: N1 RS-485-drop confirm, N2 SBOM pick, R6 ATR pick → then apply v1.2.0 (signed off, staged)
- [x] [added 2026-07-02 19:52] NINTH RULING ("Adopt these recs"): RS-485 drop CONFIRMED (REQ-043), SBOM = SPDX+CycloneDX-derived (REQ-014), ATR = passive-only/defer-active (draft OQ-78) · done 2026-07-02 19:52 → registers + brief scoreboard + owner-queue updated
- [x] [added 2026-07-02 19:52] APPLY SPEC v1.2.0 · done 2026-07-02 20:55 → ALL 10 EDITS APPLIED + verified (req-lint + corpus-lint green, all grep anchors pass); post-application pass: OQ-11 fully closed in spec §6.4/§10/revision-log (delegated selection; the phantom K-1L00F never existed in the spec — no fix needed), Phase-4 gates flipped (030/112/114/MOD-013), OQ-11 gates flipped (051 satisfied, 031 narrowed to OQ-10/12), CLAUDE.md header → v1.2.0, verification map re-frozen post-review
- [x] [added 2026-07-02 20:02] OQ-11 shunt selection (delegated, 9th-ruling R3) · done 2026-07-02 20:02 → oq-11-shunt-selection-2026-07-02.md: R/K series DON'T OVERLAP (R ≤1.0mΩ, K ≥1.8mΩ — the "choice" was an existence question); all 4 values picked+verified (2mΩ K / 25mΩ Vishay WSK Kelvin / 0.5mΩ R / 1mΩ R); every already-sourced part was already correct; spec K-1L00F citations = nonexistent part → K→R text fix rides the post-v1.2.0 pass
- [x] [added 2026-07-02 20:08] Wave-0 tail (3 agents) · done 2026-07-02 20:55 → ALL LANDED: 8 bench/FMEA files, 5 process docs, 3 security drafts (heartbeat wire protocol w/ CAN 0x7A0-0x7AF two-frame nonce + T1 EtherType 0x88B5, untrust state machine w/ FREEZE-never-gated carve-out, custody ceremony 2-of-3 proposed → OWNER FINAL SIGN-OFF pending)
- [ ] [added 2026-07-02 20:18] BOARD PROGRAM STAND-UP (owner directive): KiCad intake manifest agent running (inventory + net-new parts + sourcing routes + priority tiers) → then intake fan-out (P1 = shared P4+T1 block parts first), then scaffold hubs/hub-enterprise + module ENT projects, then schematic capture via the repo tooling (gen-modules ENT branch / cec_sch splices). FINDING: 12vhpwr-pro sch = 99-line stub, NO P4 symbol exists anywhere — intake is from scratch
- [ ] [added 2026-07-02 20:55] KiCad intake fan-out (manifest landed: ~36 net-new rows, 5 groups A-E; easyeda2kicad v1.0.1 NOW INSTALLED; OQ-11 shunts = zero library work, BOM-property pass only) — launch groups A-D; group E (MPFS BGA script-gen + LAN9370) as its own workstream; owner-download list: LTC2358 (deferred), JXD1 (check SnapEDA first)
- [x] [added 2026-07-02 21:05] ENT trust-barriers + variant-map chart doc (owner ask, "easily accessible place") · done 2026-07-02 21:05 → ENT-TRUST-AND-VARIANTS.md at REPO ROOT (trust-zone ASCII map, B1-B7 barrier table w/ honest limits, surface-independence rule, 3-axis SKU cube, hub/module tables, cross-tier guarantee; marked DERIVED VIEW w/ canonical sources)
- [x] [added 2026-07-02 21:20] Upgrade trust/variants doc to Mermaid + SVG (owner ask) · done 2026-07-02 21:20 → 3 Mermaid diagrams (trust-zone map w/ validated palette tints, untrust state machine w/ status colors, variant cube), render-VERIFIED headlessly (playwright + local mermaid, 3/3 SVGs, 0 errors), standalone SVG exports vendored at docs/enterprise-requirements/assets/
- [ ] [added 2026-07-02 21:35] ENT HUB SCHEMATIC (owner directive, hierarchical multi-sheet): SCHEMATIC-PLAN.md committed (10-sheet map, verification protocol, capture order 01→10, MCX sheet last); 4 library agents running (cec-ent-{power,net,compute,mcu}.kicad_sym — collision-safe separate files, lib-table registration = my consolidation pass after); then capture starts sheet 01 (power-input, all-reuse) per the plan
- [x] [added 2026-07-02 22:10] Library intake: ALL FOUR GROUPS LANDED (power 9/9, net 6/8 + 2 owner-download flags, MPFS generated w/ 484-ball assertions, MCU 3/4 + S32K skip) — library-complete sheets 01-05/07/08; 06 blocked on LAN9370 datasheet, 09 on S32K RM · done 2026-07-02 22:10
- [ ] [added 2026-07-02 22:10] Sheet-01 capture agent running: project scaffold (lib tables w/ cec-ent-* registration, root + placeholder sheets, DRAFT) + full 01-power-input capture from BOM-D computed values + ERC + netlist assertions (check_hub_ent_sch.py) — verify + commit on landing
- [x] [added 2026-07-02 22:45] Sheet-01 capture + project scaffold · done 2026-07-02 22:45 → hubs/hub-enterprise/: .kicad_pro + lib tables (4 cec-ent-* libs registered) + hierarchical root (10-sheet map, 15 exports) + 01-power-input FULL (59 parts from BOM-D computed values) + placeholders 02-09 + gen_hub_enterprise.py (reproducible, hierarchical instances via cec_sch primitives); VERIFIED independently: check_hub_ent_sch.py exit 0 (eFuse→cascade chain, ILIM/UVLO/OVLO/sense values, no cross-source short), ERC = 4 benign classes only; REAL CATCH: BOM-D's CP2→GND rec contradicted the shipping hub-standard (CP2→IN2) — as-built wiring followed, BOM-D corrected
- [ ] [added 2026-07-02 23:05] FORMAT CORRECTION (owner): one functional block per LITERAL sheet, no dashed pseudo-frames — restructure agent running: sheet 01 → thin parent + 7 leaf sheets (01a-g), plan §1 amended w/ per-subsystem leaf breakdowns (05a-port ×8 instanced etc.); wiring must be UNCHANGED (assertions + flattened-netlist identity check) — verify + render + commit on landing
- [ ] [added 2026-07-02 23:15] Schematic layout-quality engine (owner ask: rotations, drawn wires to decouplers, text-overlap detection instead of label-aliasing everything) — scripts/cec_sch_layout.py building in parallel (standalone, additive: rotation pin-math round-trip-verified, wire_adjacent, place_decouplers w/ netlist ownership, text-collision detect+nudge, --check-overlaps CLI as a verification-protocol gate); INTEGRATE into gen_hub_enterprise once the restructure agent releases the project, then regenerate 01a-g with it
- [x] [added 2026-07-02 23:30] Schematic-quality charter + start hook (owner directive) · done 2026-07-02 23:30 → docs/schematic-quality-charter.md (tool ladder T1-T6, non-negotiable principles, verification-protocol wiring, integration order) + .claude/hooks/schematic-quality-context.sh registered in settings.json SessionStart (smoke-tested — injects the charter head every session)
- [ ] [added 2026-07-02 23:30] T2 pin-type auditor + T3 style linter building (parallel agents; T1 layout engine also in flight; restructure agent also in flight) — on landing: charter status flips, T1 integrates into gen_hub_enterprise, gates 5/6 join the sheet protocol, sheet-02 gates on the T2 audit
- [x] [added 2026-07-03 00:20] T2 pin-type auditor · done → scripts/cec_sym_audit.py (real s-expr parser, 9/9 tests, TWO false-positive classes caught+fixed during calibration [W25Q IO3 bidir, MDIO open-collector alias]); REAL findings: 191 high / 40 med across cec-ent libs — 4 LOAD-BEARING on the MPFS symbol (SPI_EN/DEVRST_N/DDR_CS0/CS1 typed bidirectional, should be input) = exactly the sheet-02-gate class
- [ ] [added 2026-07-03 00:20] T2 --fix reviewed pass on cec-ent libs (191 high: mostly untyped easyeda pulls + the 4 MPFS control pins) — DEFERRED until the module-block agent releases lib/cec-ent-mcu (mid-flight consumer); runs as the sheet-02 gate with the review file committed
- [x] [added 2026-07-03 00:05] FCVG484 breakout/fanout feasibility study (agent; feeds sheet-02) · done 2026-07-03 00:30 → VERDICT: 6-layer OK WITH CONDITIONS (~87/484 balls real demand; package center ring 8-10 = 100% power; JTAG_SYSCTRL+SGMII silicon-fixed at ring 4-7 = the only forced depth, ~23 balls; via-in-pad Type VII on a bounded ~35-50-ball set; shallow-ring choice covers everything else); 8-layer triggers named (DDR populated = the big one, USB outside shallow pool, no via-in-pad fab, MCX growth). TWO new scopes → FOLLOWUPS: USB-OTG ball GAP in the cached map (MSSIO tightest bank), new BGA-fanout .kicad_dru netclass (platform 0.22mm Signal doesn't fit the 0.4mm escape channel). Deliverables: board-program/fcvg484-breakout-study-2026-07-03.md + scripts/cec_fcvg484_breakout_study.py + ring-quadrant CSV (script+CSV were swept into commit 8ad392f by the shared tree; doc lands now)
- [x] [added 2026-07-03 00:45] P4+T1 shared module block (modules/ent-common, agent) — verify + commit on landing · done 2026-07-03 00:50 → LANDED + INDEPENDENTLY VERIFIED: check_p4_t1_block.py exit 0 (~60 assertions: eFuse real series not bypassed, DETECT 10k ENT chain, pin-7 clamp chain, T1 MDI CMC→caps→TRD_P/M + ESD, CAN pins 3/6, 12/12 RMII names vs vendored PHY symbol); ERC = exactly the 3 documented-benign classes (51 lib_symbol_mismatch + 59 pin_to_pin + 7 pin_not_driven, all from Unspecified-typed vendored pins = the T2 finding); T0 render eyeballed (6 subsystem frames present). 11 honest flags → FOLLOWUPS; format note (single-sheet reference vs one-block-per-sheet rule) → FOLLOWUPS
- [x] [added 2026-07-02 23:05] (flip of the restructure item) One-block-per-sheet restructure · done 2026-07-03 00:55 → LANDED + INDEPENDENTLY VERIFIED: check_hub_ent_sch.py exit 0 (all wiring assertions + NEW 59-component/46-group electrical-equivalence guard + leaf-existence/no-dashed-frame checks); ERC = same benign classes + endpoint_off_grid 19 (warning-only, documented in KNOWN_BENIGN — side effect of the agent's REAL KiCad bug fix: a sheet-pin X gridsnapped off its box edge is SILENTLY DROPPED from the netlist); agent also found global-power-nets ≠ hierarchical-label plumbing (build_thin_parent global_power_exports path). MY POST-LANDING FIX: kicad-cli "annotation errors" warning root-caused to #PWR/#FLG refs colliding across leaves (each leaf restarted at #PWR01) → pwr_base disjoint 100-blocks per sheet in build_lib/gen, regenerated, netlist-identity re-proven (0 non-tstamp diff lines), warning GONE, no dup refs. Tree rendered (17 sheets) + thin parent/cascade leaf eyeballed
- [x] [added 2026-07-03 00:20] (flip of the T2 --fix deferred item) T2 reviewed --fix pass on cec-ent libs · done 2026-07-03 01:10 → 191 high fixes applied w/ committed review logs (board-program/pin-audit/): net 17 (PHY VDD rails, MDIO bidir, RClamp NC/GND), compute 4, mcu 170 (P4 VDD field + ADS131M08 + eMMC). THREE hand overrides vs the heuristic (all master-side): MPFS MSS_DDR_CS0/CS1 + ESP32-P4 FLASH_CS input→OUTPUT (bus masters drive CS) — logged in the review files. Auditor calibration: CS/RST orientation-dependent rules now ACCEPT a deliberate input-or-output (only unspecified/bidirectional stay suspect) + regression test (10/10). FINAL GATE: high=0 all four libs, both board checkers still pass. Sheet-02 T2 gate = SATISFIED
- [ ] [added 2026-07-03 01:10] OWNER ASK (render feedback): center sheet content, fix the spread-out composition, wire decouplers AT their IC instead of floating label-aliases = T1 integration into gen_hub_enterprise + regenerate 01a-g (netlist-identity proven) — in progress
- [x] [added 2026-07-03 01:10] (flip of the owner composition ask) T1 integration into gen_hub_enterprise · done 2026-07-03 01:45 → LANDED + INDEPENDENTLY VERIFIED: checker exit 0 (59/46 identity guard), netlist export stderr clean, ERC 100→81 (endpoint_off_grid class ELIMINATED 19→0 — parent boxes now grid-aligned AND pin-on-edge), gate 5 = 0 overlaps on all 8 sheets (was 45 pairs), gate 6 = 0 lint ERRORs (was 38), 12/12 layout tests. Leaves recomposed on A4 centered (entry left / IC center / exports right / GND down / decouplers WIRED at rails via place_decouplers+wire_decouplers), parent = real flow diagram w/ drawn eFuse→cascade wires. Agent render-iterated 3 eye-caught defects + found a REAL T1 bug (rotated-field bbox — field angle is symbol-rotation-relative; RotatedFieldBBoxTest added). Honest flags: UVLO/OVP/PGTH taps stay short label pairs (pin-pitch interleave), 15 SL-04 WARNs documented. Charter T1 row + gates 5/6 ADOPTED; my post-fix: stale endpoint_off_grid KNOWN_BENIGN note updated to RESOLVED-expect-0
- [ ] [added 2026-07-03 02:00] OWNER APPROVED generalization: promote build_lib → scripts/cec_sch_compose.py (shared composition engine) + T4 archetype extraction + recompose modules/ent-common as one-block-per-sheet hierarchy using it (proof of generality; closes the ent-common format gap) — agent launched; hub netlist identity + ent-common checker must survive
- [~] [added 2026-07-03 02:35] Generalization agent died at final turn (64k output-token API limit — tried to emit too much in one message) · obsolete 2026-07-03 02:40 → stages 1-2 were already in-tree: VERIFIED + COMMITTED dd995c6 (compose engine promoted, hub BYTE-identical netlist, archetypes w/ real callers); agent RESUMED from transcript for stage 3 (ent-common recomposition + compose tests) under hard output discipline (≤300-line writes, ≤600-word report)
- [~] [added 2026-07-03 02:40] Stage-3 resume of the generalization agent · obsolete 2026-07-03 03:45 → KILLED: it crashed a SECOND time on the 64k output cap (three ~15-min thinking-only turns, my discipline message queued behind the crash) — context poisoned toward giant single responses; no tree damage (thinking turns wrote nothing, tree clean at bf0d0f6)
- [ ] [added 2026-07-03 03:45] Stage 3 FRESH agent: ent-common recomposition (root + 6 leaves: power/misplug/can/mcu/t1-phy/usb-debug) via shared cec_sch_compose, mirroring gen_hub_enterprise as the worked example; + tests/test_sch_compose.py; build-leaf-by-leaf discipline, incremental writes — verify 8 gates + commit on landing
- [x] [added 2026-07-03 03:45] Stage 3 fresh agent: ent-common recomposition + compose tests · done 2026-07-03 04:25 → LANDED + INDEPENDENTLY VERIFIED: harness exit 0 (extended w/ 61-comp/130-group equivalence guard, no assertion weakened), node-set identity 0 missing/0 extra vs bf0d0f6 baseline, ERC 3 benign classes only (117→91 — regeneration picked up the a9e06d5 pin-typed symbols), netlist stderr clean, overlaps 0 on all 7 sheets, lint 0 ERROR, tests 4/4 compose + 12/12 layout. Root = thin parent w/ drawn sheet-pin lanes (zero crossings); 6 leaves (power/misplug/can/mcu/t1-phy/usb-debug); 8 archetypes exercised w/ real callers; 2 measured findings recorded in code (lane-through-stub-column net merge; protected_rail mid-segment label endpoint rule). FOLLOWUPS format-gap entry RETIRED. Generalization pass COMPLETE (stages 1-3)
- [ ] [added 2026-07-03 05:05] OWNER READABILITY CRITIQUE (Nuand reference sheet): my own close-read confirms — (a) pin-name glyphs invisible to the overlap gate (J1/U2 garble at "0 overlaps"), (b) hier labels scattered mid-sheet not edge-anchored, (c) diagonal flow not horizontal bands, (d) no uniform rhythm/section captions, (e) 02-misplug postage-stamp. Agent launched: composition-standard doc + pin-name-aware overlap teeth + engine iteration (edge I/O columns, flow baselines, captions, uniform grids) + regen both projects under identity gates
- [ ] [added 2026-07-03 05:40] STANDARD-TIER REFINEMENT PASS (owner: rising demand, consumer+space lens): 6 parallel review agents launched (hub-standard / atx-24pin / eps-8pin / pcie-SKUs / 12vhpwr-std / product-crosscut) → reports to docs/standard-tier-review/ → my synthesis into a refinement plan + owner decision list on landing. Read-only pass; LOCKED decisions inviolate, OQs surfaced not resolved
- [x] [added 2026-07-03 05:55] OWNER DIRECTIVES (demand message): alpha/beta revision convention + ENT-not-dropped + ~1-month ENT presentation clock · done 2026-07-03 05:58 → CLAUDE.md alpha/beta paragraph (alpha = validated prototypes, beta = owner-approved refinements, cleanly denoted, never overwrite alpha artifacts; Standard/Pro outrank ENT polish but ENT continues on its own clock); owner-queue row (presentation clock makes the dev-kit/TC order genuinely time-bound); review synthesis will frame all output as BETA proposals
- [x] [added 2026-07-03 05:40] STANDARD-TIER REFINEMENT PASS (6 reviewers) · done 2026-07-03 07:05 → ALL SIX LANDED + SYNTHESIS written (docs/standard-tier-review/SYNTHESIS-beta-plan.md): fleet reality table (hub nearest / 12vhpwr near w/ 1 shipped defect / 24-pin CRITICAL PATH w/ live rev2 erratum + R1 placeholder / EPS+PCIe placement-only 0 copper), repo-wide USB-C footprint hole-clearance defect (3 boards, hub CI red), 8-item no-decision hygiene wave W1-W8, 11-item deduped owner decision list D-1..D-11 (mirrored to owner-queue). Next: owner walks the D-list; W1-W5+W8 hygiene wave can start on approval of the beta flag
- [ ] [added 2026-07-03 07:25] BETA HYGIENE WAVE W2-W4 (sonnet agent): OQ-11 MPN writes (EPS/PCIe CSS2H-2512R-L500F; 12VHPWR stale "NOT locked" text; 24-pin gap-fill only), R1 DETECT 2.2kΩ backport on reference 24-pin (ERC/netlist-neutral proof), stale-doc reconciliation (CLAUDE.md hub items 0/3 + EPS/PCIe C6 claims, EPS 96×37, 24-pin directory-naming README + manifest + J6 mismatch flag, alpha/beta breadcrumbs) — verify + commit on landing. W1(D-11)/W5/W6 wait on owner decisions
- [x] [added 2026-07-03 07:25] BETA HYGIENE WAVE W2-W4 · done 2026-07-03 08:40 → LANDED + SPOT-VERIFIED (R1=2.2kΩ + C25879 backported, placeholder gone; CSS2H-2512R-L500F into EPS+PCIe×2 schematics+BOMs; 12vhpwr "NOT locked" text swept; 24-pin RS6 WSK2512 gap filled; rev3 RS1-3 filled; CLAUDE.md items 0/3/-1 reconciled to measured state; EPS 96×37; 24-pin directory-naming README + rev3 manifest w/ J6 mismatch flag; alpha/beta breadcrumbs). ERC identical before/after on all 6 schematics (100/100/119/39/101/68), connectivity unchanged. 4 design-level flags → FOLLOWUPS (rev3 RS4 Kelvin footprint, rev2 RS6 divergent part = owner eyeball, stale connector text in EPS/PCIe BOMs, EPS $32-vs-$34)
- [x] [added 2026-07-03 05:05] Nuand-standard composition iteration · done 2026-07-03 09:10 → LANDED + INDEPENDENTLY VERIFIED: both checkers exit 0 (hub 59/46, ent 61/130/53), all 4 test suites OK (32 tests), overlaps 0 on ALL 24 generated sheets under the RAISED pin-glyph bar (teeth: new detector found 38 real pairs on the old sheets incl. the exact J1/U2 garble; noise floor eps 55 / hub-standard 129 recorded untouched), widened symbols verified ENT-only consumers, netlist stderr clean both. Engine: edge-anchored io columns (zero io-io crossings by construction), flow-baseline place_pin, caption/note/region (KiCad 10 per-text color VERIFIED rendering), pin glyphs as nudge obstacles, 2 calibration fixes (label bbox wrong-sided at 180/270; pin-number cap-height boxes). Root cause of the garble was INTRA-SYMBOL (bodies too narrow) → 5 symbols widened. 2 measured KiCad shorts caught by identity gates (pin-under-wire-interior CONNECTS) + documented. docs/schematic-composition-standard.md = the codified spec. THIS CLOSES THE SCHEMATIC-POLISH ERA per the owner's good-enough calibration — next: capture throughput (sheet 05) + beta lanes
- [ ] [added 2026-07-03 09:20] SHEET-05 CAPTURE (module ports, sonnet agent): thin parent + 05a-port ×8 instanced per plan; per-port FTP jack / DETECT 10k+ESD / pin-7 REQ-112/114 conditioning / T1 pair exports toward 06 / CAN taps / REQ-053+110 protection posture; ALL gates incl. pin-glyph bar + checker extension w/ sheet-05 assertions + 01-subtree identity proof — verify + commit on landing
- [ ] [added 2026-07-03 09:50] 24-pin OUTPUT-INTERFACE DESIGN PANEL (owner not sold on long stub — "stub + extension = cram"): workflow wf_f5b0c3df-c8d, 5 champions (long stub / short stub / male+FF-cable / compact-proprietary+pigtails / wildcard) → 3 adversarial lenses (case-geometry, reliability, product-support) → opus scored synthesis vs owner value ordering — deliver matrix + recommendation on completion; feeds D-5/D-5a
- [x] [added 2026-07-03 09:50] 24-pin output-interface design panel · done 2026-07-03 10:15 → 9/9 agents; VERDICT: Form B corrected (12cm soldered female stub, HCS 5556 terminals, ≥90N/500-cycle strain-relief bar) 100/120 vs C (locked §2.8 incumbent) 83/120; cram objection answered = junction hides BEHIND THE TRAY; D (proprietary Micro-Fit) KILLED on derated current (~3-3.5A/ckt < 6A ATX); E killed on execution (no AOI, no retail shelf); C's telemetry defense circuit-false (INA228 senses upstream of J4). Record: atx24-output-interface-panel-2026-07-03.md. OWNER: B↔C customer-mix call + blocking strain-relief bench + §2.8 revision auth
- [x] [added 2026-07-03 10:45] BETA LOCK REGISTER (owner ask: what to lock/tweak/redesign now for long-term) · done 2026-07-03 11:20 → see the completion entry below (duplicate add-line; register landed at docs/standard-tier-review/beta-lock-register-2026-07-03.md): workflow wf_7dfb54cb-dbd — 6 web-enabled investigations (female-ATX24 board-mount part hunt, hub C1+supervisor stock/identity, USB-C footprint root-cause → D-11, pegless PCIe-keyed search → D-9, sensor supply-chain at volume, hub power-in consolidation form) each + adversarial verify → I write docs/standard-tier-review/beta-lock-register-2026-07-03.md merging with the established corpus items on completion
- [x] [added 2026-07-03 10:45] BETA LOCK REGISTER · done 2026-07-03 11:20 → docs/standard-tier-review/beta-lock-register-2026-07-03.md (12-agent verified): A-locks (C1=Samxon C487318 — Panasonic was docs-only AND out of stock; 45586 stays, NO pegless exists, 0-stock/9wk fab flag; sensors stay, no cross-vendor drop-ins; power-in = S3B-XH-A GND-center), B-work (NEW SYSTEMIC DEFECT: Manufacturer↔LCSC swap across EPS+PCIe×2 ~35-45 pairs/file = whole-board JLC import failure — W10 fix agent launched; USB-C LCSC = wrong manufacturer part, correct = XKB C319148), C-hedges (INA237 alternate, INA238 hedge-buy), D-open items sharpened (D-11: community modforjlc footprint prior art found; D-5a: F-F cable has NO commodity supply — verified)
- [x] [added 2026-07-03 09:20] SHEET-05 CAPTURE · done 2026-07-03 11:50 → LANDED + INDEPENDENTLY VERIFIED: checker exit 0 (134 comps = 59 sheet-01 + 75 sheet-05, 110 nets, 28 root exports; 01-subtree connectivity PROVEN unchanged by group projection), ERC 5 benign classes only, lint 0 ERROR, overlaps 0 all sheets, port-1 leaf render eyeballed (VCC SS110+SMAJ58A REQ-110 chain / pin-7 REQ-112/114 / DETECT §2.3 chain + notes). Fallback mechanism: 8 generated files from one compose_port() template (shared engine can't express N-instances-of-one-file — documented in plan §3); 2 additive engine extensions (global_nets for the 9-endpoint CAN bus, extra_sheets on root). 05b CAN front-end (TJA1051T/3 + split term), 05c ADS7830 DETECT ADC. 4 honest flags: SMAJ58A-vs-PESD hub/module pin-7 clamp inconsistency (reconcile), R values placeholder pending bench, ADS7830 not yet ratified per BOM-C §5, 0402-vs-1206 package Q
- [x] [added 2026-07-03 15:55 · done 2026-07-03 20:30 → see the completion entry below (duplicate add-line)] BETA SCHEMATIC WAVE (owner go): 5 sonnet agents — hub (A4 power-in + W12 comparator + H2 rung-3 DNP), 12vhpwr (W11+H3a+fan header off 12V input bus), eps (W11+H3a), pcie×2 (W11+H3a), 24-pin (J6 reconciliation FIRST + rev3-as-beta-base + W11 + INA228×4 working basis + D-5a short-stub section) — verify ERC/netlist splice gates + commit each on landing. Mezzanine D-3 RESOLVED (alternative SKU approved, K1)
- [x] [added 2026-07-03 15:55] BETA SCHEMATIC WAVE (5 agents) · done 2026-07-03 20:30 → ALL FIVE LANDED + INDEPENDENTLY VERIFIED, every board at Rev BETA-1: EPS (suite+ferrites+bypass, CAN continuity proven), PCIe×2 (same + W5 netclass/dru/USB-rename prep → W6 unblocked), 12VHPWR (same + fan header w/ pre-shunt tap PROVEN {J2.1,J3.6,R5.1,RFH6.1,RS6.1}), 24-pin rev3 (J6 contradiction CLOSED via hub-rev2 J_MEZZ evidence — K1 gate cleared; RS4 true 4-terminal Kelvin verified; bonus +5VSB/+5V_SYS bug fixed), Hub (J_PWR 3-pin consolidation, TLV7011 comparator @4.28V trip → IO14 RTC-wake GPIO bound via full pin audit, hold-up ladder DNP provisions w/ boost CORRECTED TPS61023→TPS61040/11.51V after the agent caught my 5.5V-ceiling spec error). Wave catches: CMC-DNP bypass pattern (broadcast mid-wave), 2 register MPN/part errors fixed, CMC part reconciliation + FL1 footprint + U3.EN re-strap → FOLLOWUPS. Next: W6 routing wave (3 boards ready), hub/12vhpwr beta LAYOUT passes, D-5a still the BOM-freeze gate
- [x] [added 2026-07-03 21:20 · done 2026-07-04 00:40 → see the rounds-1+2 completion entry below (duplicate add-line)] READABILITY PASS (owner flagged: labels-over-wires/arrows at U3, ref/value scatter on caps, value-through-pin-names, cramming — "spread everything out, full readability"): workflow wf_d32cbeff-1e9 — 6 boards × (sonnet fix agent w/ mandatory self-read zoomed renders ≥3 iterations, hard gates netlist-identity/ERC/overlaps<25%-of-start) → adversarial verify agent each (structured verdict; pass needs >50% overlap cut + zero label-on-wire in read tiles). My own closeup eyeball on the owner's exact regions (24-pin U3/C2) before landing
- [x] [added 2026-07-03 23:55 · done 2026-07-04 01:10 → see the completion entry below (duplicate add-line)] SPEC v1.3.0 CONSOLIDATION (owner "go ahead and do the pass"): workflow wf_0846938a-cda — opus writer folds the full 2026-07-03 ruling inventory (register A-L + D-rulings: standalone suite/ferrites/bypass pattern, enclosed product + 12VHPWR thermal menu, hub J_PWR/comparator/hold-up ladder + section-L budget, antenna keepout drop, C1 identity, fan provision, D-2, board-sharing doctrine, mezzanine OQ-77 formalization, D-1 partials incl. internal-USB-header fact, QoL adoptions, new OQ-82+ set) into the ground truth as a controlled rev + CLAUDE.md header → sonnet adversarial traceability verify (lint gates, no-unruled-changes, D-5a stays open). Verify + commit on landing
- [x] [added 2026-07-03 21:20] READABILITY PASS (rounds 1+2) · done 2026-07-04 00:40 → ALL SIX BOARDS AT ZERO text overlaps under the pin-glyph gate (from 121/209/398/71/36/42) with netlist connectivity-group identity proven per board per round (my own hub check vs the pre-pass baseline: 94/94 groups, 0/0). Round-2 lib fixes: RJ45 SHIELD names hidden + STREAM_P/N font 1.1mm (pin endpoints frozen, 6 caches refreshed); INA226-base Alert name 0.7mm fixes INA228/238 fleet-wide (they render through the same base symbol). Regressions from round 1 all fixed + verified by eye (R30-32 off the frame, +3V3 clear of TH1, C10 value regrouped, TEMP_HUB dup deleted w/ warranted junction, redundant +5VSB flag merged). 12vhpwr 6-channel label rework uniform; tab-hygiene cleaned. Verify agents read every fixed region at up to 8x12/dsf5 tiles
- [x] [added 2026-07-03 23:55] SPEC v1.3.0 CONSOLIDATION · done 2026-07-04 01:10 → APPLIED + adversarially verified: full traceability (ruling→location table; explicit deferral list), lint green (114 refs + corpus), locked decisions intact, no renumbering (new §6.14 + OQ-82..85 ascend), CLAUDE.md header flipped. Verifier's 4 findings ALL FIXED by me: H2 rung-2 buck-boost restored to the ladder text + revision log; F2 "adopted"→QUEUED (no explicit owner confirmation unlike F4/F7); §6.14 now records the EPS as-built VBUS-clamp divergence (reconcile at BOM-freeze w/ OQ-83); two index citations corrected. THE SPEC LEADS THE BOARDS AGAIN
- [x] [added 2026-07-04 02:30] ORCHESTRATOR-DIRECT readability finish (owner: "as yourself, test the toolkit") · done 2026-07-04 04:15 → toolkit was NOT fully featured: detectors existed, mutators didn't. FIVE new ops built+hardened live: snap_orphan_values, spread_power_flags (w/ 3 connectivity guards learned from 2 REAL breaks the identity gate caught: foreign-wire-interior merge, net-split via flag deletion), dedupe_power_flags (w/ wire-island union-find — flags CONNECT nets by name; the overlapping-cuts corruption also found+fixed), flip_label_collisions, wire-aware nudge. All 6 boards swept: identity OK on every board (name-aware group compare), 0 new ERC errors, findings ~770→~140 fleet-wide. Residue = mutator-immovable stacks + near-threshold cases; final targeted pass owed. 5 bare #PWR refs numbered (pre-existing annotation warning cleared)
- [x] [added 2026-07-04 04:30] cec-schematic MCP server (owner "go ahead") · done 2026-07-04 04:50 → scripts/cec_sch_mcp.py: 11 tools (check_overlaps/check_wires/adjacency_scan + 5 identity-GATED mutators w/ byte rollback + readability_pipeline + render + verify_identity); smoke-tested over stdio JSON-RPC (init, tools/list, read tool, gated dedupe = "58 groups, exact"); registered in .mcp.json beside playwright; provisioned in setup-kicad-cli.sh (pip mcp w/ PyJWT workaround). The identity rail is IN the tools — no caller can receive a silently-broken schematic
- [~] [added 2026-07-04 05:10 · obsolete 2026-07-05 21:05 → superseded: round-3 pass reconciled every board to the documented waiver set (entry 201) and round 4 then CONVERTED the 4 module boards to hierarchical composed form (entry 209), regenerating those sheets entirely; residual composed-engine floor tracked in FOLLOWUPS "Composed-engine G5 floor"] RESIDUE POLISH (sonnet agent, MCP-gated toolkit): per-finding fix-or-waiver across all 6 boards (~140 remaining) — gates: overlaps+wires 0-or-waivered, identity exact, ERC errors not increased; waivers to docs/standard-tier-review/readability-residue-waivers.md → on landing: MY closeup approval read → high-res PNG set to owner. W6 DEFERRED (owner 2026-07-04): placement-corridor supersedes routing scope (pcie-3port H56 grow, INA238 LO-tap blocker) — see FOLLOWUPS
- [x] [added 2026-07-04 07:30 · done 2026-07-04 09:40 → see the part-(a) execution entry below (all four items landed)] OWNER READABILITY ROUND 3 (four items, his crop = the +3V3/C2 case): (1) REGION FRAMES missing on 12vhpwr/eps/pcie — only hub+24pin got them; emit the accent frames per functional block on the other four (region() exists in the compose engine). (2) PCIe-2port page tall/portrait — normalize sheet size/orientation to match the fleet (check .kicad_sch paper clause). (3) FLAG ORIENTATION class NOT caught: supply flag ABOVE a cap with arrow pointing DOWN INTO the wire = needs 180° — my MISROT model used a symmetric glyph box so wire-stops-at-origin never exceeded through_len; FIX = DIRECTIONAL glyph geometry (glyph extends opposite the pin; pin direction = f(symbol type, rotation); flag when glyph side == wire side), then a rotate-180 mutator (rotation change only, origin fixed, identity-gated) swept fleet-wide. ALSO the "+3V3" value text overlaps its own flag graphic in the crop — the own-flag 0.6mm carve-out is too permissive, tighten. (4) DECOUPLER OWNERSHIP CONVENTION (owner ruling): per-IC decoupling caps must be WIRED ADJACENT to their owner IC (derive_owners + place/wire_decouplers exist in T1 — retrofit onto the flat boards), NOT label-aliased onto rails; at-a-glance ownership is the requirement. EXEMPTION: bulk decoupler FIELDS/banks may stay as rail rows. This is a real per-board layout pass w/ identity gates
- [x] [added 2026-07-04 07:50 · done 2026-07-04 18:10] OWNER GO (round 3 + routing foundation): (a) EXECUTE readability round 3 (the 4 items above: frames on 12vhpwr/eps/pcie, pcie2 page normalize, directional flag model + 180° mutator + own-flag carve-out tighten, wired-decoupler retrofit per the ownership convention). (b) ROUTING FOUNDATION SPEC: a per-board stackup register for ALL consumer beta boards — layer count, stackup NAME (owner wants named stackups), copper weights, per-class via/track geometry, pour doctrine per the CLAUDE.md board-class rules (cable boards = 12V outers + GND inners; hub/24pin = one GND inner + one routing inner) — as a committed doc + .kicad_dru/netclass reconciliation; AND the FOOTPRINT NEEDS AUDIT: every schematic part on all 6 boards vs lib/ (missing footprints, DNP provisions incl. FL1 CMC [no footprint exists — OQ-83], fan header J2, boost/buck rung-3 parts, bypass 0Rs), producing the get-list with sourcing status so routing starts unblocked. Deliverable: docs/standard-tier-review/routing-foundation-2026-07-04.md + lib additions. Respect placement-corridor deferral (W6 itself stays deferred; this is the FOUNDATION only). PART (a) DONE — see the entry below. Part (b) still open.
- [x] [added 2026-07-04 08:20 · done 2026-07-04 09:40] READABILITY ROUND 3, part (a) of entry 200 executed (part (b) routing-foundation spec NOT started, stays open above) → ALL FOUR ITEMS, gates clean on all six boards (check-overlaps/check-wires 0-or-waivered vs the existing waivers doc, identity byte-exact vs HEAD on every board, ERC errors unchanged). (1) REGION FRAMES: added to 12vhpwr-standard (8 regions), eps-8pin (9), pcie-8pin-2port (9), pcie-8pin-3port (10) — dashed frames matching the 24pin-rev3 style + BLUE (26,95,180) bold titles per the ask; per-cluster bounding boxes derived from real part-origin extents with margin iterated live after two real collisions the check-overlaps gate caught: a Value sitting 13mm off its connector's origin, and a 65-pin ESP32's own pin-name glyphs extending 30mm. (2) PCIe-2port paper "User 600 1120" (portrait, non-standard) → "A3" landscape, matching the fleet; zero identity/ERC impact. (3) DIRECTIONAL MISROT MODEL: replaced the symmetric glyph-box test with real geometry — _power_pin_dir() reads the embedded lib symbol's own pin (at)/angle + instance rotation (the same validated rotate_local convention already used for placement); MISROT now fires when the attached wire's unit vector dots negatively against the pin's outward direction, independent of wire length or box size (the old model could only ever clip glyph_half of a symmetric box, so a short stub NEVER tripped it — root cause of the owner's escaped crop). Swept fleet-wide via a byte-rollback+ERC-gated driver: 9/29/28/20/20/24 real misrotations found and fixed (hub/24pin/12vhpwr/eps/pcie2/pcie3) — ALL landed clean except one pre-existing atx-24pin own-glyph finding newly surfaced by the tightened carve-out (verified benign by render, added to the waivers doc, 13→14 waivered). rotate_flag_180() mutator added (rotation-only, origin fixed) + MCP tool `rotate_flag`. Own-flag carve-out TIGHTENED: replaced the old +/-0.6mm box (so undersized it never reached a real glyph's ~2.54mm extent — the actual bug behind the "+3V3 prints on its own arrow" crop) with _glyph_real_bbox_abs(), the ACTUAL drawn polyline/circle extent of the symbol, rotated to the instance placement. Owner's exact crop (C2/+3V3 on eps-8pin) rendered post-fix: arrow points UP away from the cap, label clear. Directional-MISROT fixture test added (tests/test_sch_layout.py::DirectionalMisrotTest, 5 cases incl. the owner's exact "flag above a cap, short stub, misrotated" reproduction — fires pre-fix, silent post-rotate). (4) DECOUPLER RETROFIT: built retrofit_decoupler_adjacency() — rigidly relocates an EXISTING cap + its own private +3V3/GND flags next to its owner IC's power pin and draws ONE real new wire IC-pin→cap-pin, identity-safe BY CONSTRUCTION (adds/deletes no #PWR/#FLG ref; verified live via netlist diff that a wire merely joining two already-same-net nodes never changes a net's (ref,pin) membership — only ref MOVES do). Gap-search closed-loop uses the real overlap/wire checkers as ground truth (a hand-modeled clearance radius missed two real collisions while building this: an unrelated pre-existing flag, and the IC's own Reference/Value text sitting on the exact same search line). Two teeth tests added (clean-layout success + congested-layout honest-refusal, byte-unchanged on failure). STATUS: capability built + safety-verified, but NOT yet applied to any board — the two real attempts (eps-8pin C10/U10, 12vhpwr-standard C10/U10) both returned False (straight-line search exhausted at max_gap on these dense boards); a full application needs a 2D/L-bend placement search (reusing the existing wire_adjacent/route_L engine) — scoped as follow-up, not done this pass. MCP tool `retrofit_decoupler` added alongside. Per-board final gate state (overlaps/wires/ERC): hub 0/29/0, 24pin-rev3 4/14(was 13)/1, 12vhpwr 1/9/0, eps 0/4/2, pcie2 0/2/2, pcie3 0/4/2 — all match the pre-existing documented waivers, only atx-24pin's +1 wire finding is new (waivered inline).
- [x] [added 2026-07-04 10:20 · done 2026-07-05 ~06:00 → all four directive items realized by the round-4 execution (see ROUND 4 ALL WAVES LANDED below)] OWNER ROUND 4 + COST DIRECTIVE: (1) MULTI-SHEET division APPROVED — "readability over all else": split crowded flat boards across real sheets; (2) GND ARRAYS: the ESP 30-pin GND ladder w/ per-pin arrows is noise — bus them to one GND link, ALL boards; (3) REGION CONTAINMENT: parts escaping subregions; 12VHPWR regions clip each other + overrun the sheet edge; all except hub+24pin need FULL rearrange. STRATEGY RULING (mine, for cost): STOP agent-polishing flat sheets — CONVERT each module board to the hierarchical composed form (one block per real sheet via cec_sch_compose, like ent-common: correct BY CONSTRUCTION, netlist-identity gated) = kills 1-3 structurally in ONE deterministic pass per board. Add cheap deterministic gates: region-containment + sheet-bounds checkers (no vision reads). (4) COST: sonnet/haiku only, NO render-read iteration loops (checkers are the judge; ONE final render for the owner), generator-code-once instead of per-defect agent nudging; budget-capped runs
- [x] [added 2026-07-04 19:05 · done 2026-07-05 ~06:00 → see ROUND 4 ALL WAVES LANDED below (duplicate add-line; wave entries carry the detail)] **ROUND 4 EXECUTION** (plan of record: docs/standard-tier-review/round4-hier-conversion-2026-07-04.md; design probe done — root local lane labels preserve flat net names, measured). Wave 1: [T] checker/mutator toolkit (region-containment + sheet-bounds + symbol-inventory + prose-preservation checkers, GND-ladder bus helper+mutator applied to hub+24pin-rev3) ∥ [R] cec_pcb_reconcile.py (relink-by-ref + group-matched net-rename + DRC parity). Wave 2: compose lane-label extension + gen-module-beta.py proven on EPS. Wave 3: PCIe×2 + 12vhpwr (strictest gates, routed PCB reconciled). Wave 4: adversarial sweep + one render/board + docs + commit. Sonnet agents only per the cost directive.
- [x] [added 2026-07-04 19:55 · done 2026-07-04 20:10] **D-5a OWNER RULING captured (connector daughterboard + sellable extension assembly)**: recorded in SYNTHESIS-beta-plan.md D-5a (ruling + addendum), owner-queue row (3 remaining owner items), FOLLOWUPS agent queue (5 items); connector kill-check research agent launched → docs/standard-tier-review/output-daughterboard-study-2026-07-04.md. Round-4 conversions UNAFFECTED (identity-gated readability; J_OUT stays as-is until the §2.8 revision lands as its own change).
- [x] [added 2026-07-04 19:05 · wave-1 done 2026-07-04 21:40] **ROUND 4 WAVE 1 LANDED** (see the execution entry above for the plan): [T] gates toolkit (cec_sch_gates.py — region-containment teeth on live 12vhpwr flat sheet = 15 findings, sheet-bounds = 423; inventory w/ DNP; prose-preservation; GND-ladder bus → 24pin-rev3 U1 18 flags→1 at 79/79 identity + reconcile-tool cross-check, hub honest no-op with 3 already-merged GND pins; MCP registered; 26 tests) + [R] cec_pcb_reconcile.py (hub calibration 73/73 exact; (path ...) = sheet-instance chain w/o root doc uuid; eps/pcie PCBs carry ZERO path fields, 12vhpwr 77/84; --schematic-parity confirmed in 10.0.4; 23 tests). Commits 49b85aa + fb85796. Wave 2 (EPS conversion agent, engine lane_labels + name_pin_nets + gen-module-beta.py) LAUNCHED.
- [x] [added 2026-07-04 23:55 · done 2026-07-05 ~07:30 → see D-5a IMPLEMENTATION WAVE COMPLETE below (duplicate add-line)] **D-5a IMPLEMENTATION WAVE (owner sign-off: blade-config part arc RATIFIED conditional on physical fit; mating-force gate DISMISSED — high force is a feature; ordered: parts onto schematics + footprints + datasheets + daughterboard projects)**: [A] spec v1.4.0 application agent (SS2.8 revision + CLAUDE.md sync) RUNNING ∥ [B] library intake agent (Keystone 3586/C238113 + 3557-2/C352820 clips, TE 63849-1/C86469 tab; footprints/symbols/datasheets/fit-memo) RUNNING → [C] main-board schematic edits (24-pin flat edit after A+B; EPS/PCIe ride their hierarchical cable-power leaves AFTER round-4 lands) → [D] daughterboard projects (per-cable, passive, tabs + TH field + MODDIY DNP alt + keying + 2oz) after B. 12VHPWR untouched. Owner-queue updated w/ the sign-off record.
- [x] [added 2026-07-05 00:30 · done 2026-07-05 01:50] **ROUND 4 WAVE 2 LANDED** (eps-8pin hierarchical conversion, proving the compose-engine extension + gen-module-beta.py chain end-to-end; working tree only, NOT committed per instruction). Engine (scripts/cec_sch_compose.py): `lane_labels` (build_thin_parent, opt-in) + `name_pin_nets` (build_leaf + build_thin_parent, opt-in) landed; backward-compat re-verified TWICE this session (original-engine-output vs modified-engine-output, both freshly regenerated, masked-UUID, 0 diff across ent-common's 7 files + hub-enterprise's 27 files — 34/34 files, every time). scripts/gen-module-beta.py (new, ~700 lines): parametric cable-i2c-family converter; partition = 7 leaves (01-hub-link 6 parts, 02-can 6, 03-ldo 3, 04-mcu 7, 05-sensing 16, 06-cable-power 6, 07-usb-flash 7 = 51); 16 inter-sheet lanes + 13 name-pinned internal nets (29 named nets total, matching the flat baseline). APPLIED to modules/eps-8pin only. FOUR real engine/driver bugs found+fixed via measured connectivity-group diffing (never assumed): (1) `tapx=lane-7.62` aliased `tapx(k)==lane(k+3)` once a by_dst group reached 4+ simultaneously-tapped members (unreachable before lane_labels) — fixed w/ a 6.35 half-step offset for groups≥4; (2)/(3) two driver-level placement collisions (a 4-wide net row sharing one attach-Y, two leaf boxes sharing one lane-math X) shorting nets together — fixed via placement changes; (4) the tap's fixed `sy+5.08` vertical drop landed exactly on the NEXT stacked pin's own row when multiple lanes share one source-box edge (lane_labels taps every lane, unlike the root_exports-only prior path) — fixed w/ a 2.54 half-pitch drop, gated on `lane_labels` so byte-identical elsewhere. Gates on the applied board: G1 (58/58 groups match) PASS, G2 (51/51 inventory incl. DNP) PASS, G3 (rename map EMPTY) PASS, G4 (ERC) 14 errors vs baseline's 2 — 13 are `label_dangling` on the 13 name-pinned nets, a DOCUMENTED KiCad ERC engine limitation (WebSearch-confirmed class, GitLab #12165/#12814-style false positives on hierarchical bare-local-label-only connectivity) that cannot be cleared without either editing the forbidden `.kicad_pro` (erc_exclusions) or using `global_label` (which breaks the zero-rename policy) — reported as the identified wall per the task's own escalation instruction, not forced; G5 (overlaps/wire_collisions/power_glyph) reduced from 56→46 findings via real fixes (dedup a 4x-duplicate-label bug, a placement double-occupancy, caption spacing) — residual wire_collisions/power_glyph counts are consistent with (often lower than) the SAME checker's findings on the reference ent-common/hub-enterprise boards used for backward-compat proof, so treated as a pre-existing engine characteristic, not a new regression; G6 (region-containment+sheet-bounds) 0/0 PASS; G8 (prose preservation) 0 missing PASS; G11 (BOM equality) byte-identical PASS. `cec_pcb_reconcile.py` extended (`_find_root_sch`, additive) to locate a hierarchical board's root via its `.kicad_pro` basename (the tool only knew "exactly one .kicad_sch" before, which broke the moment leaves existed) — reconcile run clean: 0 net renames, PCB unchanged (0 copper diff), DRC parity 361/361 equal. Two more real bugs found+fixed making the driver ACTUALLY idempotent (measured, not assumed): a `title_block` rev field never threaded through (root/leaves always said "DRAFT" regardless of source, violating the alpha/beta rev-line convention) — added an opt-in `rev=` param to both `build_leaf`/`build_thin_parent`; and a genuine non-determinism bug (`for name in POWER_NETS` iterating a hash-randomized `set` instead of the insertion-ordered `POWER_PORTS` dict, so GND/+5VSB/+3V3 stub assignment could shuffle between separate Python processes) — fixed, then RE-VERIFIED 0-diff idempotency three ways: two independent fresh-from-baseline runs, and a `--force` self-regen directly over the already-hierarchical output. `--force` refusal-without-force also verified (clean, reachable message; was previously unreachable because the pre-existing `find_flat_sch`'s "exactly one file" assumption crashed first once leaves existed — same root-fix pattern as cec_pcb_reconcile.py). tests/test_sch_compose.py +4 tests (LaneLabelTest, NamePinNetsTest, netlist-measured), 8/8 passing. Files: scripts/cec_sch_compose.py, scripts/cec_pcb_reconcile.py, scripts/gen-module-beta.py (new), tests/test_sch_compose.py, modules/eps-8pin/{eps8pin-module,01-hub-link,02-can,03-ldo,04-mcu,05-sensing,06-cable-power,07-usb-flash}.kicad_sch. NOT done this pass (Wave 3+): PCIe×2, 12vhpwr conversion; ONE final render (Wave 4, explicitly deferred per the no-render-loop cost rule).
- [x] [added 2026-07-04 19:05 · wave-2 done 2026-07-05 ~03:30] **ROUND 4 WAVE 2 LANDED (EPS)**: 7-leaf hierarchy + labeled thin parent, G1 58/58 / G2 51/51 / G3 zero renames / G4 ERC errors 1 (< baseline 2; label_dangling name-pin-stub class measured as a KiCad FP -> per-board severity=warning, audit-sch keeps real-dangler teeth) / G5 1/17/7 waivered at the composed-engine floor (driver runs the round-3 mutator battery) / G6 0/0 / G8 / G11 / audit 8/8 ok / reconcile 0 renames + DRC parity. Engine lane_labels + name_pin_nets (34/34 back-compat byte-stable); audit-sch upgraded HIERARCHY-AWARE (per-dir instance-path chains + hier/sheet-pin wire anchors; teeth verified both ways; ENT noise collapsed to real residuals -> FOLLOWUPS). Commits 336de67 + 926cc09. WAVE 3 LAUNCHED: [P] pcie x2 ∥ [H] 12vhpwr (flow partition, routed-PCB reconcile w/ full DRC parity).
- [x] [added 2026-07-04 19:05 · ROUND 4 COMPLETE 2026-07-05 ~06:00] **ROUND 4 ALL WAVES LANDED**: 4 module boards hierarchical (eps 7 leaves / pcie2 7 / pcie3 7 / 12vhpwr 11-leaf flow partition), every board zero-rename + identity-exact + ERC-errors ≤ baseline + audit ok; 12vhpwr routed PCB reconciled (77 path relinks, copper byte-identical, DRC parity 19/19); hub + 24pin GND-bused (owner item 2; 12vhpwr S3-MINI partial 24→6 stamps, FOLLOWUP); audit-sch hierarchy-aware; test suites 57/57 green (stale flat-era fixtures repointed to git-frozen baselines; reconcile tool baseline resolution now hierarchical-capable). Renders delivered (eps 8 sheets + 12vhpwr/pcie packet). Commits ff2a7a1..9dcfdc5+. REMAINING from the D-5a implementation wave: task 9 (blade interface onto main-board schematics) + task 10 (daughterboard projects) — unblocked, next up.
- [x] [added 2026-07-04 23:55 · done 2026-07-05 ~07:30] **D-5a IMPLEMENTATION WAVE COMPLETE**: spec v1.4.0 applied (03c73fe); library intake (clips/tabs vendored w/ 2 drill corrections + fit memo); blade interface on all 4 main boards (J4/J_OUT* -> TB clips + J_SIG, modeled-delta assertions PASS, Rev BETA-2, no remote-sense net existed on J4 — pins reserved); output-daughterboard projects (atx24/eps/pcie: TE tabs 1:1 to TB order, keying programmatically distinct, ERC/DRC 0 at all severities, check_output_daughterboards.py green; MODDIY not invented per OQ-88; sense-return provisioned only). Renders delivered. REMAINING = OWNER GATES: fit-check samples (~\$20), OQ-87 chassis strain-relief numbers, clip stock-depth before a production run.
- [x] [added 2026-07-05 18:50 · done 2026-07-05 20:00 → landed: 81.2×16.6 (atx24) / 53.0×14.6 (eps) / 34.6×14.6 (pcie) mm at pitches 8.9/8.6/8.2, no mounting holes, ≤15mm standing height honored, geometric no-subset-seating proof green, ERC/DRC 0; renders sent to owner. Follow-on .250-flat-tab top-entry rework is the separate active entry below] **DAUGHTERBOARD SHRINK (owner: "huge... absolutely small as possible")**: measured 238×77 (atx24!) / 110×67 (eps) / 110×63 (pcie) — the single-row + 15mm-gap-keying floorplan was wrong. Rework agent running: mezzanine 2D tab grid at the clip-body pitch floor (~8.5×6mm from the 3586 body), keying = per-family grid pitch + NEW no-subset-seating teeth in check_output_daughterboards.py (replaces the count/pitch/gap signature), gaps deleted, ceilings ≤60×28 / ≤36×28 / ≤30×28 (~7-10× area cut). Tab grid = the authoritative main-board clip mating pattern (main-board PCBs have no clips placed yet).
- [x] [added 2026-07-05 19:20 · done 2026-07-05 20:30 → docs/bench-mode-exploration-2026-07-05.md (Pro) + docs/bench-mode-max-stack-2026-07-05.md (Max, incl. §7 voltage/ceiling analysis) + docs/max-part-selection-2026-07-05.md; OWNER RULED the architecture ("I agree with everything here and it can be ruled as such") — spec application deferred to task 13 pending tab outcome + pigtail-channel answer + part-pick reaction] **BENCH-MODE EXPLORATION (owner ask)**: can Pro/Max stream ALL data raw (no event gating)? Agent running → docs/bench-mode-exploration-2026-07-05.md — per-variant rate tables (LTC2358-18 real ceiling vs the 900kB/s design point), path A (RS-485→Hub Pro→USB HS, OQ-5 becomes load-bearing) vs path B (module's OWN USB per §6.14 standalone posture — the natural bench mode), delta list, Standard-tier contrast, PROPOSED spec hook drafted for owner. Analysis only, no spec edit.
- [x] [added 2026-07-05 21:05 · done 2026-07-05 21:45 → FOUND + LANDED: TE 63951-1 (.250×.032 FASTON, true 90° mount = in-plane blade, vertical drop into the 3586 clip top entry; LCSC C591344, stock 4,345, $0.099-0.164; leg/hole geometry identical to retired 63849-1, verified vs TE drawing C=63951; every other .250 candidate confirmed straight). All 3 daughterboards regenerated, sizes unchanged (81.1×16.6 / 52.9×14.6 / 34.5×14.6); I re-verified: ERC 0 + DRC 0/0 severity-error on all 3, checker+keying proof green, net maps unchanged. OWNER FIT-CHECK ADDITION: ~0.27mm blade-hang (6.89mm) vs clip-body-height (7.16mm) shortfall at zero standoff — paper-unresolvable, recorded in owner-queue item (a) + fit memo addendum; spec/CLAUDE.md MPN sync → FOLLOWUPS pending owner confirm. Renders sent] **.250 FLAT-TAB HUNT + top-entry daughterboard rework** (owner uploaded TE 1217061-1 = .187 flat in-plane tab, wants the blade "coming out at a 90" for vertical drop into the clip's top entry)
- [x] [added 2026-07-05 21:05 · done 2026-07-05 23:15 → docs/pricing-study-2026-07-05.md FINAL (236 lines, provenance-marked, 42 arithmetic cross-checks pass; my spot-checks pass incl. bundle multiples 3.21-3.25×). Landed@100→retail: Hub 15.05→49, 24pin(+db) 36.30→129, EPS 22.60→79, PCIe2 21.05→69, PCIe3 27.55→99, 12VHPWR-Std 34.75→119 (or 99 PMD2-match), HubPro ~21.60→79, 12VHPWR-Pro 95.85→329. Bundles: base 149, Complete System 309/329/349 (owner-defined SKU), loaded 399, Pro bench 369. HEADLINE: INA228 real ladder (2.8× INA238) makes the 24-pin the priciest Standard module — INA238 fallback = LOCKED-spec owner decision → owner-queue. Open items → FOLLOWUPS] **CONSUMER PRICING STUDY (owner ask: Std/Pro Hub+module BOM vs retail after manufacture, bundle pricing)**: agent a97ac9e8 → docs/pricing-study-2026-07-05.md — Hub-standalone correction folded (base bundle repositioned on guaranteed-standby value)
- [x] [added 2026-07-05 22:20 · done 2026-07-06 00:30 → LANDED + coordinator-verified (commit 1208e20): 63951-1 legs-vertical, blade-down at 2.54-8.89mm face standoff, floating vertical-drop seat (board floats 8.4-11mm, full 7.16mm jaw engagement, clip slot rotated ⊥ wall line); boards atx24 145.1x13.6 / eps 67.8x11.0 / pcie 49.4x11.0 (≤15mm heights); ERC 0 / DRC 0/0 ×3, checker 110 OKs, keying re-proved, net maps unchanged; owner-queue fit-check row rewritten (0.27mm item retired obsolete; new items: 15.75mm chain-dim, top-entry drop, 0.84mm tolerance). Renders sent] **DAUGHTERBOARD TAB ITERATION 3 — owner sketch (flag-tab, descender down)**: owner rejected the in-plane-hang form ("tabs would just mount directly and collide... need to be rotated vertical") then sketched the true geometry: 63951-1 STAYS (the L IS the part — "the blade that has the 90 degree rotation in it"); legs stacked VERTICALLY through the standing board's face, flat L runs horizontally off the face then bends 90° DOWN, descender slots into an UP-FACING 3586 clip on the main board (slot axis perpendicular to the wall line); board edge stays clear — the tab reaches, not the board. My first correction brief (revert-to-63849-1 straight fins) was MY misread — OVERRIDDEN in full before any of it landed. Agent aa4d9c83 reworking: true-L footprint from TE C=63951 drawing, standoff + seating model, rotated clips w/ pitch-fit numbers, checker geometry re-derive w/ teeth, gates unchanged. Prior 0.27mm-hang owner-queue item + fit-memo text to be retired on landing (owner-queue re-edit mine, after)
- [x] [added 2026-07-05 22:20 · done 2026-07-05 23:15 → folded: MODDIY ruling, verified LCSC batch 1+2, connector DIRECT prices, JLC fee schedule, Complete System Bundle (owner quote recorded as source); the $70.60 anchor retained but explicitly caveated. See study-complete entry above] **PRICING STUDY REVISION PASS in flight**: v1 of docs/pricing-study-2026-07-05.md landed WITHOUT the research package (wrote "Mini-Fit Jr couldn't be priced live" though Digi-Key 87427-0802 $1.67@100 was in the relay; used the $70.60 anchor the research child had rejected as untraceable — reconcile). Resumed agent a97ac9e8 is folding in: owner MODDIY ruling (~$1 Molex at volume, JLC-population moot), full verified LCSC batch 1+2, connector DIRECT prices, JLC fee structure w/ provenance markers. Present at-a-glance to owner on landing after my BOM-CSV spot-check
- [x] [added 2026-07-05 23:20] **ARGB CONTROLLER STANDARD — schematic design (owner: "design out the standard tier ARGB hub... fully standalone... but comes with everything needed to slot right in")** · done 2026-07-06 03:30 → LANDED: modules/argb-standard/ (8 leaves + thin parent, scripts/gen-argb-standard.py) hierarchical-from-birth per spec §7. Gates ALL PASS: ERC 0 errors/123 benign warnings, netlist 103 nets/81 refs (no leaf silently dropped), audit-sch.py 0 findings ×9 files, cec_sch_gates region-containment+sheet-bounds 0 findings. Fixed several REAL bugs found only by full verification (not just the deterministic generator output): a shared-column net-merge (CAN_H_RJ/CAN_L_RJ, then the whole USB-C group VBUS/D+/D-/CC1/CC2 merged into one node via wire-through-foreign-pin transits — the worst one), 8 wire_through_body hits (FL1/R6/R7 in 04-can via choke-bypass relocation, J1 in 01-power-input, U1 EN-pullup in 05-mcu, J3 in 07-usb-flash), an off-sheet caption, and — via a full BOM LCSC cross-check (live LCSC + cross-repo) — 6 wrong LCSC part numbers (DL1 pointed at an unrelated cap, U6 at a 404, C1 at the wrong value+package, R2 a decade off, 9× R10-17/R20 at the wrong resistance, C7-9 at a mismatched variant). README.md written (OQ-29 MCU/OQ-36 mechanical/RJ-45-5VSB-proposal/BAT54W→BAT54S all flagged, not silently assumed); BOM ≈$8.30 raw parts (below the §9 $14-20 band, delta drivers noted: no PCB yet, J1 SATA connector consigned/estimated, JLCPCB Extended-part fees not modeled). NOT committed (left for coordinator verification/render). On landing: coordinator render + owner ratification list (OQ-29, OQ-36, the RJ-45 5VSB proposal, J1 sourcing)
- [x] [added 2026-07-05 23:55 · done 2026-07-06 01:20 → APPLIED + agent-verified twice + coordinator-reviewed: spec v1.5.0 (incl. §6.4 LSB recalc for 16-bit, §6.5 rationale rewrite, OQ-13 "scope unchanged/implementation basis changed", die-temp sensor RETAINED on INA238 — §6.1 12VHPWR cross-ref stays honest), 24pin-rev3 U10-U13 props-only swap (shared INA226 base symbol, netlist connectivity BYTE-IDENTICAL), bom.csv regenerated (JLC-format CSV never existed for any 24-pin variant — full sourcing pass is a separate open task), CLAUDE.md synced (+1 checklist-line fix, kept), pricing addendum-2 (24-pin landed $25.46 → $89-99 retail; Complete System held $259/$279/$299 at 2.87-2.89× within the study band; supply: 680 stock vs 850-900 full-run need → C2 hedge-buy MORE load-bearing). Work captured in WIP commits 576baac+e1b9f52, verified final] **INA238 RULING APPLICATION (owner picked INA238 over INA228 — supply/assembly-flow: LCSC-native, "tons of stock", no DigiKey consignment)**: application agent running — spec v1.5.0 controlled rev (§6.1 LOCKED edit: 24-pin 4×INA228 → 4×INA238, energy → firmware integration, OQ-13 scope unchanged) + atx-24pin-rev3 4-symbol splice swap (UUID-preserving, ERC/netlist gates) + BOM CSVs + CLAUDE.md sync + pricing addendum-2 ($259/$279/$299 Complete System re-anchor). My verification + commit on landing. Supply note: LCSC 680 vs ~850-900/100-run platform-wide → hedge-buy stands (owner-queue)
- [x] [added 2026-07-06 00:50 · done 2026-07-06 01:50 → LANDED + coordinator-verified: HEIGHTS atx24 21.4 / eps 20.0 / pcie 20.0mm (lengths 72.8/43.0/26.3); pitch floor 7.10mm derived (clip SMD pad span 6.60 binding + 0.5 clearance), chosen 7.1/7.6/8.4 (atx24 lattice-aligned); keying held on pitch differentiation alone (margins ≥1.5× tolerance, teeth re-verified at the floor); float uniform 12.41mm; ERC 0 / DRC 0/0 ×3 re-run by me, checker 110 OKs, net maps 9/6/4 unchanged. Renders sent] **DAUGHTERBOARD ITERATION 4 — compact two-band ("stack the blades right next to each other and put them below the pinout... tell us how tall these are really going to be")**: agent aa4d9c83 — field band above, tab row below at MINIMUM pitch (floor from rotated-clip row-axis dims ~3.8mm body / ~6.6mm pads + clearance), ≤15mm height cap RELAXED by owner for the honest number; keying must survive the packed floor (pitch spread or pattern offsets — the measured pcie-subset-of-eps failure mode is the guard; checker teeth re-verified); floating-seat model + net maps + gates unchanged. Report leads with the three heights
- [x] [added 2026-07-06 02:30] **DAUGHTERBOARD ITERATION 5 — bare 3557 universal clip, rotated (owner: "rotate 90... couldn't we lower the pitch that way?")**: agent aa4d9c83 — Keystone CAT 3557 top-entry bare clip (30A UL, THT, 3.8mm wide) rotated narrow-side-along-row → pitch floor ~4.5mm (from 7.1); blades unchanged (already thin-edge-on — my prior blade-width-binds claim was WRONG, corrected to owner); atx24 lattice re-derivation; keying re-proof at the new floor; main-board TB clips swap 3586→3557 (3586 = documented SMD fallback); NAMING CORRECTION: vendored "3557-2"/C352820 = the 2-in-1 housed fuse holder, NOT the bare clip — LCSC check for bare 3557 in flight. NEW #1 fit-check item: 0.81mm FASTON tab vs 0.64mm fuse-blade jaw design (27% over — expected stiffer grip, sample-gated). Expected dims ~60/33/23mm + SCOPE ADD (owner: smaller 24-pin signaling — measured J20 = 4 live pins of 10, rest = OQ-88 sense-return reservation): stub → 1×4 (PS_ON#/PWR_OK/-12V/GND), 6 reservations → DNP pad row (provision FORM change only, OQ-88 stays open), main-board J_SIG mirrored, signal crossing REVISED to owner's blind-mate form: 1×4 RA Dupont-class long-tail header on the daughterboard (pins down past the edge, parallel to blades) into a 1×4 female socket on the main board — depth-compliant like blade-in-jaw so no float over-constraint; single-motion drop-in; engagement-range + misalignment math required; JST PH demoted to cabled-fallback footnote · done 2026-07-06 ~04:30 → LANDED + coordinator-verified (ERC 0 / DRC 0/0 ×3, checker 104 OKs, keying margins 1.00/0.75/1.35, teeth re-verified): dims atx24 69.5×21.4 / eps 38.5×20.0 / pcie 26.6×20.0. HONEST FINDING: measured floor = 6.3mm NOT ~4.5 (catalog mounting details put the 3.4mm LEG PAIR along the row — the brief's jaw-axis assumption was wrong); 3557 buys only 0.3-0.9mm/pitch vs 3586; the real wins were the stub shrink (atx24) + eps pitch. Signal stub landed (1×4 RA long-tail blind-mate + SR1-SR6 DNP). Main boards: 41 TB instances property-swapped to 3557, ERC unchanged; rev3 J_SIG rewire DEFERRED → FOLLOWUPS. 3557-vs-3586 choice = owner sample-check decision (both 30A, sourcing favors 3586/LCSC-native)
