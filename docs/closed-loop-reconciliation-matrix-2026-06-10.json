{
 "_provenance": "10-agent reconciliation sweep 2026-06-10 vs branch claude/manager-tier-wiring; every claim file-read-verified then adversarially spot-checked; see docs/closed-loop-parity-plan.md for the synthesized plan",
 "counts": {
  "exists": 2,
  "partial": 47,
  "missing": 40,
  "conflict": 5
 },
 "matrix": [
  {
   "id": "CL-01",
   "status": "partial",
   "evidence": [
    "corpus/general/README.md \u2014 SB-13 single-entry schema EXISTS: class A/B/C/H, typed source object (type/ref/date), status lifecycle proposed -> sim_validated -> bringup_validated | human_approved -> deprecated, supersedes field; 'compiler consumes only *_validated/human_approved' is stated as policy",
    "corpus/general/can.json (2 entries) + corpus/general/thermal.json (3 entries) \u2014 live entries carry class/status/source/supersedes but NO state/signoff/promotion blocks (verified by loading JSON and dumping key sets)",
    "scripts/cec_corpus_lint.py:37-43 \u2014 enforced vocabularies CLASSES={A,B,C,H}, STATUSES (5-state lifecycle), SOURCE_TYPES={standard,datasheet,fab,spec,decision,measurement}",
    "scripts/constraints/corpus-extracted.json \u2014 258 rows (NOT 269); per-row fields are exactly {id,title,rule,severity,scope,rationale,source,check_method,deterministically_checkable,existing_checker,suggested_directive,category}: NO state, NO signoff, NO promotion, NO class field",
    "corpus/staging/ and corpus/promoted/ DO NOT EXIST (ls fails on both); the only corpus dirs are corpus/general/ and scripts/constraints/"
   ],
   "gaps": [
    "Create the two-zone split (staging/ agent-writable, promoted/ human-only) under whichever root DECISION 1 picks",
    "Schema additions: signoff block (human identity/date/evidence link), promotion block (date/shadow-record pointer/PR), and a state-vs-status reconciliation (see conflict)",
    "Project corpus (corpus-extracted.json) rows lack class and any lifecycle field entirely \u2014 migration must add them",
    "One-time migration of the 258 extracted rows into staging/ (DECISION 2: owner re-sign pass); no entry currently has a signoff to grandfather",
    "Linter rules for the new fields (CL-03 dependency)"
   ],
   "conflicts": [
    "The doc's proposed lifecycle vocabulary (state: draft/under-review/promoted/deprecated/superseded) contradicts the ALREADY-BUILT and lint-enforced SB-13 lifecycle (status: proposed/sim_validated/bringup_validated/human_approved/deprecated, with supersedes as a pointer field, not a state). Implementing the doc's field names verbatim breaks cec_corpus_lint.py and the seeded corpus/general entries; the two vocabularies must be reconciled (e.g. promoted ~= human_approved), not layered.",
    "Doc says the PR #18 corpus is 269 rows; the current tree holds 258 (11 double-extracted rules deduped 2026-06-09 per CLAUDE.md). Migration tooling must target 258."
   ],
   "blocked_by_decisions": [
    1,
    2
   ],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-02",
   "status": "missing",
   "evidence": [
    "No CODEOWNERS anywhere: .github/CODEOWNERS, ./CODEOWNERS, docs/CODEOWNERS all absent (ls verified)",
    "gh api repos/nathanfraske/CEC-Platform/branches/main/protection returns 404 'Branch not protected' \u2014 NO branch protection on main at all (no required review, no code-owner requirement)",
    "Agents currently operate under the owner's own identity (git user nathanfraske; gh authenticated as owner) \u2014 exactly the 'server-side gate is theater' condition the doc flags; no machine account exists",
    "corpus/promoted/ does not exist, so there is nothing for a CODEOWNERS path rule to bind yet (CL-01 dependency)"
   ],
   "gaps": [
    "Add CODEOWNERS mapping corpus/promoted/** (and the future cec-policy.json) to the owner",
    "Enable branch protection on main: require PR + code-owner review, no bot bypass, dismiss-stale-approvals, require-approval-of-most-recent-push",
    "Create a dedicated machine account for agents/orchestrator with no approval rights; move the night-box token off the owner's PAT",
    "Document the branch topology (staging branch vs PRs-everywhere) per DECISION 1",
    "Verify with a test PR from the agent account against promoted/** (unmergeable) vs staging-only (mergeable)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    1
   ],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-03",
   "status": "partial",
   "evidence": [
    "Linter-in-CI leg EXISTS: scripts/cec_corpus_lint.py (source-type whitelist line 40; model-source REJECT lines 108-112 and 171-173; Class C must cite ledger run lines 113-117; Class H may never apply to deterministic consumers lines 129-131; status=proposed warned as judge-visible-never-compiled lines 132-134) wired via scripts/checklist.sh:49-54 into .github/workflows/kicad-checks.yml:27-28 (runs on push + pull_request)",
    "Registry/checker plane EXISTS: scripts/cec_constraints.py \u2014 Constraint dataclass with status ratified|proposed and severity incl. 'advisory' (lines 39-56), 30+ deterministic checkers, run()/report()/directives(); consumed by cec_place.py:32,462, cec_loop.py, cec_sweep.py:52-61, cec_cascade.py:35,51",
    "Artifact emitters EXIST: cec_constraints.derive_cross_section_dru (line 986) + ratify_cross_section (line 1021) append connection_width rules to a board .kicad_dru behind an explicit human write=True gate; cec_router.spec_to_dru:197; cec_pcb.write_netclasses:319 + write_dru:329",
    "Severity-advisory handling exists piecemeal: min-pour-cross-section is severity=advisory and explicitly does not hard-gate (cec_constraints.py:96-107); cec_cascade.py FEM splits blocking vs advisory flags (~lines 163-188)",
    "MISSING core: NOTHING compiles corpus entries into artifacts \u2014 corpus/general/*.json is read ONLY by the linter (grep: no other consumer), corpus-extracted.json only by the linter + cec_ledger input hash; the load-bearing registry is hand-maintained Python, not compiled from corpus state",
    "MISSING: no ADV- namespace anywhere (grep 'ADV-' over scripts/ and docs/ is empty); cec_synth_pipeline.human_signoff (line 2329) counts blocking flags by conf>=0.5, with no advisory-namespace exclusion"
   ],
   "gaps": [
    "Build the actual corpus->artifact compiler: promoted-state filter -> blocking set (.kicad_dru fragments, netlist assertions, keep-apart tables, scorer limits); staging -> separate ADV- advisory set",
    "Wire ADV- flag namespace through the cascade/decision logs and exclude it from human_signoff's blocking count",
    "Add the new linter rules: blocking artifact may only reference promoted entries; promoted requires signoff + resolvable source (needs CL-01 fields first)",
    "Decide how the hand-curated cec_constraints REGISTRY relates to compiled corpus entries (today the registry IS the enforcement and ignores corpus state entirely \u2014 run() executes every checker regardless of status)"
   ],
   "conflicts": [
    "The doc's dual-target premise \u2014 'compile geometry constraints into the netclass layer the router consumes' so candidates don't violate during routing \u2014 contradicts the repo's MEASURED finding: Freerouting IGNORES netclass widths even when the DSN export carries them correctly (CLAUDE.md:1053 'TRACE SIZING IS NOT HONOURED'; FR routes everything at 0.2mm defaults). The repo's adopted mechanism is post-route copper construction instead (cec_fr.add_power_pours:268, cec_fr.synthesize_power_copper:525). A netclass-layer compile target will not prevent in-route violations; the dual-target design needs rework against this fact (e.g. compile into post-route pour/width synthesis + DRC, not into router-consumed netclasses)."
   ],
   "blocked_by_decisions": [
    1
   ],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-04",
   "status": "partial",
   "evidence": [
    "Landing points EXIST and are live: cec_synth_pipeline.run_stage:785 + run_full_cascade:793 (six-stage cascade); scripts/cec_cascade.py (PLACE->ROUTE->FEM->APEX swarm cascade consuming cec_constraints.report); scripts/cec_dispatch.py (compute-as-tools + budgeted tier loop); scripts/cec_ledger.py append:158 (per-RUN JSONL with constraint_corpus_sha256 in input_hashes:137-150) wired fail-safe into cec_router.py:947-959 and cec_synth_pipeline.py:2453-2464; cec_overnight.py REVIEW blocks run cec_judge_local.corpus_fit_review (~lines 204-214) on the host",
    "MISSING core: scripts/cec_shadow.py does not exist (full scripts/ listing checked); ledger records are per-run (run_id/board/mode/verdict/hashes), NOT per-entry fire events with entry ID + candidate hash + locus",
    "MISSING: no judge true/false-positive annotation mechanism in cec_overnight.py or cec_judge_local; no promotion-request generator; the SB-02 inbox does not exist (only referenced as open in docs/self-building-pipeline-addendum-2026-06-09.md:400)",
    "MISSING: no advisory set exists for the cascade to run nightly (CL-03 prerequisite)"
   ],
   "gaps": [
    "Per-entry fire events appended to the ledger (entry ID, board, candidate hash, run ID, locus) \u2014 extend cec_ledger.append usage or the extra field",
    "Build scripts/cec_shadow.py aggregator (per-entry fire history + judge annotations)",
    "Judge annotation pathway in the morning review (CL-12 coupling) \u2014 cec_overnight REVIEW blocks currently only do corpus-fit critique",
    "Promotion-request generator emitting inbox items \u2014 blocked on the SB-02 inbox existing at all",
    "Depends on CL-01 zones + CL-03 advisory compilation before any of this has input"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-05",
   "status": "partial",
   "evidence": [
    "Conflict-detection seed EXISTS: cec_corpus_lint.py:135-145 \u2014 duplicate (kind, scope) conflict across the general corpus errors with 'supersede (supersedes + deprecate the old entry) instead of shadowing'; deprecated entries are excluded from the scope map",
    "Lifecycle exit states EXIST in schema: STATUSES includes 'deprecated' (cec_corpus_lint.py:39); 'supersedes' field present in corpus/general/README.md schema and in every live can.json/thermal.json entry",
    "MISSING core: no promoted/ zone and no CL-02 server gate, so 'transitions out of promoted are human-only' is currently unenforceable; no misfire-evidence flag mechanism (nothing in the judge/bench path attaches a misfire record to an entry); no inbox to surface it (SB-02 absent); no promotion machinery to block while a conflict stands"
   ],
   "gaps": [
    "Cross-zone (staging-vs-promoted) contradiction detection \u2014 today's check is exact duplicate (kind,scope) within corpus/general only, not contradictory-assertion overlap between zones",
    "Misfire-evidence record attachment (judge/bench flag -> entry record -> inbox item)",
    "Promotion-blocked-while-conflict rule (no promotion path exists to block yet)",
    "Both depend on CL-01 zones and CL-02 gate landing first"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-06",
   "status": "conflict",
   "evidence": [
    "Spec-resolution mechanism PARTIALLY EXISTS: cec_corpus_lint.lint_project (cec_corpus_lint.py:162, 179-186) verifies every spec-section citation (section N.N) in a project-corpus row's source still resolves in CEC-Platform-Ground-Truth-Spec.md and errors otherwise ('stale after a spec rev?') \u2014 the mechanical SB-11 half, wired into CI via checklist.sh",
    "Class taxonomy + spec source type EXIST: corpus/general/README.md lines 9-22 define the classes; SOURCE_TYPES includes 'spec' (cec_corpus_lint.py:40)",
    "MISSING: lint_general performs NO spec-line resolution at all \u2014 a general-corpus entry with source.type='spec' citing a nonexistent section passes the linter; no inbox templates exist (SB-02 absent) to default agents onto the spec-first path"
   ],
   "gaps": [
    "Resolve the class-taxonomy mismatch with the owner/doc first (see conflict), then add a spec-line resolution check to lint_general for whichever class is 'spec-derived' (today that is Class B / source.type='spec')",
    "Linter rejection test (cite a nonexistent spec section -> fail) for the general corpus",
    "Inbox templates making spec-revision-first the default for spec-class assertions (blocked on SB-02 inbox existing)"
   ],
   "conflicts": [
    "The doc asserts 'Class A entries are spec-derived; their source must resolve to a line in the current spec version.' The as-built SB-13 taxonomy is INVERTED: corpus/general/README.md defines Class A = pointer/parameter citing an EXTERNAL authoritative source (IPC-2221 etc. \u2014 see thermal.json's IPC k-param entries) and Class B = the spec-derived platform rule carrying the spec-section source (see can.json). Implementing CL-06 verbatim would bind the spec-resolution rule to the wrong class and reject every legitimate IPC-sourced Class A entry. Either the doc's class letters must be remapped to the repo's (CL-06 logic applies to Class B / source.type='spec'), or the owner renames the taxonomy \u2014 a reconciliation decision before any linter change."
   ],
   "blocked_by_decisions": [],
   "part": "corpus-enforcement"
  },
  {
   "id": "CL-07",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py:main/route_block/review_block \u2014 deadline-bounded unattended nightly driver alternating ROUTE/REVIEW blocks; per-route try/except (route_block catches and logs, run continues); manifest.json persisted after every route/review via _save_manifest; per-stage wall-times recorded (elapsed_s per route at route_one, per review at review_block); review-side resume via _reviewed_set() (skips corpus logs that already have a -corpus-fit.json sidecar)",
    "/home/nathan/CEC-Platform/scripts/cec_dispatch.py:runner_slots/runner_slot (lines 122-165) \u2014 cross-process flock slot semaphore bounding total concurrent FR jobs (the team-safe local compute pool); request_candidates(where='runner') routes through it",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:append/manifest \u2014 ledger line with determinism manifest, wired into routes at scripts/cec_router.py:947-959 (fail-safe cec_ledger.append after every route) and CEC_PARENT_RUN_ID restart lineage via lineage()",
    "Doc's R-gates verified landed: R-01 scripts/cec_dispatch.py:_spread_params (line 168) + sha256 dedupe in _generate_scored; R-02 scripts/cec_score.py:Metrics.drc_types/drc_loci (lines 133-134, one DRC per candidate); R-05 scripts/cec_toolchain.py (imported by cec_dispatch line 45 and cec_ledger line 44, render degrades at cec_dispatch.py:246)"
   ],
   "gaps": [
    "No scripts/cec_orchestrator.py phase scheduler (generate/check/score/analyze/bundle phases as first-class stages) \u2014 cec_overnight.py is a two-phase (route/review) driver only",
    "No queue consumption: no GitHub Issues inbox (SB-02 still open per CLAUDE.md) and no file queue; BOARDS is a hardcoded list (cec_overnight.py:40)",
    "No per-candidate quarantine directory and no watchdog process; no resume-from-LEDGER \u2014 a restarted cec_overnight re-runs routes from round 0 (only the review sidecars resume); cec_overnight itself appends no ledger lines (manifest.json only; the inner cec_router appends route lines)",
    "No scheduled-task unit in the repo (Task Scheduler / systemd for the orchestrator; the only systemd unit is the LLM broker, outside the repo)",
    "No measured-cost budget allocator (successive halving): route_block escalates passes/opt on a fixed round-index schedule (cec_overnight.py:160-161), not from measured stage costs"
   ],
   "conflicts": [
    "The doc's landing point 'cec_dispatch.request_candidates where=\"runner\" seam' assumes the synth.yml self-hosted GitHub-dispatch leg (R-12). In the current tree where='runner' was REPURPOSED to mean the bounded LOCAL slot-semaphore pool \u2014 explicitly 'NOT a GitHub Actions runner' (scripts/cec_dispatch.py:122-127, 226-229). R-12 (the GitHub-dispatch leg) remains OPEN per CLAUDE.md, so the doc's gate R-12 is the one unlanded prerequisite, and an orchestrator built on the 'runner' seam gets local compute, not remote dispatch"
   ],
   "blocked_by_decisions": [
    3
   ],
   "part": "loop-closure"
  },
  {
   "id": "CL-08",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_loop.py:run_loop \u2014 the inner\u2192middle re-entry slice exists: residual MOVABLE directives after the routed-board check feed the next placement iteration (loop break condition at lines 171-184), and step 3b RESERVE-REROUTE-PLACE (lines 144-169) is a concrete rip-route+keepout+re-route re-entry mechanic; run_candidates (lines 213-238) is a crude wall-attribution (no candidate clears hard FAILs -> escalate_to_human=True)",
    "/home/nathan/CEC-Platform/scripts/cec_router.py:default_manager (line 301) + MANAGER_REPAIRS registry (line 539, incl. kelvin_inversion_repair geometric perception) + default_escalator (line 347, Kmax-stall structural re-plan, Spec.Kmax line 85) \u2014 the inner routing loop's repair/escalate attribution primitive",
    "Deterministic features partially available: scripts/cec_score.py:Metrics.drc_types/drc_loci (lines 133-134), scripts/cec_dispatch.py:CandidateMetrics.unconn_nets (line 118), check-ID verdicts from scripts/cec_constraints.py (registry lines 60-220), robust-z/MAD metric prepass in scripts/cec_judge_local.py corpus_fit_review (~line 1060)"
   ],
   "gaps": [
    "No scripts/cec_attribution.py and no attribution RULE TABLE (unroutable-across-all-param-sets\u2192placement fault; across-all-placements\u2192relaxed-but-legal rules variant before outline fault; hotspot/violation-at-via-field\u2192routing-rule fault; promoted-entry fire\u2192design fault)",
    "No OUTER (dimensions/outline) loop anywhere: no bisection exists (only a 'real bisection later' comment at scripts/cec_synth_pipeline.py:2510); board outline is never varied by any loop, so 'outline fault, bisection direction up' has no consumer",
    "No DRC-violation clustering by component/net as an extracted feature; FEM hotspot loci (electrothermal_solve) not fed into any attribution path",
    "No bandit reward/penalty state in the ledger; no per-board-family outline priors from ledger success rates; manager-tier attribution narrative not written to ledger (DecisionLog records route verdicts only)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "loop-closure"
  },
  {
   "id": "CL-09",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_synth_pipeline.py:place_candidates (line 1830, strategy\u00d7seed parallel candidate generation on a spawn pool) and place_with_consent (line 1854, user-pin consent loop: a binding pin triggers ask(APPROVE/KEEP/EDIT) rather than a silent override)",
    "/home/nathan/CEC-Platform/scripts/cec_loop.py:run_candidates + BOARD_PARAMS (lines 40-45, 213-238) \u2014 param-varied placement candidates ranked by hard-constraint FAILs, with the board-specific human-ratified override mechanism",
    "/home/nathan/CEC-Platform/scripts/cec_constraints.py registry \u2014 the pre-route hard checks the doc lists exist as check IDs: hot-sensitive-separation (line 115, the keep-apart class), kelvin-sense-adjacent-shunt (line 65), decoupling-cap-owner (line 171), connector-mouth-faces-edge (line 152); scripts/cec_cascade.py:placement_metrics judges a placement pre-route with route-time IDs excluded (ROUTE_TIME_IDS)",
    "/home/nathan/CEC-Platform/scripts/cec_place.py:refine/tighten_kelvin/apply_adjacent (rotation-searching perturbation operators, lines 216-314)"
   ],
   "gaps": [
    "No portfolio generator emitting N legal placements per board per night with the doc's operators (seeded perturbation of unpinned refs, block-level alternatives, rotation sets on symmetric blocks, cluster swaps) \u2014 cec_loop varies one param (kelvin_tighten_mm), cec_place applies constraint directives to ONE placement",
    "No mandatory portfolio\u2192hard-constraint pre-route gate wiring (cascade's placement_metrics judges a single floorplan; nothing iterates a portfolio through cec_constraints before routing compute)",
    "No soft-pin class (ref movable within a stated radius) anywhere; no consent FILE format at all \u2014 consent is an in-process ask() callable (cec_synth_pipeline.py:1854-1885), not a persisted artifact; no placement-hash distinctness/dedupe machinery and no pinned-ref byte-identity check"
   ],
   "conflicts": [
    "Doc landing-point naming drift: scripts/cec_hc.py is the deterministic HIGH-CURRENT/Kelvin copper toolkit (route_high_current, escape_gnd_pins, escape_reservations, kelvin_keepouts), NOT a placement hard-constraint checker \u2014 the hard-constraint registry is scripts/cec_constraints.py (consumed via cec_place._check). Also CLAUDE.md action item -2 records the synth placer (place_candidates' engine) as DESIGN-PASS DEFERRED by owner direction (domain-blind: no high-current corridor / Kelvin-adjacency hard constraints) \u2014 a portfolio built directly on it inherits that deferred wall; the doc's verify step (N legal placements all passing pre-route checks) requires exactly the integration the deferral notes as missing"
   ],
   "blocked_by_decisions": [],
   "part": "loop-closure"
  },
  {
   "id": "CL-10",
   "status": "missing",
   "evidence": [
    "cec-policy.json ABSENT (repo-wide find for *policy* returns only docstring mentions in scripts/cec_cascade.py / cec_router.py / cec_synth_pipeline.py referring to pluggable judge policies, an unrelated concept); no CODEOWNERS file anywhere (.github/CODEOWNERS, /CODEOWNERS, docs/CODEOWNERS all absent)",
    "Role-to-model bindings currently live as ENV-VAR resolution, not policy: scripts/cec_judge_local.py MODEL (line 59), WORKER_MODEL (64), MANAGER_MODEL (71), REVIEWER_MODEL (83); the broker's models.json registry lives OUTSIDE the repo (/home/nathan/cec-llm-broker)",
    "scripts/cec_ledger.py:manifest() exists as the hook where a policy hash would be recorded, but records only kicad/freerouting/scripts-sha/python \u2014 no policy_version or policy hash field"
   ],
   "gaps": [
    "Create cec-policy.json (role-to-model bindings with model ID/quant/license_cleared/eval-gate fields, bandit bounds, per-night budgets, cascade thresholds, prompt hashes, policy version) + CODEOWNERS protection",
    "Orchestrator load-at-start, policy-hash into every ledger manifest line, refuse-to-run on license_cleared:false or failed eval gate, in-bounds clamp with logged clamps \u2014 none exist (and the orchestrator that would host them is itself CL-07, partial)",
    "Migrate the scattered env-var bindings (cec_judge_local) and hardcoded budgets (cec_overnight CLI defaults, cec_dispatch budget=3) under policy bounds"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "loop-closure"
  },
  {
   "id": "CL-11",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_golden.py:run_golden/make_bands/compare \u2014 SB-08 golden regression exists with expectation BANDS, hard-gate (kelvin/diffpair) no-regress rules, --freeze human-approved band bump, and a fail-safe ledger append; fixture tests/golden/eps-8pin/eps8pin-module.kicad_pcb + tests/golden/expectations.json (baseline frozen 2026-06-09: drc 4, tracks 556, thermal_max_T 128.2)",
    "/home/nathan/CEC-Platform/.github/workflows/kicad-checks.yml \u2014 CI runs checklist.sh (incl. cec_corpus_lint, line 51) + check-all.sh ERC/DRC (the R-03 gate) but does NOT invoke cec_golden; cec_golden.py header records it as a manual pre-merge run in the routing container"
   ],
   "gaps": [
    "Only ONE fixture (eps-8pin). The doc's four audit board-states are absent: no Hub Standard TPS2121-defect pre/post pair and no 12VHPWR 136-signal-size-via pre/post pair under tests/golden/ (verified: tests/golden contains only eps-8pin + expectations.json; fab/ snapshots hold gerbers/BOM/CPL only, zero .kicad_pcb)",
    "No CI job running the cascade against fixtures asserting FLAG-ID invariants (known-bad fires expected check IDs, known-good fires none) \u2014 cec_golden compares metric bands, not check-ID sets; no blocking trigger scoped to scripts/, cec-policy.json, corpus/promoted/ (corpus/promoted/ does not exist; only corpus/general with can.json + thermal.json)",
    "No expected-fail marker mechanism; no held-out eval pool separate from the goldens; no TPS2121 Class B entry in corpus/general (TPS2121 appears only inside scripts/constraints/corpus-extracted.json constraint rows)"
   ],
   "conflicts": [
    "Doc claims 'the via case is checkable today with the netclass-conformance parser check' \u2014 no such check exists: scripts/cec_constraints.py has no netclass-conformance or via-size-vs-netclass check ID (grep 'netclass' hits only a comment at line 104; nearest are trace-width-high-current and kelvin-sense-fcu-no-via); the in-tree via machinery is cec_fr.normalize_via_annular (annular-ring repair), a different mechanism",
    "Doc treats the execution venue (DECISION 4) as open; the tree already records the owner decision \u2014 cec_golden.py header (2026-06-09): run ON-DEVICE in the routing container (docker compose run routing). Not blocking; the doc's anchor has drifted"
   ],
   "blocked_by_decisions": [],
   "part": "loop-closure"
  },
  {
   "id": "CL-12",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:VERDICT_SCHEMA (lines 118-126) \u2014 a machine-readable judge verdict exists but is minimal ({action: accept|repair|escalate, reason}); CORPUS_FIT_SCHEMA (line 779) + corpus_fit_review (line 1111) \u2014 a RICH structured verdict (fit_classification, recommendation, confidence, concerns, matched/violated precedents, trajectory) emitted as guided-JSON sidecars (<stem>-corpus-fit.json), with reviewer model identification via REVIEWER_MODEL/REVIEWER_URL",
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block + manifest.json \u2014 the proto-morning artifact: per-route and per-review records (gates/drc/objective, fit/rec/conf/headline) accumulated nightly under build/overnight/",
    "Render primitives exist: scripts/cec_dispatch.py:render (line 245, kicad-cli pcb render), scripts/cec_plot.py copper_plot/render_3d used for the human-review handoff in scripts/cec_loop.py:run_loop (lines 186-197)",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:append \u2014 the substrate where labeled owner-override cases would land (append-only, corrects= primitive exists)"
   ],
   "gaps": [
    "No bundle builder: nothing assembles per-finalist {render, score row with deltas vs a ledger incumbent, decision log, fired checks gate-vs-advisory, resolved generator config, novel-flag list}; the ledger has no incumbent concept and no novel-flag tracking; no ADV- advisory flag class exists anywhere in the tree",
    "The verdict schema is NOT the doc's: VERDICT_SCHEMA lacks run ID, board, ranking, cited check IDs per reason, drafted-entry references, novel-flag list, confidence, and judge-model manifest (model ID/quant/prompt hash); CORPUS_FIT_SCHEMA is per-board precedent-fit, not a per-candidate finalist verdict \u2014 the field set the doc wants is unlocked (DECISION 7)",
    "No advisory true/false-positive annotation path (CL-04 shadow records have no producer)",
    "No owner verdict action surface (inbox or CLI); an owner accept/override writes nothing \u2014 no labeled case (judge verdict + owner verdict + board + run) lands in the ledger, so the SB-09 calibration stream has no source"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    7
   ],
   "part": "loop-closure"
  },
  {
   "id": "CL-13",
   "status": "missing",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:main (lines 239-281) \u2014 the CLI has exactly three subcommands: append, query, lineage. NO `label` subcommand and no outcome-label schema anywhere in the module; lineage() (line 222) exists as the run-lineage attachment point the labels would render through",
    "/home/nathan/CEC-Platform/scripts/cec_bench_manager.py \u2014 despite the name, this is an LLM manager-model verdict BENCHMARK (posts the three differentiated manager contexts through the broker), NOT hardware bench write-back; do not mistake it for a CL-13 landing",
    "Prediction sources exist to label against (scripts/cec_synth_pipeline.py electrothermal_solve outputs, e.g. thermal_max_T consumed in cec_golden.run_golden) but no predicted-vs-measured record type exists"
   ],
   "gaps": [
    "Outcome-label schema attached to run lineage: predicted hotspot loci/margins vs measured temperatures, predicted Kelvin channel matching vs measured spread, escaped-defect\u2192check-ID mapping with a 'no check existed' branch that drafts a staging corpus entry \u2014 all greenfield",
    "`cec_ledger.py label <run_id> ...` CLI for bench-session entry \u2014 absent",
    "Calibration feed wiring into CL-12 (judge calibration) and CL-04 (entry justification) \u2014 no consumers exist yet either",
    "Note: the ledger's append-only + corrects= + extra= fields mean the schema can land as a new record mode without touching existing lines \u2014 the substrate is ready, only the label record type and CLI are missing (the doc's 'S now' sizing holds)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "loop-closure"
  },
  {
   "id": "CL-14",
   "status": "conflict",
   "evidence": [
    "/home/nathan/cec-llm-broker/models.json \u2014 the de-facto role registry: cec-worker (Qwen3.6-35B-A3B UD-Q4_K_M, vram_gb 27, max_inflight 4), cec-worker-quality (Qwen3.6-27B Q4_K_M, vram_gb 22), cec-manager (MiniMax-M2.7 229B-A10B UD-Q3_K_XL, vram_gb 28 + ram_gb 96), cec-manager-fast (gpt-oss-120b MXFP4, vram_gb 9 + ram_gb 60), cec-judge (old vLLM 30B AWQ); per-model residency fields (vram_gb/ram_gb/idle_stop_s/max_inflight/start_timeout_s) exist but NO format_reliability, reasoning_depth, license, or eval-gate fields",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:59-115 \u2014 code-side tier bindings via env vars: MODEL/WORKER_MODEL/MANAGER_MODEL (default cec-worker, deliberately in-loop worker-class per the 2026-06-10 bench verdict), REVIEWER_MODEL resolution order (CEC_VLLM_REVIEWER_MODEL > CEC_VLLM_MANAGER_MODEL > 'cec-manager-fast'), _FLOOR_MODELS={'cec-manager'} as a per-model behavioral contract (M2.7 sampling floors)",
    "Verified absent: `find / grep` over the repo finds no cec-policy.json or any *policy* file; no license/eval-gate fields anywhere; CLAUDE.md lists SB-10 policy-as-code as STILL OPEN"
   ],
   "gaps": [
    "cec-policy.json itself: role declarations (worker/verifier/extractor/manager/analyst/judge/frontier) with contract fields (format_reliability, reasoning_depth, throughput, context budget, residency cost, license status, eval-gate status)",
    "Orchestrator refusal path: nothing today refuses a binding on a failed/absent eval gate or license flag (cec_judge_local and the broker bind whatever the env/registry names)",
    "Verifier and extractor ROLES do not exist \u2014 the 27B is registered only as cec-worker-quality (a swap-in quality worker); the extractor function exists only as the anonymous scribe call inside _chat_json",
    "Name reconciliation: doc role 'manager'=gpt-oss-120b maps to registry 'cec-manager-fast', while registry 'cec-manager' is M2.7 \u2014 the policy file must resolve this naming collision explicitly"
   ],
   "conflicts": [
    "Doc binds analyst = Qwen3.5-397B-A17B (~165-175GB UD-Q3_K_XL): the model is NOT in the store (/mnt/e/'AI Models' holds only gpt-oss-120b, minimax-m2.7-ud-q3kxl, qwen3.6-27b, qwen3.6-35b-a3b), E: has only 66GB free (df: 3.6T/3.7T used) so it cannot even be downloaded, and measured RAM is 125GB total (free -g) so it cannot be RAM-resident \u2014 the measured Path C lesson (141GB M2.7 Q4 > RAM => ~20GB/expert 9p rotation at 3.73 tok/s) says it would be unusable even if downloadable",
    "Doc lists M2.7 BLOCKED pending CL-20: in the current tree M2.7 is an ACTIVE registered tier \u2014 broker models.json 'cec-manager', docker/compose.yaml manager-m27 profile (default model dir/file), cec_judge_local documented opt-in deep auditor (CEC_VLLM_REVIEWER_MODEL=cec-manager) with bench-verified miner->scribe support; no license gate exists",
    "Doc 'worker ~22GB Q4' vs registry vram_gb 27 for cec-worker (UD-Q4_K_M); re-verify-at-download caveat applies"
   ],
   "blocked_by_decisions": [
    5
   ],
   "part": "analyst-tier"
  },
  {
   "id": "CL-15",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:_chat_json (lines 170-199) \u2014 the analyst/extractor SEAM in recovery form: empty `content` with the answer stranded in `reasoning_content` triggers a second SCRIBE call instead of raising (bench-verified miner->scribe protocol, 2026-06-09)",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:SCRIBE_SYSTEM (202-207) + _scribe_json (210-229) \u2014 extractor contract ('Do NOT re-derive, re-reason, extend, or second-guess the trace; extract and transcribe its answer exactly'), grammar-constrained via json_schema response_format, trace TAIL-capped (_SCRIBE_TRACE_CHARS=36000) to fit the 16k manager context",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:105-108,188-190 \u2014 _FLOOR_MODELS sampling floors (temp>=0.3 + presence_penalty 0.8) auto-applied to M2.7, the measured anti-decode-loop guard",
    "/home/nathan/CEC-Platform/tests/test_judge_local_scribe.py \u2014 stub-HTTP unit tests of the floors, the empty-content->scribe recovery, the trace tail cap, and REVIEWER_MODEL resolution (no GPU/broker needed)",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:corpus_fit_review (1111-1175) + _cf_no_opinion/_cf_clamp \u2014 a caller-level fail-safe no_opinion outcome and a post-parse gate clamp (the safety-property analogue at the reviewer level)"
   ],
   "gaps": [
    "No `deep_verdict(bundle)` unit \u2014 the composite exists only as a FAILURE-RECOVERY path inside _chat_json (fires only when content comes back empty), not as a deliberately-invoked analyst(prose-contract)->extractor pipeline",
    "Analyst trace is never stored, hashed, or ledgered (grep confirms no persistence of reasoning_content anywhere; cec_ledger manifest carries kicad/FR/scripts fields only)",
    "No span machinery at all: VERDICT_SCHEMA and CORPUS_FIT_SCHEMA have no quoted-evidence-span fields; no deterministic span verifier (string-match vs trace); no conclusions-only sourcing rule for verdict-class fields",
    "No 'no conclusion reached' extraction outcome: _scribe_json raises ValueError on empty content (-> deterministic fallback) and would happily transcribe from a conclusionless trace \u2014 the hallucinated-verdict failure mode is unguarded and untested",
    "No re-extract attempt + frontier-judge / owner-inbox escalation ladder (current behavior on scribe failure is a silent deterministic fallback, which the doc explicitly forbids for the deep path)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    7
   ],
   "part": "analyst-tier"
  },
  {
   "id": "CL-16",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_router.py:961-975 \u2014 deep review is gated (CEC_CORPUS_REVIEW=1), runs once per routed board, writes an advisory <board>-corpus-fit.json sidecar, never feeds back into route()",
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block (200-231), _pending_corpus (180-182), ensure_manager (115-118) \u2014 pending deep jobs QUEUE and BATCH into the reviewer residency window (broker-warmed), with a per-block wall-clock budget (review_timeout + --review-block + the --hours deadline, route runs set CEC_CORPUS_REVIEW=0 at line 125 so reviews only happen in review blocks)"
   ],
   "gaps": [
    "Routing is FREQUENCY-based (every routed board gets exactly one review), not TRIGGER-based: none of the doc's deterministic triggers exist (attribution-ambiguity tie, novel flag type, gate-vs-advisory contradiction, judge-confidence threshold, Pareto-anomaly, swarm contention per CL-24) \u2014 'a night with zero triggers spends zero analyst tokens' is currently false by design",
    "No token caps (only wall-clock); no policy-resident per-night deep-path budget (no policy file)",
    "Deep-path invocations are not ledgered with a trigger reason \u2014 overnight manifest.json records fit/rec/conf/headline/elapsed only, so the CL-19 analyst-usefulness denominator does not accumulate"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    6
   ],
   "part": "analyst-tier"
  },
  {
   "id": "CL-17",
   "status": "conflict",
   "evidence": [
    "/home/nathan/cec-llm-broker/broker.py:ensure (~264-321) \u2014 on-demand backend start with single-GPU budget arbitration (SWAP: stops a conflicting model when vram_gb sums exceed gpu_budget_gb=30, lets in-flight drain); _reaper (~324-348) idle-stop frees GPU+RAM; status() exposes residency; registry residency fields in models.json (vram_gb, ram_gb, idle_stop_s, start_timeout_s, max_inflight)",
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py \u2014 phase-based loading in driver form: alternating ROUTE blocks (ensure_worker -> cec-worker resident) and REVIEW blocks (ensure_manager -> REVIEWER_MODEL resident), broker-mediated, deadline-bounded",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:manifest (123-135) \u2014 the determinism manifest carries kicad pin, kicad-cli, freerouting, scripts SHA, python \u2014 NO model/quant/residency fields"
   ],
   "gaps": [
    "Load/unload events, model hashes, and quant identifiers are NOT recorded in the run ledger (broker logs to broker.log only) \u2014 a verdict is not attributable to the exact resident stack from the ledger; cec_ledger.manifest() needs model fields and the broker needs a ledger hook",
    "No measured-residency write-back: no policy file exists to receive measured sizes/tok-s (the measurements live as prose in CLAUDE.md/docs, not machine-readable policy fields)",
    "Broker arbitration keys on vram_gb only (ensure() lines ~290-301); ram_gb is registry data, not arbitrated \u2014 RAM-resident co-residency (e.g. worker page-cache + manager) is unmanaged",
    "Doc names ik_llama.cpp for expert offload; the tree runs plain llama.cpp (ghcr.io/ggml-org/llama.cpp:server-cuda in docker/compose.yaml)"
   ],
   "conflicts": [
    "The '192 GB ceiling' premise is wrong for the runtime that actually loads models: measured total RAM is 125GB (free -g; the WSL2 VM where the Docker llama.cpp backends run). docs/local-compute-exploration.md:34 records 192GB as the physical workstation spec, but the effective ceiling is 125GB \u2014 consistent with the recorded Path C finding (102GB M2.7 UD-Q3_K_XL fits fully in page cache; the 141GB Q4_K_XL did not and collapsed to 3.73 tok/s). The doc's analyst residency range '~108 to 175GB' is feasible only at its bottom end",
    "Measured offload finding contradicts naive offload tuning: partial GPU expert offload (--n-cpu-moe 80) was SLOWER (2.7 tok/s) than experts-fully-in-RAM and cost +15GB VRAM (recorded in CLAUDE.md manager-tier notes) \u2014 residency planning should use the measured numbers, not size-only estimates"
   ],
   "blocked_by_decisions": [
    10
   ],
   "part": "analyst-tier"
  },
  {
   "id": "CL-18",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_corpus_lint.py:40 \u2014 SOURCE_TYPES whitelist = {standard, datasheet, fab, spec, decision, measurement} ('decision' ~= the doc's 'named human decision'); lines 103-117: REJECTS missing/empty source, REJECTS source.type=='model' ('model knowledge is not a source'), rejects unknown types, requires Class-C entries to cite a ledger run ('run:R-...'); lines 171-173: the project corpus' 'model' source string likewise rejected",
    "/home/nathan/CEC-Platform/scripts/checklist.sh:51 \u2014 the linter is wired into the repo checklist, which runs in CI (.github/workflows/kicad-checks.yml step 'Repo hygiene checklist')"
   ],
   "gaps": [
    "No trace artifacts exist yet (CL-15 gap: traces are never stored/hashed), so the derivation rule has nothing to bind to \u2014 the trace-hash-as-attached-rationale field is absent from the corpus entry schema",
    "The linter cannot specifically detect a source REF that resolves to a model artifact (e.g. a trace hash smuggled under type 'decision'); the doc's 'rejects any source field resolving to a model artifact' needs a ref-resolution check once trace hashes exist",
    "No owner-signature upgrade flow (heuristic prose distilled from a trace acquiring source type 'named human decision' at signature) \u2014 corpus lifecycle has classes/status but not this transition"
   ],
   "conflicts": [],
   "part": "analyst-tier"
  },
  {
   "id": "CL-19",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_bench_manager.py \u2014 promoted (2026-06-10) manager-VERDICT bench: 3 differentiated contexts (accept/repair/escalate) against the production SYSTEM prompt + VERDICT_SCHEMA, deliberately RAW single-call (bypasses miner->scribe so thinking overruns surface as findings); results recorded in /home/nathan/CEC-Platform/docs/llm-manager-bench-2026-06-09.md + build/bench-gptoss.log (gpt-oss-120b 3/3)",
    "/home/nathan/CEC-Platform/tests/test_judge_local_scribe.py \u2014 offline stub-server tests of the extractor MACHINERY (floors, scribe recovery, tail cap) \u2014 tests the code path, not model fidelity",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:differentiated_test (635-683) \u2014 live swarm differentiation check (needs the server up)",
    "Verified absent from CI: .github/workflows/kicad-checks.yml runs only scripts/checklist.sh + scripts/check-all.sh \u2014 no unit tests, no bench, no eval gate"
   ],
   "gaps": [
    "Extractor-FIDELITY eval (the P0 half) is greenfield: no labeled (trace, gold extraction) pair set (the doc's seed source \u2014 the two fab-gate audits rewritten as traces+gold tables \u2014 has not been done), no span-existence / field-accuracy / hallucinated-verdict metrics, zero-tolerance gate not implemented",
    "Nothing GATES a model or prompt change on an eval (no eval-gate status field \u2014 CL-14 missing; no CI hook; cec_bench_manager is a manual measurement driver, not a gate)",
    "Analyst-usefulness eval (P2): no deep-path trigger ledger exists (CL-16 gap) so the denominator is not accumulating; no labeled hard cases, no human-verdict/bench-outcome agreement scoring",
    "CI venue question: the fidelity eval needs the local extractor model, i.e. the self-hosted runner or the workstation \u2014 interacts with owner decisions 4/10"
   ],
   "conflicts": [],
   "part": "analyst-tier"
  },
  {
   "id": "CL-20",
   "status": "conflict",
   "evidence": [
    "Verified absent: no license machinery anywhere \u2014 grep over scripts/*.py, docker/compose.yaml, and /home/nathan/cec-llm-broker/models.json finds no license/license_cleared fields (models.json entries carry only residency/lifecycle fields); no policy file exists to hold the flag; nothing in the orchestrator/broker refuses a model on license grounds",
    "M2.7's CURRENT role (the conflict's substance): broker models.json 'cec-manager' = MiniMax-M2.7 UD-Q3_K_XL (active registry entry, on-demand bootable); docker/compose.yaml manager-m27 profile (default model); scripts/cec_judge_local.py:77-80 documents it as the opt-in deep AUDITOR (CEC_VLLM_REVIEWER_MODEL=cec-manager) with dedicated support machinery (_FLOOR_MODELS default 'cec-manager', miner->scribe); scripts/cec_overnight.py:17-18 names the same opt-in pin"
   ],
   "gaps": [
    "Build the gate itself: license_cleared flag in policy, orchestrator/broker refusal of an uncleared model for load-bearing roles (depends on CL-14's policy file + CL-10-style enforcement), and a non-load-bearing marker in the ledger for bench-only outputs",
    "Owner action: re-verify the current M2.7 Modified-MIT text and obtain authorization or counsel sign-off (doc decision 5)",
    "Decide the bench-only-experimentation policy and how its artifacts are marked"
   ],
   "conflicts": [
    "Doc assumes M2.7 is BLOCKED pending license review; the tree has already shipped it as a first-class, immediately-usable tier (registered, composed, documented, bench-characterized, with model-specific guards) \u2014 its outputs (e.g. corpus-fit sidecars when the opt-in pin is set) are not marked non-load-bearing anywhere. Default paths mitigate but do not gate: the in-loop manager defaults to cec-worker and the reviewer defaults to cec-manager-fast (gpt-oss-120b), so M2.7 is opt-in by env var, not blocked by policy",
    "Doc's interim analyst occupant (Apache Qwen3.5-397B-A17B) is infeasible on this hardware: not in /mnt/e/'AI Models', 66GB free on E: (cannot download a ~165-175GB GGUF), 125GB measured RAM (cannot be resident). If the license flag holds, the only deep-reasoning occupants actually available are gpt-oss-120b (Apache-2.0, already the default reviewer) \u2014 or M2.7 itself, which is what the flag would block"
   ],
   "blocked_by_decisions": [
    5
   ],
   "part": "analyst-tier"
  },
  {
   "id": "CL-21",
   "status": "partial",
   "evidence": [
    "scripts/render.sh: single top-side PNG via `kicad-cli pcb render` only",
    "scripts/cec_router.py:884 render() and scripts/cec_dispatch.py:245 render(): best-effort full-board top PNG, degrade-if-no-kicad-cli (R-05); no view set",
    "scripts/cec_plot.py copper_plot(): PIL board-accurate copper plot drawn DIRECTLY from pcbnew-parsed geometry (F.Cu red/B.Cu blue/inner faint, vias, pads, LOGO, DRC violations classified real-X vs finishing-O) \u2014 exactly the doc's recommended 'generate overlay from parsed board data, not SVG post-processing' mechanism, but colorized by LAYER not by net family",
    "scripts/cec_zoomplot.py: annotated FULL + ZOOM crops of a problem area (pads numbered + net-labelled, Kelvin keepout, unconnected pins highlighted) \u2014 ref-driven problem crops, not placement-cluster view sets",
    "scripts/cec_pcb.py:253 routing_plan_png(): matplotlib net/pour/via plan over real placement (planned routes, not as-routed copper)",
    "scripts/cec_score.py:119 Metrics.drc_types/drc_loci + :152 _types_loci(): per-violation type+locus data from the single DRC run \u2014 the raw data a coverage overlay needs, not painted anywhere",
    "scripts/cec_ledger.py:123 manifest() / :137 input_hashes(): ledger with sha256 hashes + pointers exists (the 'incumbent' identity), but no diff-render machinery",
    "grep across scripts/: ZERO calls to `kicad-cli pcb export svg`; no per-copper-layer export step exists",
    "pad-to-net parsers verified real: scripts/cec_synth_pipeline.py:1036 read_placement() (positions/rotations/courtyards/pads_by_net via pcbnew), scripts/cec_constraints.py:245 _pads_by_net(), scripts/cec_pcb.py:82 pad_global()"
   ],
   "gaps": [
    "One-command per-candidate VIEW-SET generator emitting into a candidate bundle dir (no CL-12 bundle builder exists anywhere \u2014 grep 'bundle' in scripts/ hits nothing relevant)",
    "Per-copper-layer SVG exports (kicad-cli pcb export svg never invoked)",
    "Placement-cluster-derived block crops with the 25 px/mm budget (cec_zoomplot crops are arg-driven by ref, not derived from placement clusters)",
    "Net-FAMILY colorized overlays (power classes, Kelvin pairs, DETECT, CAN) \u2014 cec_plot colors by layer; needs a net-family layer on top of the existing pcbnew parse (all coordinates+nets already available)",
    "Diff renders against the ledger incumbent (cec_ledger stores hashes/pointers; no incumbent lookup + changed-region highlight)",
    "Locating aids contract: coordinate grid + region IDs on crops (zoomplot has refs/pad labels only)",
    "Coverage overlay painting drc_loci/constraint-flag loci as examined/fired (data exists in Metrics.drc_loci and cec_constraints Flags; no renderer), plus the coverage-agnostic blind-sample budget rule",
    "Bottom-side composite (all existing render paths are top-only)"
   ],
   "conflicts": [],
   "part": "visual-swarm"
  },
  {
   "id": "CL-22",
   "status": "partial",
   "evidence": [
    "scripts/cec_judge_local.py:376 _panel(): generic concurrent multi-lens panel machinery (lens-diverse prompts, temps, max_workers) used by make_manager_swarm(395)/make_worker_swarm(457)/make_dispatch_swarm_tier(533) \u2014 the seat/fan-out/vote skeleton exists, but only over route Metrics, never over renders",
    "scripts/cec_judge_local.py:210 _scribe_json(): bench-verified miner->scribe extractor (free-form reasoning trace -> strict json_schema) \u2014 the 'extractor compiles findings' half of the analyst deep path exists for TEXT; no span verifier",
    "scripts/cec_judge_local.py:1111 corpus_fit_review(): the out-of-loop REVIEWER deep pass (deterministic prepass -> compact evidence -> LLM critique, fail-safe + clamp) \u2014 the deep-path pattern the analyst seat would extend",
    "scripts/cec_constraints.py:115 C(id=\"hot-sensitive-separation\") + :843 _chk_hotsep(): the keep-apart parser check the doc's verify step needs for deterministic triage of the REF3030 adjacency finding EXISTS, with corpus rule ref3030-away-from-heat (8mm shunt / 4mm LDO) in scripts/constraints/corpus-extracted.json (258 rows, fields: id/rule/severity/source/check_method/existing_checker)",
    "find: NO cec-policy.json anywhere in the repo (CL-14 role-binding file absent)",
    "tests/golden/ contains ONLY eps-8pin (frozen floorplan) + expectations.json route/score/electrothermal bands \u2014 the CL-11 pre-fix/post-fix Hub and 12VHPWR render fixtures (REF3030 proximity, lane-via) do not exist",
    "scripts/probes/fem_probe_12vhpwr.py exists \u2014 a real FEM verification path triage could invoke for thermal findings",
    "/home/nathan/cec-llm-broker/models.json: full local catalog = cec-worker (Qwen3.6-35B-A3B, llama.cpp), cec-worker-quality (Qwen3.6-27B, llama.cpp), cec-manager (MiniMax-M2.7, llama.cpp), cec-manager-fast (gpt-oss-120b, llama.cpp), cec-judge (Qwen3-Coder-30B AWQ, vLLM) \u2014 no mmproj/vision config on any backend; grep mmproj/vision/multimodal across models.json, broker.py, docker/compose.yaml: zero hits"
   ],
   "gaps": [
    "The panel orchestrator itself: seats, per-charter independent review, two-pass blind-then-context protocol, locus+mechanism dedup/aggregation, cross-seat agreement routing \u2014 all greenfield",
    "Finding contract schema (locus/mechanism/severity/falsifiable verification path) + the return-once-then-drop rule",
    "Deterministic-triage-first pipeline wiring findings into cec_constraints checkers / FEM probe / staging drafts (CL-18 staging machinery also absent)",
    "Per-seat per-charter calibration records from adjudicated outcomes",
    "Golden render eval: requires CL-11 pre/post-fix Hub + 12VHPWR fixtures, which don't exist (tests/golden has only eps-8pin)",
    "cec-policy.json with role bindings + modality field (CL-14, absent)",
    "SB-05 fab preflight hook (CLAUDE.md lists SB-05 as STILL OPEN) \u2014 the mandatory-at-preflight placement has no anchor",
    "A vision-capable local binding (add a VLM with mmproj to the broker registry, or run the analyst seat frontier-only initially)"
   ],
   "conflicts": [
    "Doc assumes 'the local analyst ... is the natively multimodal model in the local set'. The current broker registry (/home/nathan/cec-llm-broker/models.json) has NO vision-capable binding: all five backends are text-only servers (llama.cpp without mmproj / vLLM Qwen3-Coder-30B AWQ); gpt-oss-120b is text-only as the doc itself notes, and MiniMax-M2.7 + both Qwen3.6 workers are bound as text servers. The doc's own 'verify modality at binding' check FAILS today \u2014 the local analyst seat cannot be filled from the current lineup; the panel is frontier-seat-only until a VLM is added to the registry."
   ],
   "blocked_by_decisions": [
    5,
    8,
    11
   ],
   "part": "visual-swarm"
  },
  {
   "id": "CL-23",
   "status": "partial",
   "evidence": [
    "scripts/cec_facts.py: ABSENT (confirmed by directory listing) \u2014 the item's core deliverable does not exist",
    "Producers for ~60-70% of the facts field families already exist: (1) refs+positions+rotations+courtyards: scripts/cec_synth_pipeline.py:1036 read_placement() (pcbnew positions, orientation degrees, courtyard bbox) + scripts/cec_pcb.py:107 courtyard_bbox(); (2) pad-to-net map: read_placement().pads_by_net, scripts/cec_constraints.py:245 _pads_by_net(), scripts/cec_pcb.py:82 pad_global(); (3) per-net via attributes: scripts/cec_synth_pipeline.py:~2211-2228 (per-via net, GetDrillValue, parallel-split current), scripts/cec_fr.py:660-661 (dia via GetWidth(TopLayer), drill), scripts/cec_dcir.py:294; (4) zone coverage: cec_synth_pipeline electrothermal cross-section per net, scripts/cec_constraints.py:505 _chk_pour_integrity / :534 _chk_min_cross (DC field solve via cec_dcir); (5) pairwise distances: scripts/cec_constraints.py:257 _min_pad_dist_mm() + class checkers (_chk_hotsep:843, kelvin-sense-adjacent-shunt); (6) check flag loci: scripts/cec_score.py:119 Metrics.drc_loci/drc_types from the single R-02 DRC run, :152 _types_loci()",
    "scripts/cec_ledger.py:137 input_hashes() + cec_toolchain.sha256_file: the ledger-manifest hashing slot the facts hash would join already exists (hashes corpus/netlist/board today)",
    "Netclass membership: WRITERS only \u2014 scripts/cec_pcb.py:319 write_netclasses() / :310 netclass(); scripts/cec_score.py:104 Rules.from_board derives net families by NAMING convention (_derive_pairs/_derive_nets_12v), no .kicad_pro netclass/minima reader exists",
    "scripts/cec_judge_local.py:239 _context(): the existing serialization fed to local models is Metrics-shaped (candidates_best_first + gate failures), NOT board facts \u2014 confirms the substrate gap"
   ],
   "gaps": [
    "The generator itself: scripts/cec_facts.py emitting canonical per-board JSON + flat text rendering (greenfield, but largely an aggregation of the verified producers above)",
    "Netclass membership + class minima READER (parse .kicad_pro net_settings / .kicad_dru; only writers exist)",
    "Per-net via TABLES (size/drill/layer-pairs aggregated per net \u2014 attributes are read piecemeal today, never tabulated; layer-pair read specifically absent, only SetLayerPair writes found)",
    "Layer-transitions-per-net summarizer (no producer)",
    "Generic declared-part-class pairwise-distance tables (regulators/references/shunts/sense pairs) \u2014 only specific hardcoded checkers exist, no class registry",
    "Slicing API (by region / net family / ref set / charter -> few-hundred-token slices)",
    "Determinism guarantee + hash wiring into the ledger manifest (input_hashes has the slot pattern; facts hash not wired)",
    "Verify fixtures: pre-fix 12VHPWR fixture for the lane-via facts does not exist (tests/golden has only eps-8pin)"
   ],
   "conflicts": [],
   "part": "visual-swarm"
  },
  {
   "id": "CL-24",
   "status": "partial",
   "evidence": [
    "scripts/cec_judge_local.py:289 swarm_judge(): concurrent local swarm against the batched server EXISTS and is exercised (scripts/cec_sweep.py:123 sweep_swarm; scripts/cec_router.py:934-937 --swarm wiring; scripts/cec_overnight.py:128 nightly --judge local --swarm 3) \u2014 but its substrate is _context() (cec_judge_local.py:239) = cec_score.Metrics + gate reasons, i.e. VERDICT-over-METRICS, not findings-over-FACTS slices; the output contract is accept/repair/escalate verdicts, not the micro-schema finding lines",
    "scripts/cec_judge_local.py:376 _panel() + make_manager_swarm(395)/make_worker_swarm(457): decorrelation-by-LENS (diverse perspective prompts + temps) already implemented \u2014 structurally the doc's decorrelation-by-input/framing pattern, on the wrong substrate",
    "Deterministic triage backends exist: scripts/cec_constraints.py checker registry (~30+ C(id=...) checkers incl. hot-sensitive-separation:115, min-pour-cross-section:96, detect-resistor-code, trace-width-high-current) + scripts/constraints/corpus-extracted.json (258 provenance-carrying rows with deterministically_checkable/existing_checker fields) \u2014 most fact-derived claims would be mechanically checkable as the doc requires",
    "/home/nathan/cec-llm-broker/models.json: worker model bound and arbitrated (cec-worker, max_inflight=4, vram_gb=27; gpu_budget_gb=30; broker swaps conflicting backends + idle-reaps) \u2014 residency/phase-sequencing is already enforced by infrastructure, not left to the swarm runner",
    "find: NO cec-policy.json (no role registry, no adversarial-verifier role, no prompt hashes in policy)",
    "CL-23 facts substrate absent (scripts/cec_facts.py does not exist); CL-16 deep-path trigger machinery absent (no trigger code found; the only deep path is corpus_fit_review, gated by CEC_CORPUS_REVIEW=1, not contention-triggered)"
   ],
   "gaps": [
    "The adversarial-verifier role + binding (needs cec-policy.json from CL-10/CL-14, absent)",
    "Charter set + framings (netclass conformance, distance-rule hunting, corpus-entry violation hunting, spec-line conformance) and the per-job one-charter/one-slice/one-framing dispatcher \u2014 all greenfield; depends on CL-23 facts slices which don't exist",
    "Micro-schema output contract (N one-line findings: charter/locus/claim; no severity/verification-path) \u2014 current swarm contract is the verdict schema",
    "The funnel: locus+claim dedup, clustering, deterministic-triage-first wiring into cec_constraints/facts checks, CL-18 staging drafts, refutation logging, contention -> CL-16 deep-path trigger",
    "Charter-granularity calibration: per-charter precision from triage outcomes, budget allocation, precision-floor suspension, PR-gated prompt hashes",
    "Night-one throughput measurement harness (doc's 120-240 tok/s is unmeasured; only measured worker facts are cold swap 90.7s and 4 concurrent requests in 1.75s wall)"
   ],
   "conflicts": [
    "Doc's residency numbers drifted from the live registry: it states worker ~22 GB + 27B extractor ~17 GB vs 32 GB VRAM; /home/nathan/cec-llm-broker/models.json actually records cec-worker (Qwen3.6-35B-A3B) vram_gb=27 and cec-worker-quality (27B) vram_gb=22 under a gpu_budget_gb=30 ceiling. The conclusion (no co-residency; phase-sequential) HOLDS and is moot anyway: the broker already enforces single-occupancy via vram arbitration + swap, so 'aggregation and extraction run phase-sequential' is the existing default behavior, not a design choice CL-24 must implement; 'measure on first load' is already answered by the registry."
   ],
   "blocked_by_decisions": [
    9
   ],
   "part": "visual-swarm"
  },
  {
   "id": "CL-25",
   "status": "partial",
   "evidence": [
    "scripts/cec_constraints.py:REGISTRY + CHECKERS (stable-ID constraint registry with deterministic checkers; the real check substrate)",
    "scripts/cec_constraints.py:843 _chk_hotsep ('hot-sensitive-separation') = the thermal aggressor/victim keep-apart class (sep_mm 8.0, board-temp-NTC exemption)",
    "scripts/cec_constraints.py:737 _chk_decap ('decoupling-cap-owner') = cap-to-node proximity (measures the real bypass loop cap-pad->IC-power-pad, max 3.5mm)",
    "scripts/cec_constraints.py:815 _chk_tw ('trace-width-high-current') + :986 derive_cross_section_dru/:1021 ratify_cross_section = a high-current-only slice of the netclass-geometry class",
    "scripts/cec_synth_pipeline.py:767 CONFORMANCE stage (chk_rj45_link, chk_pin_allocation, chk_detect_resistor, chk_shunt_values, chk_rs485_tiering, chk_no_module_can_term) + cec_constraints.py:665/684/725 (_chk_rj45/_chk_detect_r/_chk_shunt_val) = the per-board spec-conformance netlist assertions class",
    "scripts/cec_synth_pipeline.py:470 chk_bom_complete (missing-LCSC lint, OQ-11/THT exemptions at lower confidence) = a slice of the BOM-field-lint class",
    "scripts/cec_synth_pipeline.py:2388 run_pipeline gates on ERC_BOM (the intake-gate shape, but only inside the synth pipeline)"
   ],
   "gaps": [
    "No netclass-MEMBERSHIP audit (the originating Hub bug class: .kicad_pro patterns silently failing to capture nets so DRU rules never fire) \u2014 nothing reads .kicad_pro net_settings and asserts net coverage",
    "No per-net via/track geometry vs CLASS minima check (only the high-current >=1mm / pour-exempt check; nothing for Power/Sense/USB/CAN classes)",
    "BOM field lint covers only missing LCSC; no placeholder/empty-value pattern lint across MPN/Manufacturer/Value fields",
    "Schematic-and-PCB sync + artifact freshness checks are entirely absent (audit-sch.py is sch-internal geometry only; nothing compares sch/netlist/pcb content or mtimes)",
    "No unified six-class pack with stable IDs + pre-fix/post-fix fixtures (no fixtures exist anywhere)",
    "Intake gate exists only in cec_synth_pipeline.run_pipeline; the actual nightly route path (cec_router.route / cec_overnight.route_one) generates candidates with NO schematic-side intake refusal \u2014 a board with a broken netlist routes all night"
   ],
   "conflicts": [
    "The doc's landing assumption ('PR #18 carries cec_hc.py' as the check-pack home) is wrong: cec_hc.py is the deterministic high-current ROUTING pass (route_high_current/kelvin taps/escape vias), not a check pack. The correct extension points are cec_constraints.py (registry+checkers) and the cec_synth_pipeline stage lists."
   ],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "CL-26",
   "status": "exists",
   "evidence": [
    "scripts/cec_overnight.py \u2014 deadline-bounded plain-Python overnight driver (route_block/review_block alternation, per-route and per-review elapsed_s recorded into build/overnight/manifest.json; no daemon, no inbox, no orchestrator), broker-mediated models",
    "scripts/cec_dispatch.py:168 _spread_params (R-01 per-seed param spread) + :189-205 sha256 content dedupe before scoring",
    "scripts/cec_router.py:916-920 CLI --judge local / --swarm N (worker-swarm review in the loop); scripts/cec_judge_local.py corpus_fit_review + make_manager_swarm/make_worker_swarm",
    "scripts/cec_ledger.py:158 append (elapsed_s, manifest, input hashes per run)",
    "scripts/cec_golden.py + tests/golden/expectations.json (frozen 2026-06-09 eps-8pin baseline the night is measured against)"
   ],
   "gaps": [
    "No per-stage cost split: manifest records whole-route and whole-review wall times, not route vs score vs swarm separately (the budget-allocator inputs the doc wants)",
    "No charters, charter stats, or triage-confirmation channel for swarm findings (reviews emit fit/recommendation sidecars; nothing confirms/refutes them)",
    "Measured costs feed no budget allocator (none exists)",
    "CL-23 facts layer absent \u2014 review context is corpus-fit metrics, not sliced facts"
   ],
   "conflicts": [
    "The doc treats the minimal viable night as unbuilt ('weeks of build before any signal'). It is built and has RUN: cec_overnight.py is broker-native on the current branch and a real 6.5h night completed 2026-06-09 (63 routes, EPS at the cosmetic floor, both PCIe kelvin findings \u2014 recorded in CLAUDE.md). The Decision-18 'bank a baseline on pinned 1.7.0 first' branch has de facto been taken (cec_fr pins FR 1.7.0)."
   ],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-01",
   "status": "missing",
   "evidence": [
    "Verified absent: no audit-fraction/recall sampling anywhere in scripts/cec_judge_local.py or scripts/cec_overnight.py (the only 'audit' hits are the M2.7 deep-AUDITOR naming)",
    "scripts/cec_golden.py scope is route+score+physics only \u2014 and there are no triage verifiers in the tree to add to golden scope (the triage step itself, CL-22/CL-24, does not exist)"
   ],
   "gaps": [
    "Prerequisite: the triage/verifier layer (CL-22/CL-24) must exist first",
    "Random audit fraction of non-surfaced candidates and triage-refuted findings routed to owner/frontier weekly",
    "Golden (CL-11) cases for the triage verifiers themselves"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    11,
    12
   ],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-02",
   "status": "missing",
   "evidence": [
    "scripts/cec_corpus_lint.py:83 \u2014 required entry keys are id/class/kind/scope/applies_to/source/status; NO fixture field exists or is enforced",
    "corpus/general/README.md schema \u2014 no fixture field; no fixture files anywhere under corpus/ or scripts/constraints/",
    "Landing points that exist: cec_corpus_lint.py (wired into scripts/checklist.sh => CI) to enforce it; tests/golden/ harness pattern to run fixtures"
   ],
   "gaps": [
    "Fixture-snippet schema field per corpus entry + linter enforcement (entry rejected without a firing fixture)",
    "A runner that executes each entry's compiled check against its fixture (fires on pre-fix, quiet on post-fix)",
    "The pre-fix Hub board as the first fixture (TPS2121-class entry)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-03",
   "status": "missing",
   "evidence": [
    "Verified absent: zero hits for bandit/hypervolume/allocator-reward across scripts/*.py \u2014 the CL-08 allocator this amends does not exist",
    "The epoch KEY already exists as a primitive: cec_ledger.py:123 manifest() (scripts_sha, kicad/FR pins) + :137 input_hashes() (constraint_corpus_sha256) on every ledger line \u2014 exactly the policy-plus-corpus manifest hash AM-03 wants to partition by"
   ],
   "gaps": [
    "The bandit/budget allocator itself (CL-08) is greenfield",
    "Then: reward-state partitioning by manifest hash or a discounted sliding window"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-04",
   "status": "partial",
   "evidence": [
    "scripts/cec_synth_pipeline.py:2042-2046 \u2014 the solver is analytic IPC-2221 closed-form with 'the constant k is the tuning knob to fit 2152 data' (knob exists, never fit)",
    "scripts/cec_golden.py + tests/golden/expectations.json thermal_max_T_max=147.4 band \u2014 a regression anchor for the thermal number (not an analytic golden)",
    "scripts/cec_constraints.py:96-107 min-pour-cross-section is ADVISORY with 'the solver is not yet bench-validated' stated in the rule text; scripts/cec_dcir.py:24 'stage it as a calibration, not a hard re-gate' \u2014 the uncalibrated POSTURE exists in two places"
   ],
   "gaps": [
    "No analytic golden suite: no IPC-2152 trace-heating curve points or closed-form 1D conduction unit cases checked against electrothermal_solve/dt_ipc",
    "No machine-readable 'uncalibrated' flag propagated into score bundles/verdicts (the posture lives in comments/rule prose, not in outputs)",
    "Known model debt strengthens the item and is still open (CLAUDE.md 12vhpwr item 4): cross_mm2 sums all net segments instead of the serial min-cut (~5x optimistic lane dT), via split should be per transition cluster, IPC k keyed to pour membership \u2014 fixing it changes the SB-08 golden thermal band"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-05",
   "status": "partial",
   "evidence": [
    "scripts/cec_synth_pipeline.py:2329 human_signoff (cautious headless: release only if nothing blocking) \u2014 the 'nothing auto-signs in absence' guarantee the doc itself cites as already present"
   ],
   "gaps": [
    "No inbox classes exist (no inbox at all \u2014 SB-02 open), so no WIP caps to enforce",
    "No stated review-minutes budget, no absence mode, no orchestrator to enforce either (the orchestrator/CL-07 is greenfield)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    12
   ],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-06",
   "status": "partial",
   "evidence": [
    "scripts/cec_ledger.py:158-192 append() \u2014 append-only single global runs/ledger.jsonl in the sibling cec-runs repo; corrects-line discipline; 'large artifacts stay artifacts \u2014 the ledger stores hashes and pointers' is already the design (input_hashes/outputs.board_sha256)",
    "Multi-writer confirmed: cec_router and run_pipeline (and the CLI) each call append() directly, which does a bare open(path,'a') write with no lock \u2014 the exact concurrent-append exposure AM-06 names"
   ],
   "gaps": [
    "No sharding (per-run or per-month files); one global ledger.jsonl",
    "No sidecar-file pattern for high-volume streams (swarm fires) with hashes in the ledger",
    "No single-writer rule: appends are not funneled through one owner process, and there is no file locking"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-07",
   "status": "partial",
   "evidence": [
    "docker/compose.yaml routing service (image cec/routing:kicad10) \u2014 the de facto Linux-container compute venue; this host has no kicad-cli/pcbnew/java (memory: toolchain-on-device-compute), so the container IS the compute plane already",
    "scripts/cec_golden.py header \u2014 recorded owner decision (2026-06-09) to run the golden ON-DEVICE in the routing container",
    "scripts/cec_overnight.py runs route blocks via 'docker compose exec routing' and reviews on the WSL2 host; the broker is a systemd unit on WSL2"
   ],
   "gaps": [
    "Decision 10 (WSL2 vs native vs container as THE pinned night venue) is not formally recorded",
    "No one-time end-to-end golden run of the ENTIRE chain (route+score+physics+judge+ledger) in the pinned venue exists as an artifact \u2014 cec_golden covers route/score/physics only and omits the judge/broker leg"
   ],
   "conflicts": [
    "The doc's framing 'the rig is Windows' is stale for the local night path: the live overnight pipeline runs on the WSL2/Linux side through the routing container (the Windows self-hosted runner is a separate route.yml leg used for dispatched routes, not the overnight driver)."
   ],
   "blocked_by_decisions": [
    10
   ],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-08",
   "status": "missing",
   "evidence": [
    "Verified absent: no hypervolume/dominance/Pareto-front code anywhere in scripts/ (grep empty); cec_score.objective() is a scalar ranking",
    "Substrate exists: cec_ledger.py:222 lineage() reconstructs restart chains explicitly 'so stage-6 convergence reads improvement across the chain' \u2014 the data spine a hypervolume curve would be computed from"
   ],
   "gaps": [
    "Multi-objective front representation per board; hypervolume-improvement (or dominance-count) below-epsilon-over-N-runs computation from the ledger"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-09",
   "status": "exists",
   "evidence": [
    "scripts/cec_hc.py:270 route_high_current \u2014 rips ALL FR tracks/vias on *_HI/*_LO and template-routes the Kelvin taps deterministically (inner-shunt-edge to INA input, F.Cu, no via, do-no-harm clearance gate); skipped taps reported as needs_placement, never a short",
    "scripts/cec_hc.py:100 kelvin_keepouts + :139 keepouts_from_pours \u2014 pre-FR corridor reservations so Freerouting's scope is the routable bulk",
    "scripts/cec_loop.py:87-110 \u2014 the place->route->check loop wires both (hints pre-FR, cec_hc.py pass post-FR) plus the power-escape passes",
    "scripts/cec_score.py \u2014 kelvin_ok is a HARD gate (a candidate with an unrouted/ratlined sense pair cannot be rewarded)"
   ],
   "gaps": [
    "Critical nets are template-routed POST-FR (rip+relay), not pre-routed and locked before FR sees them",
    "No consent-system lock registry for templates (which nets are locked is hardcoded by *_HI/*_LO convention)",
    "The scorer half is absent: no 'candidate touched a locked net = dead on arrival' and no compare-on-unlocked-portion semantics (cec_hc repairs the touch instead, which masks rather than penalizes router behavior on those nets)",
    "Coverage is the Kelvin sense pairs only; other measurement-integrity nets (e.g. 12VHPWR sideband taps, VRAIL_DIV) are not templated"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-10",
   "status": "partial",
   "evidence": [
    "scripts/cec_judge_local.py:239 _context + the make_manager/make_dispatch_swarm_tier user payloads \u2014 judge inputs are deterministic metrics serialized as JSON data fields (json.dumps), not free prose",
    "scripts/cec_judge_local.py:202 SCRIBE_SYSTEM + :210 _scribe_json \u2014 the one model-authored text that re-enters a model context (the reasoning trace) is framed as a labeled data block ('REASONING TRACE:') under an editor-only instruction"
   ],
   "gaps": [
    "No provenance-label regime: corpus-fit precedent summaries and any future drafted/staging entries enter contexts without trust-boundary labels",
    "No sanitization layer for fetched datasheet extractions before context entry",
    "No policy that staging-authored text never enters a load-bearing context unlabeled (staging tier does not exist yet)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-11",
   "status": "partial",
   "evidence": [
    "scripts/cec_judge_local.py:95-115 \u2014 hard per-tier OUTPUT token budgets (MANAGER_MAX_TOKENS 4096, WORKER_MAX_TOKENS 1200, _SCRIBE_MAX_TOKENS 4096) and one hard INPUT ceiling (_SCRIBE_TRACE_CHARS 36000 tail-cap sized to the 16k manager context)"
   ],
   "gaps": [
    "No slicer exists (CL-23 facts layer is greenfield), so no per-job slice token budgets or fail-fast on oversized slices",
    "No slicer version or slice hash in the ledger manifest (cec_ledger.manifest() carries toolchain pins only) \u2014 a finding is not attributable to exactly what its agent saw"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "AM-12",
   "status": "missing",
   "evidence": [
    "Verified absent: no edit-distance or fab-milestone tracking anywhere in scripts/ (grep empty); cec_ledger record schema (cec_ledger.py:163-183) has no such fields",
    "Landing point: cec_ledger.append(extra=...) can carry the two numbers without schema surgery"
   ],
   "gaps": [
    "Define + ledger the human-edit-distance metric (winning candidate vs board actually ordered) and the first-loop-fabbed-board milestone; trend both"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-01",
   "status": "partial",
   "evidence": [
    "corpus/general/README.md:33 + scripts/cec_corpus_lint.py:83,139 \u2014 every corpus entry already carries a required 'scope' field under lint, with duplicate-(kind,scope) conflict detection",
    "scripts/cec_constraints.py REGISTRY \u2014 named checks carry category/params but no coverage-scope declaration"
   ],
   "gaps": [
    "Existing 'scope' is parameter-applicability (e.g. {layer: external}), NOT coverage scope in facts dimensions \u2014 the CL-23 facts vocabulary (net families/netclasses/part classes/regions) does not exist",
    "No computed blind map (complement of declared scopes)",
    "No two-way review-budget split (scope-complement share + random blind floor), no criticality-tier weighting \u2014 no review-budget mechanism exists at all",
    "No scope declarations on the cec_constraints named checks"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-02",
   "status": "partial",
   "evidence": [
    "Per-stage enforcement LEGS exist: scripts/cec_constraints.py:1046 directives() (FAIL -> typed placement directive: pin/adjacent/keepout/separate/power_escape), scripts/cec_loop.py keepout hints -> Freerouting (routing stage), scripts/cec_constraints.py:986 derive_cross_section_dru + :1021 ratify_cross_section (.kicad_dru/DRC stage, human-ratified write), scripts/cec_router.py:197 spec_to_dru",
    "corpus/general/README.md \u2014 per-entry 'applies_to \u2208 physics|compiler|preflight|judge|informational' is a coarse hand-declared stage target; the Class-H bound ('a heuristic NEVER becomes a deterministic gate' and proposed entries 'never compiled into hard gates') is the doc's heuristics-never-below-review rule, already stated and lint-adjacent"
   ],
   "gaps": [
    "No GENERATED pushdown table: nothing computes per-entry true earliest horizon or records the artifact emitted per stage; stage targeting is hand-declared and per-leg ad hoc",
    "Residuals that cannot move earlier are not recorded anywhere (and there is no blind map to feed)",
    "No AM-02 fixtures, so multi-target representation drift between a checker, a DRU rule, and a keepout is undetectable"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-03",
   "status": "partial",
   "evidence": [
    "The extraction path RB-03 amends exists and is bench-verified: scripts/cec_judge_local.py _chat_json miner->scribe recovery + :210 _scribe_json (conclusions transcribed from the trace TAIL under a json_schema grammar, anti-loop sampling floors) \u2014 conclusions-sourced extraction is the de facto default"
   ],
   "gaps": [
    "No detection/classification of whether a verdict was sourced from a conclusions section vs elsewhere in the trace",
    "No forced-choice ratification loop (extracted verdict vs deterministically perturbed distractor + neither-escalation)",
    "No ratification eval cases (CL-19 does not exist)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-04",
   "status": "missing",
   "evidence": [
    "Verified absent: no .github/CODEOWNERS (only .github/workflows/); no consent-surface enumeration, machine-account permission manifest, or credential audit script anywhere in the repo",
    "Related primitives only: scripts/cec_synth_pipeline.py:2329 human_signoff (human rung exists in-process); the run ledger lives in the private sibling cec-runs repo (owner decision)"
   ],
   "gaps": [
    "Enumerate consent surfaces (corpus promotion, spec revision, policy bounds, golden --freeze, scope declarations, fab spend)",
    "CODEOWNERS-protected machine-account permission manifest; owner identity/spend credentials kept off the night box; weekly audit script diffing actual credentials/permissions vs the manifest",
    "Phone/web approval path so the safe path is the convenient one"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    1
   ],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-05",
   "status": "partial",
   "evidence": [
    "scripts/cec_dispatch.py:168 _spread_params (R-01 constructed diversity) + :189-205 sha256 content-hash dedupe with _dup_of_seed tagging \u2014 a degenerate outcome-space identity floor (byte-identical boards are not counted as independent evidence)"
   ],
   "gaps": [
    "No measured outcome-space diversity: no score-vector spread, check-fire fingerprints, or routed-topology hashes over the survivor set",
    "No floor-as-constraint \u2014 there is no allocator (CL-08) for the floor to constrain",
    "No decay coupling to convergence (RB-06/AM-08 missing)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-06",
   "status": "missing",
   "evidence": [
    "Verified absent: no allocator or hypervolume code; scripts/cec_overnight.py:158-161 route_block allocates by fixed round-robin over BOARDS with monotonic effort escalation (passes/opt grow with round), independent of any convergence signal",
    "Substrate: cec_ledger.py lineage() + manifest()/input_hashes() (the epoch-normalization keys) exist"
   ],
   "gaps": [
    "Per-board hypervolume curves from the ledger (needs AM-08 first), epoch-normalized by manifest+corpus hash",
    "An allocator reading curve derivatives and shifting nightly budget; novelty-injection-before-converged-declaration logic"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "redteam-rebuilds"
  },
  {
   "id": "RB-07",
   "status": "missing",
   "evidence": [
    "Verified absent: no escape-label mechanism, no downstream-defect-vs-declared-scope attribution anywhere; and no scope claims exist to label against (RB-01 coverage scopes missing)",
    "Substrate: cec_ledger.py append-only discipline with the 'corrects' field (cec_ledger.py:181) is the natural label channel; no inbox exists"
   ],
   "gaps": [
    "Escape definition + writer (defect confirmed downstream of a stage whose declared scope covered it -> label against check+scope claim in the ledger and the inbox)",
    "Inform-never-actuate policy wiring (demotion stays human-only) \u2014 trivially satisfied today only because nothing actuates"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    3
   ],
   "part": "redteam-rebuilds"
  },
  {
   "id": "DF-01",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:append() \u2014 run-level record today: run_id, ts, board, mode, inputs (input_hashes: constraint_corpus_sha256/netlist_sha256/input_board_sha256), manifest(), outputs{board, board_sha256, artifact}, verdict (free string), elapsed_s, parent_run_id, corrects (append-only corrections), extra; lineage() walks parent_run_id chains",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:manifest() \u2014 determinism manifest = kicad_series_pin, kicad_cli, freerouting, scripts_sha, scripts_dirty, python; NO model/quant/prompt fields",
    "/home/nathan/CEC-Platform/scripts/cec_router.py:DecisionLog.add() (line 119) \u2014 per-decision entries: relative ts, region, iteration, candidates metrics (drc/unconnected/tracks/vias/length/kelvin_ok/diffpair_ok/cu12v/balance/gates_pass/drc_types via _m), chosen, verdict{action, reason<=200ch free text, tier string, edit}, note; to_json() embeds cec_ledger.manifest(); archive_log() accumulates logs under build/route/corpus/",
    "/home/nathan/CEC-Platform/scripts/cec_dispatch.py:agent_route() (line 288) \u2014 per-tier decision log entries {tier, budget_left, params, note} incl. R-08 coercion notes ('budget-coerced escalate')",
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block() \u2014 reviewer verdicts persisted to manifest['reviews'] {corpus, fit, rec, conf, headline, elapsed_s, t} + per-board sidecars"
   ],
   "gaps": [
    "No decision-class taxonomy (corpus lifecycle / verdict / preflight-disposition / policy-change / spec-revision classes) anywhere in the ledger or DecisionLog schemas",
    "Decider identity is a free-text tier string ('local-swarm-mgr(3)', TIER); no decider manifest \u2014 model name, quant, prompt hash are never recorded with a verdict (cec_ledger.manifest() covers only KiCad/FR/scripts)",
    "No evidence-bundle hash: DecisionLog inlines candidate metrics unhashed; cited reasons are free text, no click-to-cite from bundle items, so reason-evidence coherence is not mechanically checkable",
    "No counterfactual stub field; no covariates (queue depth is underivable \u2014 no queue/inbox exists, SB-02 still open; session-position is derivable from DecisionLog relative ts but not computed)",
    "No consent line \u2014 no policy file exists (SB-10 open; find for *policy* returns nothing)",
    "Schema-migration choice: ledger is append-only jsonl with an open `extra` field \u2014 DF-01 fields fit as DecisionLog entry extensions + ledger `extra`/sidecar records in the cec-runs repo rather than a line-format migration"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    1
   ],
   "part": "forensics-process"
  },
  {
   "id": "DF-02",
   "status": "missing",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:_context() (line 239) \u2014 route-loop manager verdict view is metrics-only (DecisionLog._m fields + best-candidate gate failures); generator configs absent, which is incidentally consistent with the doc's verdict-view blinding but is not a deliberate blinding layer",
    "/home/nathan/CEC-Platform/scripts/cec_dispatch.py:_spread_params()/_generate_scored() (lines 168-209) \u2014 per-seed resolved FR params are deliberately recorded in CandidateMetrics.params 'so the judge can see the spread' (R-01), and dup candidates are tagged _dup_of_seed for the judge"
   ],
   "gaps": [
    "No view-layer blinding mechanism exists anywhere; no policy declaring which fields are blinded",
    "The promotion view does not exist at all (no promotion pipeline \u2014 CL-02/CL-04 are unbuilt; corpus/general entries go through cec_corpus_lint, not a drafted-by-tier promotion flow)",
    "The morning-bundle verdict view (CL-12) does not exist \u2014 blinding has nothing to attach to until that view lands",
    "Boundary adjudication needed: dispatch-path generator params are load-bearing (the judge issues request_more(new_params)), so per the doc's own red-pass bound they likely stay visible \u2014 the blind/visible field split must be decided per view, not globally"
   ],
   "conflicts": [
    "The current dispatch judge view intentionally exposes generator configs to the judging tier (cec_dispatch.py R-01 design: resolved per-seed params recorded so the judge can see them) \u2014 the opposite of the doc's verdict-view default. Not a hard contradiction because the doc exempts load-bearing fields, but DF-02 cannot be applied to the dispatch loop as written."
   ],
   "blocked_by_decisions": [
    1,
    7
   ],
   "part": "forensics-process"
  },
  {
   "id": "DF-03",
   "status": "missing",
   "evidence": [
    "No owner decision queue exists to inject probes into: no inbox (SB-02 GitHub-Issues queue is still open per CLAUDE.md; no queue substrate in the repo); owner decisions today are PR merges and cec_golden.py --freeze acts, not queue items",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:input_hashes() \u2014 constraint_corpus_sha256 is the only existing epoch-matching anchor (a probe could pin 'corpus manifest at re-presentation equals original' against it); nothing consumes it that way"
   ],
   "gaps": [
    "Entire probe mechanism: probe selection, epoch-matched re-presentation, aggregate-announced opt-in, probe bookkeeping separate from real decisions",
    "Prerequisite queue/inbox (CL-07/SB-02) must exist first \u2014 probes are a fraction of a queue that does not exist",
    "Opt-in consent line requires the policy file (CL-10/SB-10, unbuilt)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    12,
    14
   ],
   "part": "forensics-process"
  },
  {
   "id": "DF-04",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:corpus_fit_review() (line 1111) + cf_new_summary/cf_family_digest/cf_compute_evidence \u2014 the exact stats-find/model-narrate architecture the item demands is already built for board-fit: robust MAD/median z-scores, envelope flags, gate-flips all computed deterministically in Python; the reviewer model narrates only the precomputed EVIDENCE block; min_peers floor (<3 peers -> insufficient_precedent rather than narrating thin data)",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:query() \u2014 deterministic query API over run records (board/mode/since)",
    "/home/nathan/CEC-Platform/scripts/cec_router.py:archive_log() (line 172) \u2014 accumulating decision-log corpus under build/route/corpus/ as the analysis substrate",
    "/home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block() \u2014 retrospective review blocks run after route blocks, never live during decision sessions (consistent with the red-pass bound)"
   ],
   "gaps": [
    "Nothing analyses DECISIONS: no base rates by decision class (no classes exist, DF-01), no session-position effects, no reason-evidence coherence checks (no bundles), no cross-decider divergence, no probe agreement (no probes), no automation-bias signature",
    "Regret lens impossible until CL-13 bench write-back exists (no fabbed-board outcome data in the repo)",
    "No inbox for findings and no disposition capture (acknowledge/contest/correct) \u2014 SB-02 open",
    "Min-N/effect-size floors exist only inside corpus_fit_review; need a general deterministic forensics layer over the ledger + decision logs"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    13
   ],
   "part": "forensics-process"
  },
  {
   "id": "DF-05",
   "status": "missing",
   "evidence": [
    "No policy file exists (find over the repo for *policy* returns nothing; SB-10 policy-as-code is listed STILL OPEN in CLAUDE.md) \u2014 the policy assertion has no home",
    "No bandit, no reward configs, no drafting-prompt revision machinery exist anywhere (grep across scripts/*.py finds zero reward/settlement/acceptance-rate code) \u2014 the banned signals currently have nothing to contaminate",
    "No orchestrator daemon (CL-07 unbuilt) to check the assertion at load"
   ],
   "gaps": [
    "CL-10 policy file with the two-channel separation declared and the banned-signal list (promotion likelihood, acceptance rate, proxies)",
    "Orchestrator load-time assertion that no reward/revision config references a decision-rate field",
    "Routing of outcome-validated findings vs raw acceptance patterns \u2014 both channels are greenfield (no outcome data, no acceptance-pattern aggregation exists)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "DF-06",
   "status": "partial",
   "evidence": [
    "Four decision-point families already produce structured records: /home/nathan/CEC-Platform/scripts/cec_router.py:DecisionLog (route-loop manager verdicts per iteration, candidates+chosen+verdict+edit), /home/nathan/CEC-Platform/scripts/cec_dispatch.py:agent_route decision_log ({tier,budget_left,params,note} with R-08 coercions recorded), /home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block manifest['reviews'] + per-board corpus-fit sidecars, /home/nathan/CEC-Platform/scripts/cec_synth_pipeline.py lines 2435-2464 (stage log embeds cec_ledger.manifest(), appends mode=synth-run ledger lines with CEC_PARENT_RUN_ID lineage)",
    "Verified absence: grep across scripts/*.py finds NO claim, verification-hook, settlement-state, or grade mechanism anywhere \u2014 the three DF-06 fields are 100% greenfield"
   ],
   "gaps": [
    "The claim / verification-hook (closed vocabulary of check IDs, fixtures, bench measurements) / settlement-state field triple on every record",
    "Consequence-tiered capture policy (full vs aggregate counters) \u2014 must be declared in the CL-10 policy file, which does not exist; no aggregate-counter machinery exists (also moot: no bandit)",
    "Swarm findings and triage verdicts (CL-23/CL-24) do not exist, so the verify clause ('one night yields full records for every swarm finding') has no producers yet",
    "Schema choice: the existing append-only runs/ledger.jsonl + per-run DecisionLog JSON favor claims as NEW sidecar records in the cec-runs repo (hashes in ledger lines) over migrating either existing format; DecisionLog entries and the ledger `extra` field are the natural attachment points for hooks on already-captured verdicts"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "DF-07",
   "status": "partial",
   "evidence": [
    "Grade 2 substrate genuinely exists and is dense: /home/nathan/CEC-Platform/scripts/cec_score.py gate()/Metrics hard gates (kelvin_ok/diffpair_ok), /home/nathan/CEC-Platform/scripts/cec_golden.py run_golden()/compare() deterministic frozen fixtures with bands, /home/nathan/CEC-Platform/scripts/cec_corpus_lint.py deterministic corpus checks, cec_synth_pipeline cascade stage checks \u2014 these are the 'existing dense rewards' the doc says Grade 2 renames",
    "Grade 1 substrate absent: no bench write-back schema (CL-13 unbuilt), no fab-outcome records in the repo or ledger",
    "Verified absence: no reward currency, no novelty weighting, no bandit consumes anything (grep finds zero reward/vindication code); no policy file to carry the banned-rewards assertion"
   ],
   "gaps": [
    "The reward rule itself: graded settlement values, novelty weighting against check-simulation (requires RB-01 declared-scope complement, also unbuilt), the promoted-entry-first-fire paying event (requires the CL-04 promotion + fixture + fire pipeline, unbuilt)",
    "Policy assertion extension banning acceptance/consensus-agreement/volume/token-thrift rewards \u2014 no policy file (CL-10) and no loader to refuse a test policy",
    "Nothing consumes rewards today; landing the rule is a policy+schema act, the consumers (bandit, charters) are future items"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    15
   ],
   "part": "forensics-process"
  },
  {
   "id": "DF-08",
   "status": "missing",
   "evidence": [
    "No claims exist to settle (DF-06 fields greenfield, verified by grep \u2014 zero settlement/grade code in scripts/)",
    "Nearest substrate: cec_synth_pipeline triage/cascade stages exist as deterministic checking events that WOULD settle Grade 2 claims same-night, and cec_overnight.py review_block is the existing nightly analysis slot a settlement pass could ride (no new daemon, matching the doc's constraint)",
    "Role separation unenforceable today: decisions carry only a tier string, not a model manifest (cec_router.Verdict.tier; cec_ledger.manifest() has no model fields), so 'no manifest may author and settle the same claim class' has no identity to check against"
   ],
   "gaps": [
    "Settlement matcher (open claims vs settling events) inside the DF-04 analysis job \u2014 both the claims and the job are greenfield",
    "Per-decision decider/model manifests (prerequisite for role separation and calibration-weighted Grade 3 authority)",
    "Calibration records per decider (none exist anywhere)",
    "Per-claim scoping and chain-blame suppression \u2014 moot until claims exist"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "DF-09",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_golden.py \u2014 the revision-gate half exists: frozen fixture (tests/golden/eps-8pin + tests/golden/expectations.json), compare() bands, --freeze as a human-approved baseline bump committed in the same PR, with the standing 'run before merging scripts/** changes' discipline",
    "/home/nathan/CEC-Platform/scripts/cec_corpus_lint.py STATUSES lifecycle (proposed -> sim_validated -> bringup_validated -> human_approved -> deprecated) \u2014 the PR-gated revision path pattern exists for corpus entries",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:make_manager_swarm/_vote \u2014 panel disagreement is computed every verdict (vote tallies) but DISCARDED except as a tally string in Verdict.reason/tier; this is an existing, currently-wasted disagreement-harvesting signal"
   ],
   "gaps": [
    "No holdout pools exist (corpus two is a single golden fixture; no labeled-case store, no per-role eval sets)",
    "Counterfactual mining impossible: no Grade 1/2-vs-Grade 3 contradictions are recorded anywhere (no claims/settlements)",
    "No prompt versioning at all: SYSTEM and lens prompts are hardcoded constants in cec_judge_local.py \u2014 no prompt hashes, no epoch windows, no churn control, and cross-version statistics already pool silently (the exact failure the item names)",
    "Routing-decision claims (manager invocation deep-vs-fast) are not captured; cec_overnight ensure_manager picks paths without recording a claim",
    "Lone-dissent report greenfield"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "DF-10",
   "status": "conflict",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_bench_manager.py \u2014 the probe-harness germ exists: three fixed differentiated contexts (CTX) re-presented to a named model at temperature 0 with expected verdicts and a durable results record (docs/llm-manager-bench-2026-06-09.md); it gated the 2026-06-09 model-lineup adoption",
    "/home/nathan/CEC-Platform/scripts/cec_ledger.py:manifest() \u2014 the pinned-manifest pattern exists but covers KiCad/Freerouting/scripts/python only; NO model name, quant, or serving-flag manifest is recorded with any decision",
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:_chat_json() (line 170) \u2014 request payload carries model/temperature/max_tokens/response_format and NO seed parameter; reproducibility is not pinned even at temp 0 under batched serving"
   ],
   "gaps": [
    "Per-decision model manifest capture (model + quant + serving flags + prompt hash) \u2014 prerequisite for 'pinned manifest' to be checkable",
    "Seed pinning through the broker/serving stack for any probe expecting byte-identical reproduction",
    "Drift-alarm wiring (probe divergence -> environment-drift flag) and frontier-seat variance -> calibration record (no calibration records exist)",
    "Input-hash computation for decision contexts (contexts are built ad hoc in _context, never hashed)"
   ],
   "conflicts": [
    "The doc's premise \u2014 identical input to a pinned local manifest MUST reproduce the decision byte-identically \u2014 is contradicted by a bench-verified operating rule in the current tree: cec_judge_local.py _FLOOR_MODELS/_FLOOR_TEMP=0.3/_FLOOR_PRESENCE=0.8 (bench 2026-06-09: MiniMax-M2.7 'NEVER at temp <=0.1-0.2', it degenerates), and panel replicas deliberately run at temps>0 (swarm workers at temperature=0.5, cycled lens temps in _panel). The deep manager/reviewer tier is therefore stochastic BY DESIGN today, and no seed is passed. Byte-identical probes are achievable only for the temp-0 seats unless per-request seed pinning is added; for floor-model tiers the canary must be redefined (seeded reproduction or distributional bounds), which is exactly the frontier-seat treatment the doc reserves for API seats."
   ],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "PC-01",
   "status": "partial",
   "evidence": [
    "Custody pattern exists for run records: the sibling private cec-runs repo (verified present at ../cec-runs with runs/) holds runs/ledger.jsonl per cec_ledger.py module header (owner decision 2026-06-09: run churn out of the design repo, default private), with output hashes in each line \u2014 the doc's 'sidecar storage, hashes in the ledger, default private' shape",
    "/home/nathan/CEC-Platform/scripts/cec_router.py:archive_log() (line 172) \u2014 full decision logs accumulate under build/route/corpus/ as 'the labeled substrate the surrogate candidate-ranker trains/evaluates on'; cec_overnight.py review sidecars land under build/ too",
    "Appendix D de-identified-corpus discipline exists in the spec as the named template (CEC-Platform-Ground-Truth-Spec.md, v1.1.0 Appendix D)"
   ],
   "gaps": [
    "The contents do not exist: no DF-06 claim records, settlement linkage, grade history, re-grades, or per-decision manifests to store",
    "Custody split: the richest process records (decision-log corpus, review sidecars) live in the gitignored LOCAL build/ tree, not the cec-runs sidecar \u2014 a WSL/disk loss destroys them (precedent: the 2026-06-09 bench logs were lost to a WSL restart, noted in cec_bench_manager.py header); moving the corpus archive target into cec-runs is the cheap first step",
    "Capture criterion (claim-bearing -> full, claimless -> counters) undeclared; no counters machinery",
    "Retention horizon and de-identification rules unset"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    16
   ],
   "part": "forensics-process"
  },
  {
   "id": "PC-02",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_judge_local.py:corpus_fit_review() (line 1111) with cf_family_of/cf_family_digest/cf_compute_evidence/cf_budget_exemplars and CORPUS_FIT_SCHEMA requiring matched_precedents/violated_precedents cited by precedent_id \u2014 k-similar same-family precedent retrieval with mandatory citations is built and live-verified (caught trajectory thrash citing precedents by id)",
    "Wired at decision-adjacent points: /home/nathan/CEC-Platform/scripts/cec_router.py:965-975 (CEC_CORPUS_REVIEW=1 -> <board>-corpus-fit.json sidecar) and /home/nathan/CEC-Platform/scripts/cec_overnight.py:review_block (nightly review over pending corpus logs, failures reviewed first per _pending_corpus sort)",
    "The family digest is computed over ALL same-family peers including failed/gate-false runs (cf_family_digest over the full peer set; the fit classification includes gate_regression), so it is not a success-only highlight reel"
   ],
   "gaps": [
    "Retrieval is OUT-OF-LOOP and advisory ('never feeds back into the route') \u2014 the doc wants precedents handed to the DECIDER at decision time, in the manager/judge context, with citations joining DF-01 reason capture; today no decider context includes precedents",
    "Matching is on board family + metrics, not on settled CLAIMS (scope/charter/claim class) \u2014 impossible until DF-06/DF-08 exist",
    "No explicit settled-misses inclusion guarantee in the exemplar budgeter, and no retrieval-free permanent control arm measured in vindication currency (no currency exists)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "forensics-process"
  },
  {
   "id": "PC-03",
   "status": "partial",
   "evidence": [
    "/home/nathan/CEC-Platform/scripts/cec_corpus_lint.py:lint_general() lines 102-110 \u2014 REJECTS source.type=='model' ('model knowledge is not a source'): the item's verify clause 'linter rejects a sibling entry whose source is the narration alone' is already enforced for corpus one; Class C entries must cite a ledger run (header lines 12-13), the provenance rail a mined regularity would ride; wired into scripts/checklist.sh CI",
    "/home/nathan/CEC-Platform/corpus/general/ (can.json, thermal.json, README) with the lifecycle STATUSES vocabulary (proposed -> sim_validated -> bringup_validated -> human_approved -> deprecated) \u2014 the staging germ for drafted heuristic entries",
    "SOURCE_TYPES already includes 'decision' and 'measurement' types pointing at ledger runs"
   ],
   "gaps": [
    "The analyst miner itself is greenfield (no settled records exist to mine; no regularity-mining job)",
    "No 'versioned deterministic query' source format: SOURCE_TYPES has no reproducible-query type, and there is no machinery to re-execute a query and check it reproduces the result set",
    "Staging/PR promotion flow (CL-02/CL-04) unbuilt \u2014 'through staging and PR gates as always' has no enforcement today (lint warns on `proposed` but there is no promotion gate)",
    "CL-18 derivation-context treatment of analyst traces is doc-only; nothing stores or distinguishes narration from source for mined entries yet"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    17
   ],
   "part": "forensics-process"
  },
  {
   "id": "PC-04",
   "status": "partial",
   "evidence": [
    "Replay preconditions half-exist: every decision log and ledger line carries the determinism manifest + input hashes (cec_router.py DecisionLog.to_json line 147-156; cec_ledger.py append/manifest/input_hashes), and the decision-log corpus accumulates (archive_log) \u2014 'records plus manifests' is real, 'settled' is not",
    "/home/nathan/CEC-Platform/scripts/cec_bench_manager.py \u2014 the replay-before-bind pattern in primitive form: a candidate manager manifest is replayed over fixed differentiated contexts with expected outcomes, results recorded durably, and it actually gated the 2026-06-09 model-lineup adoption (gpt-oss-120b 3/3)",
    "/home/nathan/CEC-Platform/scripts/cec_golden.py \u2014 frozen-fixture eval harness with human-approved band bumps: the quarantined-eval-gate pattern the training rung requires",
    "Hardware consistency check: the doc's '27B verifier class on the 32 GB card' matches the current lineup (cec-worker-quality = Qwen3.6-27B dense Q4_K_M serving on the 32 GB RTX 5090 per CLAUDE.md broker registry) \u2014 no conflict; the doc itself defers fine-tune feasibility to measurement"
   ],
   "gaps": [
    "Replay driver over settled history scoring in vindication currency \u2014 greenfield (no settlement, no currency, no manifest-binding registry for judge/verifier/router roles; CR-01 registry unbuilt)",
    "Training rung entirely greenfield: no fine-tune infrastructure, no Grade-filtered label selection (no grades), no regime-bound marking (the named main technical risk), no discriminative-only enforcement, no quarantined-audit-case gate beyond the single golden fixture",
    "The 'refuse Grade 3 labels at training time' and 'refuse same-distribution eval' guards need the policy file (CL-10) and the grade fields (DF-06/07) first",
    "Meta-Goodhart guard is a policy assertion with no policy home yet"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    17
   ],
   "part": "forensics-process"
  },
  {
   "id": "FR-01",
   "status": "partial",
   "evidence": [
    "scripts/cec_fr.py:91 FR_VERSION = \"1.7.0\" (the pin) + FR_JAR_URL (lines 92-95); ensure_jar (lines 108-150) resolves arg > $CEC_FREEROUTING_JAR > /tmp/fr_1.7.0.jar > ~/.cache/cec/freerouting-1.7.0.jar > download \u2014 with NO hash verification on the download",
    "scripts/route-prereqs.sh:71-77 hardcodes the 1.7.0 cache paths (the search paths the migration must update); scripts/route.ps1 carries no jar path logic at all (delegates to ensure_jar), so the migration touches cec_fr.py + route-prereqs.sh only",
    "scripts/cec_ledger.py:115 _fr_version() + manifest() (lines 123-130) already embed the Freerouting version in the determinism manifest written into every decision log \u2014 the doc's 'jar version joins the determinism manifest' bullet is ALREADY satisfied; bumping FR_VERSION is automatically an epoch event",
    "scripts/cec_dispatch.py:168 _spread_params and scripts/cec_router.py:752-763 opt_spread are the R-01 seed-diversity machinery the doc says must be revalidated on 2.2.4",
    "scripts/cec_router.py:618-634 _candidate_pool does sha256 content dedupe (cec_toolchain.py:60 sha256_file) explicitly on the assumption 'FR is deterministic, so identical params yield byte-identical boards' \u2014 the exact property the doc's determinism probe must re-establish on 2.2.4"
   ],
   "gaps": [
    "No 2.2.4 jar vendored or referenced anywhere (grep '2.2.4|2.2.x' across scripts/ and docs/ = zero hits); migration not started",
    "No hash pinning exists even for 1.7.0 \u2014 ensure_jar downloads unverified (CLAUDE.md records the jar is deliberately NOT vendored); the doc's 'vendored, hash-pinned jar' posture requires adding a sha256 check to ensure_jar",
    "No determinism probe harness (route the identical DSN twice, compare output hashes) \u2014 sha256_file exists but nothing drives the twice-route comparison",
    "No param-surface revalidation against the 2.x settings model (global settings file + CLI overrides); run_freerouting (cec_fr.py:178) passes 1.7-era -mp/-oit/-mt flags and notes 'no -seed flag in FR 1.7.0' (line 215)",
    "No pour-handling A/B retest harness (eps-8pin / 12vhpwr-standard side-by-side on both jars)",
    "No CLI scoring-JSON ingestion as a stage-0 pre-kill (nothing parses any FR output JSON; 1.7.0 emits none)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "FR-02",
   "status": "partial",
   "evidence": [
    "scripts/cec_fr.py:727-796 bake_hints() \u2014 adds rule-area keepout ZONES (DoNotAllowTracks+Vias) to a board copy so they export into the DSN and FR routes around them: the working substrate for the doc's secondary 'sequential corridors' mechanism",
    "scripts/cec_router.py:562-615 _vital_keepouts_from_rules() \u2014 deterministic corridor derivation (cable connector -> shunt span, clipped at the shunt to keep the Kelvin tap window open, allow_vias=True) already ENFORCES corridor topology for high-current/Kelvin nets; wired into default_planner (line 297) and route()",
    "scripts/cec_router.py:227-233 apply_edit 'keepout'/'drop_keepout' edit types give a programmatic keepout add/lift lifecycle (the carve/route/lift sequence could be scripted on these)",
    "scripts/cec_route.py:62-225 Router (.track/.via/.run/.to_pad) could materialize DRC-legal waypoint stubs as real copper; cec_route.verify() runs the real DRC",
    "scripts/cec_verify_hc.py:60 verify() \u2014 precedent for deterministic geometric facts extraction from a board (pad rects, hit-tests), the nearest thing to the CL-23 facts file the compiler is supposed to resolve through"
   ],
   "gaps": [
    "No intent vocabulary or compiler: nothing accepts relational intents (ordered waypoint refs, regions, edges, layer plan, avoid-regions) and resolves them to geometry",
    "No waypoint-stub mechanism: nothing materializes fixed track stubs, and no fixed/protect attribute handling exists \u2014 export_dsn (cec_fr.py:156) is a plain pcbnew ExportSpecctraDSN passthrough; the gating bench test (locked stub -> DSN protect attribute -> survives headless route) has never been run and FR 2.2.4 is not yet adopted (FR-01)",
    "No per-net sequential routing loop: cec_router.route() routes all nets of a region in one FR pass; route-one-net-alone-then-fix sequencing must be built",
    "No CL-23 facts file exists (item outside this range, unbuilt)",
    "No stub-hygiene post-pass (orphan stub cleanup)"
   ],
   "conflicts": [
    "Doc: 'the parser infrastructure already exists' for injecting the protect attribute into the DSN \u2014 false against the current tree: there is NO DSN parser in the repo; DSN/SES files are opaque pass-throughs between pcbnew and the FR jar (the only s-expr parser is the .net netlist parser in cec_synth_pipeline). The one-s-expression-edit fallback needs new DSN-touching code."
   ],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "FR-03",
   "status": "partial",
   "evidence": [
    "scripts/cec_route.py:62 class Router with track() (line 98), via() (106), run() polyline track laying (157), to_pad() pad approach + via (170), check_keepouts() (184), verify() = real kicad-cli DRC as the only legality judge (225) \u2014 the executor's geometry+DRC substrate exists and is the sanctioned programmatic-copper path",
    "scripts/cec_fr.py:525 synthesize_power_copper + :334 derive_power_pours + :419 derive_via_field \u2014 working precedent for 'copper generation is geometry rather than search' on the power nets (corridor pours + stitch via fields, ZONE_FILLER-filled, DRC-verified)"
   ],
   "gaps": [
    "The corridor executor itself is unbuilt: nothing takes (resolved polyline, layer plan, netclass) and emits offset segments at class width with vias at layer changes",
    "No bounded local nudging within a declared tolerance \u2014 Router.run() lays exactly the points given, with no obstacle avoidance",
    "No named-obstacle failure attribution (fail with the blocking item identified, per-net, back to the manager) \u2014 verify() returns DRC violations, but no obstacle-naming contract exists",
    "Depends on FR-02's resolved-polyline input format, which does not exist"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "FR-04",
   "status": "partial",
   "evidence": [
    "scripts/cec_router.py:562 _vital_keepouts_from_rules + scripts/cec_score.py Rules.from_board hard gates (kelvin_ok/diffpair_ok) \u2014 a de facto two-rung embryo of the ladder: deterministic corridor-forcing for measurement-integrity nets layered over free FR routing for the bulk; scripts/cec_route.py:141 mark_vital",
    "scripts/cec_router.py:110 DecisionLog (replayable, manifest-stamped via cec_ledger) \u2014 the substrate any intent-claim record would hook",
    "scripts/cec_loop.py:40 BOARD_PARAMS \u2014 the only per-board manifest-like structure (board-scoped human-ratified params), precedent for owner-editable per-board config but with no per-net content"
   ],
   "gaps": [
    "No per-net tier assignment and no board manifest carrying it (human-template / directed / free)",
    "No human-template tier: no mechanism to mark nets' copper as locked/untouchable through a re-route",
    "No claims framework at all: grep claim/settle/vindicat across scripts = nothing \u2014 DF-06 claims, DF-07 currency, PC-02 case law promotion, PC-03 distillation are absent platform-wide",
    "No control arm: directed-vs-free A/B on sibling candidates, measured vindication, and a budget allocator that moves on settlements \u2014 none exist",
    "Depends on FR-02 (missing core) and FR-03 (missing core) for the directed tier's execution"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-01",
   "status": "partial",
   "evidence": [
    "scripts/cec_synth_pipeline.py:1105-1128 rudy() \u2014 a real congestion grid: per-net bounding-box wire density spread over a 2mm-bin grid; :1181 proxy_reject \u2014 deterministic relative congestion-growth rejection (rudy_peak vs baseline x factor)",
    "scripts/cec_router.py:289 default_planner regions/hints structure \u2014 the slot where contested-call corridor assignments would be injected as hints"
   ],
   "gaps": [
    "rudy() discards the grid (returns only peak/mean scalars) \u2014 no per-cell hotspot localization, which GR-01 needs to identify the contested channel",
    "No contested-net identification (which nets' bboxes overlap the hot cells)",
    "No pre-route corridor + layer assignment for the contested subset, and no manager-presiding contested-call protocol",
    "No compilation into FR-02 directed intents (FR-02's compiler does not exist) and no contested-nets-routed-first ordering in cec_router.route()",
    "Input is the synth pipeline's pads_by_net, not airwire bboxes from a facts file (CL-23 facts file does not exist)",
    "No seeded two-net congestion fixture"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-02",
   "status": "partial",
   "evidence": [
    "scripts/cec_router.py:360-409 targeted_repair() \u2014 deterministic, runs in the DEFAULT (no-LLM) policies: reads the worst real DRC violation, emits a worker 'keepout' rip-up (reserve the conflict locus so FR re-routes around it) or a manager 'place_nudge' for part conflicts",
    "scripts/cec_router.py:539-542 MANAGER_REPAIRS registry + :436 kelvin_inversion_repair (geometric perception -> place_rotate 180; unit-tested, demonstrated autonomously healing an inverted board) + :500 logo_finishing_repair (written, deliberately not wired); walked by default_manager (:301) before any model is involved",
    "scripts/cec_router.py:212-270 apply_edit \u2014 the move-execution primitives: fr_params / keepout / drop_keepout / place / place_nudge / place_rotate / seeds"
   ],
   "gaps": [
    "The doc's specific ordered battery is absent: no perpendicular obstacle shift computed as clearance+width (place_nudge uses a fixed (0.4,0.4) delta, cec_router.py:399), no layer-swap move, no via-insertion move",
    "No single-net re-route: the rip-up keepout triggers a full region/board FR re-route, not re-routing only the blocked net",
    "No corridor-scoped obstacle enumeration ('each free-class obstacle in the corridor') \u2014 repair is driven from the worst DRC violation, not from a blocked net's corridor",
    "No claim settlement: the repair's success is observed by the loop's next score, but there is no Grade-2 claim record (claims framework absent platform-wide)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-03",
   "status": "partial",
   "evidence": [
    "scripts/cec_dispatch.py:98-117 Metrics carrying drc_types + drc_loci off the single score-run DRC (R-02), :278 JudgeContext, :288 agent_route \u2014 the budgeted tier-escalation loop (K-attempt budget, forced escalate on exhaustion, ladder -> human) with a live Haiku-judge demonstration on record; :245 render tool for visual evidence",
    "scripts/cec_router.py:545 make_subagent_policy \u2014 the model-tier plug-in slot; :360 targeted_repair already attributes part-level conflicts upward to the manager tier (component moves outside the worker's vocabulary)",
    "scripts/cec_judge_local.py:257 make_manager / :395 make_manager_swarm \u2014 broker-backed local-model manager tiers (guided-JSON Verdict schema) that the locus agent would ride on"
   ],
   "gaps": [
    "No locus BUNDLE builder: nothing assembles airwire endpoints + corridor obstacles with net/class/MOBILITY (template-locked / directed-renegotiable / free-movable \u2014 mobility classes don't exist, see FR-04) + clearance numbers + a render CROP; renders are whole-board and no CL-21 locating-aid machinery exists in the repo",
    "No closed copper-only move vocabulary: apply_edit has NO shift_segment / add_via / swap_layer / reroute_net(corridor_intent) \u2014 copper-level moves don't exist; current repairs are keepout-mediated full re-routes and part moves",
    "Execution path depends on FR-02/FR-03 plus a shift primitive \u2014 all missing cores",
    "No same-run claim settlement (claims framework absent); capacity-shortfall upward attribution to placement exists only as the human-ratification wall in cec_loop, not as a K-attempt automatic attribution"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-04",
   "status": "partial",
   "evidence": [
    "scripts/constraints/corpus-extracted.json + scripts/cec_corpus_lint.py \u2014 the provenance-checked case-law corpus (the PC-02 analog repairs would enter), with corpus/general/README.md classes A/B/C/H and human-review promotion lifecycle",
    "scripts/cec_judge_local.py:867 cf_family_of + :779 CORPUS_FIT_SCHEMA (corpus_fit_review) \u2014 per-board-FAMILY precedent-fit audit that already classifies repair-pattern trajectories (repair_then_escalate / thrash) and residual-treatment drift vs the family, with citable precedent ids",
    "scripts/cec_ledger.py \u2014 the durable cross-run ledger (sibling cec-runs repo) any recurrence detector would mine"
   ],
   "gaps": [
    "No repair-recurrence counter per pattern per board family (decision logs record edits but nothing aggregates same-pattern repairs across runs)",
    "No staging pipeline and no automated draft generation (plan heuristic / placement constraint drafted from three same-pattern settlements)",
    "No settlements to cite: vindication/settlement records don't exist (claims framework absent platform-wide)",
    "corpus_fit_review is advisory (sidecar JSON) \u2014 it does not feed anything back into planner heuristics or placement constraints"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-05",
   "status": "missing",
   "evidence": [
    "grep 'pathfinder|negotiat' across scripts/*.py = zero hits; the only congestion structure is cec_synth_pipeline.py:1105 rudy(), which returns (peak, mean) scalars and retains no per-cell cost state",
    "scripts/cec_router.py route() (:702) plans regions once via default_planner and has no iterative all-nets replanning or cost-escalation loop"
   ],
   "gaps": [
    "Everything: PathFinder-style iterative negotiation on a retained grid, contested-cell cost escalation per round, equilibrium / iteration-cap termination, deterministic seeded output of corridor+layer plans for every net, manifest discipline for the negotiation seed",
    "Prerequisite GR-01 grid retention + hotspot localization is itself a gap (rudy discards the grid)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "router-repair"
  },
  {
   "id": "GR-06",
   "status": "missing",
   "evidence": [
    "grep 'homotopy|topolog' across scripts = only Kelvin 'sense topology' MEASURE-stage checks (cec_constraints.py, cec_synth_pipeline.py) \u2014 no homotopy assignment, rubber-band relaxation, crossing-resolution, or channel-capacity machinery exists anywhere"
   ],
   "gaps": [
    "Entirely greenfield: homotopy class assignment for contested nets, joint rubber-band relaxation, crossing resolution as via insertions / class reassignments, channel-capacity check against the GR-05 grid, model adjudication of discrete contested class choices, geometrization through FR-02/FR-03",
    "Depends on GR-05 (missing), FR-02/FR-03 (missing cores), and the facts-native relational representation (CL-23 facts file, missing)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    20
   ],
   "part": "router-repair"
  },
  {
   "id": "CR-01",
   "status": "missing",
   "evidence": [
    "Verified absence: `find . -name '*policy*'` over the repo (excluding .git) returns nothing \u2014 no cec-policy.json or any policy registry file exists anywhere in the tree",
    "CLAUDE.md (Done/agentic-pipeline-punchlist section): 'SB-10 policy-as-code' listed as STILL OPEN \u2014 confirms no policy file landed",
    "Nearest landing points verified: scripts/cec_corpus_lint.py exists (corpus enforcement, wired into checklist.sh) and corpus/general/README.md carries a status-lifecycle field \u2014 but neither is a capability-ladder registry"
   ],
   "gaps": [
    "Greenfield: create the policy file (CL-10) and its registry section: per-tier frozen components / owned-iteration mechanisms / rungs / current rung / climb-gate metric / exit condition",
    "Orchestrator-side loader + refusal of a rung not marked current (no orchestrator reads any policy today \u2014 cec_synth_pipeline/cec_dispatch tiers are hardcoded callables)",
    "Policy assertion that fails a climb without its gate metric satisfied",
    "Depends on CL-10 which is itself unbuilt (build-order step 1)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    21
   ],
   "part": "registry-buildorder"
  },
  {
   "id": "CR-02",
   "status": "partial",
   "evidence": [
    "Routing repair rungs exist de-facto: scripts/cec_router.py:539 MANAGER_REPAIRS registry + cec_router.py:436 kelvin_inversion_repair (deterministic battery) + cec_router.py:545 make_subagent_policy (model-agent rung)",
    "Escalation ladder machinery exists: scripts/cec_dispatch.py:288 agent_route(board, tiers, budget) budgeted tier-escalation loop; scripts/cec_synth_pipeline.py:931 resolve() ladder (worker->manager->frontier->human rungs, pluggable callables)",
    "Placement P1 rung exists (current): scripts/cec_loop.py:40 BOARD_PARAMS + run_loop/run_candidates (PR #18 constraint-aware place->route->check loop, merged to main) + scripts/cec_place.py, cec_hc.py, cec_dcir.py, cec_verify_hc.py all present",
    "Scoring S1 rung exists (current): cec_synth_pipeline electrothermal_solve/physics gates + scripts/cec_score.py Metrics (uncalibrated solver + analytic anchors)",
    "Review R1 rung exists (current): scripts/cec_judge_local.py make_manager / make_dispatch_swarm_tier (:533) / corpus_fit_review (:1111) prompted base models; scripts/cec_bench_manager.py = bench/calibration driver (2026-06-09/10 bench verdicts recorded in cec_judge_local.py header)",
    "Corpus ladder exists with a DIFFERENT lifecycle: corpus/general/README.md:47 status lifecycle 'proposed -> sim_validated -> bringup_validated | human_approved -> ...' (not the doc's draft->shadow->promoted)"
   ],
   "gaps": [
    "The ladders themselves are not enumerated anywhere as data: no registry rows, no rung names (G1..G4 / P1..P4 / S1..S4 / R1..R3), no climb-gate metrics, no exit conditions",
    "Routing G2 (negotiated congestion), G3 (topological plan), G4 (learned, watch-only) all greenfield \u2014 GR-01/GR-05/GR-06 do not exist",
    "Placement P2 (congestion-aware vs GR-01 grid), P3 (case-law-guided/templates), P4 \u2014 greenfield",
    "Scoring S2 bench calibration (SB-06) explicitly awaits hardware per CLAUDE.md ('SB-04/06/09/12 await hardware/history'); S3/S4 surrogates greenfield",
    "Review R2 retrieval (PC-02) and R3 trained manifests (PC-04) greenfield; no calibration-tracking metric is persisted per role today (bench results are docs, not a tracked currency)",
    "Doc lifecycle terms (draft/shadow/promoted) differ from the tree's actual corpus lifecycle states \u2014 CL-01 mapping needed when the zones land"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    17,
    20,
    21
   ],
   "part": "registry-buildorder"
  },
  {
   "id": "CR-03",
   "status": "missing",
   "evidence": [
    "Verified absence: grep -rn 'replay' scripts/*.py finds only replayability COMMENTS \u2014 scripts/cec_ledger.py:10 ('is actually replayable') and scripts/cec_router.py:113 ('Replaying the log + the same Freerouting version reproduces the run'); no replay harness, no control-arm mechanism, no quarantined gate-case set exists",
    "Nearest landing points verified: scripts/cec_ledger.py:123 manifest() embeds the determinism manifest (KiCad pin, resolved kicad-cli, FR version, scripts SHA+dirty) in every run \u2014 the 'enters only as a manifest' substrate; scripts/cec_fr.py:91 FR_VERSION='1.7.0' is a real frozen-component pin; scripts/cec_golden.py run_golden/compare + tests/golden/expectations.json is a regression gate that could host the replay stage"
   ],
   "gaps": [
    "The protocol itself: manifest-only entry for any new frozen component, replay over settled history, the quarantined reality-anchored gate cases (no such quarantine set exists), control-arm execution against the incumbent on sibling candidates, vindication-gated budget promotion",
    "No settled-history corpus to replay against yet (no overnight nights have produced settlements; cec-runs ledger holds run records, not settlements)",
    "Standing-rule text belongs in the missing CL-10 policy file (CR-01)",
    "First concrete application is the FR 1.7.0 -> 2.2.4 migration (FR-01), which cannot run without Decision 18"
   ],
   "conflicts": [],
   "blocked_by_decisions": [
    18
   ],
   "part": "registry-buildorder"
  },
  {
   "id": "BUILD-ORDER",
   "status": "partial",
   "evidence": [
    "Step-3 punchlist contingency ('R-01, R-02, R-05 if not landed') is SATISFIED: scripts/cec_dispatch.py:168 _spread_params (line 189 comment 'R-01: per-seed spread'); scripts/cec_score.py:132-167 Metrics.drc_types/drc_loci + standalone drc_types() ('ONE definition (R-02)' comment at :154); scripts/cec_toolchain.py exists, header explicitly cites R-05 (require_kicad_cli fail-fast + have_kicad_cli degrade)",
    "Step-3 R-12 note '(read PR #18's cec_loop first)': scripts/cec_loop.py is on main (PR #18 merged) with BOARD_PARAMS/run_loop/run_candidates verified",
    "Step-1 ledger half exists: scripts/cec_ledger.py (SB-01, append/manifest/lineage to the sibling cec-runs repo)",
    "Step-2 golden venue exists AND is de-facto decided: scripts/cec_golden.py:17 'run ON-DEVICE in the routing container' + tests/golden/eps-8pin + tests/golden/expectations.json (SB-08); CLAUDE.md: 'RUN IT (in-container) BEFORE MERGING scripts/** CHANGES'",
    "Corpus substrate exists: corpus/general/{README.md,can.json,thermal.json} (SB-13/14) + scripts/cec_corpus_lint.py wired into checklist.sh + scripts/constraints/corpus-extracted.json (258-row project corpus)"
   ],
   "gaps": [
    "Step 1 core is unbuilt: CL-01/CL-02/CL-03 zones + both enforcement points (cec_corpus_lint.py is one proto-enforcement point only), CL-10 policy file (absent \u2014 verified), CL-06 Class A routing rules in the doc's zone scheme",
    "SB-02 inbox is ABSENT (spec only, docs/self-building-pipeline-addendum-2026-06-09.md:76; CLAUDE.md lists it STILL OPEN) \u2014 step 9's DF-01/DF-06 capture 'land with the ledger and inbox in step 1' is therefore half-blocked",
    "Steps 4-13 mechanisms (CL-04/05/07/08/09-as-amended/12..26, DF-*, PC-*, FR-02..04, GR-*, CR-* operative registry) are all greenfield",
    "Step 11 baseline state: scripts/cec_fr.py is pinned FR_VERSION='1.7.0'; no 2.2.4 work anywhere \u2014 Decision 18 gates whether CL-26 runs on 1.7.0 first",
    "Step 2's AM-07 environment decision is de-facto pre-empted for the golden run (in-container) but the night-shift venue (Decision 10) is still unrecorded"
   ],
   "conflicts": [
    "Minor drift, not contradiction: the doc treats PR #18 as a branch to read before building ('read PR #18's cec_loop first'); PR #18 is MERGED to main \u2014 cec_loop/cec_hc/cec_place/cec_dcir/cec_verify_hc are current-tree code, so step-3's CL-07/CL-08 work builds ON them, not alongside them"
   ],
   "blocked_by_decisions": [
    1,
    3,
    4,
    7,
    10,
    18
   ],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-1",
   "status": "missing",
   "evidence": [
    "De-facto state verified: corpus already lives in BOTH candidate locations \u2014 corpus/general/ at repo root (classes A/B/C/H) AND scripts/constraints/corpus-extracted.json (project corpus, 258 rows)",
    "Branch topology de-facto 'PRs everywhere': git branch -a shows only main + claude/* feature branches, no staging branch; recent history is feature-branch->PR->main (PRs #18, #24, #25, #26)",
    "CLAUDE.md records the owner explicitly leaving corpus location OPEN: corpus/general 'owner: stays OPEN, revisit at Class C'"
   ],
   "gaps": [
    "Decision unrecorded; the tree de-facto practices PRs-everywhere and a split corpus location (root corpus/general + scripts/constraints project corpus), and the owner has already deferred the location call to Class C \u2014 CL-01/CL-02 need this ratified before the zone scheme lands"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-2",
   "status": "missing",
   "evidence": [
    "scripts/constraints/corpus-extracted.json exists on main (PR #18 merged), 258 rows after the 2026-06-09 dedup (was 269), full provenance per CLAUDE.md audit note",
    "No staging/promotion lifecycle has been applied to it \u2014 the doc's promoted-vs-staging zones (CL-01) do not exist in the tree"
   ],
   "gaps": [
    "Decision unrecorded and cannot be executed until CL-01 zones exist; the corpus is currently live-and-enforced as-is (cec_router._vital_keepouts_from_rules and cec_constraints consume it), which any all-to-staging migration must not break"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-3",
   "status": "missing",
   "evidence": [
    "The addendum already SPECIFIES GitHub Issues as the queue: docs/self-building-pipeline-addendum-2026-06-09.md:76-101 SB-02 'GitHub Issues as the queue. Label taxonomy: gate:netlist-freeze, ...'",
    "CLAUDE.md STILL-OPEN list echoes it: 'SB-02 inbox (GitHub Issues queue)'",
    "No queue implementation of either substrate exists (no inbox code anywhere in scripts/)"
   ],
   "gaps": [
    "Decision is de-facto leaning GitHub Issues (the owner-reviewed addendum names it), but is unratified and unimplemented; the doc's 'same decision as SB-02' framing is correct \u2014 record it once for CL-07+SB-02 together"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-4",
   "status": "missing",
   "evidence": [
    "De-facto SETTLED = container: scripts/cec_golden.py:17 header 'run ON-DEVICE in the routing container'; CLAUDE.md SB-08 entry: 'RUN IT (in-container) BEFORE MERGING scripts/** CHANGES'",
    "Memory file toolchain-on-device-compute.md + CLAUDE.md: this box has NO host kicad-cli/pcbnew/java \u2014 the cec/routing:kicad10 container IS the compute plane, so the runner-vs-container question has only one workable answer on current hardware"
   ],
   "gaps": [
    "Formal ratification only: the tree already runs SB-08 in PR #18's container; the doc's alternative (self-hosted runner) remains viable for CI-triggered runs (route.yml runner is live) but the golden's current venue is the container \u2014 record the de-facto choice for CL-11"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-5",
   "status": "missing",
   "evidence": [
    "M2.7 is ALREADY DEPLOYED in the tree: docker/compose.yaml:183-202 MANAGER service = MiniMax-M2.7 229B-A10B UD-Q3_K_XL; scripts/cec_judge_local.py:72-84 REVIEWER tier with M2.7 as the opt-in deep auditor (CEC_VLLM_REVIEWER_MODEL=cec-manager)",
    "The 2026-06-10 bench DEMOTED M2.7 to opt-in: cec_judge_local.py:74-77 \u2014 M2.7 truncated twice with empty content at 8k/14k caps, so the bounded-JSON reviewer DEFAULTS to cec-manager-fast (gpt-oss-120b, Apache-2.0)",
    "The Apache 235B analyst the doc means is RETIRED AND DELETED: CLAUDE.md 2026-06-09 owner call (files deleted from the VHDX, registry entry removed); commit d38fccc removed the orphaned 235B manager compose service"
   ],
   "gaps": [
    "The actual decision (who reviews the Modified-MIT terms) is still unrecorded \u2014 the tree adopted M2.7 operationally without a logged license review; ratify retroactively or pin the reviewer default to the Apache gpt-oss-120b and record M2.7 as license-pending"
   ],
   "conflicts": [
    "The doc's fallback option 'stay on the Apache analyst indefinitely' meaning the Qwen3-235B no longer exists as-built \u2014 the 235B was retired and its files deleted on 2026-06-09; the available Apache-licensed analyst is now gpt-oss-120b (cec-manager-fast), which is already the default reviewer. The doc's premise that M2.7 adoption awaits the license decision is overtaken: M2.7 is wired, benched, and opt-in today"
   ],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-6",
   "status": "missing",
   "evidence": [
    "Partial knobs exist but no ratified nightly budget: scripts/cec_judge_local.py:86-95 MANAGER_TIMEOUT=600s, MANAGER_MAX_TOKENS=4096, WORKER_MAX_TOKENS=1200 (per-call caps); scripts/cec_overnight.py is deadline-bounded (wall-clock blocks) but has no token-spend cap or deep-path-specific budget"
   ],
   "gaps": [
    "Decision unrecorded: nightly token + wall-clock caps for the deep path (CL-16) do not exist; current caps are per-call engineering limits, not a budget policy"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-7",
   "status": "missing",
   "evidence": [
    "A minimal verdict shape exists de-facto: scripts/cec_judge_local.py:233 _judge_verdict guided-JSON {action, reason} constrained to the Verdict schema; scripts/cec_dispatch.py Verdict dataclass; neither is the CL-12 morning-bundle field set"
   ],
   "gaps": [
    "The CL-12 verdict schema itself is undesigned and unbuilt; the decision is to review+freeze a field set that does not yet exist \u2014 schema drafting must precede the lock"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-8",
   "status": "missing",
   "evidence": [
    "No visual panel exists (CL-22 greenfield); render machinery is the only landing point: scripts/render.sh + cec_router --render + cec_synth_pipeline render handoff"
   ],
   "gaps": [
    "Decision unrecorded: panel cadence, Pareto-finalist seat count, frontier-seat nightly-vs-fab-preflight \u2014 an API spend decision with nothing in the tree bearing on it; interacts with Decision 11 (egress) before any frontier seat can see a render"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-9",
   "status": "missing",
   "evidence": [
    "A swarm tier mechanism exists: scripts/cec_judge_local.py:533 make_dispatch_swarm_tier(panel=3, ...) (local worker-model panel wired into cec_dispatch --swarm), but no charters, per-night budget, or precision floor are defined anywhere"
   ],
   "gaps": [
    "Decision unrecorded: initial charter set, per-night swarm budget, charter-suspending precision floor (CL-24); the existing panel knob is a count, not a charter system"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-10",
   "status": "missing",
   "evidence": [
    "De-facto hybrid already operating: scripts/cec_overnight.py:9-21 \u2014 ROUTE blocks run in the `routing` docker container (pcbnew lives there, broker via host.docker.internal:8080), driver + REVIEW blocks run on the WSL2 host, broker is a systemd unit; CLAUDE.md + memory: no host kicad-cli/pcbnew/java, container is the compute plane"
   ],
   "gaps": [
    "Decision unrecorded but heavily constrained by hardware reality: pure-native-Windows and pure-WSL2 options are not viable without installing the toolchain on the host; ratify the existing hybrid (WSL2 host driver + PR #18 routing container) and run the AM-07 end-to-end golden in it"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-11",
   "status": "missing",
   "evidence": [
    "Nothing in the tree records an egress policy; current frontier usage is session-based (Claude Code sub-agents see board metrics/renders in-session) with no written artifact-class policy"
   ],
   "gaps": [
    "Decision unrecorded: which artifact classes (board files, renders, facts, BOMs) may leave the box to an API; prerequisite for CL-22 frontier seats and any cloud-swarm where='runner' expansion"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-12",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no WIP caps, no review-minutes budget anywhere (AM-05 greenfield; the inbox it would cap \u2014 SB-02 \u2014 is also unbuilt)"
   ],
   "gaps": [
    "Decision unrecorded: stated owner review minutes/day and derived per-inbox-class WIP caps; meaningless to enforce until SB-02/CL-07 exist but should be stated before the first night per the doc"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-13",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no decision-forensics report exists (DF-04 greenfield), so no second-reviewer arrangement is recorded"
   ],
   "gaps": [
    "Decision unrecorded: non-hardware second reader (CEC Chris) vs accepting the single-operator residual; only needed once DF-04 reports exist (build-order step 9, decision-volume gated)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-14",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no consistency-probe mechanism (DF-03 greenfield). Note scripts/probes/ exists but contains only fem_probe_12vhpwr.py (a physics FEM probe) \u2014 unrelated to DF-03 decision-consistency probes"
   ],
   "gaps": [
    "Decision unrecorded: probe opt-in and queue fraction within the AM-05 budget; blocked in practice on Decision 12 (the budget it spends from) and on the inbox existing"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-15",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no settlement grades, no vindication currency, no reward weights anywhere (DF-07 greenfield; the policy file that would hold them \u2014 CL-10 \u2014 is also absent)"
   ],
   "gaps": [
    "Decision unrecorded: Grade 1 vs Grade 2 settlement pay and novelty multipliers; needed before DF-07's reward rule lands (build-order step 1 for the rule, weights set in policy)"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-16",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no process-corpus capture exists (PC-01 greenfield). The template the doc cites IS in the spec: CEC-Platform-Ground-Truth-Spec.md Appendix D's de-identified corpus discipline (per CLAUDE.md v1.1.0 summary)"
   ],
   "gaps": [
    "Decision unrecorded: retention horizon, private-by-default confirmation, de-identification rules for any future pooling/publication; the doc is right that capture cannot be retroactive, so this fronts PC-01 in step 1"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-17",
   "status": "missing",
   "evidence": [
    "Nothing in the tree records a moratorium or its lift conditions; no training of any kind exists yet (all model use is prompted inference via the broker \u2014 scripts/cec_judge_local.py, scripts/cec_overnight.py)"
   ],
   "gaps": [
    "Decision unrecorded: define the evidence that would ever lift the generative-training moratorium (a holdout regime detecting distribution narrowing) or make it permanent on the record; gates the PC-03/PC-04 training rung and caps the CR-02 review ladder at R3"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-18",
   "status": "missing",
   "evidence": [
    "De-facto current state = pinned 1.7.0: scripts/cec_fr.py:91 FR_VERSION='1.7.0' (jar URL, cache paths, run_freerouting docstring all 1.7.0); zero references to 2.2.4 anywhere in scripts/ \u2014 no migration work has started"
   ],
   "gaps": [
    "Decision unrecorded: bank a baseline on pinned 1.7.0 first vs migrate to 2.2.4 immediately; the tree's inertia is the baseline-first path (everything is built and measured against 1.7.0, including the Windows-runner fixes and the spawn-pool ceiling); blocks FR-01 and sequences CL-26"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-19",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no plan stage exists (GR-01/GR-05 greenfield). Closest existing machinery is route-time keepouts/corridors (scripts/cec_router.py _vital_keepouts_from_rules force-corridor enforcement), which is constraint enforcement, not a detection/assignment plan stage"
   ],
   "gaps": [
    "Decision unrecorded: ship GR-01 detection-plus-assignment as v1 vs jump to the GR-05 negotiation loop; per build-order step 12 this lands with the plan stage after first overnight failures"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-20",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no GR-06 topological planner, no completion/edit-distance shortfall metrics per family (no overnight settlement history exists to measure them against)"
   ],
   "gaps": [
    "Decision unrecorded: quantify the evidence threshold justifying GR-06; per CR-01 the doc intends the registry to absorb exactly this class of climb gate, so it should be set during Decision 21's ratification rather than ad-hoc"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  },
  {
   "id": "DECISION-21",
   "status": "missing",
   "evidence": [
    "Nothing in the tree: no registry exists to ratify (CR-01 missing \u2014 verified no policy file anywhere); the de-facto starting rungs are inferable from code (routing G1-ish corridor enforcement in cec_router, placement P1 in cec_loop/cec_place, scoring S1 in cec_synth_pipeline physics, review R1 in cec_judge_local) but are nowhere written down as rungs with gates"
   ],
   "gaps": [
    "Decision unrecorded: the one-time review fixing each tier's starting rung, climb-gate metric, and exit condition; requires CR-01's registry table drafted first, then this ratification makes climbs ordinary PRs"
   ],
   "conflicts": [],
   "blocked_by_decisions": [],
   "part": "registry-buildorder"
  }
 ],
 "verification": {
  "exists": [
   {
    "id": "CL-01",
    "claim": "Corpus schema exists (classes/lifecycle/source/supersedes) but no zones, no signoff/promotion fields; extracted corpus is 258 rows without lifecycle fields",
    "verdict": "confirmed",
    "detail": "All evidence held under attempted refutation. corpus/general/README.md states the full SB-13 schema incl. 'compiler consumes only *_validated/human_approved' policy; can.json (2) + thermal.json (3) entries carry exactly {applies_to,class,id,kind,notes,scope,source,status,supersedes,units,value} \u2014 no state/signoff/promotion keys. cec_corpus_lint.py:37-43 has CLASSES/STATUSES(5-state)/SOURCE_TYPES exactly as claimed. scripts/constraints/corpus-extracted.json = 258 rows (linter header still says '269 rows' \u2014 stale comment), field union is exactly the 12 claimed fields, zero rows carry state/signoff/promotion/class. corpus/staging/ and corpus/promoted/ do not exist (ls fails; only corpus/general/). Status partial is correct."
   },
   {
    "id": "CL-03",
    "claim": "Linter-in-CI + hand-maintained constraint registry + artifact emitters exist; no corpus->artifact compiler, no ADV- namespace",
    "verdict": "confirmed",
    "detail": "Linter rules verified at the cited lines (model-source reject 108-110 + 171-173; Class-C ledger-run 113-117; Class-H 129-131; proposed-warn 132-134; SOURCE_TYPES :40), wired via checklist.sh:49-54 into kicad-checks.yml:27-28 (push+PR). cec_constraints.py Constraint dataclass :39-51 (status ratified|proposed, severity incl. advisory), ~30 C(id=...) checkers :60-220, consumed by cec_place.py:32 + :462 (subprocess --json), cec_sweep:51-61, cec_cascade:35/:51. derive_cross_section_dru:986 + ratify_cross_section:1021 (write=True gate), spec_to_dru:197, write_netclasses:319/write_dru:329. min-pour-cross-section advisory :96-107; cascade FEM blocking/advisory split :163-188. MISSING-core verified: corpus/general/*.json is read ONLY by cec_corpus_lint (repo-wide grep); cec_constraints references corpus-extracted.json only in a COMMENT (:15) and cec_ledger only hashes it (:141) \u2014 the registry is hand-maintained Python and run() ignores corpus state. grep 'ADV-' over scripts/+docs/ = zero. human_signoff (cec_synth_pipeline.py:2329) counts blocking as conf>=0.5 with no advisory-namespace exclusion."
   },
   {
    "id": "CL-04",
    "claim": "Landing points (cascade/dispatch/ledger/overnight-review) exist; cec_shadow.py, per-entry fire events, judge annotation, promotion generator, SB-02 inbox all absent",
    "verdict": "confirmed",
    "detail": "run_stage:785 / run_full_cascade:793; cec_cascade consumes cec_constraints.report (:51); cec_ledger.append:158 with constraint_corpus_sha256 in input_hashes:137-148, wired fail-safe at cec_router.py:~948-959 and cec_synth_pipeline.py:2453-2464; cec_overnight.review_block calls corpus_fit_review at :214. scripts/cec_shadow.py ABSENT (directory listing). Ledger records are per-run (run_id/board/mode/inputs/manifest/outputs/verdict) \u2014 no per-entry fire events. No TP/FP annotation anywhere in overnight/judge_local. SB-02 exists only as addendum spec (docs/self-building-pipeline-addendum-2026-06-09.md:76, build-order ref :400); no inbox in the tree."
   },
   {
    "id": "CL-05",
    "claim": "Duplicate-(kind,scope) conflict detection + deprecated/supersedes lifecycle exist; promoted zone, misfire-evidence, inbox, promotion-blocking all absent",
    "verdict": "confirmed",
    "detail": "cec_corpus_lint.py:139-145 errors on duplicate (kind, scope) with the exact 'supersede (supersedes + deprecate the old entry) instead of shadowing' message; deprecated entries excluded from the scope map (:140, :144). STATUSES includes deprecated (:39); supersedes present in README schema and all 5 live entries. corpus/promoted/ absent; no misfire-record mechanism in the judge/bench path; no inbox; no promotion machinery \u2014 all confirmed by absence."
   },
   {
    "id": "CL-07",
    "claim": "Overnight driver + flock slot semaphore + ledger/lineage exist; no orchestrator, queue, quarantine, ledger-resume, scheduled unit, or measured-cost allocator",
    "verdict": "confirmed",
    "detail": "cec_overnight.py: route/review alternation with per-route try/except (:165-168), _save_manifest after every route/review (:173/:231), per-phase elapsed_s, review resume via _reviewed_set (:179-182). cec_dispatch runner_slots:130/runner_slot:135-165 cross-process flock semaphore (CEC_RUNNER_SLOTS). cec_ledger.append wired at cec_router:~948-959, lineage():222, CEC_PARENT_RUN_ID. R-01 _spread_params:168, R-02 drc_types/drc_loci (cec_score:133-134), R-05 cec_toolchain (imported cec_dispatch:45, cec_ledger:44; render degrade :245-247). Gaps verified: no cec_orchestrator.py; BOARDS hardcoded :40; fixed round-index escalation :160-161; overnight itself appends NO ledger lines (manifest.json only \u2014 confirmed, no cec_ledger import); find for *.service/*.timer in repo = none."
   },
   {
    "id": "CL-08",
    "claim": "Inner/middle re-entry slices + repair/escalate primitives + deterministic features exist; no attribution module/rule table, no outer outline loop, no bandit state",
    "verdict": "confirmed",
    "detail": "cec_loop.run_loop: residual-MOVABLE break at ~:171-184, step-3b RESERVE-REROUTE (escape_reservations + keepouts + re-route) at ~:137-167; run_candidates:213-238 returns escalate_to_human when no candidate clears hard FAILs. cec_router default_manager:301 + MANAGER_REPAIRS:539 + default_escalator:347 + Spec.Kmax:85. Features: Metrics.drc_types/drc_loci:133-134, CandidateMetrics.unconn_nets ~:118, registry :60-220, robust-z/MAD prepass (cf_compute_evidence:976). scripts/cec_attribution.py ABSENT; the only bisection reference is the 'real bisection later' comment (cec_synth_pipeline.py:2509-2510). Nuance that does not change the verdict: a one-shot --sweep size sweep exists (CLI :2541) but is not a feedback loop and consumes no attribution \u2014 'no outer loop' stands."
   },
   {
    "id": "CL-09",
    "claim": "Candidate generation + consent loop + pre-route check IDs + perturbation operators exist; no portfolio generator, no portfolio gate, no soft-pins/consent file",
    "verdict": "confirmed",
    "detail": "place_candidates:1830 and place_with_consent:1854 verified; cec_loop BOARD_PARAMS:40-45 + run_candidates:213-238; check IDs at the exact claimed lines (hot-sensitive-separation:115, kelvin-sense-adjacent-shunt:65, decoupling-cap-owner:171, connector-mouth-faces-edge:152); cec_cascade.placement_metrics:48 excludes ROUTE_TIME_IDS:43. ONE evidence correction: cec_place.py has NO `def refine` \u2014 the rotation-searching operators are apply_separate:159 / apply_adjacent:216 (explicit position-AND-rotation search per its docstring) / tighten_kelvin:298; substance of the claim holds, the function name 'refine' does not exist. Gaps confirmed: consent is an in-process ask() callable, no persisted consent format, no placement-hash dedupe, cec_loop varies only kelvin_tighten_mm."
   },
   {
    "id": "CL-11",
    "claim": "Golden regression with bands + --freeze exists but is not in CI; only one fixture; no flag-ID invariant job; no TPS2121 corpus entry",
    "verdict": "confirmed",
    "detail": "cec_golden.py run_golden:43/make_bands:81/compare:105/--freeze:127 + fail-safe ledger append :155-158; tests/golden/ contains ONLY eps-8pin/ (pcb+pro+dru+prl) + expectations.json (frozen 2026-06-09: drc 4, tracks 556, thermal_max_T 128.2, band max 147.4). grep cec_golden over .github/ = NOT in CI (workflows = kicad-checks/route/synth only); header records the manual in-container discipline. fab/ holds zero .kicad_pcb (find). corpus/general ids = 2 CAN + 3 thermal entries, none TPS2121 (TPS2121 appears only in corpus-extracted rows e.g. hub-tps2121-mux). No expected-fail markers, no held-out pool \u2014 confirmed by absence."
   },
   {
    "id": "CL-12",
    "claim": "Minimal verdict schema + rich corpus-fit schema + proto-morning manifest + render primitives exist; no bundle builder, no owner-verdict surface, no ADV- class",
    "verdict": "confirmed",
    "detail": "VERDICT_SCHEMA:118-126 is exactly {action: accept|repair|escalate, reason}; CORPUS_FIT_SCHEMA:779 is the rich guided-JSON contract (fit/recommendation/confidence/trajectory/matched+violated precedents by precedent_id); corpus_fit_review:1111 emits sidecars; REVIEWER_MODEL/URL :82-83. Overnight manifest accumulates per-route and per-review records under build/overnight/. Render primitives: cec_dispatch.render:245; cec_plot.copper_plot/render_3d consumed by cec_loop's human-review handoff (~:186-195). cec_ledger corrects= primitive :180-181. Gaps verified: grep 'bundle' across scripts = nothing relevant; no ADV- anywhere; no incumbent concept in the ledger; no owner accept/override path writes anything."
   },
   {
    "id": "CL-15",
    "claim": "Miner->scribe recovery + floors + tests + reviewer fail-safe exist; no deliberate deep_verdict unit, no trace persistence, no spans, no conclusionless guard, no escalation ladder",
    "verdict": "confirmed",
    "detail": "_chat_json:170-199 recovers empty-content via scribe only as a FAILURE path (fires on empty content + non-empty reasoning_content with _AUTO_SCRIBE); SCRIBE_SYSTEM:202-207 exact extractor contract; _scribe_json:210-229 tail-caps at _SCRIBE_TRACE_CHARS=36000 (:113) and RAISES ValueError on empty scribe content (callers catch -> deterministic fallback); floors _FLOOR_MODELS:105-110 applied :188-190. tests/test_judge_local_scribe.py covers floors, recovery (asserts 2 requests, same model, temp>=0.35, tail cap), no-trace raise, REVIEWER_MODEL resolution. corpus_fit_review + _cf_no_opinion:1070/_cf_clamp:1093. Gaps verified: reasoning_content is read transiently only in cec_judge_local + cec_bench_manager (length only) \u2014 never persisted/hashed/ledgered; no span fields in either schema; SCRIBE_SYSTEM presumes the trace 'already contains a final conclusion' with no conclusionless detection \u2014 the hallucinated-verdict mode is unguarded as claimed."
   },
   {
    "id": "CL-16",
    "claim": "Deep review is env-gated, once-per-routed-board, batched into reviewer residency windows; trigger-based routing, token caps, and trigger-reason ledgering absent",
    "verdict": "confirmed",
    "detail": "cec_router.py ~:965-975: CEC_CORPUS_REVIEW=1 gate, one review at end of route(), sidecar written, comment states 'advisory; never feeds back into the route'. cec_overnight: route runs force CEC_CORPUS_REVIEW=0 (:125), review_block:200-231 with ensure_manager:114-118 broker warm and wall-clock budgets only (review_timeout/--review-block/--hours); manifest review records carry exactly {corpus,fit,rec,conf,headline,elapsed_s,t} \u2014 no trigger reason. Minor line nit: _pending_corpus is :185-197 (:179-182 is _reviewed_set). No token caps and no deterministic triggers anywhere \u2014 frequency-based by design, as claimed."
   },
   {
    "id": "CL-18",
    "claim": "Source-type whitelist + model-source rejection enforced in CI; trace-hash binding, model-artifact ref resolution, owner-signature upgrade absent",
    "verdict": "confirmed",
    "detail": "SOURCE_TYPES :40 = {standard,datasheet,fab,spec,decision,measurement}; :103-117 rejects missing/empty source, source.type=='model' ('model knowledge is not a source'), unknown types, and requires Class-C 'run:' refs; :171-173 rejects the project-corpus 'model' source string. checklist.sh:51 runs it; checklist runs in kicad-checks.yml:27-28. Gaps confirmed by absence: no trace artifacts exist anywhere to bind (CL-15 verified), no ref-resolution check beyond spec-section regex (:182-186), no signature-upgrade transition in the lifecycle."
   },
   {
    "id": "CL-19",
    "claim": "Manager bench + offline extractor tests + live differentiation check exist; extractor-fidelity eval, eval gating, and analyst-usefulness denominator are greenfield",
    "verdict": "confirmed",
    "detail": "cec_bench_manager.py exists, header confirms: promoted 2026-06-10, deliberately RAW single-call (posts directly, NOT through _chat_json) so thinking overruns surface; raw 2026-06-09 logs lost to a WSL restart, build/bench-gptoss.log survives (gpt-oss-120b 3/3, 495 tok/33.2s) and docs/llm-manager-bench-2026-06-09.md records results. tests/test_judge_local_scribe.py = machinery tests only; differentiated_test:635 (live). CI verified: kicad-checks.yml runs only checklist.sh + check-all.sh \u2014 no unittest, no bench, no eval gate (route.yml/synth.yml are manual dispatch). No (trace, gold) pair set, no zero-tolerance gate, no trigger ledger \u2014 all confirmed absent."
   },
   {
    "id": "CL-21",
    "claim": "Render primitives (top PNG, layer-colored copper plot, ref-driven zoom crops, plan PNG) + loci data + parsers exist; no view-set generator, no SVG export, no net-family coloring, no diff/coverage renders",
    "verdict": "confirmed",
    "detail": "render.sh = single top render; cec_router.render:884 + cec_dispatch.render:245 (R-05 degrade). cec_plot.copper_plot:54 draws from pcbnew geometry and colorizes BY LAYER (legend: F.Cu track (214,40,40) red, B.Cu (40,90,214) blue) with DRC real-X/finishing-O classification \u2014 exactly as characterized. cec_zoomplot = ref-arg-driven problem crops. routing_plan_png:253. Metrics.drc_types/drc_loci :133-134 + _types_loci:152. ledger manifest:123/input_hashes:137. grep across scripts: ZERO 'export svg' invocations. Parsers real: read_placement:1036, _pads_by_net:245, pad_global:82. grep 'bundle' = nothing relevant. All gaps confirmed by absence; every render path is top-only."
   },
   {
    "id": "CL-22",
    "claim": "Panel machinery + scribe + reviewer deep pass + hot-sep checker exist; panel orchestrator, finding contract, policy file, render fixtures, and any vision-capable binding absent",
    "verdict": "confirmed",
    "detail": "_panel:376 (lens prompts + temps + max_workers) feeding make_manager_swarm:395/make_worker_swarm:457/make_dispatch_swarm_tier:533 \u2014 all over Metrics contexts, never renders. _scribe_json:210; corpus_fit_review:1111 (prepass->evidence->LLM->clamp). hot-sensitive-separation C() at :115 + _chk_hotsep:843; ref3030-away-from-heat present in the 258-row corpus. find cec-policy* = nothing; tests/golden = eps-8pin only; scripts/probes/fem_probe_12vhpwr.py exists. /home/nathan/cec-llm-broker/models.json = exactly the 5 claimed backends; grep mmproj/vision/multimodal across models.json, broker.py, docker/compose.yaml = zero hits. Status partial correct."
   },
   {
    "id": "CL-23",
    "claim": "cec_facts.py absent but most field-family producers exist; netclass reader, via tables, slicing API, facts hash all missing",
    "verdict": "confirmed",
    "detail": "scripts/cec_facts.py ABSENT. Producers verified at the claimed sites: read_placement:1036 (positions/orientations/courtyards/pads_by_net), courtyard_bbox:107, pad_global:82, _pads_by_net:245; per-via attrs in cec_synth_pipeline ~:2211-2229 (net, GetDrillValue, per-net parallel split), cec_fr.py:660-661 (GetWidth(TopLayer)/drill), cec_dcir ~:294; _chk_pour_integrity:505/_chk_min_cross:534; _min_pad_dist_mm:257 + _chk_hotsep:843; drc_loci via _types_loci:152; input_hashes:137 + cec_toolchain.sha256_file. Netclass claim verified hard: grep net_settings -> only cec_pcb WRITERS (netclass:310-312, write_netclasses:319-323); Rules.from_board:104 derives families by NAMING convention only. _context:239 serializes DecisionLog-shaped Metrics, not board facts. All gaps stand."
   },
   {
    "id": "CL-24",
    "claim": "Concurrent swarm over Metrics + lens decorrelation + deterministic triage backends + broker arbitration exist; charters, micro-schema findings, funnel, policy registry, facts substrate absent",
    "verdict": "confirmed",
    "detail": "swarm_judge:289 exercised by cec_sweep.sweep_swarm (~:120), cec_router --swarm wiring :916-937, cec_overnight route_one :128 ('--judge', 'local', '--swarm', '3'). Substrate confirmed as _context():239 = candidates_best_first metrics + gate failures; output contract is VERDICT_SCHEMA accept/repair/escalate. _panel:376 lens-diverse. Triage backends: ~30 C(id=) checkers + 258-row corpus with deterministically_checkable/existing_checker fields. Broker: cec-worker max_inflight=4 / vram_gb=27 / gpu_budget_gb=30 confirmed in models.json. find cec-policy* = none; cec_facts.py absent; only deep path is corpus_fit_review behind CEC_CORPUS_REVIEW=1. All gaps stand."
   },
   {
    "id": "CL-25",
    "claim": "Registry checkers cover slices of the six classes + synth-pipeline intake gate; netclass-membership audit, class-minima checks, sch/pcb sync, and a route-path intake gate absent",
    "verdict": "confirmed",
    "detail": "Verified: _chk_hotsep:843 (hot-sensitive-separation), _chk_decap:737, _chk_tw:815 + derive_cross_section_dru:986/ratify_cross_section:1021, CONFORMANCE stage list :767 (+ _chk_rj45:665/_chk_detect_r:684/_chk_shunt_val:725), chk_bom_complete:470, run_pipeline ERC_BOM gate :2388-2392. Gaps verified hard: no code anywhere reads .kicad_pro net_settings (writers only, cec_pcb); audit-sch.py header confirms sch-internal geometry audit only; cec_router.py contains no ERC/netlist intake (grep: no erc hits in code paths; route() goes planner->generate directly) and overnight route_one invokes cec_router straight \u2014 a broken-netlist board would route all night, as claimed."
   },
   {
    "id": "CL-26",
    "claim": "Status 'exists': plain-Python overnight night-shift with spread/dedupe, local swarm judging, ledger, and a golden baseline",
    "verdict": "confirmed",
    "detail": "'Exists' is supported: cec_overnight.py is a real deadline-bounded driver (route/review alternation, broker-mediated models, per-phase elapsed_s into build/overnight/manifest.json, routes in the routing container via docker compose exec); _spread_params:168 + sha256 dedupe :198-205; cec_router CLI --judge local/--swarm :916-937; cec_ledger.append:158; cec_golden + frozen expectations.json. Gaps accurate: route_one/review_block each record a single whole-phase elapsed_s (no route-vs-score-vs-swarm split), no charters/triage-confirmation, no allocator, no facts layer."
   },
   {
    "id": "AM-04",
    "claim": "Analytic solver with an unfit k knob; uncalibrated posture stated in rule prose; golden band as the only thermal anchor",
    "verdict": "confirmed",
    "detail": "cec_synth_pipeline.py:2042-2047 comment: 'the constant k is the tuning knob to fit 2152 data' (never fit \u2014 no fitting code exists); tests/golden/expectations.json bands thermal_max_T_max=147.4 over baseline 128.2 (regression anchor, not an analytic golden); min-pour-cross-section rule text :96-107 says 'the solver is not yet bench-validated' with status=proposed; cec_dcir.py:24 'stage it as a calibration, not a hard re-gate'. No machine-readable uncalibrated flag in any output dataclass \u2014 posture lives in prose only. The CLAUDE.md model-debt items (cross_mm2 segment-sum, per-cluster via split, pour-keyed k) remain open per the 12VHPWR action item."
   },
   {
    "id": "AM-05",
    "claim": "Cautious headless sign-off exists; inbox/WIP caps/review-minutes budget/absence mode entirely absent",
    "verdict": "confirmed",
    "detail": "human_signoff (cec_synth_pipeline.py:2329-2340): headless path returns 'not blocking' i.e. sign off ONLY when nothing blocking remains \u2014 the no-auto-sign-in-absence guarantee. No inbox of any kind exists in the tree (SB-02 spec-only at addendum :76), so there are no classes to cap; no budget or absence-mode config anywhere; no orchestrator (cec_orchestrator.py absent)."
   },
   {
    "id": "AM-06",
    "claim": "Append-only global ledger with hashes/pointers; multiple writers do bare unlocked appends; no sharding or sidecar pattern",
    "verdict": "confirmed",
    "detail": "cec_ledger.append:158-192 writes one JSON line via bare open(p,'a') at :190-191 \u2014 no fcntl/lock of any kind. Multi-writer confirmed: cec_router (:~948), cec_synth_pipeline run_pipeline (:2456), cec_golden (:158), and the CLI all call append() directly. Single global file ../cec-runs/runs/ledger.jsonl (verified present). Header states the hashes-and-pointers + corrections-as-new-lines discipline. The concurrent-append exposure is real exactly as named (small JSON lines on POSIX append are mostly atomic in practice, but nothing enforces it)."
   },
   {
    "id": "AM-07",
    "claim": "Routing container is the de facto compute venue; golden venue decision recorded; night venue (Decision 10) and full-chain venue golden absent",
    "verdict": "confirmed",
    "detail": "docker/compose.yaml routing service: image cec/routing:kicad10, CEC_VLLM_URL=http://host.docker.internal:8080/v1 + extra_hosts host-gateway. cec_golden.py header records the owner decision (2026-06-09) to run ON-DEVICE in the routing container with the exact compose command. cec_overnight route_one runs via `docker compose exec -T ... routing`, reviews on the host (pcbnew-free); broker is a systemd unit outside the repo (no unit files in repo, verified). cec_golden's chain is route+score+physics only (run_golden has no judge/broker leg) \u2014 the end-to-end venue golden including the judge leg does not exist, and no Decision-10 record found."
   },
   {
    "id": "AM-09",
    "claim": "Status 'exists': deterministic Kelvin template-route + pre-FR corridor reservations + hard kelvin gate; gaps are post-FR ordering, hardcoded conventions, no scorer DOA, Kelvin-only coverage",
    "verdict": "confirmed",
    "detail": "'Exists' supported. route_high_current (cec_hc.py:270-344): computes hc_nets = *_HI/*_LO, rips ALL prior tracks/vias on them, lays inner-shunt-edge->INA taps on F.Cu no-via ONLY where _tap_clear passes (do-no-harm), skipped taps returned as needs_placement, re-fills in a fresh process. kelvin_keepouts:100 + keepouts_from_pours:139 wired pre-FR in cec_loop run_loop. cec_score.gate() makes kelvin_ok a hard gate (:440-452). Gaps verified: the loop runs cec_hc AFTER FR (rip+relay, not pre-locked); net selection is the suffix convention, no lock registry; no candidate-touched-locked-net DOA scoring (cec_hc silently repairs); coverage is exactly the _HI/_LO Kelvin set \u2014 sideband taps/VRAIL_DIV untemplated."
   },
   {
    "id": "AM-10",
    "claim": "Judge inputs are serialized deterministic data; the scribe trace is the one model-authored re-entrant text and is data-framed; no provenance-label regime beyond that",
    "verdict": "confirmed",
    "detail": "_context:239-255 returns json.dumps of region/iteration/candidates_best_first/gate failures (deterministic metrics only); _scribe_json frames the trace as 'REASONING TRACE:\\n' under the editor-only SCRIBE_SYSTEM. Refutation attempt found one nuance that supports the gap rather than the evidence: corpus_fit_review's exemplars/digest enter the reviewer context as plain payload fields with no trust-boundary labels \u2014 consistent with the listed gap. No sanitization layer or staging-text policy exists (staging tier absent)."
   },
   {
    "id": "AM-11",
    "claim": "Hard per-tier output budgets + one input ceiling exist; no slicer, no slice hashes in the manifest",
    "verdict": "confirmed",
    "detail": "cec_judge_local.py: MANAGER_MAX_TOKENS=4096 (:95), WORKER_MAX_TOKENS=1200 (:101), _SCRIBE_MAX_TOKENS=4096 (:114), _SCRIBE_TRACE_CHARS=36000 tail cap (:113) sized to the 16k manager context (comment :111-112). cec_ledger.manifest():127-134 carries only kicad/freerouting/scripts/python fields \u2014 no slicer version or slice hash slot. cec_facts.py absent, so no per-job slice budgets exist. (corpus_fit_review adds its own CEC_CORPUS_MAX_TOKENS=6144/CEC_CORPUS_CTX knobs \u2014 more budget machinery than listed, same conclusion.)"
   },
   {
    "id": "RB-01",
    "claim": "Per-entry 'scope' field exists under lint; it is parameter-applicability, not coverage scope; no blind map or review-budget split",
    "verdict": "confirmed",
    "detail": "corpus/general/README.md:33 shows scope in the schema ({\"layer\": \"external\"} \u2014 parameter applicability exactly as characterized); cec_corpus_lint requires scope (:83) and uses it only for duplicate-(kind,scope) detection (:139). cec_constraints REGISTRY rows carry category/params/source but no coverage-scope declaration in facts dimensions. No facts vocabulary (cec_facts.py absent), no blind-map computation, no review-budget mechanism anywhere \u2014 confirmed by absence."
   },
   {
    "id": "RB-02",
    "claim": "Per-stage enforcement legs exist (directives, FR keepouts, ratified DRU writes) plus hand-declared applies_to; no generated pushdown table or residual record",
    "verdict": "confirmed",
    "detail": "directives():1046 (FAIL -> typed directive), cec_loop assembles keepout hints into FR (kelvin_keepouts + keepouts_from_pours + escape reservations), derive_cross_section_dru:986 + ratify_cross_section:1021 behind write=True, spec_to_dru:197. README applies_to \u2208 physics|compiler|preflight|judge|informational with the Class-H never-deterministic rule enforced at lint :129-131 and proposed-never-compiled warned :132-134. Nothing computes an earliest-horizon table or records per-stage artifacts/residuals; no AM-02 fixtures exist (tests/ has only golden eps-8pin + 2 unit-test files)."
   },
   {
    "id": "RB-03",
    "claim": "Conclusions-sourced extraction is the de facto default via miner->scribe; no source-section classification or forced-choice ratification",
    "verdict": "confirmed",
    "detail": "_chat_json auto-recovers via scribe (_AUTO_SCRIBE default on), and _scribe_json transcribes from the trace TAIL (conclusions-end assumption, comment :111-112 'conclusions form at the trace END, so the TAIL is kept'). No code classifies where in the trace the verdict came from; no perturbed-distractor ratification loop; no eval cases (CL-19 fidelity set absent). All confirmed."
   },
   {
    "id": "RB-05",
    "claim": "Constructed param diversity + byte-identity dedupe floor exist; no measured outcome-space diversity, no allocator coupling",
    "verdict": "confirmed",
    "detail": "_spread_params:168-185 (0.5x..1.5x opt_time spread) + sha256 dedupe with _dup_of_seed tagging :198-205 in cec_dispatch; the same dedupe also in cec_router._candidate_pool:618-635 ('reuse the duplicate's metrics, skip the DRC'). No score-vector spread / check-fire fingerprint / topology-hash diversity measures anywhere; no CL-08 allocator exists to constrain; no convergence-decay coupling. Confirmed."
   },
   {
    "id": "DF-01",
    "claim": "Run-level ledger + per-decision DecisionLog/dispatch/overnight records exist; no decision-class taxonomy, decider manifest, evidence-bundle hash, or consent line",
    "verdict": "confirmed",
    "detail": "Ledger record fields verified exactly (:163-183: run_id/ts/board/mode/inputs/manifest/outputs/verdict/elapsed_s/parent_run_id/corrects/extra); manifest():127-134 has NO model/quant/prompt fields. DecisionLog.add:119-131 stores candidates via _m (drc/unconnected/tracks/vias/length/kelvin/diffpair/cu12v/balance/gates/drc_types), verdict {action, reason, tier, edit} with tier a free string; to_json embeds cec_ledger.manifest() (:147-158); archive_log:172 -> build/route/corpus. cec_dispatch agent_route logs {tier,budget_left,params,note} incl. R-08 coercions; overnight manifest['reviews'] verified. find *policy* = nothing (no consent line possible). All gaps stand; the proposed extension points (extra field, sidecars) are real."
   },
   {
    "id": "DF-04",
    "claim": "Stats-find/model-narrate architecture exists for board-fit (corpus_fit_review); nothing analyses decisions; no inbox/disposition capture",
    "verdict": "confirmed",
    "detail": "corpus_fit_review body verified: cf_family_digest/cf_new_summary/cf_compute_evidence compute the MAD/median robust-z evidence deterministically, the payload instructs 'robust_z/flags/evidence are precomputed; reason about meaning, do not recompute', min_peers=3 floor -> _cf_insufficient, fail-safe _cf_no_opinion + _cf_clamp. cec_ledger.query:211; archive_log:172 corpus; review blocks run after route blocks, never live. No decision-class base rates, no coherence checks, no cross-decider divergence, no inbox \u2014 confirmed by absence (no producers for any of it)."
   },
   {
    "id": "DF-06",
    "claim": "Four structured decision-record producers exist; claim/verification-hook/settlement-state fields are 100% greenfield",
    "verdict": "confirmed",
    "detail": "Producers verified: cec_router.DecisionLog (per-iteration candidates+chosen+verdict+edit), cec_dispatch.agent_route decision_log (tier/budget_left/params/note + R-08 coercion notes), cec_overnight manifest['reviews'] + sidecars, cec_synth_pipeline :2435-2464 (stage log embeds manifest, appends mode=synth-run with CEC_PARENT_RUN_ID lineage). Greps for settlement/vindication/verification-hook/grade mechanisms across scripts/*.py return nothing (only incidental English words 'rewards symmetry' in scorer prose). Schema-attachment observations (extra field, sidecar records) are accurate."
   },
   {
    "id": "DF-07",
    "claim": "Grade-2 substrate dense (gates/golden/lint/cascade); Grade-1 absent; no reward currency, novelty weighting, or policy file",
    "verdict": "confirmed",
    "detail": "Grade-2 substrate verified: cec_score.gate()/Metrics hard gates, cec_golden bands, cec_corpus_lint deterministic checks, cascade stage checks. No bench write-back schema or fab-outcome records anywhere in repo or ledger (ledger modes are route/synth-run/golden/etc.). grep reward/vindicat: zero reward machinery (only docstring words in cec_place:197/cec_score:486-500 describing scoring terms). find *policy* = none, so the banned-rewards assertion has no home. All confirmed."
   },
   {
    "id": "DF-09",
    "claim": "Revision-gate half exists (golden --freeze, corpus lifecycle); panel disagreement computed then discarded; no holdouts, prompt versioning, or routing-decision claims",
    "verdict": "confirmed",
    "detail": "cec_golden --freeze + same-PR band-bump discipline verified; STATUSES lifecycle :39. _vote (:346-374) computes (action, tally, picks) per verdict \u2014 tally/picks survive only as strings folded into the Verdict (no structured persistence, no consumer; verified no other tally storage). Prompts are hardcoded module constants (SYSTEM:128, SCRIBE_SYSTEM:202, lens prompts); grep prompt_hash/prompt_sha = zero \u2014 cross-version pooling is silent exactly as claimed. ensure_manager picks the reviewer with no claim record. Single golden fixture is the only 'holdout'. Confirmed."
   },
   {
    "id": "PC-01",
    "claim": "Custody pattern exists (private cec-runs sidecar, hashes in ledger); richest process records live in gitignored build/; bench-log loss precedent real",
    "verdict": "confirmed",
    "detail": "../cec-runs/runs/ledger.jsonl present; cec_ledger header records the owner decision + hashes-and-pointers design. archive_log:172 targets build/route/corpus and overnight sidecars land under build/overnight \u2014 /build/ is gitignored (.gitignore:31), so the decision-log corpus and reviews are local-only and disk-loss-exposed as claimed. The precedent is real and documented: cec_bench_manager.py header states the raw /tmp/cec-bench logs were LOST to a WSL restart (2026-06-10), only build/bench-gptoss.log survived. Spec Appendix D exists (TOC :104). Gaps stand."
   },
   {
    "id": "PC-02",
    "claim": "k-similar same-family precedent retrieval with mandatory citations is built and wired at decision-adjacent points; family digest covers all peers including failed/gate-false runs; failures reviewed first",
    "verdict": "adjusted",
    "detail": "Status stays PARTIAL \u2014 the core mechanism is real and wired exactly as cited (cf_family_of/cf_family_digest/cf_compute_evidence/cf_budget_exemplars; CORPUS_FIT_SCHEMA requires matched/violated precedents by precedent_id; router :965-975 sidecar 'advisory; never feeds back'; overnight review_block) and the out-of-loop gap is accurate. But TWO evidence sub-claims are wrong and should be corrected: (1) INVERTED: _pending_corpus (:185-197) sorts failed/None routes LAST ('low value' comment; cand.append((failed, -mtime, p)) then sort()) \u2014 failures are reviewed last, not first. (2) OVERSTATED: corpus_fit_review's peer selection EXCLUDES failed logs ('not _cf_is_failed(L)', where _cf_is_failed:886 = no board output or no drc in verdict), so the family digest is NOT over the full peer set \u2014 it does include routed gate-FALSE boards (those aren't 'failed'), and the gate_regression classification + _cf_clamp:1093 are real, but no-candidate runs are filtered out of the digest."
   },
   {
    "id": "PC-03",
    "claim": "Linter enforces no-model-source + Class-C ledger citation; lifecycle germ exists; miner, reproducible-query source type, promotion flow absent",
    "verdict": "confirmed",
    "detail": "lint_general :102-117 rejects sourceless and source.type=='model' entries and requires Class-C measurement run refs ('run:R-...'); wired into CI via checklist.sh:51. corpus/general with the 5-state lifecycle and decision/measurement SOURCE_TYPES verified. SOURCE_TYPES contains no reproducible-query type and no query re-execution machinery exists; no promotion gate (lint only warns on proposed at :132-134); no mining job. All confirmed."
   },
   {
    "id": "PC-04",
    "claim": "Replay preconditions half-exist (manifests everywhere, bench-manager replay-before-bind, golden eval pattern); replay driver, training rung, grade guards greenfield",
    "verdict": "confirmed",
    "detail": "Every decision log + ledger line embeds the determinism manifest (DecisionLog.to_json :147-158, cec_ledger.append:169) and the corpus accumulates (archive_log). cec_bench_manager replays a candidate model over 3 fixed differentiated contexts with expected outcomes and actually gated the lineup adoption (bench-gptoss.log 3/3; promoted driver). cec_golden = frozen-fixture eval with human-approved band bumps. Hardware consistency holds (cec-worker-quality 27B at vram_gb=22 within the 30 GB budget in models.json). No replay-over-settled-history driver, no manifest-binding registry, no fine-tune infra, no grade fields, no policy file \u2014 all verified absent."
   },
   {
    "id": "FR-01",
    "claim": "1.7.0 pin + unverified jar download + manifest FR version + determinism-assuming dedupe exist; zero 2.2.4 work, no hash pin, no determinism probe",
    "verdict": "confirmed",
    "detail": "FR_VERSION='1.7.0' (cec_fr.py:91) + FR_JAR_URL :92-95; ensure_jar :108-150 resolution order exactly as claimed with urlretrieve and ZERO hash verification (no sha256/hashlib in cec_fr). route-prereqs.sh :70-78 hardcodes the 1.7.0 cache paths; route.ps1 carries no jar logic (only a comment mentions Freerouting). cec_ledger._fr_version:115 + manifest :127-134 embed the FR version \u2014 the manifest bullet is already satisfied as claimed. _spread_params:168 / opt_spread :752-768; _candidate_pool :618-635 dedupes on the explicit 'FR is deterministic' assumption. run_freerouting:178 passes -mp/-oit/-mt with the 'no -seed flag in FR 1.7.0' note at :215. grep 2.2.4/2.2.x across scripts/+docs/ = zero. No twice-route probe harness, no A/B pour retest, no FR-output-JSON parsing \u2014 confirmed absent."
   },
   {
    "id": "FR-02",
    "claim": "Keepout-zone baking + deterministic corridor derivation + keepout edit lifecycle + Router stub substrate exist; intent vocabulary/compiler, protect-attribute stubs, per-net sequencing absent",
    "verdict": "confirmed",
    "detail": "bake_hints:727 adds DoNotAllow rule-area zones that export into the DSN; _vital_keepouts_from_rules :562-617 derives connector->shunt corridors clipped at the shunt (tap window open) with allow_vias=True, wired into default_planner :297-298; apply_edit keepout/drop_keepout :228-233; Router :62-225 could materialize stubs; cec_verify_hc.verify:60 geometric-facts precedent. export_dsn:156 is a plain pcbnew ExportSpecctraDSN passthrough \u2014 no fixed/protect attribute handling, the gating bench test has never run, FR 2.2.4 not adopted. No relational-intent compiler, no per-net sequential loop, no facts file, no stub-hygiene pass \u2014 all absent as claimed."
   },
   {
    "id": "FR-03",
    "claim": "Router geometry+DRC substrate and power-copper synthesis precedent exist; the corridor executor, bounded nudging, and named-obstacle attribution are unbuilt",
    "verdict": "confirmed",
    "detail": "Router class :62 with track:98/via:106/run:157/to_pad:170/check_keepouts:184/verify:225 (verify = real kicad-cli DRC); synthesize_power_copper:525 + derive_power_pours:334 + derive_via_field:419 verified as the geometry-not-search precedent. run() lays exactly the given points (no obstacle avoidance \u2014 confirmed in body); verify() returns DRC violations with no blocking-item naming contract; nothing takes (polyline, layer plan, netclass) to offset segments. FR-02's input format does not exist. Confirmed."
   },
   {
    "id": "FR-04",
    "claim": "Two-rung embryo (corridor-forcing over free FR) + DecisionLog substrate + BOARD_PARAMS precedent exist; per-net tier manifests, locked-template tier, claims framework, control arm all absent",
    "verdict": "confirmed",
    "detail": "_vital_keepouts_from_rules:562 + hard gates (Rules.from_board/kelvin_ok/diffpair_ok) + cec_route.mark_vital:141 verified; DecisionLog:110 manifest-stamped; cec_loop.BOARD_PARAMS:40-45 is the only per-board manifest-like structure and carries no per-net content. No tier assignment anywhere; cec_hc RIPS and re-lays rather than locking copper through a re-route (no human-template tier); greps for claim/settle/vindicat machinery = nothing platform-wide; no directed-vs-free A/B or allocator. Confirmed."
   },
   {
    "id": "GR-01",
    "claim": "RUDY congestion grid + relative rejection exist; the grid is discarded (scalars only), and contested-net identification/corridor assignment/fixture are absent",
    "verdict": "confirmed",
    "detail": "rudy:1105-1129 builds the 2mm-bin grid then flattens and returns ONLY (max, mean) \u2014 the grid is discarded exactly as claimed; proxy_reject:1181 relative growth + absolute thermal ceiling; default_planner:289-299 is the hints injection slot (currently filled by _vital_keepouts_from_rules only). No per-cell hotspot localization, no net-bbox-vs-hot-cell intersection, no contested-call protocol, no FR-02 compiler to target, input is synth pads_by_net not a facts file, no two-net fixture. Confirmed."
   },
   {
    "id": "GR-02",
    "claim": "Deterministic targeted_repair (worst-violation -> keepout rip-up or fixed-delta nudge) + repair registry exist; the doc's ordered battery (computed shift, layer swap, via insertion, single-net re-route) is absent",
    "verdict": "confirmed",
    "detail": "targeted_repair:360-409 verified: reads worst real DRC violation, emits worker keepout rip-up (1.2mm square at the locus) or manager place_nudge with a FIXED (0.4, 0.4) delta (:399) \u2014 not clearance+width computed. MANAGER_REPAIRS :539-543 = kelvin_inversion + part_nudge only (logo_finishing_repair:500 written, deliberately not registered). apply_edit vocabulary (:212-270) confirmed to contain NO layer-swap or via-insertion edit types; the keepout mechanism triggers a full region FR re-route, not a single-net re-route; repair is violation-driven, not corridor-obstacle-enumerated; no claim settlement. Confirmed."
   },
   {
    "id": "GR-03",
    "claim": "Budgeted tier ladder + drc_types/loci + render + plug-in slots exist; locus bundle builder, copper-move vocabulary, mobility classes, claim settlement absent",
    "verdict": "confirmed",
    "detail": "cec_dispatch CandidateMetrics with drc_types/drc_loci/unconn_nets (:105-122), JudgeContext:278, agent_route:288 (budget decrement, R-08 forced/coerced escalate recorded), render:245 (whole-board top PNG); cec_router.make_subagent_policy:545; targeted_repair escalates part conflicts to manager tier; cec_judge_local make_manager:257/make_manager_swarm:395/make_dispatch_swarm_tier:533. apply_edit verified to have NO shift_segment/add_via/swap_layer/reroute_net \u2014 copper-level moves don't exist; no mobility classes (FR-04 confirmed); renders are whole-board with no crop machinery; no claims framework. Confirmed."
   },
   {
    "id": "GR-04",
    "claim": "Provenance corpus + family-trajectory classification + durable ledger exist; recurrence counters, staging drafts, settlements, and any feedback loop absent",
    "verdict": "confirmed",
    "detail": "corpus-extracted.json (258 rows) + lint + corpus/general lifecycle verified; cf_family_of:867 and CORPUS_FIT_SCHEMA:779 whose trajectory enum includes repair_then_escalate/thrash and residual_treatment drift classes; cec_ledger durable in ../cec-runs. Nothing aggregates same-pattern repairs across runs (no shadow/aggregator script exists; archive_log only accumulates raw logs); no draft generation; no settlement records anywhere; corpus_fit_review writes advisory sidecars that feed nothing back (router comment explicit). Confirmed."
   },
   {
    "id": "CR-02",
    "claim": "Current rungs exist across routing/placement/scoring/review + escalation machinery; ladders are not enumerated as data and higher rungs are greenfield",
    "verdict": "confirmed",
    "detail": "All cited rungs verified: MANAGER_REPAIRS:539 + kelvin_inversion_repair:436 + make_subagent_policy:545; agent_route:288; cec_loop BOARD_PARAMS:40 + run_loop/run_candidates with cec_place/cec_hc/cec_dcir/cec_verify_hc all present; electrothermal_solve:2140 + physics gates + cec_score.Metrics; make_manager/make_dispatch_swarm_tier:533/corpus_fit_review:1111 + cec_bench_manager. One line correction: the resolve() ladder is at cec_synth_pipeline.py:979 (worker->manager->frontier->human via pluggable tiers), not :931. Corpus lifecycle is proposed->sim_validated->bringup_validated|human_approved->deprecated (README), which differs from draft/shadow/promoted as claimed. No registry rows/rung names/climb gates anywhere (grep G1..G4 etc. = nothing). Status partial correct."
   },
   {
    "id": "BUILD-ORDER",
    "claim": "Punchlist contingencies (R-01/02/05), ledger, golden venue, and corpus substrate are landed; step-1 zones/policy/inbox and steps 4-13 mechanisms are unbuilt; FR still 1.7.0",
    "verdict": "confirmed",
    "detail": "R-01 _spread_params:168 ('R-01: per-seed spread' comment at :189); R-02 Metrics.drc_types/drc_loci :133-134 + 'ONE definition (R-02)' at cec_score:154; R-05 cec_toolchain header explicitly about the R-05 KiCad-less-box case, require/have_kicad_cli both used. cec_loop.py present (PR #18 merged per repo history). cec_ledger (SB-01) writing to ../cec-runs verified live. cec_golden header lines 16-19 record the in-container venue decision; fixture + expectations present. Corpus substrate (README/can/thermal + lint in checklist.sh:51 + 258-row project corpus) verified. Gaps verified: no corpus zones, find *policy* = nothing, SB-02 inbox absent (addendum :76 spec / :400 build-order only), FR_VERSION='1.7.0' with zero 2.2.4 references, and every steps-4-13 mechanism checked in the other items (facts/shadow/attribution/orchestrator/bundle/claims) is absent. Night-shift venue Decision 10 not recorded anywhere."
   }
  ],
  "conflicts": [
   {
    "id": "CL-01",
    "claim": "Doc lifecycle vocabulary (draft/under-review/promoted/deprecated/superseded) conflicts with built SB-13 lifecycle; corpus is 258 rows not 269",
    "verdict": "confirmed",
    "detail": "scripts/cec_corpus_lint.py:39 STATUSES = {proposed, sim_validated, bringup_validated, human_approved, deprecated} \u2014 enforced as errors at line 93-94; 'supersedes' is a pointer field, not a state (lint lines 140-144: 'supersede (supersedes + deprecate the old entry) instead of shadowing'). Seeded corpus/general entries carry these status values (thermal.json: status=human_approved). scripts/constraints/corpus-extracted.json holds exactly 258 rows (json list length), matching the CLAUDE.md 269->258 dedupe record. Implementing the doc's state names verbatim would fail lint on every entry; migration tooling must target 258."
   },
   {
    "id": "CL-03",
    "claim": "Compiling geometry constraints into router-consumed netclasses contradicts the measured FR-ignores-netclass-widths finding; repo's mechanism is post-route copper construction",
    "verdict": "confirmed",
    "detail": "CLAUDE.md:1053 reads verbatim 'MAJOR REVIEW FINDING -- TRACE SIZING IS NOT HONOURED: Freerouting IGNORES the netclass widths' (DSN export correct, FR routes at 0.2mm defaults). The adopted alternative exists at the exact cited anchors: scripts/cec_fr.py:268 def add_power_pours, cec_fr.py:525 def synthesize_power_copper (post-route additive pours + via fields + ZONE_FILLER). A netclass-layer compile target cannot prevent in-route width violations on this toolchain; the dual-target design needs the rework described."
   },
   {
    "id": "CL-06",
    "claim": "Doc's 'Class A = spec-derived' inverts the as-built taxonomy (Class A = external-authority pointer, Class B = spec-derived platform rule)",
    "verdict": "confirmed",
    "detail": "corpus/general/README.md:9 'Class A \u2014 pointer/parameter: a citation to an authoritative source plus the inlined...'; line 13 'Class B \u2014 platform rule: promoted from the project corpus'. thermal.json class-A entries cite source.type='standard', ref='IPC-2221B section 6.2'; can.json class-B entries cite source.type='spec', ref='CEC-Platform-Ground-Truth-Spec.md section 3.1'. Binding spec-resolution to Class A verbatim would reject every legitimate IPC-sourced Class A entry; CL-06 logic belongs on source.type='spec' (Class B). Reconciliation decision needed before any linter change."
   },
   {
    "id": "CL-07",
    "claim": "where='runner' was repurposed to the bounded LOCAL slot-semaphore pool; R-12 GitHub-dispatch leg remains open",
    "verdict": "confirmed",
    "detail": "scripts/cec_dispatch.py: the comment block at the LOCAL-runner section reads 'the LOCAL runner: a bounded compute pool on THIS system (NOT a GitHub Actions runner)' (cross-process flock slot semaphore under build/.runner_slots, CEC_RUNNER_SLOTS), and request_candidates' docstring repeats \"where='runner' -> ... NOT a GitHub Actions runner -- the compute runs here, just centrally rate-limited.\" CLAUDE.md lists R-12 (where='runner' GitHub-dispatch leg) under STILL OPEN. An orchestrator built on this seam gets local rate-limited compute, not remote dispatch."
   },
   {
    "id": "CL-09",
    "claim": "cec_hc.py is the deterministic high-current/Kelvin copper toolkit, not a placement hard-constraint checker; the registry is cec_constraints.py via cec_place._check; the synth placer is design-pass deferred and domain-blind",
    "verdict": "confirmed",
    "detail": "scripts/cec_hc.py function inventory: kelvin_keepouts:100, route_high_current:270, escape_gnd_pins:345, escape_reservations:420 \u2014 a deterministic HC routing/copper pass. The hard-constraint registry lives in scripts/cec_constraints.py (REGISTRY at line 58, checkers + directives()), consumed by scripts/cec_place.py (imports 'cec_constraints as K' line 32; _check at line 457 shells cec_constraints.py --json). CLAUDE.md action item -2 records the synth placer (cec_synth_pipeline place_with_consent, place_candidates' engine) as DESIGN-PASS DEFERRED per owner, explicitly DOMAIN-BLIND (no HC corridor, no sense-IC-adjacent-shunt hard constraint) \u2014 a portfolio built directly on it inherits that wall."
   },
   {
    "id": "CL-11",
    "claim": "No netclass-conformance check exists in cec_constraints.py (only a comment at line 104); via machinery is cec_fr.normalize_via_annular; golden execution venue already decided (on-device routing container)",
    "verdict": "confirmed",
    "detail": "grep 'netclass' in scripts/cec_constraints.py hits exactly one line \u2014 a comment at line 104 ('min-width on the carved high-current netclass... is the follow-up, once a bench measurement'). Check IDs present: trace-width-high-current, kelvin-sense-adjacent-shunt, kelvin-sense-fcu-no-via, kelvin-sense-from-inner-pad \u2014 no netclass-conformance or via-size-vs-netclass ID. cec_fr.py:632 def normalize_via_annular is annular-ring repair, a different mechanism. scripts/cec_golden.py header records 'Owner decision (2026-06-09): run ON-DEVICE in the routing container -- docker compose ... run routing'. Both bullets hold; DECISION 4 is indeed already answered in-tree."
   },
   {
    "id": "CL-14",
    "claim": "Qwen3.5-397B analyst infeasible (not in store, 66GB free on E:, 125GB RAM); M2.7 is an active registered tier not blocked; worker vram_gb=27 not ~22",
    "verdict": "adjusted",
    "detail": "MOSTLY CONFIRMED, one stale figure. Confirmed: /mnt/e/'AI Models' holds ONLY gpt-oss-120b (59G), minimax-m2.7-ud-q3kxl (95G), qwen3.6-27b (16G), qwen3.6-35b-a3b (21G) \u2014 no Qwen3.5-397B; measured RAM 125GB total (free -g) so a ~165-175GB GGUF cannot be RAM-resident (Path C lesson recorded in CLAUDE.md: 141GB Q4_K_XL > RAM -> 3.73 tok/s); M2.7 IS an active first-class tier (models.json 'cec-manager', compose manager-m27 profile, cec_judge_local documented opt-in deep auditor CEC_VLLM_REVIEWER_MODEL=cec-manager with bench-verified miner->scribe, no license gate in code); models.json cec-worker vram_gb=27 (the 22 figure belongs to cec-worker-quality). STALE: E: free space \u2014 df now shows 302-365GB available (3.7T, 91-92% used), NOT 66GB; ~300GB was freed since the claim was written, so the 'cannot even be downloaded' sub-argument no longer holds on space (the absent-from-store and RAM-residency blocks still make the model infeasible)."
   },
   {
    "id": "CL-17",
    "claim": "Effective RAM ceiling is 125GB (WSL2 VM), not the doc's 192GB physical spec; partial GPU expert offload measured slower",
    "verdict": "confirmed",
    "detail": "free -g: Mem total 125GB (the WSL2 VM where the Docker llama.cpp backends run; OS is microsoft-standard-WSL2). docs/local-compute-exploration.md ~line 34 records the 192GB as the physical workstation spec ('RTX 5090 32 GB, 192 GB RAM, Core Ultra 7 265K... Windows 11'). Consistent with the Path C record (102GB M2.7 UD-Q3_K_XL fits page cache at 12.9 tok/s; 141GB Q4_K_XL did not, 3.73 tok/s). The offload finding is recorded in CLAUDE.md manager-tier notes: --n-cpu-moe 80 partial offload measured SLOWER (2.7 tok/s vs 4.3) and +15GB VRAM -> rejected. An analyst residency range '~108 to 175GB' is feasible only at its bottom end under the 125GB ceiling (minus OS/worker overhead). Note: the WSL2 ceiling is a .wslconfig setting, so it is config-current, not physical."
   },
   {
    "id": "CL-20",
    "claim": "M2.7 is shipped first-class and opt-in by env var (not blocked by license policy); the doc's interim Apache 397B analyst is infeasible; available deep-reasoning occupants are gpt-oss-120b or M2.7 itself",
    "verdict": "adjusted",
    "detail": "First conflict FULLY CONFIRMED: models.json registers cec-manager (M2.7, vram 28/ram 96), compose carries manager-m27 profile, cec_judge_local.py documents M2.7 as the opt-in deep auditor with model-specific guards (_FLOOR_MODELS={'cec-manager'}); defaults verified in code \u2014 MODEL default 'cec-worker' (in-loop manager tier deliberately worker-class per the 2026-06-10 bench-verdict comment) and REVIEWER_MODEL default 'cec-manager-fast' (gpt-oss-120b); no license gate exists anywhere; corpus-fit sidecars from M2.7 are not marked non-load-bearing. Second conflict ADJUSTED on one sub-fact: 397B still absent from /mnt/e/'AI Models' and non-RAM-resident at 125GB, BUT E: now has 302-365GB free (not 66GB), so download space is no longer the blocker. Conclusion stands: the practically available deep-reasoning occupants are gpt-oss-120b (Apache, default reviewer) or M2.7 itself."
   },
   {
    "id": "CL-22",
    "claim": "No vision-capable binding exists in the broker registry; all five backends are text-only; the local-analyst seat cannot be filled from the current lineup",
    "verdict": "confirmed",
    "detail": "/home/nathan/cec-llm-broker/models.json registers exactly five backends \u2014 cec-worker (Qwen3.6-35B-A3B llama.cpp), cec-worker-quality (Qwen3.6-27B llama.cpp), cec-manager (MiniMax-M2.7 llama.cpp), cec-manager-fast (gpt-oss-120b llama.cpp, text-only), cec-judge (Qwen3-Coder-30B AWQ vLLM) \u2014 all bound as text chat servers; grep finds no mmproj/vision configuration in docker/compose.yaml or the registry. The doc's 'verify modality at binding' check fails today; multimodal review is frontier-seat-only until a VLM is added."
   },
   {
    "id": "CL-24",
    "claim": "Registry records worker vram_gb=27 and 27B extractor vram_gb=22 under gpu_budget_gb=30 (doc's ~22/~17 vs 32 drifted); broker already enforces single-occupancy so phase-sequential is existing default behavior",
    "verdict": "confirmed",
    "detail": "models.json: cec-worker vram_gb=27, cec-worker-quality vram_gb=22, top-level gpu_budget_gb=30 \u2014 27+22=49 > 30, so no co-residency. The broker's vram arbitration + swap (registry _comment: 'swapping out whatever else holds the GPU'; CLAUDE.md broker-v2: 'vram_gb arbitration vs gpu_budget_gb on the single 5090 -- it SWAPS the conflicting model out') makes phase-sequential aggregation/extraction the existing enforced default, not a CL-24 design choice; 'measure on first load' is pre-answered by the registry estimates."
   },
   {
    "id": "CL-25",
    "claim": "cec_hc.py is the deterministic high-current ROUTING pass, not a check pack; correct extension points are cec_constraints.py and the cec_synth_pipeline stage lists",
    "verdict": "confirmed",
    "detail": "scripts/cec_hc.py exports route_high_current (line 270), kelvin tap/keepout helpers (kelvin_keepouts:100, _kelvin_stubs:38), escape_gnd_pins:345, escape_reservations:420, place_vias:476 \u2014 routing/copper construction, no check registry. The check-pack homes exist as claimed: scripts/cec_constraints.py (REGISTRY line 58 + checkers + directives()) and cec_synth_pipeline's six marker-criteria stage lists (ERC_BOM/PLACEMENT/ROUTE_GATE/MEASURE/DFM/CONFORMANCE per CLAUDE.md). Note PR #18 did carry cec_hc.py into the tree \u2014 the error is characterizing it as a check pack, exactly as the conflict states."
   },
   {
    "id": "CL-26",
    "claim": "The minimal viable night is built and has run (broker-native cec_overnight.py; real 6.5h night completed 2026-06-09 with 63 routes); the pinned-1.7.0 baseline branch is de facto taken",
    "verdict": "confirmed",
    "detail": "scripts/cec_overnight.py is broker-native on this branch (BROKER_URL :8080 default, _broker_models() catalog liveness, broker_warm() boot-by-request with measured 90.7s cold-swap margin noted, route blocks in the routing container). CLAUDE.md records the completed overnight: 'the overnight run (which finished its 6.5h budget despite the Claude-RC disconnect: 63 routes, all EPS at the cosmetic floor, BOTH PCIe SKUs kelvin_ok=FALSE)'. cec_fr.py:91 FR_VERSION='1.7.0' pinned, with determinism noted in cec_golden.py and cec_dispatch (R-01). 'Weeks of build before any signal' is wrong; signal already exists."
   },
   {
    "id": "AM-07",
    "claim": "'The rig is Windows' is stale for the local night path: the overnight pipeline runs on the WSL2/Linux side through the routing container; the Windows self-hosted runner is the separate route.yml leg",
    "verdict": "confirmed",
    "detail": "This box is WSL2 (kernel 6.18.33.1-microsoft-standard-WSL2); cec_overnight.py's header documents broker-mediated lifecycle with routing in the 'routing' container (host.docker.internal:8080 from inside) and host-side reviews \u2014 all Linux-side; MEMORY.md/toolchain notes confirm the cec/routing:kicad10 container is the compute plane (no host kicad-cli/pcbnew/java). The Windows runner (CEC-Workstation, route.ps1/route.yml) is the dispatched-route leg per CLAUDE.md, not the overnight driver. The physical machine runs Windows 11 hosting the WSL2 VM, but the local night path is Linux-side."
   },
   {
    "id": "DF-02",
    "claim": "The dispatch judge view intentionally exposes generator configs to the judging tier (R-01 resolved per-seed params recorded), opposite of a verdict-view default",
    "verdict": "confirmed",
    "detail": "scripts/cec_dispatch.py _spread_params docstring: 'The RESOLVED per-seed params are what gets recorded in CandidateMetrics.params, so the judge can see the spread'; CandidateMetrics carries params as a first-class field, and the dedupe path tags '_dup_of_seed' explicitly 'so the judge sees it is not independent evidence'. Hiding generator configs from the judge would break the R-01 design; DF-02 cannot be applied to the dispatch loop as written (params are load-bearing there)."
   },
   {
    "id": "DF-10",
    "claim": "Byte-identical reproduction is contradicted by design: floor-model sampling (temp>=0.3, presence 0.8 for cec-manager), swarm replicas at temp 0.5, cycled panel lens temps, no seed passed",
    "verdict": "confirmed",
    "detail": "scripts/cec_judge_local.py:105-108 _FLOOR_MODELS default {'cec-manager'}, _FLOOR_TEMP=0.3, _FLOOR_PRESENCE=0.8, applied in _chat_json (lines 188-190: temperature=max(temperature,_FLOOR_TEMP), presence_penalty set) \u2014 bench-verified guard (header: M2.7 degenerates at temp<=0.1-0.2). Swarm replicas: line 486 temperature=(0.0 if i==0 else 0.5); _panel cycles per-lens temps (lines 386/397). The chat payload contains model/messages/temperature/max_tokens/response_format only \u2014 NO seed parameter. So the deep tier is stochastic by design; byte-identical canaries work only for temp-0 seats, and floor-model tiers need seeded-reproduction or distributional bounds, as the conflict states."
   },
   {
    "id": "FR-02",
    "claim": "No DSN parser exists in the repo; DSN/SES are opaque pass-throughs between pcbnew and the FR jar; injecting a protect attribute needs new DSN-touching code",
    "verdict": "confirmed",
    "detail": "cec_fr.py round-trips via pcbnew ExportSpecctraDSN -> java -jar freerouting -> ImportSpecctraSES; no code reads or rewrites DSN content (grep 'protect' in cec_fr.py: zero hits; bake_hints adds keepout ZONES to the .kicad_pcb pre-export, not DSN edits). Minor nuance on the aside: cec_synth_pipeline's .net parser is not strictly the ONLY s-expr handling (cec_pcb/cec_sch/cec_score manipulate .kicad_pcb/.kicad_sch s-exprs), but none of them touch DSN \u2014 the load-bearing conclusion stands: the one-s-expression-edit fallback requires new DSN-parsing/editing code."
   },
   {
    "id": "BUILD-ORDER",
    "claim": "PR #18 is MERGED to main; cec_loop/cec_hc/cec_place/cec_dcir/cec_verify_hc are current-tree code to build ON",
    "verdict": "confirmed",
    "detail": "gh pr view 18: state=MERGED, mergedAt=2026-06-09T19:41:53Z ('Constraint-aware design loop: registry + checkers + directives -> place/route/check self-correction'; merge commit 35912ec). All five files present in the working tree on branch claude/manager-tier-wiring: scripts/cec_loop.py, cec_hc.py, cec_place.py, cec_dcir.py, cec_verify_hc.py. cec_golden.py header also states '(PR #17 merged, #18 merged)'. The doc's 'read PR #18's branch first' framing is drift, not contradiction \u2014 step-3 work builds on merged code."
   },
   {
    "id": "DECISION-5",
    "claim": "The 'stay on the Apache analyst indefinitely' fallback (Qwen3-235B) no longer exists as-built \u2014 235B retired and deleted 2026-06-09; available Apache analyst is gpt-oss-120b (cec-manager-fast, already default reviewer); M2.7 is wired, benched, opt-in today",
    "verdict": "confirmed",
    "detail": "models.json _comment: 'cec-manager-235b RETIRED 2026-06-09 (files deleted from the C:-backed VHDX; was a remnant)'; docker/compose.yaml comment at lines 99-100: the old 235B :8001 manager service 'was REMOVED 2026-06-10' (commit d38fccc on this branch); root VHDX ext4 at 130G used matches the recorded 234->130GB shrink. cec_judge_local.py REVIEWER_MODEL default = 'cec-manager-fast' (gpt-oss-120b, Apache-2.0); M2.7 is registered, composed, bench-characterized (miner->scribe, floor sampling) and opt-in via CEC_VLLM_REVIEWER_MODEL=cec-manager. The doc's license-decision-pending premise is overtaken."
   },
   {
    "id": "FACT-A",
    "claim": "Box RAM total is 125GB",
    "verdict": "confirmed",
    "detail": "free -g: Mem total 125 GB, swap 0 \u2014 this is the WSL2 VM ceiling where the Docker llama.cpp backends run (configurable via .wslconfig; physical workstation spec is 192GB per docs/local-compute-exploration.md). Use 125GB for all model-residency planning."
   },
   {
    "id": "FACT-B",
    "claim": "E: free space is ~66GB (figure embedded in CL-14/CL-20)",
    "verdict": "refuted",
    "detail": "df -h /mnt/e measured twice during this pass: 3.7T size, 3.3-3.4T used, 302G then 365G available (91-92% use; actively fluctuating). The 66GB figure is stale \u2014 roughly 300GB has been freed on E: since the claims were written. (Do not confuse with C:/mnt/c, which IS at ~68G free / 93%.) A ~165-175GB GGUF download is now space-feasible on E:; the binding constraints against an analyst-class model are model absence and the 125GB RAM ceiling, not disk."
   },
   {
    "id": "FACT-C",
    "claim": "Model files present under /mnt/e/AI Models",
    "verdict": "confirmed",
    "detail": "Exactly four model directories: gpt-oss-120b (59G, 3-part gpt-oss-120b-mxfp4 GGUF), minimax-m2.7-ud-q3kxl (95G, 4-part MiniMax-M2.7-UD-Q3_K_XL GGUF), qwen3.6-27b (16G, Qwen3.6-27B-Q4_K_M.gguf), qwen3.6-35b-a3b (21G, Qwen3.6-35B-A3B-UD-Q4_K_M.gguf), plus download scripts/logs. Matches the broker registry's five backends (cec-judge's vLLM 30B AWQ lives elsewhere \u2014 not under this GGUF store)."
   },
   {
    "id": "FACT-D",
    "claim": "Freerouting pin version is 1.7.0",
    "verdict": "confirmed",
    "detail": "scripts/cec_fr.py:91 FR_VERSION = \"1.7.0\"; jar URL, cache paths, and the no-seed-flag note all key off it; cec_golden.py and cec_dispatch R-01 both rely on FR 1.7.0's determinism."
   },
   {
    "id": "FACT-E",
    "claim": "No Qwen3.5-397B or other analyst-class model exists anywhere on disk",
    "verdict": "confirmed",
    "detail": "find /mnt/e for *397*/*qwen3.5* matched only Steam appmanifest temp files and unrelated jpgs \u2014 no model artifacts; the only GGUF stores on E: are the four dirs in FACT-C; find on the root filesystem (/dev/sdc, 130G used \u2014 consistent with the 235B deletion from the C:-backed VHDX) found no .gguf files; no ~/models or /opt/models. The largest models on the system are MiniMax-M2.7 UD-Q3_K_XL (95G) and gpt-oss-120b (59G). Any 397B-class analyst would have to be downloaded fresh (space now exists on E:, but 125GB RAM forbids residency)."
   }
  ]
 }
}