{"verdict":"repair","reasoning":"Both Kelvin cable pairs (SENSEC1 and SENSEC2) are fully unrouted — all four members (HI and LO for each cable) appear in failing_reasons, giving M_actual=2 with both members failing symmetrically. kelvin_unrouted=1 may be an under-count (2 cables are failing, not 1), but even at face value the Kelvin hard gate is not met. diffpair_ok=true is the only bright spot. The compiler generated 7 stubs, all targeting non-Kelvin, non-failing nets (/CAN_H, /I2C_SCL, /I2C_SDA) — nets with zero overlap with the nets currently listed in failing_reasons. All 7 stubs were then eliminated by the absorber (absorbed=3 + trimmed_spurs=4 = 7 = n_stubs), so FR received zero guidance. This is a compound failure: (1) compiler_kelvin_omission per Rule 33; (2) total-stub-absorption per Rule 34; (3) M_actual=2 triggering Rule 35 kelvin_congestion_diagnostic. Rules 33–35 should be executing, but the core upstream problem — not addressed by any standing rule — is that the compiler's candidate pool ENTIRELY excludes nets currently appearing in failing_reasons. The three nets the compiler DID target (/CAN_H, /I2C_SCL, /I2C_SDA) are non-failing signal nets that the absorber correctly identifies as redundant, producing the null-output scenario every round. Rule 37 (per_net_absorption_ceiling) would eventually exclude these nets after 5 absorbed rounds each, but the compiler may be resetting their absorption counters or they may simply not yet have hit the threshold. The proposed manager rule closes the upstream gap: when the compiler's nets list has zero overlap with failing_reasons, zero-out those priorities immediately and re-seed from failing_reasons rather than waiting for Rule 37's slow exclusion path. No scorer penalty proposed because the existing weights (kelvin_unrouted×1000, gate_fail×1000, unconnected×450) correctly dominate the objective for this failure mode; the problem is guidance quality to FR, not objective signal strength.","is_new_finding":true,"scorer_penalty":null,"manager_rule":"When the compiler's nets list for round R contains NO net that also appears in failing_reasons with 'has unconnected ratlines' (i.e., the compiler targets exclusively nets not currently failing — zero failing-net overlap), classify the event as 'compiler_zero_failing_net_overlap' and execute the following BEFORE the absorber and before Rule 33 force-injection: (1) set the compiler's internal priority score for every net in the compiler's nets list to 0 for this round; (2) re-run the compiler's candidate-selection pass using a restricted candidate pool consisting only of nets drawn from failing_reasons (both Kelvin failing nets and any non-Kelvin unconnected nets identified via Rule 27's query, if available); (3) skip the absorber's merge, trim-spurs, and orphan-removal stages for ALL stubs produced by this restricted re-run — these stubs target genuinely unrouted nets and absorption would discard valid guidance; (4) log 'compiler_zero_failing_net_overlap' with fields: (a) round number, (b) original compiler nets list, (c) the failing_reasons net set used as the replacement candidate pool, (d) the stub set produced by the re-run and their priority scores. If after the restricted re-run the Kelvin failing nets are STILL absent from the compiler's output, apply Rule 33 force-injection additively. The zero-out of original priority scores persists only for the current round R; the compiler's underlying priority model is not modified, but the nets whose scores were zeroed must have their absorption counter incremented by 1 in net_absorption_count (Rule 37) regardless of whether their stubs were generated this round, since the zero-overlap event is evidence of the same persistent-targeting pattern Rule 37 tracks. This rule fires before Rule 33 and is additive to Rules 33, 34, 35, 36, and 37; it does not replace or defer any action those rules independently mandate."}
