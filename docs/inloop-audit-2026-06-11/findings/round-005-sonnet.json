{
  "verdict": "repair",
  "reasoning": "Hard gate failure: kelvin_ok=false, gates_pass=false. /SENSEC1_HI and /SENSEC1_LO both have unconnected ratlines (kelvin_unrouted=1, the pair is fully stranded). /SENSEC2_HI also appears in failing_reasons with unconnected ratlines — a partially-stranded second pair that fails the Kelvin gate even though only one leg is disconnected; note that kelvin_unrouted=1 undercounts this because it only tallies fully-stranded pairs, but the gate_fail penalty still fires. The critical new observation this round is the stub dispatch: stub nets used were [/CAN_H, /I2C_SCL, /I2C_SDA] — entirely opportunistic, non-Kelvin nets — while /SENSEC1_HI, /SENSEC1_LO, and /SENSEC2_HI (all present in failing_reasons) are absent from the compiled stub nets list. This is a direct violation of Rules 1–4: the pre-dispatch set-membership check (Rule 3) should have caught the missing Kelvin nets and aborted the dispatch; the post-round audit (Rule 4) should have injected them as mandatory compile_failures for this round. Neither fired. The absorbed:5 / trimmed_spurs:2 activity confirms the compiler was active and running a full stub compilation pass — it simply produced opportunistic stubs and dispatched without the Kelvin nets. The existing penalty weights (kelvin_unrouted:1000, gate_fail:1000, unconnected:100) are already severe and correctly weighted; no new metric penalty is needed. The gap is a stub-compiler PRIORITY ORDER failure: the compiler is filling its output with CAN/I2C stubs before (or instead of) mandatory Kelvin stubs. Rules 1–4 are guards and abort conditions; they do not specify the compiler's internal ordering, which is why the guard can be structurally bypassed when the compiler emits a non-empty stub list that happens to contain zero Kelvin nets. A new manager rule targeting priority ordering in the compiler itself is the genuine new addition.",
  "is_new_finding": true,
  "scorer_penalty": null,
  "manager_rule": "Before finalizing the compiled stub nets list for any FR dispatch, apply a mandatory priority-order filter inside the stub compiler: (1) collect all Kelvin nets (/SENSEC*_HI and /SENSEC*_LO) that appeared in the previous round's failing_reasons and insert them at the HEAD of the stub nets list with their shunt inner-edge pad coordinates; (2) only after all required Kelvin stubs are confirmed present, append opportunistic non-Kelvin stubs such as /CAN_H or /I2C_*; (3) if a stub-count budget or slot limit would force any entry to be dropped, drop opportunistic stubs first — a Kelvin stub must never be omitted to make room for a non-Kelvin stub. A compiled stub list that is non-empty but contains zero Kelvin nets when Kelvin nets appear in the previous round's failing_reasons is itself a compile_failure and must trigger the Rule 3 abort, regardless of what the list does contain."
}
