RATIFIED DESIGN-RULE CORPUS (routing/layer/plane-relevant subset, 63 rules):
* eps-minifit-87427-connectors: The EPS 8-pin module interposer connectors (J_IN1, J_OUT1, J_IN2, J_OUT2) must use the Molex 87427-0802 right-angle 2x4 header (footprint cec-Connector_Molex:Molex_Mini-Fit_Jr_87427-0802_2x04_P4.20mm_RA). Pin assignment: pads 1-4 = GND, pads 5-8 = +12V (SENSE*_HI/LO nets). The FLIPPED assignment (1-4=+12V, 5-8=GND) is a violation.
* usb-dp-dn-diff-pair: The USB full-speed differential pair (net names /USB_D_P and /USB_D_N, or /USB_DP and /USB_DM) must be routed as a matched differential pair: equal length (length match within 0.5mm), parallel routing, 0.25mm trace width, 0.13mm gap, no vias if avoidable.
* shunt-kelvin-4wire: Every shunt resistor on all modules must be a four-terminal Kelvin-sense type (force terminals carry current; sense taps are separate). The schematic uses a 2-pad R_2512 footprint (per CLAUDE.md: 'honest 2-pad R_2512 (Kelvin taps drawn in copper at layout, §6.8) — NOT the 4-terminal WSK2512'). The Kelvin sense tap geometry is a layout task.
* can-pair3-twisted: CAN_H must be on pin 3 (T568B white-green) and CAN_L on pin 6 (T568B green). These are pair 3 — the T568B split pair — and they stay twisted in any Cat5e cable, providing the intended differential CAN protection. Do not reassign CAN to any other pin pair.
* esp32-wroom-antenna-keepout: A 21mm x 48mm keepout zone (all 4 layers: no tracks, vias, pads, copper pours, or footprints) must be present over the ESP32-S3-WROOM-1-N16R8 PCB-trace antenna lobe on the Hub Standard board. The zone corresponds to the region extending past the module edge opposite the pads, as defined by the WROOM datasheet keepout polygon.
* usb-diffpair-hard-gate: USB_D_P and USB_D_N (the module debug USB pair, and the Hub Standard native USB pair) must both be fully routed with zero unconnected ratlines. This is a HARD GATE: any candidate where either net has an unrouted stub fails outright. The pair must use the _P/_N naming convention for KiCad auto-recognition.
* usb-diffpair-impedance-window: The USB diff pair gap must be 0.10–0.20mm (EPS/PCIe/modules DRU) or 0.12–0.20mm (Hub Standard DRU). Track width must be 0.15–0.25mm on the Hub Standard board. The pair must be routed together over a continuous GND reference plane with no splits or voids directly beneath the pair.
* can-coupled-routing: CAN_H and CAN_L must be routed as a tightly coupled differential pair on the same layer over a continuous GND reference plane. Because they use asymmetric industry-standard names (not _P/_N), KiCad's auto-diff-pair router does NOT recognize them automatically — they must be routed manually as a coupled pair. Maximum uncoupled stub length: keep stubs to the transceiver (U2 TJA1051T/3) as short as possible, ideally und
* can-star-stub-equalization: On the Hub, each RJ-45 port's CAN_H/CAN_L stub from the split termination network to the port jack should be kept as equal in length as practical. Maximum stub imbalance advisory: <10mm difference between longest and shortest port stub.
* gnd-plane-continuity: CAN_H/CAN_L and USB_D+/D− must route over a continuous solid GND pour on In1.Cu (L2). Anti-pad voids from high-current via arrays in the GND plane must not interrupt the return path directly beneath any of these signal nets. Acceptable: anti-pads displaced laterally from the signal trace with at least one solid GND copper width between the void edge and the trace.
* power-trace-width-floor: Any track segment assigned to the Power netclass (nets +3V3, +5VSB, +5V_HOLD, USB_VBUS) must be at least 0.5mm wide. The 5VSB trunk (J1 power-in → mux → distribution star) must be poured or routed ≥1.5mm at 1oz. This is a DRU-enforced floor, not the primary sizing mechanism — trunk sizing is verified by IPC-2152 copper area.
* kelvin-sense-hard-gate: Every Kelvin sense pair (*_HI and *_LO) must be fully routed with zero unconnected ratlines on both members. This is a HARD GATE: any candidate where either *_HI or *_LO has an unrouted ratline fails outright, regardless of DRC count.
* kelvin-sense-layer-and-length: Kelvin sense taps (*_HI/*_LO from the shunt inner sense pads to the INA input pins) must be routed on L1 (F.Cu) only, must be ≤3mm total length per tap, and must not use any vias. The taps must connect to the shunt's INNER sense terminals (not the force terminals).
* ina240-rc-filter-symmetry: On the 12VHPWR Standard module, each INA240 input pair must have matched 10Ω series resistors (RFH_n and RFL_n, 0.1%/50ppm, Yageo RT0402BRE07-10RL or equivalent) on both IN+ and IN− inputs, plus a 470nF differential capacitor (CF_n) across the filtered inputs. fc = 1/(2π × 2×Rf × Cdiff) ≈ 16.9kHz. Both Rf resistors in a pair must use the same value (matched within 0.1%) for CMRR preservation.
* m3-mount-gnd-chassis-bond: All M3 mounting holes (MountingHole_3.2mm_M3_Pad_Via footprint) must be connected to the GND net via their copper pad. No M3 mounting hole may be isolated (no-connect or unconnected pad). The GND copper from the mounting pad must form a direct low-impedance path to the In1.Cu GND pour.
* usb-hs-hub-pro-impedance: When Hub Pro is fully specified, USB_D_P/USB_D_N must be routed to 90Ω ±5% differential impedance for USB High Speed (480Mbps). Track width and gap must be calculated from the actual Hub Pro PCB stackup using a field solver or the JLCPCB impedance calculator. Skew between D+ and D− must be ≤5ps (inter-pair delay matching).
* logo-bcu-keepout-or-gnd: Every board that carries the cec:CEC_Logo_Copper footprint (ref LOGO1, layer B.Cu) must either: (a) have a rule-area via/track keepout covering the logo bounding box so the router cannot place tracks or vias through it, OR (b) have the footprint assigned to the GND net so any copper contact is a same-net touch (not a short). Neither condition causes a DRC short. Violating both (logo with no net + no keepout + a B.Cu 
* logo-not-in-high-current-corridor: The CEC_Logo_Copper footprint must not be placed in the high-current copper corridor defined by the bounding box between the PSU-side connector (J_IN) and the load-side connector (J_OUT), covering the shunt row. This corridor is reserved for 12V power pours and thermal copper; bare copper from the logo in this region would create ambiguous copper islands that the ZONE_FILLER might merge with power pours.
* kelvin-sense-adjacent-shunt: Every primary sense IC (INA228/INA238/INA240) must be placed with its sense-input pads (IN+/IN− or HI/LO) within 3 mm of the corresponding shunt resistor's inner Kelvin-sense edge. The IC may not be separated from its shunt by another component, a pour boundary crossing, or a via field.
* kelvin-sense-on-top-layer: All traces on the *_HI/*_LO Kelvin sense nets (and IN*_P/IN*_N on 12VHPWR) must be routed entirely on the top copper layer (F.Cu / Layer 0). No via may appear on these nets between the shunt inner-edge pad and the IC differential input pin. Routing to an inner layer and back is prohibited.
* kelvin-pair-length-matched: Within each Kelvin pair (*_HI/*_LO or IN*_P/IN*_N), the routed trace lengths from the shunt inner-edge pads to the IC differential input pads must match within 0.5 mm. Asymmetric lengths create a skew-resistance imbalance in the sense loop.
* shunt-kelvin-footprint-4terminal: Every shunt resistor must use a footprint that exposes distinct force pads (F+, F−) and sense pads (S+, S−) as separate copper lands. The footprint must NOT merge sense into the force pad (i.e. a plain 2-pad R_2512 is acceptable only when Kelvin taps are drawn in copper on the outer pad edges; a WSK2512 4-terminal is preferred but the 2-pad R_2512 with Kelvin annotation is the generator default). Either way, the BOM 
* high-current-corridor-keepout: On every cable-interposer module (EPS, PCIe), the rectangular corridor from J_IN power pins through each shunt RS* to the corresponding J_OUT power pins must be reserved as a routing keepout for all copper except the high-current 12V and GND pours that belong to that corridor. No signal trace, via, or foreign net copper may enter the corridor. The keepout region per cable is defined by the bounding box of J_IN power 
* high-current-copper-area-not-traces: The +12V and GND high-current nets on every module board must be realized as filled copper zone pours (ZONE_FILLER). Fat single traces are prohibited for these nets on the outer layers. Minimum pour area per cable: ≥ 100 mm² on each outer layer (F.Cu and B.Cu) in the corridor, achieving ≥ 200 mm² total cross-section per cable at 2oz copper.
* high-current-stackup-2oz-outer: The PCB stackup for all cable-interposer modules (EPS, PCIe, 12VHPWR) must specify 2 oz (70 µm) copper on both outer layers (F.Cu, B.Cu) and 1 oz (35 µm) on both inner layers. The .kicad_pcb stackup section must reflect this. A 1oz outer layer fails the IPC-2221 temperature gate at full cable current.
* via-field-sized-for-current: Each shunt terminal pad and each high-current connector pin (J_IN/J_OUT power pins) must have a via field of at least 5 vias (drill ≥ 0.5 mm, pad ≥ 0.9 mm) connecting F.Cu to B.Cu, uniformly spaced within the pad or the surrounding copper pour. For the EPS/PCIe ~40–55 A class, a field of ≥ 6 vias per shunt terminal is required to avoid the via-over-temp physics gate.
* shunt-chassis-thermal-coupling-eps-pcie: On EPS and PCIe modules, each shunt RS* must have a clear B.Cu area (no solder mask, no silk, no via drill-out) on the underside beneath the shunt body, sized for a thermal interface material (TIM) pad to couple heat to the chassis. The B.Cu area must be at least as large as the shunt body (≥ 6.3 mm × 3.2 mm for R_2512) and must be on GND or the pour net (no isolated copper island).
* ina181-own-kelvin-pair-independent-of-ina238: Per §6.13: the INA181A2 taps the same shunt as the INA238 but with its OWN dedicated Kelvin sense pair. The INA238's _HI/_LO sense traces and the INA181's input traces must be electrically independent copper paths from the shunt inner edges — they may share the same net node (the shunt force net) only at the shunt pad itself, not via a shared trace. One INA181 per cable; muxing across cables is prohibited.
* ina181-adjacent-to-shunt: Each INA181A2 (refs U20*, U21*, U22* on EPS/PCIe) must be placed with its input pads (IN+/IN−) within 3 mm of the corresponding shunt RS* inner Kelvin-sense edge, on the same side of the board (F.Cu side). Same proximity rule as the primary INA238.
* sense-ic-thermal-separation-from-pour: No INA228/INA238/INA240/INA181 body or pad may be located inside the high-current copper pour zone (the J_IN→shunt→J_OUT corridor). The IC's courtyard must be separated from the pour boundary by ≥ 3 mm. This prevents the IC die temperature from being elevated by pour-conducted heat, which would corrupt the sense measurement via amplifier offset drift.
* ina240-antialiasing-rc-filter-values: Each INA240 differential input channel must have a matched pair of series resistors RFH*/RFL* (10 Ω, 0.1%, 50 ppm, 0402) and a differential capacitor CF* (470 nF) forming a first-order filter with fc = 1/(2π × 2×10 × 470n) ≈ 16.9 kHz. The 10 Ω value must not exceed TI's INA240 input ceiling for CMRR/gain error.
* high-current-via-stitch-spacing: Along each high-current lane (J_IN→shunt→J_OUT corridor), via stitching connecting F.Cu to B.Cu pours must be placed at ≤ 5 mm pitch. This ensures the combined F+B cross-section carries the full current without relying on a single long trace on one layer.
* high-current-pour-after-route-ordering: In any automated routing flow using Freerouting (cec_fr), the high-current copper zone pours (add_power_pours / derive_power_pours) must be applied to the board AFTER the Freerouting SES import. Adding pours before running Freerouting is prohibited: doing so reshapes Freerouting's global routing solution and breaks the Kelvin gate by stranding sense traces on inner layers inside the pour boundary.
* ina240-soic8-d-package-mandatory: The INA240 current-sense amplifier must be ordered and placed as INA240A3DR (SOIC-8, D package, LCSC C2060584). The TSSOP/PW variant (INA240A3PW) has a completely different pin assignment and must not be used — it would connect V+/IN−/IN+/GND to wrong nets.
* shunt-count-matches-sense-ic-bijection: The number of shunt resistors RS* must equal the number of primary sense ICs (INA228/238/240) on each module board. Each sense IC must be in a one-to-one Kelvin-sense relationship with exactly one shunt. No IC may share a shunt with another IC (except INA181 which has its own sense path on the same shunt as the INA238).
* high-current-path-inline-no-bypass: In the J_IN → shunt → J_OUT path, there must be no alternate low-resistance copper connection that allows current to flow from J_IN to J_OUT without passing through the shunt element. This includes: no direct via connecting the J_IN HI net to the J_OUT HI net outside the shunt body; no GND pour copper bridging the HI and LO shunt pads on the same side of the board.
* oq10-vertical-transition-open: The vertical transition strategy for the ~40–55 A EPS/PCIe per-cable shunt sites (copper coin vs. filled-via field vs. plated slot) must NOT be assumed as via field until OQ-10 is resolved. Any automated placer or router must treat this as an advisory flag rather than assume a via-based solution. If a via-field approach is used (current default), the via count and sizing per via-field-sized-for-current constraint sti
* m3-mounts-on-gnd-net: All M3 mounting holes (MountingHole_3.2mm_M3_Pad_Via refs) on every module board must have their copper pad connected to the GND net and include at least 2 stitching vias to the inner GND plane. Isolated mounting holes floating at an undefined potential create ESD ingress paths from the metal chassis.
* sense-ic-adjacent-shunt-kelvin: Each INA238, INA240, or INA181 sense IC must be placed such that its IN+ and IN- pads (the Kelvin sense inputs) are within 3 mm of the inner sense-tap edge of its associated shunt resistor. No other components may be interposed between the shunt inner edge and the INA sense pins.
* kelvin-sense-top-layer: The *_HI and *_LO Kelvin sense net traces from shunt inner pads to INA sense inputs must be routed entirely on F.Cu (the top copper layer), with zero vias between shunt pad and INA input pin. They must run over the In1 GND plane for controlled impedance return.
* kelvin-tap-inner-shunt-edge: The *_HI Kelvin tap must connect to pad 1 of its shunt (the pad facing the sense IC / inner edge at rot270), and *_LO must connect to pad 2 (the lower inner edge). The trace must leave from the inner-facing edge of the pad, not the outer force-current edge.
* kelvin-taps-matched-length-sense: For each Kelvin pair (*_HI / *_LO), the total routed copper length from shunt pad to INA sense pin must be matched within 0.5 mm between the two members of the pair.
* rc-antialiasing-filter-at-ina-pins: For each INA240 on the 12VHPWR Standard board, the matched 10-ohm series filter resistors (RFH_n, RFL_n) and 470 nF differential cap (CF_n) must be placed within 2 mm of the INA240 IN+/IN- pads. RFH and RFL must be equidistant from the INA to maintain CMRR.
* ref3030-not-ref3033: The BOM value for U4 on the 12VHPWR Standard board must be REF3030 (VOUT = 3.0V, LCSC C38423). REF3033 (3.3V) is strictly forbidden: it would over-range the ESP32 ADC (absolute max ~3.6V, but 3.3V = rail = no headroom). The Standard module's reference feeds the ADC; the Pro module's REF3033 feeds the LTC2358-18 reference input — entirely different use.
* esp32-wroom-antenna-keepout-hub: The ESP32-S3-WROOM-1-N16R8 PCB antenna keepout area (as defined in the ESP32 hardware design guidelines, approximately 2 mm extension from the module edge on the antenna side) must have no copper fills, pours, or traces on any layer. The keepout must be maintained on F.Cu, B.Cu, In1.Cu, and In2.Cu.
* can-routing-paired-coupled-trace: CAN_H and CAN_L traces must be routed as a coupled pair (CAN netclass, 0.25 mm width, consistent spacing). They must run in parallel for their entire length, with spacing <= 0.3 mm between the two. Length mismatch between CAN_H and CAN_L traces must be <= 2 mm.
* usb-diff-pair-matched-side-swap: USB_D_P and USB_D_N must be routed as a differential pair (USB netclass, 0.25 mm width, 0.13 mm gap). Length mismatch must be <= 0.5 mm. Exactly one via is permitted per trace for the D+/D- side-swap (U1 to J5 layer crossover), not more.
* thermal-separation-shunts-from-logic: No precision voltage/current sense IC (INA228, INA238, INA240, INA181, REF3030, LP5907) should have its centroid within 5 mm of any shunt resistor centroid, except for the INA directly paired with that shunt (Kelvin adjacent pair). The paired INA-to-shunt proximity is required (see sense-ic-adjacent-shunt-kelvin constraint); other precision ICs must be separated.
* shunt-rotation-hi-upper-terminal: All shunt resistors on EPS/PCIe boards must be placed at rotation 270 degrees. At rot 270: pad 1 is the upper terminal (connected to _HI net, facing J_IN / PSU side), pad 2 is the lower terminal (connected to _LO net, facing J_OUT / load side). A rot 90 orientation would invert HI and LO, causing Kelvin tap crossings.
* ipc-2221-high-current-trace-width: Any copper carrying 12V rail current (nets matching /SENSEC*_HI or +12V) must either: (a) be a filled copper zone/pour of >= 1.5 mm^2 cross-section per ampere (at dT=20°C, 2oz copper), or (b) a trace of at least 2.5 mm width for single-layer or IPC-2221 equivalently. The preferred form is additive pours after routing (cec_fr.add_power_pours), not fat traces.
* ina240-soic8d-package-not-tssop: All INA240 instances must use the D-package (SOIC-8, 1.27mm pitch, 8 pads, MPN INA240A3DR, LCSC C2060584). The PW package (TSSOP-8) has a DIFFERENT pin numbering and is electrically incompatible with the D-package schematic symbol. Never substitute PW.
* sensing-ic-24pin-ina228: The 24-pin ATX module must carry exactly 4 INA228 (20-bit, VSSOP-10) instances. INA238 is explicitly NOT acceptable (it is a pin-compatible drop-in but different part). Each INA228 must be on a distinct rail shunt (12V, 5V, 3V3, 5VSB).
* sensing-ic-12vhpwr-std-ina240-6: The 12VHPWR Standard module must carry exactly 6 INA240 current-sense amplifiers, one per pin of the 12V-2x6 connector. Part must be INA240A3 D-package (SOIC-8 / INA240A3DR), NEVER the PW (TSSOP) package which has a different pinout.
* button-ts1088-not-evq: BOOT (SW1) and RESET (SW2) buttons on all boards must be TS-1088-AR02016 (XKB, LCSC C720477). The Panasonic EVQ (EVQPUJ/EVQPUA) is explicitly replaced — it uses a different 4-pad footprint vs the TS-1088's 2-pad, so it is not a drop-in and cannot be silently substituted.
* 12vhpwr-std-ina240-rc-filters: Each of the 6 INA240 channels on the 12VHPWR Standard module must have: 10Ω series resistors on both IN+ and IN- inputs (RFH/RFL, 0.1% tolerance, Yageo RT0402BRE0710RL, LCSC C705642) and a 470nF differential capacitor (CF) across the filtered inputs. This creates fc≈16.9kHz matching the §6.10 acquisition bandwidth.
* shunt-copper-heatsink: Every shunt (RS* refs) must be surrounded by wide 2 oz copper pours on the high-current nets that cover its full pad area and extend to the nearest high-current connector pads; no discrete or finned heatsink is allowed anywhere on any module.
* shunt-thermal-vias: Each shunt pad (RS* parts) must have a thermal-via array punching through to the inner GND plane(s). Via drill must be sized for current contribution (at minimum 0.5 mm drill / 0.9 mm pad per netclass). For EPS/PCIe per-cable shunts at 40-55 A design current, copper-filled vias, plated slots, or copper coin/inlay are required to avoid via fusing (verified by physics_gates 'via over-temp').
* conductor-temp-budget: For any routed high-current net (nets with _HI/_LO suffix, 12V nets, GND return), the IPC-2221/2152 analytic temperature rise at design current must be <= 30 C dT and <= 105 C absolute, with ambient = 50 C for enclosed_passive / 35 C for airflow environments.
* sense-ic-away-from-shunt-selfheat: Sense ICs (INA238*, INA228*, INA240* refs) must be placed adjacent to the shunt's Kelvin sense edge (inner edge, per §6.8) but positioned so the IC body is NOT directly above or below the shunt's power dissipation zone; the IC body should be beside (not on top of) the shunt in the direction of the sense tap, minimizing thermal coupling of shunt heat into the IC die.
* high-current-pour-on-outer-layers: All high-current nets (*_HI, *_LO, 12V nets carrying > 5 A) must be routed as copper POURS on F.Cu and/or B.Cu (outer layers) in 2 oz copper, not as single thin traces on inner layers. An inner-layer trace carrying a cable current (40-55 A EPS/PCIe) with only the via-array cross-section as the thermal path will over-temperature. Inner layers (L2/L3) are for GND plane and power rail distribution at module-level curren
* gnd-plane-intact-under-sense: The L2 inner GND pour must be continuous and uninterrupted under the sense ICs (INA238*, INA228*, INA240*, INA181*) and under the NTC thermistors (TH1, TH2), with no large anti-pad voids from high-current via arrays intruding into this area. Anti-pad voids from the high-current cable region (the shunt via arrays) must not extend within 3 mm of any INA* or TH* centroid.
* 2oz-outer-copper-high-current: EPS, PCIe, and 12VHPWR Standard module PCBs must be specified as 2 oz copper on F.Cu and B.Cu outer layers. This doubles the thermal conductance of the copper pour and directly halves the dT from the IPC-2221 model for a given cross-section. Inner layers also 2 oz per §6.7 guidance.
* copper-pour-after-route: The workflow order must be: (1) route all signal and sense nets first (Freerouting or manual), (2) THEN add/fill power pours as additive same-net copper on the already-routed board. Filling pours before routing can cause the router to route the force net differently and leave the Kelvin sense tap stranded on an isolated pour island.

STACKUP & NETCLASSES (platform design docs):
* 4-layer stackup, cable-interposer modules (EPS/PCIe): F.Cu (2oz) / In1 = GND PLANE / In2 / B.Cu (2oz). In1 carries the board-wide continuous GND plane; it is the return-path reference for all signal routing.
* EPS netclasses (.kicad_pro): Power12V 2.5mm pour-carried /SENSEC*; GND 0.5mm; Power 0.5mm (+3V3/+5VSB/VBUS); Signal 0.22mm (I2C/THRESH/DET*/CAN_TX,RX/EN); CAN 0.25mm coupled pair; USB 0.25mm diff pair gap 0.13.

COMPILED DESIGN-RULE FILE (assembled.kicad_dru, corpus-compiled for this board):
(version 1)

# CEC EPS 8-pin module -- custom design rules (auto-loaded by KiCad).
# Back the netclasses in eps8pin-module.kicad_pro.

(rule "Power min width"
	(constraint track_width (min 0.5mm))
	(condition "A.NetClass == 'Power'"))

# NO width floor on /SENSEC* (Power12V): ~30A/cable comes from the 2oz F.Cu+B.Cu
# POURS (split at each shunt, verified by area per IPC-2152), and the INA238/INA181
# four-wire Kelvin taps live ON these same nets as ~0.25mm stubs -- a track floor would
# false-flag them. Power12V clearance is 0.2 (not 0.25) because those Kelvin inputs enter
# a 0.5mm-pitch VSSOP-10.

(rule "USB diff pair gap"
	(constraint diff_pair_gap (min 0.1mm) (max 0.2mm))
	(condition "A.NetClass == 'USB' && B.NetClass == 'USB'"))

# CAN_H/CAN_L keep the standard (asymmetric) names -> NOT a P/N auto-pair; route them as a
# tightly-coupled pair by hand. Only termination is the Hub's split 120R, never on the module.

# === BEGIN CEC-CORPUS GENERATED (do not hand-edit; build-time assembly) ===
# manifest: corpus 36f163166605 compiler 1.0.0 (0 blocking rule(s))
# === END CEC-CORPUS GENERATED ===

