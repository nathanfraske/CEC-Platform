# KiCad library intake manifest — ENT board program (2026-07-02)

_Status: DRAFT. Work order for the intake fan-out that follows `docs/enterprise-requirements/research/next-trajectory/scope-board-program.md`'s "KiCad library intake (~30 parts)" deliverable. This document does not vendor anything itself — no `lib/vendor/*`, no `lib/cec*.kicad_sym`, no `.pretty` directory was touched to produce it. It is a plan: what already exists (reuse), what is net-new (with a verified sourcing route per part), what needs an owner action first, and how to split the fan-out across intake agents._

**Inputs read:** `lib/cec.kicad_sym`, `lib/vendor/cec-vendor.kicad_sym`, `lib/vendor/cec-power.kicad_sym`, every `.pretty` dir under `lib/` and `lib/vendor/`, `hubs/hub-standard/{sym,fp}-lib-table`, `modules/12vhpwr-pro/12vhpwr-pro-module.kicad_sch` (confirmed 99-line stub, no ESP32-P4 symbol anywhere in the repo), `docs/enterprise-requirements/spec-sheets/bom-detailed/{hub-ent-bom-detailed,bom-a-compute,bom-b-uplink,bom-c-module-if-base-secio,bom-d-power}.md`, `docs/enterprise-requirements/spec-sheets/module-ent-spec-sheets.md`, `docs/enterprise-requirements/ratification/oq-11-shunt-selection-2026-07-02.md`, `docs/enterprise-requirements/ratification/rfq-package-2026-07-02.md`, `docs/enterprise-requirements/module-requirements-common.md`, `docs/enterprise-requirements/research/phase2/survey-{8,10,11}-*.md`, `docs/enterprise-requirements/research/next-trajectory/scope-board-program.md`, `CEC-Platform-Ground-Truth-Spec.md` (REF3033/LTC2358-18/OQ-3 sections).

## Tool-availability findings

- **`easyeda2kicad` is NOT installed in this environment.** `which easyeda2kicad` → exit 1 (not on PATH). `pip show easyeda2kicad` → `WARNING: Package(s) not found`. The repo's provenance notes (CLAUDE.md — Kinghelm RJ-45 FTP jack, JST S5B-PH-K-S, TS-1088 button, NTC thermistor, INA181A2/TLV7011, ESP32-C6-MINI-1, all "pulled via easyeda2kicad") establish this is the established route for LCSC-stocked parts, but the tool must be installed (`pip install easyeda2kicad`) in whatever environment actually runs the intake fan-out — it is not available here to test-run against any of the ENT MPNs. Flag this as a prerequisite step for every intake agent, not an assumption.
- No other footprint-generation tooling (kicad-cli fp upgrade path, `vendor-libs.sh`) was exercised against a real MPN this pass — same caveat.
- Given the tool gap, every "Route (a) easyeda2kicad" entry below is a **recommended route contingent on installing the tool**, not a verified pull.

## Part 1 — reuse inventory (already exists; do not re-vendor)

### Symbols

| Library | Symbols |
|---|---|
| `lib/cec.kicad_sym` (nickname `cec`) | `CEC_ATX_24`, `CEC_CONN_12V2x6`, `CEC_CONN_2x4`, `CEC_MEZZANINE_16P`, `CEC_NANOKVM_AUX_5P`, `CEC_PWR_IN_2P`, `CEC_RJ45_8P8C_FTP` |
| `lib/vendor/cec-vendor.kicad_sym` (nickname `cec-vendor`) | `74LVC1G17`, `CEC_SHUNT_2T`, `CEC_SHUNT_4T`, `C_Small`, `D_Schottky`, `ESP32-C6-MINI-1-N4`, `ESP32-S3-MINI-1`, `ESP32-S3-WROOM-1`, `INA181A2IDBVR`, `INA226`, `INA228`, `INA238`, `INA240`, `LED`, `LP5907MFX-1.2`, `LP5907MFX-3.3`, `PESD5V0S1UL`, `REF3030`, `R_Small`, `SB120`, `SK6812MINI`, `SN74AHCT1G08`, `SS14`, `SW_Push`, `TJA1051T-3`, `TJA1462AT`, `TLV7011DBVR`, `TPS2121RUXR`, `TPS3839DBZ`, `TestPoint`, `Thermistor_NTC`, `USBLC6-2SC6`, `USB_C_Receptacle_USB2.0_16P` |
| `lib/vendor/cec-power.kicad_sym` (nickname `cec-power`) | `+12V`, `+3V3`, `+5V`, `+5VSB`, `+5V_MAIN`, `+5V_SYS`, `GND`, `PWR_FLAG` |

No other `.kicad_sym` files exist under `lib/` (confirmed via `find lib -name "*.kicad_sym"`).

### Footprints (`.pretty` dirs)

`lib/cec.pretty` (universal-interface parts): `CEC_12V2x6_Horizontal`, `CEC_Logo_Copper`, `CEC_Logo_Triangle`, `RJ45_FTP_Shielded_Horizontal` (Kinghelm KH-RJ45-58-8P8C, C2683360).

`lib/vendor/*.pretty`: `Button_Switch_SMD` (TS-1088), `Capacitor_SMD`, `Connector_JST` (`JST_PH_B5B-PH-K-S_1x05_P2.00mm_Vertical`, `JST_PH_S5B-PH-K-S_1x05_P2.00mm_Horizontal`, **`JST_XH_S2B-XH-A_1x02_P2.50mm_Horizontal`** — the exact 2-pin power-in JST-XH BOM-D's J_MAIN/J_SVB reuse), `Connector_Molex`, `Connector_PinHeader_2.00mm`, `Connector_PinSocket_2.00mm`, `Connector_RJ`, `Connector_USB`, `Diode_SMD` (`D_SMA`, `D_SOD-323`), `Fiducial`, `LED_SMD`, `MountingHole`, `Package_DFN_QFN` (`RUX0012A` — TPS2121's VQFN-HR-12 land), `Package_SO` (`SOIC-8_3.9x4.9mm_P1.27mm`, `VSSOP-10_3x3mm_P0.5mm`), `Package_TO_SOT_SMD` (`REF3030_DBZ3`, `SOT-23`, `SOT-23-5`, `SOT-23-6`), `RF_Module` (`ESP32-C6-MINI-1`, `ESP32-S2-MINI-1` [+NoAntKeepout, used as the S3-MINI land], `ESP32-S3-WROOM-1`), `TestPoint`.

### Sym/fp-lib-table convention (confirmed from `hubs/hub-standard/`)

Every module and hub board's `sym-lib-table`/`fp-lib-table` points at the same three `.kicad_sym` files and the same set of `cec-*`-namespaced `.pretty` libraries via `${KIPRJMOD}/../../lib/...`. No board carries a private symbol/footprint beyond this shared set. This means **the ENT hub and module boards, once created, will need their own `sym-lib-table`/`fp-lib-table` following the identical pattern**, extended with whatever new `cec-*` libraries this intake creates (e.g. a new `cec-Package_BGA` or `cec-RF_Module` addition, `cec-Connector_Kinghelm` for the RJ-11 jack, etc.) — not a new naming convention.

### Platform parts directly reusable for the ENT program, verified present

- **TJA1051T/3** (`TJA1051T-3` symbol + `SOIC-8_3.9x4.9mm_P1.27mm`) — CAN transceiver, every ENT board.
- **RJ-45 FTP jack** (`CEC_RJ45_8P8C_FTP` + `RJ45_FTP_Shielded_Horizontal`) — every ENT module port; hub module-port jacks (BOM-C J_PORT1–8) reuse this exactly.
- **PESD5V0S1UL/BA** (symbol name is generic `PESD5V0S1UL`, footprint `D_SOD-323`; MPN/LCSC corrected to PESD5V0S1BA via schematic properties per the platform convention) — DETECT ESD clamp, LOOP_IN_A/B clamp (BOM-C §5), reused as-is.
- **USB-C receptacle** (`USB_C_Receptacle_USB2.0_16P`) — BOM-C J_USB.
- **USBLC6-2SC6** — USB ESD array, BOM-C D_ESD_USB.
- **SN74AHCT1G08** — LED level shifter, BOM-C U_LVL.
- **SK6812MINI** — status LEDs, BOM-C DL1–7.
- **TS-1088 buttons** (`SW_Push` + `Button_Switch_SMD`) — BOM-C SW_RESET/SW_USER.
- **NanoKVM aux header** (`CEC_NANOKVM_AUX_5P`, JST `S5B-PH-K-S`) — BOM-C J_KVM.
- **Thermistor_NTC** (Murata NCP15XH103F03RC) — BOM-C TH1.
- **TPS3839DBZ** (SOT-23-3) — reused for BOM-A's U9 (K33) directly, and as a **duplicate-with-new-value** for U10/U11/U12 (~0.9 V / ~1.7 V / ~2.3 V threshold codes) and BOM-D's U_SV — same symbol/footprint, only the MPN/threshold property changes per instance. Not a new intake item, just new BOM property rows.
- **TPS2121RUXR** (`TPS2121RUXR` symbol + `RUX0012A` footprint) — BOM-D's U_PC1/U_PC2 priority cascade is an EXACT reuse of the existing hub-standard part (same LCSC C485916). No intake needed.
- **SS14 / SB120 / D_Schottky** — BOM-D D_ISO reuses SS14 exactly (LCSC C2480); generic Schottky footprint (`D_SMA`) covers SS110 below.
- **JST-XH 2-pin** (`JST_XH_S2B-XH-A_1x02_P2.50mm_Horizontal`) — BOM-D J_MAIN/J_SVB reuse this exactly (LCSC C157931).
- **REF3030** (SOT-23-3, `REF3030_DBZ3` footprint) — not the exact part the 12VHPWR ENT/Pro needs (that's REF3033, 3.3 V), but it is the **same TI decivolt-reference family, same SOT-23-3 3-pin footprint, same pinout** (1=IN, 2=OUT, 3=GND per the platform's own datasheet-corrected pin map) — trivial in-house duplicate (see manifest row).
- **CEC_SHUNT_2T / CEC_SHUNT_4T** — the OQ-11 shunt selection (now locked) needs **no new symbol/footprint work at all**. Bourns CSS2H-2512 (2-pad, hand-routed Kelvin taps) maps to `CEC_SHUNT_2T`; Vishay WSK2512 (true 4-terminal Kelvin land) maps to `CEC_SHUNT_4T`. Both generic bodies are already vendored; closing OQ-11 in KiCad is a BOM-property pass (MPN/LCSC per rail), not a library-intake item — explicitly **excluded** from the net-new table below.
- **INA226/228/238/240/181A2, TLV7011DBVR** — all four ENT module families' sensing (INA228 24-pin, INA238 EPS/PCIe, INA240 12VHPWR + EPS/PCIe fast path, INA181A2/TLV7011 §6.13 detection front end) is fully covered by symbols already in `cec-vendor`. No intake needed.
- **LP5907MFX-1.2/-3.3** — reusable wherever an ENT board needs a quiet LDO at those exact voltages (flagged contingent in BOM-D, not committed).

## Part 2 — net-new manifest

Effort: **S** = hand-draw or trivial duplicate of an existing part (< 1 hr); **M** = single easyeda2kicad pull + verification, or a straightforward datasheet-derived footprint; **L** = BGA/high-pin-count part, script-generated footprint, or a part needing an owner-gated ECAD download.

Priority: **P1** = blocks the shared ESP32-P4 + DP83TC814S-Q1 module reference block (the pathfinder every module family instantiates); **P2** = blocks the ENT hub schematic; **P3** = MC/MC-Max-only or deferrable (exact MPN still open, or gated behind a later owner decision).

| # | Part | Role | Symbol route | Footprint route | 3D | C-number / URL | Effort | Priority |
|---|---|---|---|---|---|---|---|---|
| 1 | **ESP32-P4NRW32X** (QFN-104, 10×10mm, 32 MB in-pkg PSRAM, needs ext. SPI flash) | Module MCU — uniform across all 4 ENT families + already the 12VHPWR Pro MCU | (b) script-generate from Espressif's QFN-104 pinout table (no SIP/MINI module exists — bare die pinout, ~104 balls/pads) — too large/dense for confident hand-draw | (b) script-generate the QFN-104 EP land from the datasheet mechanical drawing; cross-check against JLCPCB's own QFN-104 generic land if one exists | Espressif STEP model likely on their GitHub board-support repo — check before hand-modeling | LCSC **C22387510** (ESP32-P4NRW32X) — easyeda2kicad *may* pull symbol+footprint directly from this C-number even for a bare-die QFN; try route (a) first, fall back to (b) if the pull is incomplete/wrong pin count | **L** | **P1** |
| 2 | **DP83TC814S-Q1** (36-VQFN, 6×6mm) | 100BASE-T1 PHY — every ENT module family's T1 front-end | (a) easyeda2kicad if an LCSC listing exists (not confirmed this pass — TI part, DigiKey-sourced in every doc read); else (b) script-generate 36-VQFN from TI's package drawing | same as symbol — (a) or (b) | TI package STEP (VQFN 6×6, standard JEDEC MO-220) likely available as a generic library part | No LCSC C-number found in any ENT doc (DigiKey #15904180 only) — **verify LCSC/JLCPCB listing at intake time before assuming route (b)** | **L** (if (b)) / **M** (if (a) lands) | **P1** |
| 3 | **TDK ACT1210L-201-2P(-TL00)** (1210 case CMC) | 100BASE-T1 common-mode choke — every module port + every hub T1 port | (a) easyeda2kicad | (a) easyeda2kicad — 1210 2-line CMC is a simple 8-pad land | Optional; low priority for a passive | **LCSC C131444** | **M** | **P1** |
| 4 | **Nexperia PESD2ETH100-T** (SOT-23) | 100BASE-T1 ESD protection — every module port + every hub T1 port | (a)/(d) — SOT-23 body exists (`SOT-23` footprint already vendored), but this is a 2-line array with a non-generic pinout; hand-draw the symbol against the datasheet pinout, reuse the existing SOT-23 footprint if pad map matches, else pull via (a) | (a) if an LCSC listing exists, else reuse existing `SOT-23` footprint after pad-map verification | n/a | Not found in ENT docs (DigiKey #11205897 only) — **verify LCSC listing at intake** | **S–M** | **P1** |
| 5 | **REF3033** (SOT-23-3, TI decivolt reference family) | 12VHPWR ENT/Pro precision voltage reference (feeds LTC2358-18 ratiometric correction) | (d) hand-draw as a value-duplicate of the existing `REF3030` symbol (same 3-pin IN/OUT/GND map, same TI family, only the trim voltage differs) | (d) reuse `REF3030_DBZ3` footprint as-is (SOT-23 DBZ3, identical package across the REF303x family) | reuse REF3030's vendored STEP if body-compatible; else pull TI's SOT-23-3 generic | Not searched this pass — cheap SOT-23 part, low sourcing risk; confirm LCSC# at BOM pass | **S** | **P1** (12VHPWR is the pathfinder module) |
| 6 | **ADS131M08** (TSSOP-32, likely — TI 8-ch simultaneous ΔΣ ADC) | EPS/PCIe ENT fast-ADC working baseline | (a) easyeda2kicad if LCSC-stocked, else (c) TI's own ECAD export (Ultra Librarian, TI parts are commonly mirrored there) | same as symbol | TI STEP via the same UltraLibrarian/vendor export | Not found in ENT docs; price only marked `[unv]` in the RFQ package — **package/LCSC# unconfirmed, needs a datasheet pull before intake starts** | **L** | P2 (blocks EPS/PCIe ENT, not the P4+T1 pathfinder itself) |
| 7 | **LTC2358-18** (likely LQFP-64 or similar — ADI 8-ch simultaneous 18-bit SAR) | 12VHPWR Pro/ENT precision ADC (already the Pro board's spec'd part, but no symbol exists anywhere in the repo — confirmed via the 99-line stub) | (c) **owner download required** — Analog Devices parts are near-never on LCSC/JLCPCB's native EasyEDA library; the standard route is ADI's own "Symbols and Footprints" ECAD download (Ultra Librarian or ADI's own portal), which sits behind a free registration/login wall | (c) same ECAD export bundle typically includes the footprint | (c) same bundle usually includes STEP | ADI product page (`https://www.analog.com/en/products/ltc2358-18.html`) → "Symbols and Footprints" tab; no LCSC# expected | **L** | **P2** (12VHPWR ENT/Pro; the module-ent sheet notes ADS131M08 is favored unless precision forces LTC2358, so this may end up P3/never-built if the cost-vs-precision call goes the other way — do not front-load this one) |
| 8 | **MPFS095TS-1FCVG484I** / **MPFS095TC-FCVG484(I/E)** family (FCVG484, 484-ball 19×19mm 0.8mm BGA — part-agnostic land across 025/095/160/250 × T/TS/TC) | Hub compute SoC | (c) **owner/factory action required** — Microchip does not publish an EasyEDA/LCSC listing for this SoC family; the real route is Microchip's own symbol/footprint download (they publish KiCad-ready libraries for some PolarFire parts) or (b) script-generate the 484-ball BGA land directly from the packaging user guide's ball map (Microchip publishes the exact ball assignment in the FCVG484 mechanical/packaging UG — CLAUDE.md's own convention note calls out generator scripts as "the right route" for this exact class of part) | (b) script-generate from the packaging UG ball map — this is the ONLY dependable route given no ECAD-native listing was found; a 484-ball 0.8mm BGA hand-drawn footprint is not advisable | Microchip may publish a STEP for the FCVG484 package; check their design-resources page before hand-modeling | No LCSC — confirmed RFQ-only (`hub-ent-bom-detailed.md` §3/§3a); Microchip PolarFire SoC KiCad resources page (search microchip.com design resources) | **L** (the single largest single-part effort in this manifest — this is the FCVG484 breakout/fanout study's dependency, `scope-board-program.md` deliverable #1) | **P2** |
| 9 | **W25Q256JV** (FIQ SOIC-16 or EIQ WSON-8) | Hub boot NOR flash | (a) easyeda2kicad — Winbond parts are broadly LCSC-native (the base W25Q128JVSIQ is already sourced platform-wide at C97521; the 256 Mb sibling should have an equivalent listing) | (a) SOIC-16 or WSON-8, both standard JEDEC bodies — likely already exist in a generic footprint set even without a pull | n/a | No explicit C# captured in ENT docs despite "LCSC-verified" claim in `hub-ent-spec-sheet.md` — **confirm the exact C-number for the JV (not JW) SOIC-16 FIQ or WSON-8 EIQ variant at intake** | **M** | **P2** |
| 10 | **MIC22705YML-TR** (Microchip buck, QFN — exact pin count TBD) | Hub VDD-core buck (7 A, replaces the headroom-risky MPM3833C for the core rail) | (a) easyeda2kicad if LCSC-stocked, else (b) | same | n/a | Not found in ENT docs (only a $3.62@25 DigiKey-style price) — **verify package + LCSC# at intake** | **M** | P2 |
| 11 | **MPM3833CGRH-Z** (QFN-18, 2.5×3.5mm) | Hub buck ×5–6 instances (VDD18/VDD25/3V3-domain/optional DDR) | (a) easyeda2kicad — LCSC C6306422 confirmed live in BOM-A | (a) | (a) | **LCSC C6306422** | **M** | P2 |
| 12 | **TPS7A2018PDBVR** / **TPS7A2050PDBVR** (SOT-23-5, confirmed-real siblings) + **TPS7A2010/TPS7A2025x** (decivolt codes, existence-real/stock-unconfirmed) | Hub quiet LDOs (VDDA 1.0 V / VDDA25 2.5 V) | (a) easyeda2kicad for the two confirmed siblings; (d) duplicate-with-new-trim for the two unconfirmed decivolt codes once stock is confirmed (same SOT-23-5 family/footprint throughout — existing `SOT-23-5` footprint in `Package_TO_SOT_SMD.pretty` likely already fits) | (a)/(d) — reuse existing `SOT-23-5` footprint if pinout matches (verify pin map at intake) | n/a | **LCSC C963430** (TPS7A2018PDBVR), **LCSC C2864504** (TPS7A2050PDBVR); TPS7A2010/2025 unconfirmed — fallback is the adjustable **TPS7A21** family per the RFQ package's own second-source ask | **S–M** | P2 |
| 13 | **DSC1123BL5-125.0000** (SMD3225-6P, LVDS) | Hub MSS/SGMII reference clock, populated all SKUs | (a) easyeda2kicad if LCSC-stocked, else (c)/(d) hand-draw a generic 6-pad LVDS oscillator symbol/footprint (simple, low-pin-count part) | same | n/a | Not found in ENT docs with a C#; Microchip's own named 2nd source is SiTime SIT9120 — check both for LCSC listing at intake | **S–M** | P2 |
| 14 | **FTSH-105-01-L-DV-K** (Samtec 2×5, 1.27mm THT JTAG header) | Hub JTAG/FlashPro programming header | (a) easyeda2kicad | (a) | (a) | **LCSC C5155080** | **S** | P2 |
| 15 | **DP83869HMRGZR** (VQFN-48, 7×7mm) | Hub GbE uplink PHY | (a) easyeda2kicad if LCSC-listed, else (c) TI ECAD export | same | (c) TI/vendor STEP | No LCSC# found in ENT docs (DigiKey-verified only) — **verify at intake** | **M–L** | P2 |
| 16 | **TLV75801PDBVR** (SOT-23-5, adjustable LDO ×2 per uplink) | GbE PHY VDD1P1/VDDA2P5 support | (a) easyeda2kicad, or (d) hand-draw reusing the existing `SOT-23-5` footprint once the TI pin map is confirmed | (a)/(d) | n/a | Not searched this pass; cheap TI SOT-23-5, low risk | **S** | P2 |
| 17 | **ABM3-25.000MHZ-B2-T** (SMD 5.0×3.2mm, 2-pad crystal) | GbE PHY 25 MHz reference | (d) hand-draw generic 2-pin crystal symbol (trivial); no vendored generic Crystal symbol currently exists in `cec-vendor` | (d) hand-draw simple 2-pad SMD land, or (a) if LCSC-listed | n/a | Not searched — Abracon parts are commonly LCSC-listed; check before hand-drawing | **S** | P2 |
| 18 *(LANDED 2026-07-16 — owner SnapEDA pull vendored to `cec-ent-net` as **JXD0-0001NL** (TAB-DOWN); BOM-B names JXD1 (TAB-UP) — variant decision pending, owner-queue)* | **JXD1-0001NL** (Pulse Electronics shielded THT MagJack w/ integrated LEDs + Bob-Smith network) | Hub GbE uplink magnetics/jack | (c) **owner download likely required** — Pulse Electronics ECAD assets are typically distributed via their own portal or UltraLibrarian/SnapEDA, not LCSC-native; complex THT part (multiple pin groups: 8 data + 2 LED + shield) | (c) same bundle | (c) same bundle, or Pulse's own STEP library | Pulse product-finder page (`productfinder.pulseelectronics.com`); check SnapEDA/UltraLibrarian for `JXD1-0001NL` first | **L** | P2 |
| 19 | **RClamp0524PA.TCT** (Semtech, DFN-10, SLP2510P8) | GbE PHY TVS array ×2 | (a) easyeda2kicad — **LCSC C40960** confirmed | (a) | (a) | **LCSC C40960** | **M** | P2 |
| 20 | **Bourns 2038-15-SM-RPLF** (3-electrode GDT, SMD cylinder) | GbE uplink surge protection | (a) easyeda2kicad — **LCSC C720636** confirmed | (a) | (a) | **LCSC C720636** | **M** | P2 |
| 21 | **Kinghelm KH-PCB-6P6C** (RJ-11 6P6C THT right-angle) | Hub RJ-11 trust-channel jack | (a) easyeda2kicad — same Kinghelm family the platform already pulled the RJ-45 FTP jack from, strong precedent | (a) | (a) | **LCSC C2683354** (thin stock, ~150 pcs — second sources KH-9801-6P6C `C2683355`, KH-9752-6P6C `C2683356`, On-Shore PJ006 `C7026652`, all also easyeda2kicad-routable) | **M** | P2 |
| 22 | **Toshiba TLP172A(TP,F)** (SOP-4 PhotoMOS) | Hub RJ-11 ALARM_OUT isolated dry contact | (a) easyeda2kicad — **LCSC C99477** confirmed | (a) | (a) | **LCSC C99477** | **M** | P2 |
| 23 | **onsemi LM393DR2G** (SOIC-8) | Hub RJ-11 EOL-loop window comparator | (a) easyeda2kicad — **LCSC C7955** confirmed; footprint may match the existing `SOIC-8_3.9x4.9mm_P1.27mm` land already vendored (verify pin count/pitch match before assuming reuse) | (a), or reuse existing SOIC-8 footprint if it matches | (a) | **LCSC C7955** | **S–M** | P2 |
| 24 | **ADS7830IPWR** (TSSOP, TI 8-ch I2C ADC) | Candidate DETECT/rail-sense/RJ-11-loop digitization path (not yet ratified — "worth an owner/firmware-team call" per BOM-C) | (a) easyeda2kicad — **LCSC C161747** confirmed | (a) | (a) | **LCSC C161747** | **M** | **P3** — not yet an adopted design choice, don't front-load |
| 25 | **TPS25940LRVCR** (WQFN-20, 3×4mm) ×3/hub | Hub eFuse fronts (MAIN_5V/5VSB/EXT) | (a) easyeda2kicad if LCSC-listed (thin: only 26 pcs per BOM-D), else (c) TI ECAD export | same | (c) | **LCSC C2867756** (thin — 26 pcs; flagged stock risk in the RFQ package, TPS25940A sibling is the named fallback) | **M** | P2 |
| 26 | **TLV62569DBVR** (SOT-23-5) | Hub logic-rail buck (+3V3) | (a) easyeda2kicad — **LCSC C141836** confirmed | (a), or reuse existing `SOT-23-5` footprint after pin-map check | (a) | **LCSC C141836** | **S–M** | P2 |
| 27 | **TDK VLS252010HBX-2R2M-1** (1008, 2.5×2.0mm shielded inductor) | Hub logic-rail buck inductor | (a) easyeda2kicad — **LCSC C88527** confirmed | (a) | (a) | **LCSC C88527** | **S** | P2 |
| 28 | **Samxon VKMI2101C472MV** (SMD can, ⌀16×21mm, 4700 µF/16 V) | Hub hold-up reservoir — same electrical role as the existing hub-standard 4700 µF cap; **check whether hub-standard already carries a symbol/footprint for its own hold-up cap before treating this as fully net-new** (the platform history in CLAUDE.md references this part's provenance directly — likely a generic polarized-electrolytic-can symbol + a can-diameter footprint pair already exists on hub-standard's PCB even if not in the shared `cec-vendor` library) | (a) easyeda2kicad — **LCSC C487318** confirmed; verify first whether hub-standard's own (uncommitted-to-shared-lib) part can be promoted instead of a fresh pull | (a) | (a) | **LCSC C487318** | **S–M** | P2 |
| 29 | **CUI/Same Sky PJ-002AH** (THT R/A barrel jack, 2.1mm ID/5.5mm OD) | Hub EXT rear-bracket power-in | (d) hand-draw — simple 3-pin (tip/sleeve/switch) THT barrel jack, well-documented mechanical drawing, low risk to hand-draw without an ECAD pull | (d) hand-draw from the Same Sky mechanical datasheet | Optional | `sameskydevices.com/product/resource/pj-002ah.pdf` — no LCSC# (DigiKey #408446 only) | **S** | P2 |
| 30 | **Littelfuse SMAJ5.0A** (DO-214AC/SMA) | Hub EXT-feed input TVS | (a) easyeda2kicad — **LCSC C83329** confirmed; reuse existing `D_SMA` footprint, verify 2-terminal pinout matches | (a), or reuse `D_SMA` directly | (a) | **LCSC C83329** | **S** | P2 |
| 31 | **SS110** (SMA/DO-214AC Schottky, 100 V) | Module + hub mis-plug series blocking diode (pin-1 VCC) | (a) easyeda2kicad — **LCSC C2482** confirmed; reuse existing `D_Schottky` symbol + `D_SMA` footprint, this is very likely a same-footprint value swap, not a new footprint at all | (d) reuse `D_SMA` directly if the 2-pad pattern matches (near-certain for a 2-terminal SMA diode) | n/a | **LCSC C2482** | **S** | P1/P2 (used on both module and hub side) |
| 32 | **SMAJ58A** (DO-214AA/SMA TVS, 58 V) | Module + hub mis-plug supplementary TVS (pin-1 and pin-7) | (a) easyeda2kicad — **LCSC C499822** confirmed; reuse `D_SMA` footprint (verify AA vs AC body — DO-214AA and DO-214AC differ slightly, confirm before assuming a drop-in) | (a)/(d) | (a) | **LCSC C499822** (2nd-source note: SMBJ58A also listed, `LCSC C157526`, different body — DO NOT conflate) | **S** | P1/P2 |
| 33 | **TPS26621DRCT** (TI eFuse, package likely SOT-23-6/"DRC" — TI's DRC package code maps to the existing vendored `SOT-23-6` footprint; **verify pin map before reuse**) | Module mis-plug fail-safe eFuse ahead of the LDO, every ENT module family | (a) easyeda2kicad if LCSC-listed, else (c) TI ECAD export | (a)/(d) — try reusing the existing `SOT-23-6` footprint first (cheapest path if the pad map matches) | (c) | Not found with an LCSC# in ENT docs (DigiKey #9597840 only, $2.07@100q verified there) — **verify LCSC listing at intake** | **M** | **P1** (part of the shared mis-plug protection block every module instantiates) |
| 34 *(LANDED 2026-07-16 — owner UltraLibrarian pull vendored to `cec-ent-net`, pin map verified 64/64 vs product brief DS00002819B Table 3-1; package = VQFN-64 KCX **8×8**mm p0.4 EP6.5 wettable, NOT the 9×9 guess below; FULL datasheet remains login-locked — strap/clock detail owed at sheet-06 capture, LAN9371/72 family docs = proxy)* | **LAN9370-I/KCX** (64-VQFN, non-BGA) ×2/hub | Hub T1 fabric switch — bridges 8 module ports' 100BASE-T1 into 2 RGMII/MII MAC instances | (a) easyeda2kicad if LCSC-listed (Microchip parts are hit-or-miss on LCSC), else (c) Microchip ECAD export or (b) script-generate 64-VQFN from the datasheet package drawing | same | (c)/(b) | No LCSC# found (DigiKey #16915502 only) — **the RFQ package itself flags this as an open stock/NDA-exposure risk (top-5 risk #2 in scope-board-program.md); confirm open (non-NDA) distribution before committing library effort** | **L** | P2 (blocks the ENT hub schematic's T1 fabric — but gated on the NDA/distribution confirmation landing first) |
| 35 | **eMMC, FBGA-153, exact MPN TBD** (8/32/64 GB density family, single land) | Hub bulk storage | **BLOCKED — no MPN chosen yet.** The master BOM (`hub-ent-bom-detailed.md` §6 open item 6) and the RFQ package (§1d) both flag this as "no exact MPN has been picked; primary ask of the RFQ." Do not start library intake until an MPN comes back from the RFQ. | — | — | — | — | **P3 — blocked on RFQ response, not a library-effort question yet** |
| 36 | **S32K3xx non-lockstep sibling, exact MPN TBD** (watchdog block, MC/MC-Max only) | Hub supervisory watchdog | **BLOCKED — exact sibling MPN is an explicit owner gate** (`scope-board-program.md` decision point #5; ceiling part S32K344EHT1MMMST priced only as a bound, not the actual pick). Once chosen, LQFP package — NXP parts are broadly on LCSC/JLCPCB, expect route (a) | — | — | S32K344EHT1MMMST (ceiling reference only, not the real part) | — | **P3 — MC/MC-Max only, owner MPN gate first** |

## Owner-download list (parts needing a login-walled ECAD export or a factory/vendor-direct asset — cannot be resolved by a scripted LCSC pull)

| Part | Vendor/portal | Why it needs an owner action |
|---|---|---|
| **LTC2358-18** | Analog Devices ("Symbols and Footprints" tab on the product page) or UltraLibrarian/SnapEDA | ADI parts are essentially never on LCSC/JLCPCB's EasyEDA-native library; the standard route is ADI's own free-registration ECAD download. May turn out unnecessary if the ADS131M08 cost-vs-precision call goes the other way (module-ent-spec-sheets §5 open row 3) — **recommend deferring this pull until that decision lands**, rather than spending an owner login-wall action on a part that may not get built. |
| **JXD1-0001NL** (Pulse Electronics MagJack) | Pulse Electronics product finder / UltraLibrarian / SnapEDA | Complex THT magnetics part (integrated transformer + Bob-Smith network + LEDs); not the kind of part easyeda2kicad's LCSC-mirror route reliably covers even if a listing exists. Confirm SnapEDA/UltraLibrarian coverage before assuming a login-walled manual export is required — it may already be free-tier accessible. |
| **MPFS095TS/TC-FCVG484 family** (Microchip PolarFire SoC) | Microchip's own design-resources page (some PolarFire kits ship KiCad-ready libraries) | Not a typical distributor part at all — the RFQ package itself notes this line is quote-gated at every distributor. If Microchip doesn't publish a KiCad library for this exact BGA, the fallback is the script-generation route (item #8 above) using the packaging UG's published ball map — that route does NOT need an owner login, only the packaging UG PDF (already fetched once this program, per BOM-A's sources section). |
| **DP83869HMRGZR / DP83TC814S-Q1 / LAN9370-I/KCX** (TI/Microchip, if no LCSC listing turns up) | TI's own KiCad/ECAD export tool (ti.com parts pages often carry a "CAD Models" tab, no login required for many); Microchip likely needs UltraLibrarian | Flagged here as **conditional** — verify LCSC/JLCPCB coverage first (route (a)); only escalate to a vendor portal if that comes up empty. Do not pre-assume a login wall for these three without checking. |

Everything else in the net-new table has either a confirmed LCSC C-number (clean easyeda2kicad candidates) or is small/simple enough to hand-draw (S-effort passives, connectors, oscillators) without an owner action.

## Recommended fan-out grouping

Group by shared footprint-class/tooling risk and by the board-program's own pathfinder sequencing (`scope-board-program.md`: 12VHPWR ENT first, then EPS, PCIe, 24-pin last; hub proceeds in parallel since it's the long-pole BGA item), so each intake agent can work a batch without waiting on another agent's tool-installation step:

1. **Agent A — "easyeda2kicad passives & simple actives batch" (confirmed C-numbers, route (a), highest confidence, do first to validate the tool once installed).** Items #3, #11, #19, #20, #21, #22, #23, #24, #26, #27, #28, #30, #31, #32 (14 parts). All have a verified LCSC C-number in this manifest. Install `easyeda2kicad`, pull each, verify against the datasheet pin map, land in the appropriate existing `cec-*` `.pretty`/`.kicad_sym` where the body matches an existing library, or a new `cec-<category>` library otherwise. This batch also resolves whether items #12/#16/#25 can reuse existing SOT-23-5/6 footprints (verify pin maps as a side effect).
2. **Agent B — "shared P4+T1 module reference block" (P1, the pathfinder — do second, blocks all 4 module families).** Items #1 (ESP32-P4), #2 (DP83TC814S-Q1), #4 (PESD2ETH100-T), #5 (REF3033), #33 (TPS26621DRCT). This is the highest-value batch (unblocks 12VHPWR ENT, then EPS/PCIe/24-pin) and the highest-risk (ESP32-P4's QFN-104 land is the single hardest footprint in the whole manifest). Recommend this agent starts with #5 (REF3033, trivial) and #33 (verify SOT-23-6 reuse) to build momentum before tackling #1/#2.
3. **Agent C — "hub power/compute support parts" (P2, the rest of BOM-A/BOM-D).** Items #9 (W25Q256JV), #10 (MIC22705YML-TR), #12 (TPS7A20 family), #13 (DSC1123BL5), #14 (FTSH-105 — trivial, confirmed C#), #17 (ABM3 crystal), #25 (TPS25940LRVCR). Mostly small ICs/passives around the compute-core buck tree and JTAG/clock support.
4. **Agent D — "hub uplink + RJ-11 trust channel" (P2, BOM-B + BOM-C's non-reused rows).** Items #15 (DP83869HMRGZR), #16 (TLV75801PDBVR), #18 (JXD1-0001NL — flag for the owner-download list), #29 (PJ-002AH). RJ-11-side items #21/#22/#23 already live in Agent A's confirmed-C-number batch; this agent's job is mainly the uplink PHY + its magnetics, the one likely owner-download item in this grouping.
5. **Agent E — "hub compute BGA + T1 fabric switch" (P2, the two hardest/most schedule-risky parts — run in parallel with the others since each is a multi-day effort on its own).** Item #8 (MPFS095TS/TC family — the FCVG484 breakout study's dependency) and item #34 (LAN9370-I/KCX, pending the NDA/distribution confirmation). Treat this as its own workstream with its own timeline, not something folded into a batch of small parts — it is explicitly the program's long pole (`scope-board-program.md` risk #1 and #2).
6. **Do not fan out yet:** items #6/#7 (ADS131M08 vs LTC2358-18 — cost-vs-precision decision pending), #18's ADI item overlap N/A, #35 (eMMC — MPN not chosen), #36 (S32K3x — MPN not chosen). These are blocked on an owner/RFQ decision, not on intake-agent bandwidth; re-queue them once the blocking decision lands.

## Summary count

- **Reuse (no intake work):** ~30+ symbols/footprints already vendored, including the full INA-family sensing suite, RJ-45 FTP jack, TJA1051T/3, TPS2121RUXR (BOM-D cascade), JST-XH 2-pin (BOM-D power-in), TPS3839DBZ (all threshold variants via BOM property duplication), and both shunt bodies (OQ-11 is a BOM-property pass, not a library item).
- **Net-new, confirmed LCSC C-number (route (a), high confidence):** 16 parts (#3, #11, #14, #19, #20, #21, #22, #23, #24, #26, #27, #28, #30, #31, #32, plus #12's two confirmed siblings).
- **Net-new, route unconfirmed / needs a datasheet-or-distributor check at intake time:** ~13 parts (#2, #4, #9, #10, #13, #15, #16, #17, #25, #29, #33, #34, plus #12's two unconfirmed decivolt codes).
- **Net-new, owner-download likely required:** 2–3 parts (#7 LTC2358-18 conditional on the fast-ADC decision, #18 JXD1-0001NL, #8 MPFS095TS/TC conditional on Microchip not publishing a KiCad library).
- **Blocked on an owner/RFQ decision before any library work starts:** 2 parts (#35 eMMC, #36 S32K3x).

This lands close to the `scope-board-program.md` estimate of "~30 net-new parts," with the honest caveat that #8 (the FCVG484 BGA) and #1 (the ESP32-P4 QFN-104) are each disproportionately large efforts relative to the rest of the list, and neither should be batched with the small-passives sweep.
