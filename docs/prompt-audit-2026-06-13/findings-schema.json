{
  "type": "object",
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "tier": {"type": "string"},
          "failure_class": {"type": "string", "enum": ["grounding", "schema", "ambiguity", "contamination", "constraint", "calibration", "other"]},
          "title": {"type": "string"},
          "evidence": {"type": "string"},
          "why_it_misfires": {"type": "string"},
          "example_bad_output": {"type": "string"},
          "severity": {"type": "string", "enum": ["high", "medium", "low"]},
          "fix": {"type": "string"}
        },
        "required": ["tier", "failure_class", "title", "why_it_misfires", "severity", "fix"]
      }
    }
  },
  "required": ["findings"]
}
